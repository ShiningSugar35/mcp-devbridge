# MCP DevBridge v0.8.9 生产基线与 v0.9.0 当前增量架构

> 本文件以正式发布、安装并完成运行态验收的 v0.8.9 生产拓扑为基线，同时描述 `release/v0.9.0` 当前工作树中已经实现并通过质量门、尚未正式发布、但已局部热加载到本机安装实例验证的 CodexPro durable terminal-receipt 增量。正式 Release `v0.8.9` 于 2026-08-30 发布，tag/CI/资产同源于 `b679268ee6cbbed49f5a2ca93c6e81d8e94d6184`；旧 tag、Release 与资产未被本研发线修改。Regular Chat Controller 仅是未发布实验/fixture 资产，不进入默认桌面、CLI、wheel、PyInstaller、Windows/Linux build 或 installer；实验、运行时验证与平台阻断见 `experimental/regular-chat-bridge/研究.md` 和 `进度验收.md` §48–§49。

## 1. 系统目标

MCP DevBridge 把用户明确启动的本地目录根，通过 CodexPro / 可选 Windows-MCP 暴露给 ChatGPT、Gemini 等 MCP 客户端。核心产品语义是：

- 一个桌面实例可以同时运行多个项目根。
- 每个运行根的全部后代路径自动继承该根的访问边界。
- 所有 READY 根同时 active，没有“入口项目”权限优先级。
- 公网客户端使用一个 Hub/Gateway 地址，根据 path/cwd/task 自动路由到目标根。
- 多设备路由在工作区路由之上再增加一层设备选择。
- Windows 为主要桌面发行平台；Linux / SteamOS Desktop Mode 通过用户目录安装，不写只读系统基座。

## 2. 运行拓扑

### 2.1 公网 Hub 模式

```text
ChatGPT / Gemini
        │ Streamable HTTP /mcp
        ▼
Cloudflare Named / ngrok / Quick Tunnel
        │
        ▼
OAuth/Bearer Gateway (127.0.0.1)
        │
        ├── active root A ──> CodexPro A (loopback)
        ├── active root B ──> CodexPro B (loopback)
        ├── active root N ──> CodexPro N (loopback)
        │
        └── selected remote device ──> remote DevBridge Hub
                                         └── its own active roots
```

Gateway 与 Tunnel 是 Hub 级共享组件，并且完全独立于项目。`ServiceCoordinator` 只拥有 Gateway/Tunnel；CodexPro/Windows-MCP 引擎全部由 `ProjectManager` 按项目并列管理，因此不存在 bootstrap/entry 项目。

### 2.2 Local 模式

`Local` 不启动公网 Tunnel，但仍通过同一个共享 loopback Gateway。客户端使用一个本机 MCP 地址即可在所有 READY 根之间按 path/cwd/task 自动路由；Local 与公网模式的区别只在是否存在公网 Tunnel，不再存在“所选 ProjectUnit 直接作为客户端入口”的单引擎语义。

### 2.3 Transport 健康与自愈

共享 Hub 的生命周期由 `ServiceCoordinator` 串行化：相同 transport 配置的并发 `start()` 合并为一次启动，不同配置必须先停止再切换，因此多项目并发恢复不会重复拉起 `cloudflared`。公网 Tunnel 的初始启动与运行时重建采用固定小集合退避（当前为立即、2 秒、8 秒）和错误分类：DNS lookup/host-not-found、timeout、连接重置等归为可重试瞬时错误；明确的未配置、无效 tunnel、认证/授权错误归为不可重试配置错误。Cloudflare 新尝试仍默认使用官方 `auto` 协议且不额外改写历史命令；若 cloudflared 启动预检在 auto/QUIC 阶段明确报告 TCP HTTP/2 可用并给出 `suggested_protocol=http2`（或等价提示），`TunnelManager` 会立即结束该次 auto 等待并保存协议建议，`ServiceCoordinator` 在同一有限重试序列的下一次尝试显式使用 `--protocol http2`，避免 90 秒 readiness 在 cloudflared 自身较长 auto fallback 完成前误杀可恢复连接。没有明确 HTTP/2 建议信号时不会猜测协议。退避由独立 `threading.Event.wait()` 承担，`stop()` 在等待生命周期锁前先置位取消事件，因此停止不会被完整退避窗口阻塞。固定公网域名场景先保持本机 Gateway；Tunnel 重试耗尽时项目引擎与本机 Hub 不被跨层停止，Coordinator 以降级 READY 状态继续让探针重建公网入口。没有固定公网入口且 Tunnel 无法建立时仍按启动失败处理。

快速层先验证本机 Gateway `/health`；公网模式在把外部公网 URL 当作健康依据之前，还必须验证**本实例拥有的 Tunnel 进程仍在运行**。如果自有 Tunnel 已退出，即使另一条系统级/冗余 connector 让同一公网域名继续返回 200，也记录 `tunnel_process_unhealthy` 并在现有 cooldown 与有界 retry 约束下只重建 Tunnel，不让外部 200 掩盖本地进程死亡。自有 Tunnel 存活时才继续比较公网 `/health` 的可达性/TTFB；深层每 60 秒从运行项目中执行 authenticated Project MCP canary，再依次验证 project-routed 本机 Gateway MCP 与公网 MCP。Project 健康而本机 Gateway MCP 连续失败时只重建 Gateway；本机 Gateway MCP 健康而公网 MCP 连续失败时只重启 Tunnel；Project 自身 `/healthz` + MCP canary 连续失败时只重启对应 CodexPro。所有恢复均有连续失败阈值与 cooldown，并分别写入 transport/service supervisor 日志。公网 Hub 运行期间 Windows 还通过 `SystemAwakeGuard` 请求 `ES_SYSTEM_REQUIRED | ES_CONTINUOUS`，防止 Modern Standby 的 idle timeout 让后台 origin 停止执行；不会请求保持屏幕点亮。

Gateway 每个实例拥有一个长期 `httpx.AsyncClient`，连接池上限为 64、keepalive 上限为 16、keepalive expiry 为 30 秒、pool timeout 为 5 秒；实例停止时由 Gateway 唯一关闭 client。对上游 HTTP response 采用单一所有权：需要改写/提取字段的分支只 `read + close` 一次；透传流由单个 async iterator 独占，并在正常结束或下游取消时 `aclose()`，禁止同一 response 同时进入 buffered 与 streaming 路径。`initialize/ping` 允许一次安全 transport retry，`tools/call` 不盲重试。当前 worktree 已加入按 JSON-RPC/tool 类型分类的总 deadline：控制面 15 秒、普通工具默认 45 秒、`wait_task`/`long_run_status` 按请求等待预算再加 10 秒 grace；buffered upstream body 还受 8 MiB 硬上限约束。`text/event-stream` 使用 12 秒空闲保活并受同一请求总 deadline 约束：仅在完整 SSE event 边界插入注释/结构化 timeout event，绝不在被网络 chunk 拆开的 event 中间插帧；非 SSE 响应不受影响。SSE/raw stream 的工具审计采用 once-only 终态回调，只在 completed、deadline、downstream-cancel 或 stream error 后记录，不能把响应头到达误记为完整成功；`initialize/tools-list` 复用已缓冲 one-shot payload，`open_workspace/bash` 的未缓冲响应体也受同一 absolute deadline 与 8 MiB 上限约束，避免 double read、double close 和特殊分支无界等待。

共享 `FlightRecorder` 由 `ServiceCoordinator` 创建并注入 Gateway：HTTP 请求记录 `request_started → context/stage → request_terminal`，每个 trace 最多一个终态，并按 `http/mcp/oauth` 标记责任层；response stage/terminal 同时记录 content-type 与是否为 SSE。Coordinator 每个周期还记录主进程/Gateway PID、Gateway 端口与 listener、Tunnel 当前或最后 PID/exit code、最近8行脱敏 Tunnel 输出、local/public 最近成功的墙上时间与单调时间、连续失败计数，以及最多32个项目的 PID/端口/listener和已知最近成功时间；项目成功状态内部最多64项，listener 仅做50ms loopback TCP探针。记录只保存分类、计数、时长与16位哈希化 workspace/project/path 等身份字段，secret-like键及自由文本中的 Bearer/token/password 等再次脱敏，不保存请求正文、OAuth code、Authorization/cookie或完整环境。内存 active request 默认上限512、terminal去重上限2048；磁盘按日 JSONL、默认8 MiB轮转、最多当前与一个备份、默认7天留存。任何写入/轮转失败均 fail-open，不阻断 MCP 数据面。

CodexPro HTTP 数据面使用 MCP TypeScript SDK v2，并以 `createMcpHandler(..., { legacy: "stateless" })` 服务 `/mcp`：2026-07-28 流量采用 per-request stateless，2025-era 流量也走 SDK 官方 stateless fallback。当前数据面不维护 `Mcp-Session-Id → transport` Map、不存在 30 分钟 protocol-session TTL、prune timer 或 session-specific EventStore 接线。Gateway 会剥离外部 `Mcp-Session-Id` 后再访问 CodexPro，并过滤任何上游返回的 session header；404/`Session not found` 不触发工具调用重放。CodexPro HTTP server 由 `httpLifecycle.ts` 处理 SIGTERM/SIGINT：shutdown 只启动一次，先停止接受新连接并最多等待 5 秒让活动 HTTP/SSE drain；超时后关闭残留连接，最后才在独立有界预算内清理 MCP handler/runtime，避免仍在排空的流被提前 dispose。

协议无状态与业务状态分层：workspace handle/root registry、后台 task 与 durable long-run 属于 CodexPro 进程级业务状态；`selected workspace` 与 `show_changes(last_shown)` 这类客户端视图状态按有界 `client-affinity` 隔离。该 affinity 只用于 UI/选择状态，不参与鉴权、权限判断或文件根路由。

### 2.4 Durable background task 与 terminal receipt

`bash` 的后台进程仍由每个 CodexPro 项目引擎本地管理，但带 `long_run_id` 的任务在 spawn 前先生成/校验 task id，并原子写入对应 `.ai-bridge/long-runs/<run_id>.json`。这消除了“进程极快结束后才附着 task id”的竞态。

任务进入 `completed / failed / cancelled` 后，BashTaskManager 的 terminal callback 自动写入同一 LongRunState：terminal status、exit code、signal、finished time，以及不含完整 stdout/stderr/command/env/secret 的有界证据。callback 同状态重复执行幂等；冲突终态拒绝。持久写使用 LongRunStore 已有 keyed lock、schema/大小上限和原子 replace。

任务在内存中已 terminal 但 receipt 仍为 `awaiting_terminal / persisting / retrying` 时，long-run observer 把它视为未完成，阻断 PASS review 与 completion；重试耗尽则返回 `unknown`，要求显式证据。只有 `taskResolutions` 已落盘后，CodexPro/DevBridge 进程替换才可零猜测恢复。旧版本遗留且没有 terminal 证据的 task 不推断成功，按证据逐项解析或保守标为未接受。

Windows 取消只针对 manager 拥有的 shell PID：150 ms固定建树宽限后，以有限序列调用 `taskkill /PID <pid> /T /F`；不得首次未命中就单杀父 PowerShell并留下孤儿子进程，也不枚举/终止用户其它进程。取消重试、terminal callback、task registry、输出与 checkpoint 均有硬上限。

### 2.5 Stable Hub Tool Contract

公网/loopback Hub 的公开 `tools/list` 由 Gateway 内嵌、版本化 contract 直接生成，不从当前 bootstrap 项目或任一实时 CodexPro `tools/list` 动态决定：

- 单个项目的 CodexPro 引擎当前注册 38 个工具；再加 3 个 Windows bridge wrapper，形成 41-tool source contract；Gateway 最后注入 9 个 Hub/路由/短命令工具，公开 schema 固定为 50 tools。`server_config.registeredToolCount=38` 因此不是 ChatGPT 应看到的公共总数。
- contract version=`1`；canonical public fingerprint=`489cd30e8e8676ad382c752bed7c46ba9bc2f308fa2d98b2a699793d0c5ee780`。
- `tools/list` 一次性返回全部 50 项，不分页、不按项目权限裁剪，并带 `Cache-Control: no-store`；模块加载时校验 count、tool name 唯一性与 fingerprint，不匹配则启动失败，不回退动态 schema。
- 项目启动/停止、`read_only/workspace/system` 模式和 bootstrap root 不改变公开 schema；权限差异在 `tools/call` 阶段执行。
- 内部 CodexPro action inventory、41-tool source contract 与 50-tool public Hub contract 是三个不同层级，诊断必须分别记录，不能拿项目引擎的 38、source fingerprint 或 action count替代公共 contract。
- ChatGPT 对已批准 MCP app 的工具/输入定义使用平台侧 frozen snapshot；服务端新增或修改工具不会自动进入既有批准快照，需在 ChatGPT 管理端 Refresh/审核/发布更新，并优先在新聊天验证。该快照治理不属于 Gateway 缓存，DevBridge 只能保证实时 `tools/list` 正确，不能强制改写 ChatGPT 已批准的快照。

### 2.6 Regular Chat 实验边界与 host 能力

`third_party/regular-chat-controller/` 与 `experimental/regular-chat-bridge/` 只保存未发布的状态机、Playwright fixture、恢复算法及归档原型。正式产品没有 Regular Chat 顶层页、`mcpdev chat` CLI、Python supervisor 或 Playwright runtime；PyInstaller/build/installer 不收集这些文件。实验包不复用用户日常 profile，不绕过登录/CAPTCHA/安全确认，也不能因实验页面异常重启 Gateway、Tunnel 或项目引擎。

实验控制器的当前安全不变量是：同一 BrowserSupervisor 的并发 start/stop 由单个有界 Promise 合并/串行化；run 在 page、lease 或 conversation identity 建立期间失败时回收投机页面和局部登记；生成配额在 turn 持久化为 `send_started` 前取得；每个 active run 最多存在一个 in-flight send，相同显式 turn identity 合并并只提交一次，冲突请求 fail-closed。所有 map/lease 数量仍受 active run/tab 上限约束，session 文件不保存原始 prompt、response、cookie、token 或完整 conversation URL。

当前真实 ChatGPT→DevBridge connector 未声明 `io.modelcontextprotocol/tasks`。因此 DevBridge 能让本地 background task 跨 host turn/断连继续执行并落盘，却不能在 ChatGPT assistant turn 结束后自行创建下一 turn。原型也不得通过自动提取登录态 ChatGPT Output 来替代 host capability negotiation。未来只有 client/server 双向声明并通过 MCP Tasks conformance，或 OpenAI 提供明确允许的等价异步能力后，才重新评估正式接入。

## 3. 项目与生命周期

### 3.1 `ProjectManager`

`ProjectManager` 管理 `projects.json` 与所有 `ProjectUnit`：

- 每个项目有稳定 id、root、独立 CodexPro/Windows 内部端口和项目配置；Gateway 端口属于全局 `AppConfig`/共享 Hub，不属于任何项目。
- `ProjectUnit` 维护该根的 CodexPro 与可选 Windows-MCP 进程。
- `enabled` 字段只保留旧配置/API兼容，不代表当前active/inactive路由状态。
- 真正参与路由的集合来自当前 READY/STARTING 运行单元。

### 3.2 桌面控制

项目表固定为五列：

```text
名称 | 路径 | 状态 | 端口 | 操作
```

- 行内操作按钮原位复用，状态轮询不销毁正在交互的控件。
- `_busy_project_ids` 只锁目标项目的并发启停冲突。
- “启动所有项目 / 停止所有项目”执行批量生命周期控制。
- 单独停止一个项目只停止其 ProjectUnit；仍存在其它运行根时共享 Hub/Tunnel 保持运行。
- 最后一个运行根停止后，才停止共享 Gateway/Tunnel。
- “连接与权限”配置可以批量复制到全部项目；Git 参数和项目独立端口不会被无意覆盖。

## 4. 多根自动路由

### 4.1 active root 定义

每个 READY 项目的 canonical root 构成 active roots。若启动：

```text
C:\
D:\
D:\Environment\mcp
```

则这三个根同时有效；`D:\Environment\mcp\...` 由更具体的第三个根处理，`D:\other\...` 由 `D:\` 处理。

### 4.2 路由证据优先级

Gateway 处理一次工具调用时按以下优先级确定目标：

1. 显式 `devbridge_workspace_id` 等 DevBridge override：仅兼容/诊断/用户明确指定时使用。
2. `task_id` affinity：`bash` 创建任务时记录源根，任务管理调用必须回到同一根。
3. 本次调用的**绝对**路径证据：`path/root/cwd/workspace/target/...`、patch header、command argv 中绝对路径；active roots 取最具体根。
4. CodexPro opaque `workspace_id=ws_...`：现代 stateless 客户端的显式应用 handle；命中后相对 path/cwd 只在该 handle 所属 workspace 内解析。
5. legacy client/session soft context：仅用于 pathless / relative follow-up，绝不能覆盖 task 或绝对路径。
6. 没有 handle/context 时，相对路径只有唯一根命中才自动路由；同名多根必须报歧义。
7. 完全无证据时可使用稳定 bootstrap fallback 完成 schema/初始化兼容调用，但 fallback 不会写入或报告成 current workspace。

当 1–3 级强证据把调用路由到与请求中 opaque `workspace_id` 不同的引擎时，Gateway 不仅要选对目标引擎，还必须在转发前清除这个不属于目标上游的 opaque handle；否则会在正确的 D: 引擎上收到来自 C: 的 `ws_...` 并报 `Unknown workspace_id`。`codexpro(action,args)` 包装调用的嵌套 handle 执行同样净化。

当前架构不把 `Mcp-Session-Id` 作为 CodexPro protocol session 或 current-workspace存储。Gateway可在边缘把外部会话/授权上下文哈希成非敏感client-affinity，仅用于隔离selected-workspace / last-shown等视图状态；文件/Git/Shell的目标仍必须由本次调用的path、task或workspace handle等真实路由证据确定。

### 4.3 绝对路径

- 路径先 canonicalize，再与所有 active roots 比较。
- 命中多个包含根时取**最具体（最长 canonical prefix）**的根。
- workspace/read_only 模式下，未落入任何 active root 的路径拒绝。
- system/full_system 模式下，active roots 外的绝对路径可由一个运行中的 system engine 承载；workspace 只提供默认上下文，不再是文件系统安全边界。

### 4.4 相对路径

- 若调用携带有效 opaque workspace handle，则相对 path/cwd 直接在该 workspace 内解析，不再与所有 active roots 竞争。
- legacy soft context 同样只为相对/pathless follow-up 提供上下文；绝对路径仍可覆盖它。
- 无 handle/context 时才对每个 active root 生成候选；已存在目标只有一个根命中时自动选择，新文件可依据最深的已存在父目录推断。
- 同等证据命中多个根时返回歧义错误，要求绝对路径；禁止按根字符串长度、注册顺序或“入口”猜测。

### 4.5 跨根单调用

如果一个调用明确携带属于不同 active roots 的多个强路径证据，Gateway 应拒绝或要求拆分，而不是静默选其中一个。这样可避免一个命令或 patch 在错误磁盘执行。

### 4.6 workspace route persistence 与 rehydrate

Gateway 把成功 `open_workspace/open_current_workspace` 返回的 deterministic `ws_` handle 与目标 project、经过 containment 验证的 canonical root 关联，并持久化到配置目录的 `workspace-routes.json`：

- 当前文件格式为 v2，并兼容读取 v1；最多保留 512 条 route 和 512 条 removal tombstone。route 按 `last_used` 有界淘汰，tombstone 阻止短暂双实例中的陈旧 snapshot 把已删除 handle 重新写回；真实晚于删除时间的重新打开可以恢复该 handle。
- 同进程使用 `RLock`，跨进程使用 Windows byte-range lock / POSIX `flock`；每次写入在锁内读取最新状态、执行 upsert + explicit removal merge，再用 PID/thread/UUID 唯一临时文件、`fsync` 与 `os.replace` 原子发布。
- 持久 route 的有效性以**项目仍在配置目录中且 canonical root 仍存在并受该项目包含**为准，不再把“当前 ProjectUnit 暂时非 READY”误当永久删除。项目短暂离线时 handle→project/root记录继续保留，调用明确返回目标工作区尚未运行；数据面恢复后仍可使用原 handle。只有项目真实删除、canonical root失效/越界或记录损坏时，才同时从内存、hydrated set 与持久文件原子删除并写 removal tombstone，不能猜测根。
- 旧格式 `ws-...` 仅保留有界进程内 affinity，不跨 Gateway restart 持久化。
- Gateway/CodexPro restart 或项目从暂时非 READY 恢复后，已知 `ws_` 在转发依赖调用前执行一次 8 秒预算的内部 `open_workspace`；返回 handle 必须与持久 handle 完全相同。
- 同一 `(handle, project)` rehydrate 使用 keyed single-flight并有 inflight hard cap；完成后清理 task entry。
- 上游首次报告 `Unknown workspace_id` 时，只读工具可在成功恢复后 one-shot replay；有副作用工具不自动 replay，只返回“上下文已恢复，请确认后重试”，防止重复写入。
- unknown handle、project mismatch、root invalidation均 fail-closed，不降级到 `C:\`、`D:\` 或 bootstrap root。

Hub 本地 `run_command/run_program` 的显式 `devbridge_workspace_id` 只选择 DevBridge 项目根；例如 D: 项目的 `cwd='.'` 语义是 `D:\`，不会自动继承 CodexPro 子工作区 `D:\Environment\mcp`，子目录必须由 `cwd`/绝对路径明确表达。当前正式 v0.8.9 在多根环境中对缺少 path/cwd/task/workspace/credential/显式 route 的 workspace-affine `tools/call` 返回 `-32006`，不再静默选择 stable bootstrap；Hub workspace/device 管理工具和单根兼容路径不受影响。restart 窗口中的旧 session/workspace route 连续性已由 r23 fault soak，以及 2026-08-30 正式安装时同一 ChatGPT MCP 会话跨进程替换后的自动恢复共同验证。

## 5. 路径与文件系统安全

### 5.1 containment

Python Gateway 与 CodexPro 路径守卫都必须使用 canonical/real path 语义。Windows 下 junction/symlink、POSIX symlink 和 `..` 不能通过文本前缀伪装成根内路径。

Gateway 本地 `run_command/run_program` 的 `cwd` 统一经边界函数验证。workspace/read_only 仍要求 cwd 位于目标根；`system/full_system` 允许 cwd 位于工作区外。Windows system/full_system 的本地短命令只有在高权限 broker 已完成一次 UAC 授权注册后才执行，未授权时明确失败而不静默降级到普通 token；危险命令硬拦截继续生效。

### 5.2 根盘扫描容错

CodexPro `tree/listFiles/inventory/inspect` 逐目录处理权限错误：

```text
EACCES / EPERM → warning + skip subtree + continue
```

这样 `C:\`、`D:\` 级扫描不会因为 `System Volume Information` 等保护目录让整次工具失败。

### 5.3 Git

Git scoped 工具从给定目标路径向父级寻找最近的 `.git` 仓库，而不是假设 active root 本身必须是 Git 仓库。因此把 `D:\` 作为根后，可以直接对 `D:\foo\repo\...` 的嵌套仓库执行 status/diff/commit 等操作。

### 5.4 Repository analysis

CodexPro structured analysis 由 inventory、语言/角色分类、symbol/relationship extraction、rank/search 与最大 8 条 workspace analysis cache组成。当前正式 v0.8.9 的窄路径实现把请求 `root/path` 传入 inventory，并把 normalized root 纳入 cache key，因此 scoped structured search 不再固定从 `.` 扫描整个 workspace；analysis 已接入 same-key single-flight、全局 inflight hard cap=8、deadline/AbortSignal 与 waiter cancel，并在 finally 清理 inflight。Gateway/CodexPro 的 `show_changes(include_diff=false)` 使用 bounded `git diff --shortstat`，不再物化完整 diff；`include_diff=true` 采用有时限、有字节上限并显式标注 truncation 的异步捕获。上述能力已经过源码/targeted/full gate、正式 r23 35 分钟 soak 与安装后当前 ChatGPT MCP 的 `search/show_changes` 实测共同验证；旧运行实例的 ENOBUFS 现场仅保留为历史缺陷证据，不代表正式 v0.8.9 当前行为。

## 6. Shell 与异步任务

CodexPro 的公开 `bash` 是统一的后台任务入口：

```text
bash(command)
  → 安全/路径/权限检查
  → spawn background process
  → immediate task_id
  → get_task / wait_task / list_tasks / cancel_task
```

- 命令任务本身没有公开 execution timeout；长任务必须通过 task/durable状态观察，而不是保持同步工具请求。
- `wait_task` 只限制一次轮询等待，不停止后台任务。
- `BashTaskManager` 是 CodexPro 进程级共享实例，任务按 workspace隔离；active task hard cap为16，完成记录按24小时/100条有界保留。
- `task_id` 在 Gateway记录root affinity，跨MCP transport/session仍能回到正确引擎。
- stdout/stderr使用有界滚动缓存。
- `cancel_task`结束完整进程树；Windows优先使用`SystemRoot`下的绝对`taskkill.exe`。
- Windows shell resolver 采用已实现的绝对启动器解析与 cwd/env 前置校验。2026-08-30 正式 v0.8.9 安装后的当前 ChatGPT MCP 已完成标准后台 task 实测并取得明确终态；此前 staging 运行态的启动失败只保留为历史缺陷证据，不代表当前正式基线。
- 600秒编排看门狗只在快照上标记stale/resume hint，不杀进程。
- DevBridge/CodexPro重启不会恢复正在执行的任务。

Gateway 的同步 `run_command/run_program` 只用于短兼容调用；长测试/构建/安装优先使用异步 `bash`。

### 6.1 durable 长任务状态机

对于多阶段或预计超过约 2 分钟的工作，CodexPro 额外提供 `long_run_*` 工具，把“需求拆解 → plan → 执行 → 证据 → 审查 → 返工 → PASS → return”从聊天上下文提升为持久化状态机：

```text
long_run_start(objective, steps, acceptance criteria)
        ↓
long_run_update(step/checkpoint/evidence)
        ↓
bash(..., long_run_id, long_run_step_id)
        ↓
long_run_review(PASS/FAIL)
        ├── FAIL → required_rework → reopen step → 再执行/再审查
        └── PASS
              ↓
long_run_complete
              ↓
允许向用户返回最终完成结论
```

状态保存在 `.ai-bridge/long-runs/<run_id>.json`，使用 PathGuard、schema 校验、有界字段、secret-like 内容拒绝和原子写入。每次有效工作更新递增 `workRevision`；PASS review 只对它审查过的 revision 有效，之后再改代码/证据会让 PASS 自动变 stale。

后台 `bash` task 可直接绑定 long run。review/complete 在 task 仍为 running/cancelling 时拒绝 PASS；CodexPro 重启后内存 task registry 丢失时，旧 task 会变为 unknown，必须通过外部终态证据显式 resolve，不能把“找不到 task”当成功。

当前 `wait_task` 是有界观察窗口，并按客户端能力分层：没有 MCP `progressToken` 时单次最多 30 秒；客户端提供 progress token 且支持 notification 时可请求最多 120 秒，服务器约每 8 秒发一次标准 `notifications/progress`，让长 wait 保持真实协议活动而不是纯空闲 socket。**这个窗口不是后台 process 的 execution timeout，也不等于 ChatGPT host assistant-turn 的生命周期。** running 状态只返回约 2 KiB 的 UTF-8 安全 stdout/stderr 尾部与 omitted-byte 计数，终态仍保留完整有界快照。观察频率采用自适应策略：启动/状态变化附近可短间隔观察，稳定运行时退避，completion/failure/progress 等状态变化优先触发立即复查；不得把某个固定 poll interval 写成延长 host turn 的 SLA。工具说明与 long-run 摘要明确：目标仍可执行且不需要真实用户输入/授权时，assistant 应自动继续，不能仅因后台任务仍在运行就要求用户回复“继续”。原生 MCP `io.modelcontextprotocol/tasks` 仍作为未来 capability-negotiated 路径；普通 `tools/call` durable fallback 保证尚未统一支持 extension 的宿主也能使用。详见 `docs/en/LONG_RUNNING_TASKS.md` 与 `docs/en/GOAL_LONG_RUNNING_TASKS.md`。

## 7. 设备层

`DeviceRegistry` 维护本机与已配对远端设备：

- `devices.json` 只保存非敏感设备元数据。
- 设备 Bearer / heartbeat secret 进入 SecretsStore。
- 远端设备约定期上报在线状态与当前公网 endpoint。
- Quick Tunnel 地址变化可通过 heartbeat 更新，不改变主 Hub 的 ChatGPT URL。
- 多台设备在线时，设备选择可以显式完成；目标设备内部仍使用本文件第 4 节的 active-root 自动路由。
- 设备切换不应把远端 Windows/POSIX 路径拿到本机解析。

## 8. 认证与密钥

### 8.1 Gateway

公网 Gateway 同时支持 MCP OAuth 与兼容 Bearer：

- OAuth provider负责Authorization Server / Protected Resource discovery、DCR、PKCE、authorization code、refresh rotation、revoke和resource binding。
- 支持业务scope `ACCESS_VIEW_MANAGE_MCP_CONTENT`与可选`offline_access`；authorization阶段必须包含业务scope，offline-only请求拒绝；旧business-only客户端继续兼容。
- access token明文只存在于客户端输入/短期内存；SecretsStore以token SHA-256派生key持久化`client_id/scopes/expires_at/resource/subject`，新provider实例可验证仍未过期token。
- access index、authorization code与consent均有TTL/条目硬上限和prune；expiry、revoke、resource mismatch都会删除或拒绝记录。
- Hub OAuth不要求用户在consent页选择某个“入口项目”；授权的是Hub，具体本机根由工具调用证据决定。
- 旧per-project Bearer可作为兼容pin，但普通Hub会话不应因此形成新的隐形“当前项目”。
- outbound proxy使用目标项目自己的上游凭据。
- 本机loopback匿名只用于兼容本地路径；公网必须认证。

### 8.2 SecretsStore

Windows：

```text
Windows Credential Manager
  └── fallback: DPAPI encrypted file
```

Linux / SteamOS：

```text
Desktop Secret Service (if available)
  └── fallback: AES-GCM encrypted store + user-only key
```

凭据不得写入 `projects.json`、普通日志、升级接力元数据或 URL。

## 9. 权限模型

桌面产品把文件权限与命令执行档位组合显示：

| 桌面模式 | 文件权限 | Shell 档位 |
|---|---|---|
| 只读 | `read_only` | `safe` |
| 项目工作区 | `workspace` | `developer` |
| 完全访问（默认，危险） | `system` | `full_system` |

完全访问首次实际启动需要一次风险确认。`system/full_system` 下 workspace 只是默认 cwd/工作语境，允许显式绝对路径访问其它目录/盘符；`workspace/read_only` 仍以 canonical/real path containment 为硬边界。所有档位仍硬拦截已知格式化磁盘、破坏引导、递归删除系统目录等危险模式。

### 9.1 Call-stage permission enforcement

50-tool公开contract不随项目权限模式改变。Gateway在任何本地Gateway tool或CodexPro upstream调用前，按目标ProjectConfig执行第二道call-stage policy：

- `codexpro(action,args)`先unwrap为真实action与nested args，不能借supertool绕过。
- `system`允许公开contract中的本地能力，仍受危险命令硬拦截、UAC broker和CodexPro自身policy限制。
- `workspace`拒绝full-only工具，返回`feature_unavailable`。
- `read_only`对写文件、执行命令、改变task/long-run等mutation返回`permission_denied`；未在只读模式暴露的非mutation能力返回`feature_unavailable`。
- 无法确认目标项目权限模式时fail-closed。
- remote device不继承本机project mode；请求转到远端Hub后由远端Gateway按其项目配置执行同层enforcement。

### 9.2 Windows 高完整性 broker

Windows 的应用权限策略与 UAC token 是两层边界：普通 `asInvoker` 桌面进程不能自行获得管理员 token。当前维护线使用一次正规 UAC 授权 + Task Scheduler `RunLevel Highest / Interactive` 的本机 broker：

```text
MCPDevBridge UI / Gateway (standard token)
        │ 127.0.0.1 + protected bearer
        ▼
Elevated Broker (Highest token)
        ├── full-system CodexPro
        └── bounded short command/program execution
```

- 首次授权由受控当前可执行文件进入专用注册入口；不执行标准用户可写的临时提权脚本，不关闭/绕过 UAC。若桌面程序本身已经以管理员身份运行，则直接在当前高完整性进程注册，不再重复要求授权。
- Windows token 检测显式声明 64 位 WinAPI `argtypes/restype`，避免把 pointer-sized process handle 截断后把 High Integrity 误判为普通权限。
- 注册任务的 action 必须与当前 runtime 的 executable / arguments / working directory 一致；旧源码、旧安装目录或 staging task 视为 stale，重新授权覆盖，不能因“同名任务存在”就直接信任。
- Task Scheduler ACL：SYSTEM/Administrators 保持 full control，原始调用用户 SID 只获得 read + execute，允许标准 token 查询/启动已授权任务，但不能修改 elevated action 形成提权跳板。
- broker 只监听 loopback，认证值进入 Credential Manager/DPAPI；状态文件仅保存 pid/port/epoch/elevated，不保存 bearer。
- 请求体、输出、参数均有硬上限，broker 侧再次执行 execution-profile 危险命令检查。
- 多项目并发启动由 controller lock 串行 broker bootstrap，避免 Task Scheduler/state-file 竞态；任务 action 验证使用 5 秒有界缓存降低正常热路径开销。
- 若状态/认证通道丢失而同名计划任务仍在 Running，`ensure_running()` 在重新注册或 `/Run` 前返回专用 stale 状态，禁止删除 state 后在 `MultipleInstances=IgnoreNew` 下盲等超时。普通启动不得无确认终止旧管理员服务；桌面必须让用户明确选择“终止旧服务并重启 / 终止旧服务并改用项目工作区 / 取消”，而“停止所有项目”属于另一个显式全量回收入口。
- system → workspace 权限降级会替换 elevated manager，不能沿用旧高完整性进程。健康管理员服务中的旧 child 只按同一 `project_id` 精确停止；失联服务不在普通项目启停中整棵终止。新项目端口分配跳过真实 loopback listener，CodexPro/Windows-MCP spawn 前再次拒绝占用端口，避免 ghost child 或外部监听被误认成当前项目。
- 管理员权限设置失败时，桌面必须允许用户明确选择“以普通权限启动”，原子地把受影响项目保存为工作区权限后继续启动，避免高级权限故障把 MCP 自身锁死；此时不得仍显示为完全访问。
- broker 无运行 child 且空闲达到上限后退出；Windows Job Object 尽量保证 broker 异常结束时清理托管 CodexPro。

### 9.3 普通用户界面语言

工作台、项目设置与普通错误提示使用“管理员权限 / 访问码 / 连接服务 / 开发服务 / 完全访问”等用户概念。`UAC`、`broker`、`token`、`Gateway`、`full_system`、`IPC`、`CodexPro` 等实现词汇只保留在诊断、日志、代码和开发文档。工作台不展示项目内部端口列；底层端口仍作为兼容/高级配置存在。

### 9.4 一键分层连接诊断

桌面“诊断”对当前选中项目执行显式 project-bound 的真实 MCP selftest，而不是以 bootstrap/current workspace 猜测目标。selftest 依次验证 initialize、Streamable HTTP 通道、tools/list、fixed Hub contract 和只读 production canary；固定公开 contract 要求 exactly 50 tools、version=1、canonical fingerprint=`489cd30e8e8676ad382c752bed7c46ba9bc2f308fa2d98b2a699793d0c5ee780`。本机与公网模式分别做真实 MCP 调用，桌面汇总为12层：项目引擎、本机连接服务、公网入口、MCP初始化、公开工具数量、Schema指纹、只读调用、流式连接、访问授权、工作区连续性、最近自动恢复和ChatGPT会话边界。

普通界面只显示用户可行动状态；raw JSON、PID、TaskGroup/ExceptionGroup和凭据不进入普通诊断输出。只有本机与公网真实 MCP 都通过时，诊断才把“当前 ChatGPT 会话对象可能已失效”作为平台边界提示；任一可控本地层失败时保持未判定并要求先修本地链路。`ServiceCoordinator.recovery_snapshot()`仅提供最近本机/公网自动恢复距今时间和有界失败计数，用于说明最近恢复层，不暴露底层敏感状态。

## 10. 配置与数据目录

### Windows

默认：

```text
%LOCALAPPDATA%\LocalDevMCPBridge\
```

### Linux / SteamOS

默认：

```text
${XDG_CONFIG_HOME:-~/.config}/LocalDevMCPBridge/
```

只有**绝对路径**的 XDG override 有效；相对值按无效处理并回退。`LOCALDEV_MCP_CONFIG_DIR` 作为测试/显式 override 仍受支持。

主要文件：

| 文件 | 内容 |
|---|---|
| `config.json` | 全局桌面配置 |
| `projects.json` | 项目列表与非敏感项目配置 |
| `devices.json` | 非敏感设备目录 |
| `runtime.json` / `state.json` | 兼容运行状态 |
| `logs/` | 审计、transport/service supervisor 与按日 `flight-recorder-YYYY-MM-DD.jsonl` 诊断日志；均有脱敏/大小/留存边界 |
| `process_logs/` | 进程日志 |
| `upgrade-resume.json` | 升级接力的非敏感元数据 |
| `elevated-broker.json` | Windows broker 的非敏感运行态（pid/port/epoch/elevated） |
| `workspace-routes.json` | 有界、版本化的 deterministic workspace handle → project/root route cache；不含凭据 |

## 11. 平台层

`platform_support.py` 集中处理：

- Windows `creationflags` 与 POSIX `start_new_session`。
- `runtime_filename()` 的 `.exe` 差异。
- 文件管理器打开方式。
- XDG desktop/config/data 路径。
- Linux 默认用户安装根 `~/.local/opt/MCPDevBridge`。

Linux `install_linux.sh`：

- 支持 `--target-dir` 自定义位置。
- canonicalize 目标，拒绝 `/`、`$HOME`、`$HOME/.local` 等危险覆盖。
- 非空且不像 MCP DevBridge 的目录拒绝替换。
- Desktop Entry `Exec=` 对空格、引号、反斜杠、`$`、反引号和 `%` 做安全转义。
- 使用有效的 `XDG_DATA_HOME` / `XDG_CONFIG_HOME`，相对 XDG 值回退。

## 12. 发布与升级

### Windows

`scripts/build.ps1` 负责：

1. 版本一致性检查。
2. Python test/lint/typecheck。
3. CodexPro build/runtime 准备。
4. PyInstaller onedir。
5. runtime payload 校验。
6. Inno Setup installer。

`installer.iss` 使用 per-user 安装，并保留目录选择页。`live_upgrade.ps1` 通过 detached worker 在旧进程退出后安装、重启和恢复服务。若发起升级的桌面进程本身处于管理员权限，detached worker 必须以 `RunLevel Highest` 创建，否则普通 worker 无法终止 High Integrity 的旧进程并会导致安装器 `exit=5`；普通权限启动器则维持普通 worker，不额外请求管理员权限。dry-run 会同时记录 launcher/worker 的权限等级用于发布前验证。升级接力把冻结的原运行根交给单一桌面 worker 顺序恢复，只对失败根有界重试一次，共享 Hub 只启动一次；升级 worker 只有在 `upgrade-resume.json` 已消费、Gateway 已监听、每个原运行根均存在 root/port 匹配的 Node 进程，且 `system` 根的父进程是 `elevated=true` 的管理员服务时，才写入 `ok=true`。

### Linux

`scripts/build_linux.sh` 在 Linux build host 上执行：

1. 在任何 runtime/dependency/build 副作用前，使用 `scripts/check_release_version.py` 对拍 `pyproject.toml`、包 `__version__`、PyInstaller spec、Inno Setup 元数据与 `uv.lock`；任一缺失、格式错误或版本不一致立即 fail-closed。
2. portable runtime 准备。
3. CodexPro build。
4. pytest / Ruff / Pyright。
5. CodexPro smoke。
6. PyInstaller。
7. frozen headless smoke。
8. 生成 `MCPDevBridge-Linux-x86_64-<version>.tar.gz`。

`install.sh` / `live_upgrade.sh` 在用户目录安装和升级，不要求写系统根。

GitHub Actions 的 Linux baseline 为 Ubuntu 22.04；发布 tag 还必须保持与 Windows/Linux artifacts 的源 commit 一致。

## 13. 测试重点

全量测试之外，当前维护线的关键回归覆盖：

- C:/D: 多根同时 active；两个独立客户端分别持有 C:/D: workspace handle 时互不污染。
- 绝对路径最具体根、有效 handle + 相对路径定向、无 handle 相对路径唯一命中和歧义拒绝。
- `../` 与 symlink/junction 路径逃逸。
- Gateway local tool `cwd` 越界拒绝。
- stale CodexPro workspace handle 不覆盖本次明确路径。
- task affinity跨后续调用保持正确根；deterministic `ws_`跨Gateway/CodexPro restart恢复，unknown/stale root fail-closed。
- rehydrate read-only最多one-shot replay，有副作用调用不自动replay；same-handle并发使用single-flight。
- Stable Hub `tools/list` exactly 50、name唯一、fingerprint=`489cd30e...`，不受项目启停/权限/bootstrap影响。
- OAuth provider重建后旧access token连续、expiry/revoke/resource binding、refresh rotation、offline_access与business-only兼容。
- call-stage policy覆盖read_only/workspace/system、supertool unwrap、permission_denied/feature_unavailable和remote二次enforcement。
- Project/Gateway/Tunnel真实MCP canary只恢复责任层；CodexPro READY必须通过MCP而非仅日志/TCP。
- Gateway AsyncClient/response单owner关闭、initialize/ping安全retry、tools/call不盲retry、SSE保活边界。
- Bash active hard cap、spawn前task identity绑定、自动terminal receipt、Windows PATH-less shell/有界进程树取消与CodexPro graceful HTTP shutdown。
- scoped structured analysis只inventory请求root，cache key包含scope。
- 根盘无权限子目录 warning 后继续。
- nested Git discovery。
- 五列表格、单项目启停、全部项目启停、停止一个根不关闭其它根。
- system/full_system 跨工作区绝对路径访问；workspace/read_only 外部访问继续拒绝。
- Windows elevated broker 一次 UAC 注册、错误认证拒绝、并发启动、权限降级、stop/crash/high-integrity 非破坏性探针。
- Windows 自定义安装目录脚本配置。
- Linux XDG、AES-GCM、POSIX process/shell、安装/升级脚本与 release workflow。
- Regular Chat 实验资产不进入默认桌面/CLI/PyInstaller/build/installer，provider-neutral fixture与平台 blocker 文档保持可审计。

实际测试数量、构建产物和 SHA-256 只记录在 `进度验收.md`，不得把旧版本数字当成当前证据。
