# 项目架构.md

> 本文件描述 LocalDev MCP Bridge 的完整架构。任何修改架构的变更都必须同步更新本文件。
> 快速入口见 AGENTS.md；当前状态见 进度验收.md。

## 1. 系统总览

```
┌───────────────────────── 用户桌面 ─────────────────────────┐
│  [PySide6 桌面窗口]                                        │
│    ├─ 项目选择 / 权限模式 / 端口 / 测试命令配置             │
│    ├─ 启动/停止后端（子进程）                              │
│    ├─ 状态显示（端口、健康、进程数、日志）                 │
│    ├─ 获取访问令牌 / 显示固定 MCP URL                      │
│    ├─ 隧道控制（Named Tunnel / Quick Tunnel）              │
│    └─ “测试连接”按钮（selftest.py）                        │
└───────────────┬────────────────────────────────────────────┘
                │ 子进程(uvicorn, 仅 127.0.0.1:<local_port>)
                ▼
┌──────────────────── 后端（独立进程）────────────────────────┐
│  server_main.py → build_backend()                          │
│   ├─ MCPServer( mcp 2.0.0 ) + 34 个工具                    │
│   ├─ Starlette app（streamable_http_app /mcp）             │
│   ├─ /health、/control/status、/control/stop-procs        │
│   ├─ AuthMiddleware（纯 ASGI）                             │
│   ├─ AuthRateLimiter（10 次/5 分钟锁 5 分钟）              │
│   ├─ AuditMiddleware（JSONL 审计 + 脱敏）                  │
│   └─ 配置：%LOCALAPPDATA%\LocalDevMCPBridge\runtime.json  │
└───────────────┬────────────────────────────────────────────┘
                │ Cloudflare Named Tunnel（固定域名，例如 mcp.<user-domain>）
                ▼
┌──────────── 公网 MCP 客户端（ChatGPT / Gemini）─────────────┐
│   URL: https://mcp.<user-domain>/mcp                        │
│   Authorization: Bearer <token>                             │
└─────────────────────────────────────────────────────────────┘
```

关键约束（不可违背）：
- **MCP URL 永久固定**（Named Tunnel），不允许用会变化的 Quick Tunnel 作为验收路径。
- **认证与 URL 分离**：令牌不拼在 URL 中；由桌面程序生成、安全存储、客户端自行配置。
- **后端只监听 127.0.0.1**，公网可达性完全依赖 Cloudflare 隧道。

## 2. 模块职责（src/local_dev_mcp_bridge/）

| 文件 | 职责 | 关键接口 |
|---|---|---|
| `constants.py` | 路径、默认值、错误码 | `config_dir()/config_file()/rc_file()/...`（动态，尊重 `LOCALDEV_MCP_CONFIG_DIR`）；端口默认值集中 `DEFAULT_GATEWAY_PORT=8786 / DEFAULT_CODEXPRO_PORT=8787 / DEFAULT_WINDOWS_MCP_PORT=28731 / DEFAULT_LEGACY_BACKEND_PORT=8765`（`DEFAULT_LOCAL_PORT`/`GATEWAY_PORT` 为兼容别名） |
| `models.py` | 配置数据模型（pydantic） | `ProjectConfig`、`AppConfig`、`RuntimeConfig`、`TunnelState`；`PermissionMode/TunnelMode/AuthMode` Literal |
| `config_store.py` | JSON 持久化 + 功能探测 + 命令建议 | `load/save_app_config`、`upsert_project`、`load_runtime_config`、`detect_project_features`、`suggest_commands` |
| `secrets.py` | 令牌安全存储 | Windows Credential Manager（`win32cred`，用户名 `LocalDevMCPBridge`）；回退 DPAPI 加密文件 `secrets.dpapi.json`；`generate_token(256)`（urlsafe base64） |
| `audit.py` | 审计日志 | JSONL（`mcp-YYYY-MM-DD.jsonl`），`log_tool_call` 对 `content/old_text/new_text/patch/environment/command` 整值遮罩；14 天保留、50MB 轮转 |
| `shell.py` | 命令执行 | `run_command`（PowerShell）、`run_program`（参数数组）、`find_powershell`、`kill_process_tree`、`detect_binaries`、`powershell_version`；`ShellInfo/detect_shells/default_shell/get_shell_info`（默认顺序 pwsh > Windows PowerShell > cmd > Git Bash；WSL 只报告不自动选） |
| `processes.py` | 受管进程 | `ProcessRegistry`（增量日志、强制/正常停止、子进程树清理） |
| `permissions.py` | 权限策略 | `PermissionMode`：`read_only` / `workspace`(默认) / `system`；工具类别：file_read/file_write/git_read/git_write/command/process/info；自定义 `PermissionError` |
| `execution_profile.py` | 命令执行档位 | `safe`（保留项目内命令行为）/ `developer`（默认，首命令开发工具白名单 + 危险拦截）/ `full_system`（任意命令，需 `enforce_full_system_confirmation` 一次性确认）；`check_execution(command, profile)` 纯策略；硬拦截于所有档位：format/diskpart/shutdown/reboot/bcdedit/reg delete/msiexec 等，`rm -rf /`、`del /s C:`、`Remove-Item -Recurse C:\Windows` 类删盘命令 |
| `tools.py` | 34 个 MCP 工具 | `LocalDevTools` 类（`execution_profile`/`full_system_confirmed` 参数，run_command/run_program/start_process 经 `_guard_command` 检档）；`shell_self_test`（Shell + python/git/pytest/pyright 探测，pytest/pyright 走 `python -m`，规避 uv trampoline）；`read_tool/write_tool/destructive_tool` 注解工厂；路径边界 `_resolve`；`parse_unified_diff/apply_unified_patch`（支持无 `---/+++` 头的 hunk）；原子写；GB18030 回退读 |
| `server_factory.py` | 组装后端 | `build_backend(rc)` → `(MCPServer, Starlette, LocalDevTools)`；`AuthMiddleware`（纯 ASGI）、`AuthRateLimiter`、`AuditMiddleware`；`_register_tools` 注册 34 个工具 |
| `server_main.py` | 后端 CLI 入口 | `--config`、`--port`；`ensure_access_token()`、`port_in_use()`；`uvicorn.run(app, host=127.0.0.1)` |
| `standalone_server.py` | 简化 CLI | 单进程入口（不依赖桌面） |
| `selftest.py` | 自测 | `run_selftest(url, token)`：initialize → list_tools → get_workspace_info → list_directory → get_capabilities，返回 `SelftestResult.steps` |
| `backend_manager.py` | 后端子进程生命周期（已归档） | 旧桌面后端控制；桌面已切换到 `ServiceCoordinator`；保留 `backend_health`、`current/regenerate_access_token` 供兼容 |
| `engines.py` | 引擎进程层 | `CodexProManager`（node http.js，端口 8787）、`WindowsBridgeManager`（uvx windows-mcp，端口 28731）、`EngineManager` 基类（`_spawn/_fail/stop`）、`ProcessLog`（2k 行环形缓冲）、日志/命令脱敏、`port_listening/wait_port` |
| `tunnel_manager.py` | 隧道进程层 | `TunnelManager`：Cloudflare Named / Quick / ngrok / 仅本机；`_parse_public_url`（固定域名以连接确认为就绪）；`ConnectionMethod` 枚举；缺可执行文件/凭据 → `SpawnError` |
| `oauth_provider.py` | OAuth Provider（Phase 8） | `LocalOAuthProvider(OAuthAuthorizationServerProvider)`：单用户；DCR 单 scope；同意窗口/code 单次/refresh 轮换/RFC 8707；client 注册密文落盘，token 仅内存 |
| `gateway.py` | OAuth 网关（Phase 8） | `OAuthGateway`：uvicorn `127.0.0.1:8786`；SDK `create_auth_routes` + protected-resource 元数据 + `/consent` 同意页 + `/health` + `/mcp` 反向代理（OAuth 校验后换发引擎 Bearer / 旧版 Bearer 直通 / 回环匿名） |
| `app_state.py` | 服务协调状态机 | `ServiceCoordinator`：IDLE→STARTING→READY→ERROR；校验失败不动状态；隧道只起一次、URL 会话内不变（Quick 例外 `url_mutable`）；stop 顺序 Codex→Windows→隧道；`listen` 事件流 |
| `desktop_main.py` | 桌面 UI（PySide6 单窗口） | 六列多项目表（名称/路径/状态/端口/入口/操作）；行内启停按钮原位复用；全部项目并行启停；项目级配置自动/批量保存；ChatGPT/Gemini 客户端切换；四种连接方式；无滚轮下拉；组件状态/连接诊断；异步退出与升级接力 |
| `device_hub.py` | Multi-Device Hub | 设备配对、心跳、在线状态、远端 MCP URL / 凭据解析 |
| `help_content.py` | 新手帮助内容 | 搜索式使用手册、上下文帮助、连接方式推荐 |
| `project_manager.py` | 多项目管理器（Phase 9/10） | `ProjectManager`（catalog + 生命周期）、`ProjectUnit`（每项目独立 CodexProManager + WindowsBridgeManager）、`ProjectView`；projects.json CRUD + Codex/Windows/Gateway 独立端口分配；`enabled` 仅兼容旧配置，桌面不再自动恢复 |

## 3. 多项目架构（Phase 9）

```
MCP DevBridge 桌面
  ├── ProjectManager
  │     ├── ProjectUnit A (root=D:\AStockMultiAgent, port=8787)
  │     │     ├── CodexProManager  ← node http.js --root <A> --port 8787
  │     │     └── WindowsBridgeManager (可选, port=28731)
  │     └── ProjectUnit B (root=D:\meme, port=8788)
  │           ├── CodexProManager  ← node http.js --root <B> --port 8788
  │           └── WindowsBridgeManager (可选, port=28732)
  │
  └── MCP Backend (server_factory.py)
        └── WorkspaceCatalog (per-session 项目绑定)
              ├── session-GPT  → project A (mcp-session-id binding)
              └── session-Gemini → project B (独立绑定，互不干扰)
```

### 3.1 多项目配置流
1. 桌面启动时读 `config.json`（全局）与 `projects.json`（项目列表）。
2. 用户可添加多个项目；每个项目自动分配独立 CodexPro 端口（从 8787 递增）和 Windows 桥端口（从 28731 递增）。
3. 桌面不再读取 `enabled` 自动恢复；该字段仅为旧配置兼容。单项目启停只保留每行“操作”按钮；状态轮询原位更新该按钮与文本项，不销毁正在交互的 QWidget。
4. 一个项目可作为完整公网入口（Codex + Gateway + Tunnel），其它项目的 Codex/Windows 引擎仍可并行运行；“启动所有项目 / 停止所有项目”使用最多 8 路并行处理非入口引擎，并对失败项目单独记账。
5. 每项目独立保存权限、客户端、连接方式、域名、Git、Gateway/Codex/Windows 端口与 Gemini URI；Bearer/Cloudflare Token 在 SecretsStore 中按项目隔离。“保存为所有项目设置”只同步“连接与权限”卡片字段与 Tunnel Token，不覆盖 Git 或独立端口。

### 3.2 MCP Session 项目绑定
1. MCP 客户端每次连接获得唯一 `mcp-session-id`（Streamable HTTP 传输层管理）。
2. `WorkspaceCatalog` 维护 session-to-project 映射，通过 `ctx.headers["mcp-session-id"]` 区分会话。
3. `switch_workspace(project_id)` 仅影响当前 session，其他 session 保持原项目绑定。
4. GPT 和 Gemini 可以同时连接同一 MCP DevBridge 后端，各自操作不同项目，互不泄漏。

### 3.3 配置流（桌面 → 服务）
1. 桌面启动时读 `config.json`（全局）与 `projects.json`（项目列表），选择项目 → `ProjectConfig`。
2. 点击项目行或服务控制的“启动服务” → 从该 `ProjectConfig` + 项目级 SecretsStore 构建 `StartOptions`（workspace、权限、项目 Bearer、Windows 桥、连接方式、域名、隧道令牌、项目端口）。
3. `ServiceCoordinator.start()` 在后台线程编排：公网模式先建立指向 **Gateway 端口** 的 Tunnel，再启动/复用 CodexPro，随后启动 OAuth Gateway 与可选 Windows 桥；Local 直接连接 CodexPro，不要求 cloudflared。
   每步 `wait_ready`（固定端口 8787 / 28731，仅监听 127.0.0.1）；失败即清理并置 ERROR。
4. 公开 MCP 地址：固定域名连接 → `https://<host>/mcp`（URL 会话内一次确定，重启不变）；
   Quick Tunnel → 每次变化的临时地址；仅本机 → `http://127.0.0.1:8787/mcp`。
5. 桌面订阅 `coord.listen()` 状态事件刷新状态栏与日志表；`/health`（后端子进程）检测保留在 legacy 路径。


## 3.6 Phase 10 桌面与项目级路由

- 项目表状态来自 `ProjectManager/ProjectUnit`；作为公网入口的项目由 `ServiceCoordinator` 状态覆盖，QTimer 每 1 秒刷新，因此“已连接”不会再滞留为“未启动”。
- 每个项目拥有独立 Bearer。Gateway 接到直接 Bearer 时可反查项目并路由到其运行引擎；OAuth access token 通过 subject/workspace 选择项目，并替换成该项目对应的上游 Bearer。
- Gemini consent 在存在 workspace registry 时必须显式选择**运行中的项目**；空选择返回 400，未运行返回 409，均不会 approve/code。
- Cloudflare / ngrok / Quick 三种公网 Tunnel 的 origin 都是项目 Gateway；Local 才直接使用 CodexPro。
- `upgrade-resume.json` 只包含项目路径等非敏感接力元数据；新版本启动后从 SecretsStore 重新加载凭据并自动恢复服务。

### 3.7 v0.6 桌面交互层

- UI 状态不再使用全局 busy 开关；`_busy_project_ids` 只锁定正在进行启停动作的目标项目。
- 项目表与选中项目的可操作性均由目标项目自己的 `ProjectUnit / ServiceCoordinator` 状态计算：READY 可停止，IDLE/ERROR 可启动；其它项目的运行状态不参与禁用判断。
- 运行项目只锁定不能热修改的权限、连接方式与端口配置；复制、诊断、自测、日志、停止服务等仍保持可用。
- 顶层导航为工作台 / 项目设置 / 诊断 / 日志 / 设置；日志内部再区分进程 / 审计 / Gateway。
- `AppConfig.close_behavior`：`tray`（默认）表示标题栏关闭隐藏到系统托盘，`exit` 表示直接退出；标题栏最小化不受影响。托盘退出复用异步进程清理逻辑。
- 新安装没有项目时不构造机器相关的项目配置，也不显示伪造的 MCP 地址；项目目录始终来自用户主动添加。

### 3.4 工具调用流（客户端 → 工具）
```
MCP 客户端 → POST /mcp (streamable HTTP, SSE)
  → AuthMiddleware(纯 ASGI)：
      - /health 直接放行
      - 若触发限速锁 → 429
      - /control/* → 必须 Bearer
      - 本机 IP(127.0.0.1/::1/localhost) + allow_local_anonymous → 放行
      - 否则 require_public_bearer + Bearer 校验（constant-time 比较，支持 x-api-key）
  → AuditMiddleware（ServerMiddleware，记录 tools/call 的时长/成败/参数摘要）
  → MCPServer 分发到 LocalDevTools 方法
  → 返回值 str/dict 自动转 CallToolResult；ValueError/PermissionError → MCP error result
```

### 3.5 进程管理流
`start_process` → `ProcessRegistry.start`（记录进程、日志文件、增量输出）；`poll_process` 取增量；`stop_process`/`stop_all_managed_processes` → `kill_process_tree`（taskkill /T 或 psutil 遍历）。

## 3.8 v0.7 Multi-Device Hub

`device_hub.py` 新增 `DeviceRegistry`：本机身份、远端设备目录、一次性配对码、心跳在线状态和远端 URL 解析。`devices.json` 只保存非敏感元数据；远端 Bearer 与心跳凭据仍由 `SecretsStore` 加密保存。

路由从 v0.6 的“session → project”扩展为：

```text
ChatGPT / Gemini
  → 固定 Hub MCP URL
  → mcp-session-id
  → device_id（本机 / 朋友电脑）
  → 该设备自己的 project/workspace
  → MCP 工具
```

- 只有一台设备在线时自动选择，减少一次 `switch_device` 交互。
- 多台在线时本机优先；`devbridge_switch_device` 只改变当前 MCP session，不影响朋友的另一条会话。
- 远端设备自己的 `devbridge_list_workspaces / switch_workspace` 继续在远端执行，因此远端项目路径不会被错误解释成本机路径。
- 远端设备约每 15 秒上报一次当前公网 MCP URL；使用 Quick Tunnel 时随机 URL 变化会自动更新到 Hub。
- 主 Hub 仍建议使用固定 Cloudflare/ngrok 地址，因为 ChatGPT 只配置这一条固定 URL。
- Gateway 正式链路上的 `tools/call` 直接写 AuditLogger，修复旧审计页无记录问题。

`help_content.py` 保存面向普通用户的上下文帮助、搜索式手册和连接方式选择建议；`desktop_main.py` 的 `HelpButton` 使用非模态 QToolTip 浮层。

## 3.9 v0.7.2 默认异步命令任务

CodexPro 不再把命令分成“普通同步命令”和“大型异步任务”。公开 MCP 执行统一为：

```text
bash(command)
  → 校验工作区 / Bash session / 权限档 / 危险命令
  → BashTaskManager 启动后台 ChildProcess
  → 立即返回 task_id
  → get_task / wait_task / list_tasks 查看
  → 自然结束，或 cancel_task 结束完整进程树
```

- `bash` 的 MCP schema **不再包含 `timeout_ms`**，用户命令没有 execution timer。
- 删除 `start_task`；`bash` 本身就是唯一任务入口。Agent 不需要判断任务是“普通”还是“大型”。
- `wait_task` 默认等待 15 秒、单次最多 30 秒，只是短轮询窗口，不会限制任务执行；调用返回后后台任务继续运行。
- 编排看门狗：任务保存 `lastObservedAt`。连续 600 秒没有 `get_task / wait_task / list_tasks / cancel_task` 接续时，下一次快照返回 `orchestrationStale=true` 与 `resumeHint`；读取后刷新观察时间。该标记不修改任务状态，也绝不终止进程。
- `BashTaskManager` 按 workspace 隔离任务，任务 ID 使用 UUID，并复用 `assertBashSession / assertSafeCommand / PathGuard / shellInfo / makeEnv`。
- `BashTaskManager` 本身是 CodexPro **进程级单例**，不能绑定单个 `McpServer`/MCP session；否则下一次工具调用可能无法找回 task_id。任务内容仍按 workspace_id 隔离。
- stdout/stderr 使用有界滚动缓存，较早内容可被省略并标记 `truncated`，但不会因为日志过多终止任务。
- `cancel_task` 调用 `terminateProcessTree()`：Windows 使用 `taskkill /t /f`，其它平台终止进程组。
- 完成/失败/取消任务仅保存在当前 CodexPro 进程内，最长 24 小时、最多 100 条；运行任务不会被历史清理驱逐。软件关闭、升级或重启时不恢复正在执行的任务。

## 3.10 v0.8 单域名 Hub、可靠更新与自带运行时

- ChatGPT / Gemini 只连接主 Hub 的一个固定 MCP URL；Gateway 在同一 OAuth session 内按 `device_id → workspace` 路由，本机多项目与远端设备不再要求创建多个同域名 App。
- `device_hub.py` 的配对码仍为一次性、10 分钟有效；首次注册成功后，设备元数据写入 `devices.json`，Bearer/心跳凭据进入 `SecretsStore`。双方重启后直接恢复目录和心跳，**不重新配对**。
- 首次注册响应可能丢失，因此 `(pair_code, device_id)` 的成功 receipt 在进程内保留 **1800 秒**；窗口内重复注册返回原 peer secret，不产生第二台设备或第二份凭据。
- 桌面使用 `QLockFile` 保证同一 Windows 用户单实例；升级 worker 在安装前清理全部旧 `MCPDevBridge.exe` 进程树，安装后重建唯一桌面快捷方式并恢复升级前项目服务。
- `update_manager.py` 启动后检查 GitHub Release，之后每 **12 小时**检查；安装包下载同时校验 Release asset 大小和可用的 SHA-256 digest。
- 正式 Windows 包不依赖用户预装 Node.js/uv：构建阶段准备固定版本 Node.js 22.19.0、uv/uvx 0.11.25 与 cloudflared 2026.7.3；PyInstaller 将 Node/uv/uvx 放入 `_internal/runtime`，cloudflared 放在主程序旁。`engines.py` 优先解析私有运行时，源码环境才回退 `.tools` / PATH。
- 软件启动执行非阻塞运行组件预检；“诊断”页对 Node/uvx 缺失给出重装提示。Windows-MCP 仍为可选组件，由内置 uvx 按 `windows-mcp==0.8.2` 首次联网获取到用户缓存，不做系统级静默安装。
- `scripts/live_upgrade.ps1` 随 PyInstaller 产物内置，以 detached scheduled task 在退出旧进程后静默安装、重启并验证目标项目端口；`-DryRun` 用于发布前验证 detached worker 链路。

## 4. 认证与安全模型
## 4. 认证与安全模型

### 4.1 认证
- 认证模式（`AppConfig.auth_mode`）：`bearer`（默认）/ `anonymous`。
- 令牌：256-bit urlsafe base64，存 Windows Credential Manager（用户名 `LocalDevMCPBridge`，条目 `LocalDevMCPBridge/AccessToken`）；无凭据管理器时 DPAPI 加密回退文件。
- 校验：`Authorization: Bearer <token>` 或 `X-API-Key: <token>`；`hmac.compare_digest` 防时序攻击。
- **公网请求判定**：存在 `cf-connecting-ip` / `x-forwarded-for` 且非本机 → 视为公网请求，必须 Bearer。
- **限速**：同一客户端（按 IP）10 次认证失败 → 锁 5 分钟（429 + Retry-After）；窗口 5 分钟。
- **控制面**：`/control/*` 一律 Bearer（即使本机）。

### 4.2 DNS rebinding 防护（重要！）MCP SDK 2.0.0 的 `streamable_http_app()` 在 host 为 localhost 时**自动启用** DNS rebinding 保护，
`allowed_hosts` 仅含 `127.0.0.1:*`、`localhost:*`、`[::1]:*`。
经 Cloudflare 隧道到达的请求 Host 为公网域名（如 `mcp.example.com`），会被默认配置拒绝。
**Phase 4 必须在调用 `streamable_http_app` 时显式传 `transport_security`**：
- **方案 B（已实现，2026-08-08）**：`server_factory.build_transport_security(public_hostname)` — `enable_dns_rebinding_protection=True` 保持开启，
  `allowed_hosts` 恒含回环 `127.0.0.1:*` / `localhost:*` / `[::1]:*`，并追加公网域名（精确 + `域名:*` 两种形式，自动剥除 scheme/路径）。
  `RuntimeConfig.public_hostname`（`state.json`/`standalone --public-hostname`）注入；Host 门禁单元测试 12 项覆盖（见 `tests/test_transport_security.py`）。
- 方案 A（`enable_dns_rebinding_protection=False`）未采用：会完全关闭防护，收益低。
（真机公网验证为剩余步骤，见 进度验收.md。）

### 4.3 脱敏
- 审计日志不落完整内容：工具调用参数中 `content/old_text/new_text/patch/environment/command` 整值遮罩（`<redacted>`），名称含 `KEY/TOKEN/SECRET/PASSWORD/COOKIE/AUTH` 的字段不记录值。
- 环境变量读取工具：名称含上述标记时只返回 `<redacted>`。

### 4.4 权限模式（`PermissionMode`）
| 模式 | 允许 | 工具类别 |
|---|---|---|
| `read_only` | 只读 + info | file_read、git_read、info |
| `workspace` | 项目内一切 + 受控命令 | 全部（路径仍限制在 workspace 内） |
| `system`（桌面默认） | 全部（含项目外） | 全部，无路径限制；第一次实际启动仍需一次性风险确认 |

高风险：`delete_path`、`git_restore`、`git_push`、`stop_all_managed_processes` 标记 `destructive`。
桌面在用户首次选择 `system` 模式时弹一次性风险确认（`first_system_risk_accepted`）。

### 4.4.1 Shell 命令执行档位（`execution_profile.py`）

**权限模式**（什么能碰）与**执行档位**（命令怎么跑）在后端是两条独立维度，但 **v0.2.0 桌面端已合一**：
只读=read_only+safe、项目工作区=workspace+developer、完全访问=system+full_system（**桌面默认完全访问**；UI 无独立档位选择，
`--execution-profile` CLI 仍独立）。执行档位对
`run_command / run_program / start_process`（本机工具）与 CodexPro 引擎的 bash 工具生效：

| 档位 | 含义 | CodexPro 引擎 |
|---|---|---|
| `developer`（默认） | 首命令 ∈ 开发工具白名单（pytest/pyright/ruff/git，git 子命令完整/npm/uv/python/tools），危险命令硬拦截 | `CODEXPRO_BASH_MODE=developer`（fork 新增模式） |
| `safe` | 保留原项目内命令行为 | `CODEXPRO_BASH_MODE=safe`（原有 allowlist） |
| `full_system` | 任意命令；工具边界要求 `enforce_full_system_confirmation`，桌面首次启用一次性确认（`AppConfig.full_system_risk_accepted`），CLI 用 `--confirm-full-system` | `system` 权限模式下引擎本就 `full` |

危险拦截对所有档位生效（硬性拒绝，白名单无法绕过）：
`format <盘符>`、`diskpart`、`shutdown`、`reboot`、`bcdedit`、`reg delete`、`Remove-Item -Recurse HK*`、
`msiexec`/`cipher`/`takeown`/`icacls` 等系统修改命令，以及递归删除指向盘根/系统目录的
`rm -rf /`、`del /s C:`、`Remove-Item -Recurse C:\Windows`、`rd /s C:\Program Files`。
判定为保守设计：宁可误报（提示切换档位）也不放过任何删盘/格式化模式。

Shell 选择：`default_shell()` 顺序 pwsh > Windows PowerShell > cmd > Git Bash；
**WSL Bash 永远不自动选**（只能显式选择），避免引擎落入 Linux 工具链反而运行不了 Windows 项目脚本。
`get_shell_info()` 向 GUI / `shell_self_test` / AI 提供全部已检测 Shell 与可执行性。

### 4.5 OAuth 网关（Phase 8，公网 Gemini 路径）

```
Gemini 客户端 ──OAuth 2.1──> https://mcp.shiningsugar.shop/mcp
                                    │ Cloudflare Named Tunnel（路由 Service URL → localhost:8786）
                                    ▼
                      OAuthGateway（uvicorn 8786，仅 127.0.0.1）
                        ├─ SDK create_auth_routes：/.well-known/oauth-authorization-server
                        │    /authorize /token /register /revoke + protected-resource 元数据
                        ├─ /consent 浏览器同意页（允许/拒绝，窗口 10 分钟）
                        └─ /mcp 反向代理 ──> http://127.0.0.1:8787（CodexPro 引擎）
                             · OAuth access token 校验通过 → 换发引擎 Bearer（secrets 同源）
                             · 旧版 ChatGPT Bearer 常量时间比对 → 直通（引擎自验）
                             · 回环源（127.0.0.1/::1/localhost）→ 匿名放行（本机直连不影响）
                             · 其余 → 401（WWW-Authenticate 含 resource_metadata）
```

- 与 engine layer 的 Bearer 认证**正交**：OAuth 层在网关解决，引擎认证不变；`ServiceCoordinator` 仅在
  非 LOCAL 模式、codex 就绪后启动网关（8786 端口探测就绪），失败即清理；停止顺序 Codex→桥→网关→隧道。
- 密钥零落盘：授权码/同意/access 仅内存；client 注册与 refresh 经 SecretsStore（DPAPI/凭据管理器）密文；
  网关不输出审计日志。
- 证书/域名延续现状（Cloudflare 边缘 TLS），DNS rebinding 由边缘域名维度天然规避（网关只面对隧道回环端口与公网域名）。

## 5. 配置与存储位置

| 文件 | 内容 |
|---|---|
| `%LOCALAPPDATA%\LocalDevMCPBridge\config.json` | AppConfig（全局） |
| `%LOCALAPPDATA%\LocalDevMCPBridge\projects.json` | 项目列表 + 每项目非敏感 UI 配置/端口/客户端/Git/Gemini URI |
| `%LOCALAPPDATA%\LocalDevMCPBridge\runtime.json` | 当前会话 RuntimeConfig（后端读取） |
| `%LOCALAPPDATA%\LocalDevMCPBridge\state.json` | TunnelState 等运行状态 |
| `%LOCALAPPDATA%\LocalDevMCPBridge\secrets.dpapi.json` | DPAPI 加密回退；访问令牌与 Cloudflare Token 使用项目级键，不以明文进入 projects.json |
| `%LOCALAPPDATA%\LocalDevMCPBridge\logs\mcp-*.jsonl` | 审计日志 |
| `%LOCALAPPDATA%\LocalDevMCPBridge\process_logs\` | 受管进程日志 |
| `%LOCALAPPDATA%\LocalDevMCPBridge\backups\` | 保留 |

> 所有路径可通过环境变量 `LOCALDEV_MCP_CONFIG_DIR` 覆盖（测试隔离用）。
> 注意：`constants.py` 已改为动态函数（`config_dir()` 等），**不要**在 import 期缓存为常量（曾导致测试隔离失效）。

## 6. 33 个 MCP 工具

| 类别 | 工具 |
|---|---|
| 项目/系统 | `get_workspace_info`、`get_capabilities`、`get_system_info`、`get_environment_variable` |
| 文件（只读） | `list_directory`、`stat_path`、`read_file`、`read_files`、`find_files`、`search_text` |
| 文件（写） | `write_file`（原子写）、`replace_text`（expected_count 校验）、`apply_patch`（unified diff 校验）、`make_directory`、`copy_path`、`move_path` |
| 文件（危险） | `delete_path`（recursive 需显式） |
| Git | `git_status`、`git_diff`、`git_log`、`git_branch`、`git_add`、`git_commit`、`git_checkout`、`git_restore`、`git_push` |
| 命令 | `run_command`（PowerShell）、`run_program` |
| 进程 | `start_process`、`poll_process`、`stop_process`、`list_managed_processes`、`stop_all_managed_processes` |

路径边界：所有文件操作经 `_resolve()`，必须落在 workspace 内（`workspace` 模式），否则 `PermissionError`（自定义类，tools.py 需显式导入，勿用内建）。

## 7. 测试架构

- 全部走 pytest（`asyncio_mode=auto`），`tests/conftest.py` 提供 workspace 夹具。
- 单元测试直接构造 `LocalDevTools`；**集成测试用真实后端子进程**（in-process uvicorn 的 SSE 不兼容，已验证弃用），
  通过 `server_main` CLI 拉起、轮询 `/health` 就绪，再用 MCP `streamable_http_client` 客户端测。
- 当前 60 项全绿，分布在：test_fs(27)、test_commands(11)、test_git(3)、test_config(8)、test_mcp_integration(9)、test_selftest(2)。
- 运行：`$env:PYTHONIOENCODING="utf-8"; .venv\Scripts\python.exe -m pytest tests/ -q`

## 8. 已踩过的坑（避免重蹈）

1. **`BaseHTTPMiddleware` 会缓冲 SSE** → 认证中间件必须纯 ASGI（直接转发 scope/receive/send）。
2. **Mount 包裹 MCP app 会丢 lifespan** → 追加路由用 `mcp_app.add_route(...)`。
3. **`import time 的模块级 `_START_TIME`**：server_factory 里健康检查 uptime 用模块级时间戳（进程生命周期）。
4. **`parse_unified_diff` 曾不支持无文件头的 hunk**（测试暴露），已支持 `@@` 直接开头。
5. **PermissionError 类混淆**：`tools.py` 必须从 `permissions.py` 导入自定义 `PermissionError`，否则测试/客户端捕获不到。
6. **`AuditLogger` 自定义目录需先 `mkdir`**，否则首次写日志 FileNotFoundError。
7. **Windows 无 pwsh**：`find_powershell` 返回 `powershell.exe`；命令测试注意编码（UTF-8 重定向）。
8. **`local_port` 默认值不一致（已解决）**：端口已统一由 `constants.DEFAULT_*_PORT` 管理；Phase 10 进一步把 Gateway/Codex/Windows 端口按项目独立分配，并批量迁移旧项目的 0 值端口。
