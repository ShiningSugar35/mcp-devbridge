# MCP DevBridge v0.8.7：502 / 后台闲置断流根因与生产稳定性方案

> 状态：2026-08-25 根因冻结，作为 v0.8.7 开发、审查、测试与发布的工作依据。
> 维护基线：`release/v0.8.7`（由 `release/v0.8.6` 分出）；v0.9+ 历史仅保留，不回合并多 Agent runtime。
> 目标：解决“桌面程序仍在、项目 UI 仍显示可用，但后台闲置约 1–2 小时后 ChatGPT MCP 调用持续 502 / execution endpoint unreachable”的真实数据面故障，并使系统在 Windows 11 Modern Standby、网络短暂抖动、CodexPro/Gateway 子进程异常下能够自动恢复，同时从架构上移除会自然过期的上游 MCP protocol session。

## 1. 现场证据与故障分层

### 1.1 用户可见症状

一键连接诊断同时出现：

- 项目目录可访问；
- 项目引擎运行组件已就绪；
- Hub access token、固定公网域名、Cloudflare tunnel credential 均已准备；
- UI 显示项目服务正在运行；
- 但“**MCP 实际调用没有完全通过**”。

这说明配置面和进程表象不能代表 MCP 数据面真的可用。

### 1.2 本机运行态证据（2026-08-25）

1. `MCPDevBridge.exe` 主进程自 14:59:55 持续存在；故障不是简单的桌面主程序退出。
2. 当前受管 `cloudflared.exe` 与两个 CodexPro `node.exe` 的进程启动时间为约 22:57:52–22:57:54，明显晚于主进程，证明主程序生命周期与执行子进程生命周期可以发生分离。
3. `transport-health-2026-08-25.jsonl` 在约 22:50–23:01（UTC+8）连续记录：本地 `/health` 200、公网 `/health` 200；然而用户侧真实 MCP 调用在这一阶段此前已失败。现有健康检查存在确定的**假阳性**。
4. Windows System 事件日志显示本机支持 **S0 Low Power Idle / Modern Standby（Connected）**，并在：
   - 21:45:48 因 `Idle Timeout` 进入 Connected Standby；
   - 22:44:43 因 `SetThreadExecutionState` 退出 Connected Standby。
   这一约 59 分钟的系统后台低功耗窗口与“放后台一两个小时后失联”的用户复现条件高度一致。
5. 当前网络接口是 `Intel(R) Wi-Fi 6 AX201 160MHz`，当前活动电源计划为 `HP_Master_Performance`。因此“普通高性能电源计划”本身并不能阻止 Modern Standby 的 idle transition。

## 2. 代码级根因冻结

### RC-1（P0）：EngineState 是缓存值，子进程死后仍可永久显示 READY

`src/local_dev_mcp_bridge/engines.py` 当前 `EngineManager.state` 只返回 `_state`；只有 `wait_ready()` 启动阶段会检查 `BaseEngineProcess.is_running`。一旦 CodexPro 被标为 `READY`，后续 `node.exe` 意外退出并不会把状态转成 `ERROR`。

`ProjectUnit.state` 又直接信任 `self.codex.state`；`MainWindow._lookup_workspace()` 只要看到 `unit.state == READY` 就继续把该项目暴露给 Gateway。

结果：

`node.exe 已死 -> state 仍 READY -> Gateway registry 仍返回 127.0.0.1:<project-port> -> httpx 连接失败 -> Gateway 明确返回 502 {"error":"upstream_unreachable"}`。

这是用户“UI 仍显示运行但实际 MCP 502”的直接代码路径。

### RC-2（P0）：现有 `/health` 只证明 Gateway 壳子活着，不证明 MCP 数据面

`OAuthGateway._health()` 当前无条件返回：

`{"status":"ok","app":"oauth-gateway",...}`

`ServiceCoordinator` 每 15 秒只探测本地和公网 `/health`。因此：

- Gateway 线程存活；
- cloudflared 到 Gateway 可达；

就会得到 200/200，即使 Gateway 后面的 CodexPro 已死或 MCP session 已失效。

这与现场“连续 health 200 + 实际 MCP 失败”完全吻合。

### RC-3（P0）：Gateway 的 upstream MCP session 会在“正常闲置”与进程重启后陈旧

Gateway 会把一个 ChatGPT client session 映射到各 CodexPro engine 的独立 upstream session。`_ensure_upstream_session()` 当前只要缓存中有映射就直接返回；`_forget_upstream_session()` 已存在却没有任何调用方。

进一步核查发现，CodexPro `config.ts` 把 `CODEXPRO_HTTP_SESSION_TTL_MS` 的**默认值明确设为 30 分钟**，`http.ts` 每分钟最多执行一次 prune，超过 TTL 的 transport 会从内存 map 删除。因此即便 Windows、Wi-Fi、Node 和 Cloudflare 全部健康，只要 ChatGPT 对这个 upstream 超过约 30 分钟没有活动，Gateway 缓存的旧 session 也会确定性过期。用户描述的“放后台一两个小时后再回来”天然跨过这一阈值。

CodexPro 重启同样会立即丢失内存中的 session map。两种情况下，Gateway 都会把旧 `mcp-session-id` 发给 CodexPro，后者明确返回 HTTP 404 / `Session not found`。当前 Gateway 没有在这一信号上重建 upstream session 并重放被拒绝的原请求。

所以 v0.8.7 最终不采用“把 TTL 粗暴拉到 24 小时”，也不采用“404 后透明重建旧 stateful session”作为生产主方案。用户明确要求直接迁移 stateless/hybrid 后，本轮将 CodexPro 升级到 MCP TypeScript SDK v2，并以 `createMcpHandler(..., { legacy: "stateless" })` 服务 `/mcp`。旧 `Mcp-Session-Id → transport` Map、30 分钟 TTL、prune timer 和 Gateway upstream-session 虚拟化全部删除。这样 RC-3 从“自动续租”升级为“消除可过期 protocol session”。

### RC-4（P1）：Gateway 自身不健康时 monitor 只记日志，不自愈

`ServiceCoordinator._transport_health_tick()` 在本地 Gateway `/health` 失败时只写 `gateway_unhealthy` 并 `return`，没有重建 Gateway。若 Gateway 线程因异常退出，系统可以长期失联，必须用户手动停止/启动。

### RC-5（P1）：Windows 运行方式没有声明后台服务的 system-required power request

Microsoft 明确说明 `SetThreadExecutionState(ES_CONTINUOUS | ES_SYSTEM_REQUIRED)` 适用于网络管理、备份代理等需要在电源管理机器上持续工作的应用；它允许显示器熄灭，但阻止系统因为“用户无输入”自动进入睡眠。当前 MCP DevBridge 没有此声明。

现场 Kernel-Power 506/507 已证实机器确实反复进入 Connected Standby。因此 v0.8.7 必须在**公网 Hub 正常运行期间**持有 system-required request；Hub 停止时清除。不得使用 `ES_DISPLAY_REQUIRED` 强制亮屏，也不得使用 `ES_AWAYMODE_REQUIRED`。

> 边界：Windows API 无法、也不应阻止用户明确按睡眠键或合盖触发的显式休眠；该功能针对本事故中的 `Idle Timeout` 自动待机。

### RC-6（P2）：Chrome tab freeze / Memory Saver 是前端流风险，但不是 502 主根因

Chrome 官方说明 Memory Saver 会 deactivate 不活跃 tab；Page Lifecycle 的 `frozen` 会暂停可冻结 JS task，`discarded` 会卸载页面。它可以导致 ChatGPT 网页端 UI stream、计时器或页面状态被暂停/重载。

但 OpenAI 官方当前文档明确：ChatGPT 连接的是 **remote MCP server**；本地/private developer machine 不能被 ChatGPT 直接连接，需要远程 MCP 或 Secure MCP Tunnel。因此 Chrome tab lifecycle 不是 `MCPDevBridge.exe` / `node.exe` / `cloudflared.exe` 的进程管理器，不能解释本机 Gateway 返回的 `upstream_unreachable` 502。

结论：Chrome 是“用户看到流中断”的次级因素，Windows/本地服务数据面才是本次 502 的主修复对象。

## 3. 权威资料交叉验证

### OpenAI

- Developer mode and MCP apps in ChatGPT：ChatGPT 连接 remote MCP server；本地/private/on-prem developer machine 需使用 Secure MCP Tunnel 等远程连接方式。
  https://help.openai.com/en/articles/12584461-developer-mode-and-full-mcp-connectors-in-chatgpt

### Cloudflare

- Tunnel Troubleshooting：Tunnel 下的 502 `Unable to reach the origin service` 表示 tunnel 自身仍连着 Cloudflare，但 `cloudflared` 无法到达本地 origin；典型原因包括 origin 进程停止、端口/协议错误。
  https://developers.cloudflare.com/tunnel/troubleshooting/
- Tunnel Monitoring：Tunnel 的 Healthy 只表示 `cloudflared <-> Cloudflare`，**不代表 cloudflared 能访问内部 service**。
  https://developers.cloudflare.com/tunnel/monitoring/
- Routing：cloudflared 通过多条 outbound connection 接入 Cloudflare，提高 tunnel connector 链路可靠性。
  https://developers.cloudflare.com/tunnel/routing/

### Microsoft

- SetThreadExecutionState：后台网络管理类应用应通过 `ES_SYSTEM_REQUIRED | ES_CONTINUOUS` 表达持续执行需求。
  https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-setthreadexecutionstate
- Windows Wi-Fi troubleshooting：Microsoft 将关闭 Wi-Fi adapter 的“Allow the computer to turn off this device to save power”列为断连排障步骤。
  https://support.microsoft.com/en-us/windows/fix-wi-fi-connection-issues-in-windows-9424a1f7-6a3b-65a6-4d78-7f07eee84d2c

### Chrome / Google

- Chrome performance：Memory Saver 会 deactivate 不活跃 tab，并允许把关键站点加入 `Always keep these sites active`。
  https://support.google.com/chrome/answer/12929150
- Page Lifecycle API：frozen/discarded 页面会暂停/停止 JS task。
  https://developer.chrome.com/docs/web-platform/page-lifecycle-api

## 4. 生产落地方案比较

| 方案 | 优点 | 局限 | 本轮裁决 |
|---|---|---|---|
| A. 当前 Windows Desktop + Cloudflare Named Tunnel，补齐数据面 supervisor / power guard / session recovery | 不改变用户现有域名与使用方式；改动最小；可直接修复已证实根因 | 单台电脑仍是物理单点；显式关机/合盖无法保证在线 | **v0.8.7 主方案** |
| B. OpenAI Secure MCP Tunnel | 官方推荐 private/dev machine 使用；减少公开暴露面 | 目前产品/套餐/Workspace 关联仍有适用性边界；不能替代本机 origin 的进程与电源稳定性 | P2 可选评估，不阻塞本轮 |
| C. cloudflared Windows Service / 多 replica | connector 可开机自启；多 connector 可提高 tunnel 层 HA | 当前桌面产品需动态持有 tunnel credential/端口和升级生命周期；多 replica 仍共享同一笔记本、电源、Wi-Fi，不能修复死 CodexPro | 暂不替换现有 supervisor；保留未来 always-on host 场景 |
| D. always-on VPS / 家用服务器作为长期 origin | 可获得最高可用性，不依赖笔记本睡眠 | 成本、远程文件访问、安全边界显著增加 | 仅当目标提升到 24x7 / 99.9%+ 再做 |

## 5. v0.8.7 实施设计

### 5.1 Engine liveness：READY 必须由真实子进程存活约束

- `EngineManager.state` 在 `STARTING/READY` 时若已存在的 `_proc.poll()` 显示退出，立即转 `ERROR`。
- 记录 `pid / exit code / timestamp / event`，解决当前 `codexpro.log` 无时间戳、无法对齐事故的问题。
- `ProjectUnit.state` 因此不再产生“node 已死但项目 READY”的假状态。

### 5.2 Project data-plane supervisor + 自动恢复

- 对“用户明确启动且尚未明确停止”的 project 维护 desired-running runtime 集合；不复活用户已经手动停止的项目。
- 轻量周期 health probe 直连 `http://127.0.0.1:<codex-port>/healthz`，验证：
  - process alive；
  - HTTP 2xx；
  - `ok=true`；
  - `defaultRoot` 与 project root 一致。
- 连续失败达到阈值后，只重启受影响 ProjectUnit，不重启其他项目。
- restart 使用 cooldown/backoff，防止 crash loop。
- 恢复日志必须包含 project id、旧 pid、新 pid、原因、成功/失败，不记录 token。

### 5.3 MCP SDK v2 stateless / hybrid

- CodexPro 由 `@modelcontextprotocol/sdk` v1 迁移到拆包式 SDK v2 + Zod 4。
- `/mcp` 使用 `createMcpHandler(factory, { legacy: "stateless" })` + Node adapter；现代 2026-07-28 流量 per-request stateless，2025-era 流量也走官方 stateless fallback。
- 删除 `CODEXPRO_HTTP_SESSION_TTL_MS`、`CODEXPRO_MAX_HTTP_SESSIONS`、session transport map、prune timer 和 session-specific EventStore wiring。
- Gateway 永远剥离外部 `Mcp-Session-Id` 后再请求 CodexPro，并过滤上游返回的 session header；不再对 404/`Session not found` 自动重放工具调用。
- workspace handle、后台 task、durable long-run 属于进程级业务态；selected workspace / last-shown checkpoint 按有界 client-affinity 隔离，不作为鉴权或根路由依据。

### 5.4 Gateway 自愈

- 本地 `/health` 失败单独计数，不再重置后直接放弃。
- 连续失败达到阈值后，**仅重建 Gateway**；保留健康的 ProjectUnit 与 Tunnel，降低爆炸半径。
- Gateway 恢复后无需恢复任何 upstream MCP session map；CodexPro 数据面为 stateless。
- 公网 `/health` 失败而本地健康时，保持 v0.8.6 已有 tunnel-only restart。

### 5.5 Windows public-Hub awake guard

- 新增 Windows-only dedicated thread：公网 Hub READY 时调用 `SetThreadExecutionState(ES_CONTINUOUS | ES_SYSTEM_REQUIRED)`；周期 refresh；Hub stop/failure 时由同一线程调用 `SetThreadExecutionState(ES_CONTINUOUS)` 清除。
- 不要求屏幕常亮，不修改用户全局电源计划，不自动改网卡驱动设置。
- Linux/macOS no-op，保持跨平台测试。

### 5.6 诊断与可观测性

一键诊断继续做真实 MCP `initialize -> list_tools -> get_workspace_info...`，同时补充层级信息：

- Gateway liveness；
- public transport liveness；
- active project engine process/healthz；
- actual MCP data-plane。

日志必须可区分：`engine_exit`、`project_probe_failed`、`project_restart`、`gateway_restart`、`tunnel_restart`。

## 6. 不在 v0.8.7 伪解决的问题

1. **ChatGPT host hard turn/message timeout**：本地 MCP 无法修改 OpenAI 宿主的硬超时；durable long-run 仍是跨 host interruption 的正确恢复机制。
2. **Chrome tab 被 freeze/discard**：建议 `chatgpt.com` 加入 Chrome `Always keep these sites active`，但服务端/本地工作不能依赖浏览器 tab 永远 active。
3. **用户显式合盖/睡眠/关机**：Microsoft API 不应覆盖明确用户意图；若要求 24x7，应迁移到 always-on host，而不是把笔记本伪装成服务器。

## 7. 验收标准（开发 / 审查 / 测试共同门）

### A. 根因回归

1. 测试中把已 READY 的 CodexPro subprocess 置为 exited 后，下一次 state 读取必须为 `ERROR`，不得保持 READY。
2. workspace registry 不得为 exited engine 返回可路由 upstream。
3. 任意外部 `Mcp-Session-Id` 都不得转发给 CodexPro；上游即使返回 session header 也不得泄漏，404/`Session not found` 不得触发工具调用重放。

### B. 自动恢复

4. 正常运行 project 的 CodexPro 被故障注入终止后，在 supervisor 阈值 + cooldown 边界内自动恢复，最终新 PID != 旧 PID；其它 READY project PID 不变。
5. 本地 Gateway health 连续失败达到阈值后能自动重建 Gateway；健康的 project engine 不被重启。
6. 本地健康而公网连续失败时仍仅 restart Tunnel，不误重启 Gateway/Project。
7. crash-loop 时不得无限快速 spawn；必须有失败阈值与 cooldown，并留下可审计日志。

### C. Windows idle / Modern Standby

8. Windows 公网 Hub READY 时 `powercfg /requests` 或等价 API 证据应能看到该进程的 SYSTEM execution requirement；停止 Hub 后 requirement 消失。
9. 允许显示器自然熄灭；不得设置 `ES_DISPLAY_REQUIRED`。
10. 在 AC 电源、仅因用户 idle 的场景下，连续 2 小时后台运行不得因 Idle Timeout 进入会中断本地服务的待机；若测试环境时间不足，必须至少用 API 单测 + `powercfg /requests` 现场验证，并将 2h soak 作为发布后持续观察项明确记录，不能伪造已完成。

### D. 真实数据面

11. 一键连接诊断的 actual MCP call PASS。
12. 本地 `/health`、公网 `/health` 均 2xx；项目 `healthz` 2xx 且 root 匹配。
13. 从 ChatGPT 当前插件链真实执行 `server_config/open_workspace/read` 等 MCP tool 成功。

### E. 应用内更新

14. 当前版本落后多个正式版时必须直接选择最高可安装稳定版本，例如 `0.8.4 → 0.8.7`；不得逐级升级。
15. draft、prerelease、非稳定 SemVer tag 与缺当前平台资产的 release 必须被过滤。

### F. 工程质量与发布门

16. `git diff --check` PASS。
17. `uv run ruff check .` PASS。
18. `uv run pyright`：0 error。
19. `uv run pytest`：全量 PASS（平台预期 skip 除外）。
20. `third_party/codexpro` build + smoke PASS；`npm audit --omit=dev` 无 high/critical。
21. `uv lock --check` PASS；PowerShell / bash 发布脚本语法门 PASS。
22. Windows `scripts/build.ps1 -Version 0.8.7` PASS，installer payload 完整。
23. 独立代码审查必须覆盖：process lifecycle race、restart storm、stateless/client-affinity state leakage、secret logging、cross-root routing、shutdown race；发现问题必须先返工再重新 PASS。
24. 发布 commit push 到维护分支；annotated `v0.8.7` tag 与 GitHub Release / Windows+Linux assets 必须指向同一源提交，不改写 `v0.8.6` 或任何 v0.9+ tag。
25. 本机正式升级到 v0.8.7 后重启/热更新成功，桌面快捷方式目标正确，运行实例的版本、PID、public endpoint 与 actual MCP 自检均复验 PASS。

## 8. 发布裁决

只有 §7 全部可在本轮验证的门通过，且 2h soak 未被虚构时，才允许声称 v0.8.7 已完成开发、审查、测试和正式发布。若 GitHub CI、签名、网络或任何真实外部门失败，状态必须停在“未发布 / 被阻塞”，不得用本地结果替代。
