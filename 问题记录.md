# 问题记录

记录排障结论与关键决策（防止回退时重复踩坑）。

## 2026-08-09 · 端口配置统一（未发版）

**背景**：以下默认值不一致造成混乱：
- `constants.DEFAULT_LOCAL_PORT = 8765`
- `RuntimeConfig.local_port = 2865`（桌面/后端子进程）
- `ProjectConfig.local_port = 8765`（从未真正使用）
- gateway 8786、codex 8787、windows 28731 散落各模块硬编码。

**决策**：
- 端口默认值唯一来源：`constants.DEFAULT_GATEWAY_PORT / DEFAULT_CODEXPRO_PORT /
  DEFAULT_WINDOWS_MCP_PORT / DEFAULT_LEGACY_BACKEND_PORT`；`DEFAULT_LOCAL_PORT`、
  `GATEWAY_PORT`、`engines.CODEXPRO_LOCAL_PORT / WINDOWS_BRIDGE_PORT` 保留为兼容别名。
- `AppConfig` 新增 4 个持久化端口字段（校验 1-65535）；`ProjectConfig.local_port` 移除
  （读取时忽略不报错）；`RuntimeConfig` 改用 `legacy_backend_port`（默认 8765），
  `local_port` 保留为兼容 property。
- 迁移：`RuntimeConfig.model_validator(before)` 自动把旧 `local_port` 值搬入
  `legacy_backend_port`；AppConfig 无端口字段时用默认值。
- UI：主界面「公网入口端口（Gateway）+ 检测端口 + 恢复默认 + Service URL + 复制」；
  「高级设置…」对话框改内部四端口；服务运行期间锁定输入；启动前预检，
  绝不静默换端口。

**踩坑**：
1. Windows PowerShell `Set-Content -Encoding UTF8` 会把 UTF-8 无 BOM 文件按 ANSI
   读入再写回 → 中文全部损坏；恢复后统一用专用工具编辑，禁止 PS 读写中文源码。
2. `AppConfig.version` 被误升为 2 破坏了既有测试（断言 ==1），保持 1（pydantic
   validator 前向迁移即可）。

## 2026-08-09 · Windows-MCP 版本漂移

0.8.5 起 `requires-python >=3.14`，机器无 3.14 时 `uvx` 解析失败；且工具集演进快。
锁定 `windows-mcp==0.8.2`（≥3.13，含 0.7.5 CORS/DNS-rebinding 安全补丁之后的
稳定版）。升级须实测后再手工改 `engines.WINDOWS_MCP_PINNED_VERSION`。

## 2026-08-09 · venv editable 指向旧目录

目录改名为 `mcp-devBridge` 后 `.venv` 的 `_editable_impl_*.pth` 仍指向旧
`local-dev-mcp-bridge/src`，子进程 `python -m local_dev_mcp_bridge.server_main`
报 `ModuleNotFoundError`（主进程不受影响，因 conftest 插入 src）。修复：
`uv pip install -e ".[dev,package]"`。

## 历史存档

（早期排障记录详见 `进度验收.md` 的「变更记录」章节。）