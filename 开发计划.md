# MCP DevBridge v0.8.8 开发计划

> 当前维护目标：v0.8.8。本文只保存**当前尚未完成的开发任务**；正式发布并完成运行终验后，本轮计划内容必须清空为“当前无待开发任务”，历史事实只进入 `进度验收.md` 与 `docs/en/CHANGELOG.md`。任何 Agent 不得复制旧版本测试数字或发布状态冒充本轮证据。

## 1. 需求分析

### 1.1 强制研发流程固化

从 v0.8.8 起，所有非纯问答/只读诊断开发任务统一执行：需求分析 → 根因定位 → 权威调研 → 架构方案/资源评估 → 计划与验收标准 → 实现与进度证据 → defect-first 代码审查/返工 → 专业测试/返工 → 发布上线/运行终验。该流程写入 `AGENTS.md`，不再依赖用户每次重复提示。

### 1.2 工作区漂移

现状已现场复现：

- 桌面运行根：`D:\`（project id `ecb6fea6`, port 8788）与 `C:\`（project id `e1860a65`, port 8791）。
- 当前聊天通过 CodexPro 成功 `open_workspace(D:\Environment\mcp)` 并可读写。
- 随后 `devbridge_get_current_workspace` 却返回 `e1860a65`（`C:\`）。

目标语义：

1. Gateway 是**共享无状态路由器**，不是“主工作区”。所有 READY 根地位平等。
2. 新聊天指定 `D:\xxx` 后，其模型拿到的 workspace handle/route handle 应稳定指向该上下文；另一聊天可独立指定 `C:\xxx`，互不污染。
3. 文件/Git/Shell 路由优先使用本次调用的 path/cwd/task/显式 handle；“当前工作区”只能是兼容性软锚点，永远不能覆盖更强证据。
4. 无任何强证据时允许确定性 bootstrap fallback，但该 fallback **不得被返回/持久化成 current workspace**。

### 1.3 完全访问的真实语义

`permission_mode=system + execution_profile=full_system` 必须真正表示：

- 绝对路径可以读写当前 workspace 根之外，包括其它盘符；
- 可以把任意存在目录作为 CodexPro workspace 打开；
- Shell/程序可把 workspace 外目录作为 cwd；
- 工作区模式/只读模式仍严格受根目录 containment、junction/symlink 与默认敏感路径规则约束；
- 完全访问仍保留危险命令硬拦截、审计、secret 脱敏和 loopback/token 边界。

### 1.4 Windows 管理员执行

应用内 `full_system` 只是授权策略，不能凭空把普通进程 token 提升为 Windows 高完整性 token。目标是在用户首次明确启用“完全访问”时完成一次 Windows UAC 授权，此后 full-system 项目的 CodexPro/必要系统引擎可由受控高权限 broker 启动，不再每条管理员命令重复弹 UAC。

禁止方案：关闭 UAC、利用 UAC bypass、DLL 劫持、fodhelper/eventvwr 等绕过手法，或让公网直接访问无认证高权限端口。

### 1.5 清理与资源优化

- 清理旧任务临时文件、缓存、旧 release 下载中间件和已完成但仍写成“待办”的文档内容；不擅自删除无法确认归属的用户文件。
- 审计路由热路径、workspace registry、client affinity、扫描、轮询的时间/空间复杂度。
- 优先优化真实热路径与无界/大上界结构，不以微优化牺牲可读性或正确性。

## 2. 根因定位

### 2.1 漂移根因

`gateway.py` 当前存在两套状态：

- CodexPro 自己的 `WorkspaceManager.selectedWorkspaceIds`（按 upstream client-affinity）；
- Gateway 的 `_session_workspaces` 与 `_effective_workspace()`。

多根且调用无 path/task/handle 时，`_effective_workspace()` 调用 `_stable_workspace_id()`，按根路径稳定排序选择 bootstrap；当前 `C:\` 排在 `D:\` 前，因此得到 `e1860a65`。随后 `devbridge_get_current_workspace` 的正文虽然声称“未固定工作区”，`structuredContent.devbridge_workspace_id` 却返回已经 fallback 后的 `workspace_id`，造成插件/模型把 bootstrap C 误当作当前工作区。

此外，`open_workspace` 成功后 Gateway 只记录 `opaque workspace handle -> project root`，没有把现代 stateless 调用需要的显式 route handle 一并写回结果；旧 `Mcp-Session-Id` 客户端也没有在成功 open 时同步兼容软锚点。

现场还复现了第二个优先级缺陷：当调用已显式携带有效 CodexPro `workspace_id=ws_...`，但 `path` 是多个 READY 根都存在的相对路径时，Gateway 会在处理 opaque handle 前先抛“相对路径歧义”，使明确 workspace handle 失效。v0.8.8 必须让“明确 handle + 相对路径”在该 handle 所属根内解析；只有没有 handle/soft anchor 时才对多根相对路径报歧义。

### 2.2 完全访问根因

Python `LocalDevTools._resolve()` 已在 `system` 模式允许 workspace 外路径，但 CodexPro 启动环境仍固定：

- `CODEXPRO_ALLOWED_ROOTS=<project root>`
- `PathGuard.resolve()` 永远要求 target/real target/写入 parent 位于 `workspace.root`
- `WorkspaceManager.openWorkspace()` 永远要求新 workspace 位于 `allowedRoots`
- Gateway 本地 `run_command/run_program` 的 `_local_tool_cwd()` 也永远要求 cwd 在项目根内

因此 UI 的“完全访问”与真正文件系统边界不一致。

### 2.3 管理员执行根因

当前仓库没有 elevated broker/service/task runtime。`full_system` 只放开 CodexPro 的 bash/tool policy；`MCPDevBridge.exe`、Node/CodexPro 与 Windows-MCP 仍继承桌面进程的普通 token。Windows UAC 下普通进程不能无提示创建高完整性子进程，这是 OS 安全边界而非 MCP permissions 缺陷。

### 2.4 发布前 live probe 新发现（必须在最终 v0.8.8 修复）

1. 以管理员身份启动整个桌面程序时，`_require_start_confirmations()` 仍机械调用一次性注册流程，导致已经处于高完整性 token 的应用再次弹管理员确认；这是错误的状态机分支。
2. elevated process 注册计划任务后，标准 token 一度出现“高权限进程认为成功、普通进程却查询/运行不到任务”的现象。任务对象 ACL 必须显式给原始调用用户最小 `read + execute`，SYSTEM/Administrators 保持 full control，绝不能给普通 token 写任务定义的权限。
3. 管理员权限设置失败时，当前 UI 直接阻止所有 system 项目启动，形成“高级权限坏了 → MCP 起不来 → 无法远程修高级权限”的产品死锁。最终行为必须提供明确的普通权限恢复路径，且不得把降级状态伪装为“完全访问已生效”。
4. 普通工作台/错误弹窗暴露 `UAC`、`broker`、`Gateway`、`token`、`full_system` 等实现术语，违反面向普通用户的产品语言要求；这些词只允许留在诊断、日志或开发文档中。

## 3. 权威调研与架构依据（2026-08-26）

### MCP

1. Model Context Protocol, “The 2026-07-28 Specification”
   https://blog.modelcontextprotocol.io/posts/2026-07-28/
   结论：协议核心已无 session/handshake，`Mcp-Session-Id` 被移除；需要跨调用状态时应由工具 mint **explicit handle** 并由模型在后续调用显式传回。Header-based routing 是正式能力。
2. Model Context Protocol, “The 2026-07-28 Specification Release Candidate”
   https://blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/
   结论：stateless protocol 与 stateful application 分层，隐藏 transport sticky-session 不应重新成为应用路由事实源。

架构裁决：v0.8.8 不建立新的全局 main/current workspace，也不恢复 protocol sticky session；现代客户端以显式 `workspace_id`/`devbridge_workspace_id` 作为应用 handle，旧客户端可保留有界 soft affinity 兼容。

### Windows 管理员权限

1. Microsoft Learn, “Developing Applications that Require Administrator Privilege”
   https://learn.microsoft.com/windows/win32/secauthz/developing-applications-that-require-administrator-privilege
2. Microsoft Learn, “Elevated Task Model”
   https://learn.microsoft.com/windows/win32/secauthz/elevated-task-model
3. Microsoft Learn, “Administrator Broker Model”
   https://learn.microsoft.com/windows/win32/secauthz/administrator-broker-model
4. Microsoft Learn, “Security Contexts for Tasks” / `Principal.RunLevel`
   https://learn.microsoft.com/windows/win32/taskschd/security-contexts-for-running-tasks
   https://learn.microsoft.com/windows/win32/taskschd/principal-runlevel
5. Microsoft Learn, “Named Pipe Security and Access Rights”
   https://learn.microsoft.com/windows/win32/ipc/named-pipe-security-and-access-rights

结论：Microsoft 明确提供 Elevated Task / OS Service / Administrator Broker 等正规模型；低权限进程不能注册 `TASK_RUNLEVEL_HIGHEST`，需要首次由 elevated process 完成；后续 Task Scheduler 可在预授权高权限上下文运行任务。高权限 IPC 必须有明确 ACL/认证边界。

架构裁决：不用漏洞，不让整个 UI 永久高权限；采用“**标准用户控制面 + 一次授权注册的高权限 broker task + loopback 强随机 token IPC**”。broker 仅接受有界协议，并只负责启动/停止 DevBridge 自己的受控引擎或执行 Gateway 明确委派的短命令；不提供裸公网接口。

## 4. v0.8.8 架构方案

### 4.1 路由：Dispatcher 与 Workspace Context 解耦

路由证据优先级：

1. 显式 `devbridge_workspace_id`（兼容/明确覆盖）
2. `task_id` affinity
3. 本次**绝对** path/cwd/patch/command 证据（active roots 取最具体根）
4. opaque CodexPro `workspace_id=ws_...` handle affinity；若已命中，则其后的相对 path/cwd 在该 handle 根内解析，不再与其它根竞争
5. 旧客户端的 client/session soft anchor（只作 pathless/relative follow-up）
6. 无 handle/anchor 时，唯一存在相对 path 可作为弱路由证据；多根同名则报歧义
7. 稳定 bootstrap fallback（只用于 schema/初始化/完全无证据兼容调用，不写 current）

实现要求：

- `open_workspace` 成功时，Gateway 解析 opaque handle 并把其所属 project id 注入 structured result，作为显式 route handle；若请求来自旧有稳定 client/session affinity，同时更新该 affinity 的 soft anchor。
- `devbridge_get_current_workspace` 只报告显式/soft anchor；没有 anchor 时返回“未固定，所有 READY 根参与自动路由”，structured route id 为空，绝不暴露 bootstrap C/D 为 current。
- path/task 始终压过 soft anchor；一次调用明确引用多个**绝对** active roots 仍拒绝。
- 2026-07-28 客户端不依赖 `Mcp-Session-Id`，模型必须线程化 explicit handle；工具描述和 `AGENTS.md` 明确这一点。

### 4.2 system/full_system：Workspace 是默认 cwd，不是安全边界

CodexPro 增加显式 `systemAccess` 配置，由 `CODEXPRO_SYSTEM_ACCESS=1` 启用：

- 非 system：`PathGuard` 现有 containment 完全不变。
- system：绝对路径可以离开 workspace；relative path 仍以当前 workspace 为基准。
- system 下仍 canonicalize/realpath；写入仍拒绝写穿 symlink；blocked globs/secret-like 规则仍执行。
- system 下 `WorkspaceManager.openWorkspace()` 可打开任意存在目录，不再要求属于 `allowedRoots`。
- Gateway 对 active roots 外的绝对路径：有显式 handle/soft anchor时路由其引擎；否则选择一个 READY 且 permission=system 的稳定 bootstrap engine；没有 system engine 时明确拒绝，不能误送给 workspace/read-only 根。
- Gateway 本地 `run_command/run_program` 的 cwd 在 system 模式允许 workspace 外目录。

### 4.3 Windows Elevated Broker

组件：`elevation.py` + broker entry mode。

正常态：

```text
MCPDevBridge UI/Gateway (asInvoker)
       │ loopback + 256-bit bearer
       ▼
Elevated Broker (Task Scheduler, Highest)
       ├── full_system CodexPro process
       └── 按需短命令/受控 engine process
```

安全要求：

- broker 仅绑定 `127.0.0.1`，端口动态分配；状态文件只保存 port/pid/非敏感 epoch，不保存 token。
- token 由现有 `SecretsStore` 保存；每次 broker HTTP 请求使用 constant-time Bearer 比较。
- 请求体、stdout/stderr、env 大小均有上限；env/日志仍脱敏。
- engine spawn 只允许 DevBridge 自己解析出的 Node/CodexPro 与受控 runtime，不提供任意 executable 的公开 RPC。
- 短命令仍必须先经过现有 `full_system` execution-profile 危险模式检查；broker 不绕过 denylist。
- broker 空闲超时自动退出；ProjectManager/desktop 退出时请求停止，减少后台常驻资源。
- 首次 full-system 启动若 elevated task 未注册：由标准 UI 发起一次 `ShellExecute(..., runas)`/PowerShell 注册任务并等待结果；拒绝 UAC 时项目不启动并给出明确说明。之后仅启动已注册 task，不再逐命令 UAC。
- 非 Windows 或非 system 项目完全不走 broker。

### 4.4 兼容与迁移

- `read_only`/`workspace` 行为不变。
- 旧 `devbridge_switch_workspace` 继续可用，但只写 soft anchor，不成为 path 权限边界。
- 旧项目配置无新增必填字段；首次使用 full-system elevation 时按需初始化 broker secret/task。
- Linux/SteamOS 无 UAC，systemAccess 仅控制 CodexPro 文件边界，不引入 Windows broker。

## 5. 算法复杂度、CPU、内存评估

### 5.1 路由热路径

当前 `_running_workspace_roots()` 每个 route inference 都读取 `projects.json`、逐项目调用 registry 并排序，单调用约 `O(P log P)` + 磁盘 I/O；一次工具调用还可能对多个候选路径重复 realpath。

优化：

- Gateway 增加短 TTL（目标 250–500ms）的 active-root snapshot：一次构建保存 project id、canonical root、permission、port，排序后只读内存；热路径变为 `O(P)` 匹配、无重复配置磁盘读取。
- root canonicalization 在 snapshot 构建时完成，路径 containment 只 canonicalize candidate；避免每次重复 realpath parent root。
- P 通常很小，保持线性扫描比引入复杂 trie 更稳；Windows drive + nested root 场景下，`O(P)` 的常数远低于文件系统 syscall。

### 5.2 Workspace registry

CodexPro 当前通过遍历 Map value 查相同 root，最坏 `O(W)`；上限 W=4096。增加 `root -> workspace_id` 索引，使 open/default lookup 为均摊 `O(1)`，额外内存 `O(W)`，每项只存一个短字符串映射。

### 5.3 Client/task affinity

所有 affinity map 继续严格有界；不得新增无界 session map。现代 explicit handle 不需要 protocol-session TTL。

### 5.4 Elevated broker

broker 仅在 system 项目需要时运行；不做轮询忙等。健康检查采用有界 connect/read timeout；空闲退出避免长期固定进程开销。引擎日志使用现有 ring/capped file，不新增无界 stdout 缓存。

## 6. 实施顺序

1. 固化 `AGENTS.md` 强制研发流水线并切换文档到 v0.8.8。
2. 新增/修正路由回归，先让“false current=C”和“明确 handle + 相对路径仍歧义”测试失败，再实现 Gateway route/context 分离。
3. 新增 CodexPro systemAccess + PathGuard/openWorkspace 回归；修 `build_codex_env` 与 Gateway local cwd。
4. 实现 Windows elevated broker/task 注册/启动/健康/停止；接入 full-system CodexPro 和短本地命令；补安全/拒绝/非 Windows 回归。
5. 做 active-root snapshot、workspace root index 等资源优化并加行为回归/复杂度说明。
6. 清理明确旧临时资产与陈旧文档状态。
7. defect-first 审查 → 返工直至 PASS。
8. 按下述验收标准执行专业测试；失败即返工并重新审查相关 revision。
9. 构建/commit/push/tag/release；用正式资产升级当前 Windows 实例，恢复 C:/D: 项目、核验快捷方式和真实 MCP 数据面。
10. 发布完成后清空本文已完成计划，更新 `项目架构.md` 与 `进度验收.md`。

## 7. 验收标准

### 7.1 路由/多会话

- 两个模拟客户端分别 `open_workspace(root A/root B)` 后，pathless follow-up 使用各自 explicit/legacy soft handle 时稳定落到各自 engine；互不污染。
- `open_workspace` structured result 同时可恢复 opaque workspace handle 与 DevBridge route project id。
- `devbridge_get_current_workspace` 在未显式/soft 绑定时不得返回 stable fallback 的 C:/D: project id。
- stale soft anchor/opaque handle 不能覆盖本次明确 absolute path/cwd/task。
- 已有有效 opaque workspace handle 时，相对 path/cwd 只在该 handle 根内解析，不因其它 READY 根存在同名文件而漂移或报歧义。
- C:/D: 根同时 READY 时，绝对路径按最具体 active root；无 handle 的同名相对路径歧义拒绝；跨 root 单调用拒绝。
- 2026-07-28 stateless 请求不重新依赖 `Mcp-Session-Id`。

### 7.2 完全访问

- system/full_system：从 D: workspace 使用 CodexPro file/read/write/search 对 C:/E:/临时外部目录绝对路径成功（测试用临时目录，不触碰真实敏感文件）。
- system/full_system：`open_workspace()` 可打开 allowedRoots 外存在目录。
- workspace/read_only：同样外部访问继续拒绝；`..`、symlink/junction escape 继续拒绝。
- system 模式下 blocked secret path 与危险命令 hard deny 不退化。
- Gateway `run_command/run_program` system cwd 可在 workspace 外；非 system 继续拒绝。

### 7.3 Windows elevation

- 未注册管理员执行任务时，完全访问首启只能通过一次正规 Windows 管理员确认；拒绝授权时不偷偷降级为“声称完全访问”。
- 如果 MCP DevBridge 自身已经以管理员身份运行，直接在当前高完整性进程完成注册，不再重复弹管理员确认。
- 任务注册后，标准 token 必须能查询/启动该任务；任务 ACL 对原用户仅授予 read + execute，不能授予修改任务 action 的写权限。
- task 注册后，后续完全访问引擎启动不出现逐命令管理员确认。
- 管理员权限设置失败不得把整个 MCP 锁死：UI 必须提供清楚的普通权限恢复/启动路径，同时明确该次没有启用完全访问。
- broker 非授权 token/非 loopback 请求全部拒绝；错误 token 不能产生进程。
- full-system CodexPro 进程实际为 elevated/high-integrity；受保护的管理员级**无破坏性探针**成功（例如只读管理员状态/创建并删除专用测试位置）。
- dangerous command 回归仍拒绝格式化、引导修改、系统根递归删除等已定义模式。
- broker 停止/崩溃后 supervisor 能给出真实 ERROR 并按既有恢复策略重建；用户显式 stop 不自动复活。

### 7.3.1 普通用户界面语言

- 工作台、普通设置页和错误弹窗不出现 `UAC`、`broker`、`token`、`Gateway`、`full_system`、`IPC` 等实现术语；必要技术详情仅出现在诊断、日志或开发文档。
- 错误文案必须说明“发生了什么 / 有什么影响 / 下一步怎么做”；按钮使用“重新设置 / 以普通权限启动 / 取消 / 知道了”等普通用户语言。

### 7.4 资源/复杂度

- active-root snapshot 测试证明高频 route inference 不重复读取 `projects.json`；start/stop/配置变化在 TTL/显式 invalidation 后正确反映。
- WorkspaceManager root lookup 不再线性扫描全部 handles。
- affinity/workspace/task/broker输出缓存都有硬上限。
- 无新增高频 polling thread 或无界日志/Map。

### 7.5 全量质量门

- `git diff --check` PASS。
- Ruff PASS。
- Pyright Windows：0 errors / 0 warnings；Linux cross-platform：0/0。
- full pytest PASS。
- CodexPro `npm run build` + complete `npm run smoke` PASS。
- `npm audit --omit=dev --audit-level=high` 无 high/critical blocker。
- `uv lock --check` PASS。
- PowerShell parser + Linux `bash -n` PASS。
- Windows `scripts/build.ps1 -Version 0.8.8` PASS，安装器 payload 完整。
- defect-first review 当前 revision PASS；任何 review 后改动都必须重审。

### 7.6 发布闭环

- release commit、`release/v0.8.8` push、annotated `v0.8.8` tag、GitHub Release Windows/Linux 资产来自同一 SHA。
- 正式 Windows installer + Linux tarball + SHA256SUMS 均在线可核验。
- 当前 Windows 实例用正式资产升级/重启；桌面快捷方式指向最终 `MCPDevBridge.exe`。
- `C:\` 与 `D:\` 原运行根恢复，Gateway local/public health 与真实 MCP tool call 成功。
- 真实会话再次执行 `open_workspace(D:\Environment\mcp)` 后，`devbridge_get_current_workspace` 不再伪报 C:\；另一个会话可独立指定 C:\ 子目录。
- 完成后 `开发计划.md` 不再保留 v0.8.8 已完成施工计划。
