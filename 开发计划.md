# MCP DevBridge v0.8.6 开发计划

> 当前维护分支：`release/v0.8.6`。v0.9+ 历史只保留，不回合并多 Agent runtime。本轮目标不是让用户更频繁地发送“继续”，而是在 ChatGPT 宿主仍允许当前 assistant turn 存活时，让明确目标尽可能自动连续推进，同时在宿主中断后可无损恢复。

## 1. 问题定义

长任务目前有两类必须同时处理的故障表象：

- `Connection interrupted. Waiting for the complete answer`：可能来自浏览器/OpenAI 流、Tunnel/QUIC、中间代理或 MCP SSE 空闲链路。
- `Message delivery timed out. Please try again.`：可在 MCP 本地任务正常继续的情况下出现，说明 ChatGPT 宿主级 turn/message-delivery 仍有独立硬边界，MCP 服务端无法直接覆盖。

因此 v0.8.6 采用“两层目标”：

1. **尽量延长仍存活的同一 ChatGPT 回合**：减少沉默 socket、重复短轮询、重复大日志与不必要的工具调用；允许服务器持续发送标准 MCP progress，并对 SSE 做协议安全保活。
2. **宿主真的结束回合时不丢工作**：后台任务、durable `long_run_*` plan/checkpoint/review 是恢复事实源；连接恢复后从状态继续，而不是要求用户重新描述任务。

## 2. 外部依据转化

当前方案只采纳能落到维护线中的模式：

- OpenAI：长运行计算与前台连接解耦；Responses background/retrieve/sequence cursor 说明官方也把“长任务可恢复”与“一条前台流永不掉线”分开处理。长工具阶段使用 commentary/progress 类中间输出，而不是完全沉默。
- MCP Streamable HTTP：使用标准 progress notification；HTTP/SSE 会话允许 event id / `Last-Event-ID` 恢复，官方 TypeScript SDK提供 `EventStore` 接口。
- Cloudflare Tunnel：QUIC/HTTP2 都可能因网络路径、UDP idle/NAT、维护重连而中断；`text/event-stream` 可避免 Tunnel 对响应缓冲。当前 bundled `cloudflared 2026.7.3` 已高于 2026.5.2 的启动连通性预检基线，不需要为了本轮盲目降级或固定 HTTP/2。
- Temporal/LangGraph 类 durable workflow：把 workflow state 与 socket 生命周期分开，副作用与 checkpoint 可恢复；失败重试必须有状态和证据，不能靠聊天记忆。

## 3. v0.8.6 实施范围

### 3.1 P0 — SSE 空闲保活

Gateway 仅对 `Content-Type: text/event-stream` 透传流增加 12 秒空闲 comment keepalive。

硬约束：

- 保活只能插在**完整 SSE event 边界**；上游 event 被 TCP/HTTP chunk 拆开时禁止在中间插帧。
- 非 SSE 响应不得增加任何字节。
- 上游 response 仍只有一个 reader/owner，正常结束、客户端取消和异常都必须 `aclose()`。

### 3.2 P0 — MCP progress-aware `wait_task`

- 没有请求 `progressToken`：单次 wait 最多 30 秒，保持旧客户端兼容。
- 有 `progressToken` 且 SDK 提供 notification sender：单次可到 120 秒，每约 8 秒发送标准 `notifications/progress`。
- progress notification 失败只是 liveness 失败，不能取消本地后台任务。
- 后台命令执行本身继续无固定时长上限。

### 3.3 P0 — 降低轮询上下文压力

- running `wait_task` 只回传 stdout/stderr 各约 2 KiB UTF-8 安全尾部，并返回 omitted-byte 计数。
- terminal snapshot 保留完整的既有有界结果。
- `get_task` 继续提供显式完整当前快照。
- poll hint 随任务时长从 5 / 15 扩到 60 / 120 秒，减少多小时任务的重复工具调用。

### 3.4 P0 — 同一 assistant turn 自动续跑语义

工具说明、服务器 instructions 与 long-run 摘要必须明确：

- 用户给出的目标仍然清楚、当前还有可执行工作、且不需要真实产品决策/权限/安全确认时，不得仅因为 task 仍是 running 就结束当前回答并要求用户回复“继续”。
- 工具密集的等待阶段约每 45–60 秒或阶段切换时给一条简短 commentary 进展，然后继续工具工作。
- `blocked` step 不得被错误标记成“无需用户输入”。

### 3.5 P1 — Streamable HTTP 断线重放

每个活跃 CodexPro HTTP session 使用有界 EventStore：

- 默认 256 events / 4 MiB / 30 min TTL。
- 生成 SSE event id，支持客户端 `Last-Event-ID` 重放仍保留的事件。
- 单个事件大于 replay budget 时正常向客户端发送，但只存 gap marker；之后若重放跨越 gap，明确失败，不得为了 replay 让原始工具响应失败或无限占用内存。
- durable `long_run_*` 仍是跨 session/跨 turn 事实源；EventStore 不能代替 durable state。

### 3.6 P1 — Tunnel 策略

本轮保持 `cloudflared --protocol auto`，不永久强制 HTTP/2：

- 当前 private runtime 为 cloudflared 2026.7.3，已经具备当前 Cloudflare startup connectivity pre-check。
- 现有 transport monitor 继续比较 local/public `/health` 和 TTFB，并只做 tunnel-only recovery。
- 若后续日志证明 UDP/QUIC idle reset 显著高于 TCP，可做 24h `auto` vs `http2` A/B 后再决定；不能用猜测替代观测。

## 4. 软硬件可落实性

当前主机：HP ProBook 450 G8，Intel i5-1135G7（4C/8T），约 15.7 GiB RAM。

新增开销主要是网络/状态 bookkeeping：小型 SSE comment、每 session 最多数 MiB EventStore、8 秒级 progress notification 与更小的 poll payload；不新增常驻 LLM、数据库、容器或第二 Agent runtime。CPU/RAM/磁盘均不是实施瓶颈，真正不可消除的残余风险是 ChatGPT 宿主级 turn/message-delivery policy。

## 5. 验收标准

### Transport / correctness

- SSE keepalive 在空闲期可观察，但绝不切断 partial event；普通 SSE 字节顺序不变。
- 客户端取消后 upstream 只关闭一次；既有 `StreamConsumed` 回归继续为绿。
- EventStore replay/eviction/TTL/oversize-gap 行为有自动化覆盖并保持内存上限。
- 非 SSE 路径行为不变。

### Long-turn autonomy

- `wait_task` 无 progress 的实际等待 ≤30 秒；有 progress 时允许至 120 秒，并能观察请求 token 对应的标准 progress notification。
- running poll 大输出不会把 >2 KiB stdout/stderr 重复送回模型；omitted bytes 可审计。
- server instructions 与 long-run status 都明确 `user_reply_required=false`，但 blocked step 不得作此承诺。
- 不通过“更快结束回答”来规避 timeout；目标是同一回合继续自动工作。

### Quality / release

- Python Ruff pass；Pyright 0 errors/warnings；full pytest pass。
- CodexPro TypeScript build + complete smoke pass；npm production audit 无 high/critical 阻塞。
- `uv lock --check`、PowerShell parser、全部 Linux shell `bash -n` 通过。
- Windows `scripts/build.ps1 -Version 0.8.6` 成功。
- GitHub release Windows + Ubuntu 22.04 jobs 从同一 release commit 产出资产。
- defect-first review 为硬门：任何 P0/P1/P2 可行动 finding 必须打回修复并重新跑相关门；最终 review 必须 PASS。

## 6. 发布闭环

只有以下全完成才允许声称 v0.8.6 发布完成：

1. 文档与实际代码一致，`git diff --check` 通过。
2. 本机 source/build gates 全绿。
3. release commit push 到 `release/v0.8.6`。
4. `v0.8.6` tag 精确指向该 commit；不重写 v0.9.x 历史。
5. GitHub Release 的 Windows/Linux 正式资产与 checksum 已发布并核验。
6. 当前 Windows 桌面实例用最终正式资产升级并重启。
7. 桌面快捷方式重建/核验，原运行根恢复，local/public health 正常，managed tunnel/runtime 拓扑符合预期。
8. 最终可复现证据写入 `进度验收.md`。
