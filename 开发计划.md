# MCP DevBridge v0.8.7 开发计划

> 当前维护分支：`release/v0.8.7`。v0.9+ 历史只保留，不回合并多 Agent runtime。本轮目标是消除后台闲置后需要人工重启的 502 故障链，同时保留 v0.8.6 已有的 durable long-run 与同回合自动续跑能力。

## 1. 问题定义

本轮现场已经确认不是单一 Tunnel 故障，而是多个可叠加的生命周期缺陷：

1. CodexPro 子进程退出后，旧 `EngineManager.state` 仍可能缓存为 READY，Gateway 因而继续向死端口路由并返回 502。
2. 共享 Gateway `/health` 只证明 Gateway 自身还在监听，不能证明 Gateway → CodexPro 数据面可用；现场曾出现 local/public `/health` 连续 200 而真实 MCP 调用失败。
3. 旧 CodexPro Streamable HTTP 使用 30 分钟 protocol session TTL；Gateway 又长期缓存 client → upstream `Mcp-Session-Id`，闲置后形成确定性的 stale-session 404/502 风险。
4. 本机支持 Windows Modern Standby，系统事件记录到约 59 分钟的 Connected Standby；后台公共 Hub 必须显式声明 system-required，不能依赖用户持续操作。
5. Gateway 本地健康连续失败时旧 coordinator 只记录告警，不主动重建 Gateway。

Chrome Memory Saver/Page Lifecycle 可能中断浏览器前端流，但它不管理本地 `MCPDevBridge.exe` / `node.exe` / `cloudflared.exe`，因此只作为次级客户端风险，不作为本轮 502 主根因。

## 2. v0.8.7 架构裁决

### 2.1 MCP protocol 层直接升级为 stateless / hybrid

用户明确要求不以延长 TTL 或旧 stateful session 续租作为主方案。本轮因此直接升级 CodexPro：

- `@modelcontextprotocol/sdk` v1 → 官方拆包式 SDK v2：`@modelcontextprotocol/client`、`@modelcontextprotocol/server`、`@modelcontextprotocol/node`。
- Zod 升级至 4.x，完成官方 v1→v2 codemod 及手工迁移。
- HTTP `/mcp` 改用 SDK v2 `createMcpHandler` + `toNodeHandler`。
- `legacy: "stateless"`：2026-07-28 现代协议走 per-request stateless；2025-era 流量也按 SDK 官方 stateless fallback 处理。
- 删除 CodexPro 的 `Mcp-Session-Id → transport` Map、30 分钟 prune timer、`CODEXPRO_HTTP_SESSION_TTL_MS`、`CODEXPRO_MAX_HTTP_SESSIONS` 和 session-specific EventStore 接线。
- Gateway 不再为不同工作区虚拟化 upstream session，不向 CodexPro 转发外部 `Mcp-Session-Id`，也不再对 `Session not found` 自动重放工具调用。

这样消除的是“可过期 protocol session”本身，而不是把 TTL 改成无限。

### 2.2 protocol 无状态，业务状态独立持久

MCP transport 无状态不等于业务必须无状态。CodexPro 把需要跨请求存在的应用状态提升为进程级 runtime：

- workspace handle / root registry；
- 后台 BashTaskManager；
- durable long-run 文件与进程级锁；
- `show_changes(last_shown)` checkpoint。

客户端特有的“selected workspace / last shown”不再绑 protocol session，而是按 `x-mcp-devbridge-client-affinity` 隔离。Gateway 只传不可逆 SHA-256 affinity（来自外部 session 或授权上下文），该值不参与鉴权、权限、根路由或 transport 生命周期。相关 Map 设置固定容量上限，避免长期运行无界增长。

### 2.3 数据面守护与自动恢复

- `EngineManager.state` 必须实时校验子进程，死进程立即 READY → ERROR 并记录 PID/exit code。
- `ProjectManager` 对每个“用户期望运行”的项目执行认证 `/healthz` 数据面探针；连续失败达到阈值后只重启该 ProjectUnit，并受 cooldown 保护。
- 用户显式停止项目后删除 desired-running spec，supervisor 不得偷偷拉起。
- `ServiceCoordinator` 对共享 Gateway 本地健康连续失败自动重建 Gateway；Tunnel 与各项目不随之重启。
- 公网 `/health` 失败但本地 Gateway 健康时，仍保持 tunnel-only recovery。

### 2.4 Windows 后台运行

公网 Hub 运行期间启用 `SystemAwakeGuard`：

- 专用线程调用 `SetThreadExecutionState(ES_CONTINUOUS | ES_SYSTEM_REQUIRED)`；
- 周期刷新；
- 停止 Hub 时用 `ES_CONTINUOUS` 清除；
- 不使用 `ES_DISPLAY_REQUIRED`，屏幕仍可正常关闭；
- 非 Windows 平台为 no-op。

## 3. 长任务与 transport 继承能力

v0.8.7 保留并继续验证 v0.8.6 已有能力：

- Gateway SSE 12 秒空闲 comment keepalive；
- `wait_task` 无 progress token 时单次最多 30 秒；支持 progress 时最多 120 秒，并约每 8 秒发送标准 `notifications/progress`；
- running poll 只回传约 2 KiB stdout/stderr 尾部；
- durable `long_run_*` plan/checkpoint/review/rework/completion；
- 目标仍可执行且无需真实用户输入时，同一 assistant turn 自动继续。

原 v0.8.6 的 session EventStore replay 不再属于当前 HTTP transport 架构；跨请求/跨重连恢复依赖 stateless request、进程级业务状态和 durable long-run，而不是 protocol session TTL。

## 4. 应用内更新改为直达最高正式版本

更新器不再依赖“相邻版本”语义，也不把 GitHub `releases/latest` 单一指针作为唯一排序依据：

- 拉取正式 Release 列表；
- 过滤 draft、prerelease、无有效稳定版本号或缺少当前平台正式安装资产的条目；
- 按版本号选择最高可安装正式版本；
- 当前版本落后多个版本时一次升级到最高版本，例如 `0.8.4 → 0.8.7`，不逐级安装 0.8.5/0.8.6。

下载后的 size / SHA-256 校验和 detached live-upgrade 接力保持不变。

## 5. 验收标准

### Stability / protocol

- CodexPro 当前 HTTP 代码中不存在 protocol session TTL、session transport Map 或基于 `Mcp-Session-Id` 的上游会话恢复。
- 外部 `Mcp-Session-Id` 不得转发给 CodexPro；上游即使返回该 header，也不得泄漏到客户端。
- 上游 404 / `Session not found` 不得触发工具调用自动重放。
- SDK v2 build + 完整 smoke 通过，HTTP smoke 同时验证 stateless POST、workspace handle 跨请求、后台 task 跨请求及 client-affinity 隔离。
- CodexPro 子进程死亡、项目数据面失败、Gateway 本地失败均有自动恢复测试；用户显式 stop 不被 supervisor 反向拉起。
- Windows awake guard 只保持 system required，不保持 display required。

### Update

- 更新发现从任意旧版本直接选择最高可安装正式版本；自动测试至少覆盖 `0.8.4 → 0.8.7`。
- prerelease、draft、缺当前平台资产的 release 不得被选择。

### Quality / release

- `git diff --check` 通过。
- Python Ruff pass；Pyright 0 errors/warnings；full pytest pass。
- CodexPro TypeScript build + complete smoke pass；production npm audit 无 high/critical 阻塞。
- `uv lock --check`、PowerShell parser、Linux shell `bash -n` 通过。
- Windows `scripts/build.ps1 -Version 0.8.7` 成功。
- defect-first review 为硬门：发现 P0/P1/P2 可行动 finding 必须返工并重跑相关门；最终 review 必须 PASS。

## 6. 发布闭环

只有以下全完成才允许声称 v0.8.7 发布完成：

1. 文档与实际代码一致，`git diff --check` 通过。
2. 本机 source/build gates 全绿。
3. release commit push 到 `release/v0.8.7`。
4. annotated `v0.8.7` tag 精确指向该 commit；不重写 v0.8.6 或任何 v0.9.x 历史。
5. GitHub Release 的 Windows/Linux 正式资产与 checksum 已发布并核验。
6. 当前 Windows 桌面实例用最终正式资产升级并重启。
7. 桌面快捷方式重建/核验，原运行根恢复，local/public health 与真实 MCP 数据面正常。
8. `powercfg /requests` 验证公网 Hub 运行时存在 SYSTEM request、无 DISPLAY request；停止后清除。
9. 最终可复现证据追加写入 `进度验收.md`。
