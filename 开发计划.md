# MCP DevBridge v0.9.0 未完成开发计划

> 正式生产基线：`v0.8.9`，commit `b679268ee6cbbed49f5a2ca93c6e81d8e94d6184`。
>
> 当前研发分支：`release/v0.9.0`。
>
> 平台能力 durable run：`lr_mtfcuqcl_6abdc2169c31`。
>
> S1–S7 与本地 S10 工程门均已完成并迁入 `进度验收.md`。本文件只保留仍未完成或被平台能力阻断的事项。

## 1. 当前能力边界

本地 durable fallback 已实现并验收：后台任务、plan/checkpoint/evidence、terminal receipt 与重连后的零猜测恢复均可用。实现与测试证据见 `进度验收.md`。

当前真实 ChatGPT → DevBridge 连接仍报告：

```text
nativeMcpTasksExtension = not emitted by baseline connector
```

因此 DevBridge 可以让本地任务跨 ChatGPT turn/断连继续执行，但不能在当前 assistant turn 结束后自行要求 ChatGPT 创建下一轮 assistant turn。Regular Chat 浏览器控制器继续只作为隔离实验源码/fixture 保留，不进入正式桌面、CLI、PyInstaller、Windows/Linux build 或 installer，也不得用于自动提取登录态 ChatGPT Output 绕过平台能力门。

## 2. S8 · Native MCP Tasks：`blocked_platform`

### 目标

只有真实客户端与服务端完成 Tasks extension 双向协商后，才实现并验收：

- `tools/call` 返回受支持的 durable task handle；
- `tasks/get` / `tasks/cancel` / 必要 update 生命周期；
- Connector / DevBridge 重连后按同一 task id 恢复；
- input-required、取消、TTL、状态上限与错误语义；
- 与现有 LongRunState 建立单一事实源映射，不形成第二套任务真相源。

### 当前阻断

- 当前 ChatGPT Connector 未声明 `io.modelcontextprotocol/tasks`；
- 服务端不能向未 opt-in 的客户端擅自返回 Tasks extension 结果；
- 工具级 durable fallback 不是 native MCP Tasks，也不能控制 host assistant-turn 生命周期。

### 恢复开工条件

以下条件全部满足后才解除阻断：

1. 当前 ChatGPT/目标 MCP 客户端真实 capability 明确声明 Tasks extension；
2. 服务端 SDK 与 HTTP transport 存在可运行的 create/get/cancel 路径，而非仅类型或草案；
3. 本地 conformance 覆盖协商缺失、错误 method、断连、取消、重复请求、状态上限与 secret redaction；
4. 真实 Connector 验证同一 task id 跨断连恢复。

## 3. S9 · 长任务宿主验收：`blocked_platform`

在受支持的 host async primitive 存在后，执行至少 20 分钟真实无人值守工作流，覆盖：

- 多轮工具调用与本地 build/test；
- Connector / DevBridge / CodexPro 进程替换；
- 短网络故障与取消；
- 同一 durable task identity 恢复；
- duplicate mutation=0、错误新会话=0、conversation/task loss=0；
- CPU、RSS、句柄、状态文件、checkpoint 与日志有界。

当前已经验证的是本地 background process、LongRunState、terminal receipt 与下一次 MCP 调用时的恢复；尚无法由 DevBridge 单方面验证的是“一个 assistant turn 结束后，在没有用户或平台触发的情况下由 host 自动创建下一 turn 并继续模型推理”。因此 S9 保持平台阻断。

## 4. S10 · durable completion 与发布门

本地代码、测试、文档和正式/实验边界已完成。剩余终局条件仅依赖 S8/S9：

1. S8/S9 未解除前，整体 durable objective 不得登记 PASS completion review；
2. `long_run_complete` 应继续因未完成平台步骤而 fail-closed；
3. 不得创建或移动 `v0.9.0` Tag、GitHub Release，或发布宣称“ChatGPT 自动开新轮次、无人值守持续推理”的正式版本；
4. 平台能力真实变化后，先重新验证 capability，再执行 S8 → S9 → 最终 review/completion/release gate。

## 5. 回滚与非目标

- durable terminal-receipt 增量可回滚到正式 v0.8.9 CodexPro runtime；回滚不修改项目配置、Gateway、Tunnel 或凭据；
- Regular Chat 实验路径默认不可达，移除实验目录不影响正式 MCP 数据面；
- 未来 Tasks 实现必须 capability-gated；协商缺失时继续使用当前工具级 durable fallback；
- 不以浏览器私有自动化、登录态抓取或伪造 host 证据替代平台能力。
