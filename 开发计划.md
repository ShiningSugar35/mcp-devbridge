# MCP DevBridge v0.8.3 开发计划

> 本文件只保留当前维护线的目标、完成定义和后续方向。0.1–0.8.1 的施工过程已进入 Git/CHANGELOG 历史，不再把过期 Phase 当成当前待办。

## 1. 当前维护目标

v0.8.3 以已发布 v0.8.2 稳定线为基础，不引入 v0.9+ 多 Agent runtime，完成以下四项产品收口：

1. **多根自动路由**：所有运行项目根同时 active，根的后代路径自动继承访问边界；正常文件/Git/Shell 工作流无需 `switch_workspace`。
2. **桌面稳定性与批量控制**：启动/停止按钮不被 1 秒刷新替换；支持启动全部/停止全部；删除重复“当前项目”控制面；连接与权限可批量同步。
3. **跨平台恢复**：Windows 保持正式桌面发行；Linux / SteamOS Desktop Mode 恢复用户目录安装、XDG、密钥、Shell/进程、更新、PyInstaller 和 CI/Release 构建。
4. **生产发布闭环**：清理回退遗留、更新文档、跑完整发布门、构建产物、commit/push/tag/GitHub Release，并用正式产物恢复 Windows 安装实例与快捷方式。

## 2. 已完成的功能范围

### 2.1 多根路由

- [x] READY 项目组成并列 active roots，不再存在面向用户的入口根。
- [x] 绝对路径选择最具体运行根。
- [x] 相对路径只在唯一命中时自动路由，多根同名拒绝猜测。
- [x] 新路径可依据唯一最深已存在父目录推断。
- [x] `task_id` 绑定源根；opaque CodexPro workspace handle 只作弱 affinity。
- [x] 显式 DevBridge override > task > 本次 path/cwd > workspace handle > bootstrap fallback。
- [x] direct project Bearer 不再作为普通 Hub 会话的路由围栏。
- [x] `../`、symlink/junction 与本地工具 `cwd` 逃逸防护。
- [x] 根盘无权限目录 warning+skip，扫描继续。
- [x] scoped Git 发现最近嵌套仓库。

### 2.2 桌面生命周期

- [x] 项目表收敛为五列：名称/路径/状态/端口/操作。
- [x] 行按钮原位刷新，避免 mouse press/release 期间控件被替换。
- [x] `_busy_project_ids` 按项目管理异步生命周期。
- [x] 启动全部项目 / 停止全部项目。
- [x] 删除重复“当前项目”卡片与第二套启停控制。
- [x] 连接与权限可保存到全部项目，不覆盖 Git 参数和独立端口。
- [x] 停止一个根不会关闭其它根或共享 Hub；最后一个运行根停止才关闭 Hub/Tunnel。

### 2.3 Windows 安装与运行

- [x] Inno Setup per-user 安装。
- [x] `DisableDirPage=no`，安装时可自定义目标目录。
- [x] 私有 Node.js / uv/uvx / cloudflared runtime 打包。
- [x] detached `live_upgrade.ps1` 接力升级与项目恢复。
- [x] 单实例和旧进程清理。

### 2.4 Linux / SteamOS Desktop Mode

- [x] `platform_support.py` 的 Windows/POSIX 进程差异。
- [x] 绝对 `XDG_CONFIG_HOME` / `XDG_DATA_HOME` 支持，相对 XDG 值回退默认。
- [x] 用户配置目录创建权限收紧。
- [x] Secret Service 优先、AES-GCM 用户级 fallback。
- [x] Bash Shell 与 POSIX 进程树处理。
- [x] `prepare_runtime_linux.sh / build_linux.sh / install_linux.sh / live_upgrade.sh`。
- [x] Linux 自定义安装目录安全校验；拒绝危险根目录和未知非空目录覆盖。
- [x] Desktop Entry `Exec=` 安全转义。
- [x] Ubuntu 22.04 CI/Release 构建基线。

> SteamOS Desktop Mode 真机安装/升级闭环不作为本轮发布阻塞项；CI、脚本、Linux frozen build 仍必须通过。后续获得目标设备时再追加兼容验收，不回写为“已真机通过”。

## 3. v0.8.3 发布门

发布前所有项目必须同时满足：

### 3.1 源码与依赖

- [ ] `git status` 中只保留有意源文件/测试/文档改动；生成物、缓存、安装运行文件不 track。
- [ ] `git diff --check`。
- [ ] `uv lock --check`。
- [ ] Python Ruff。
- [ ] Python Pyright 0 errors / 0 warnings。
- [ ] Python 全量 pytest。
- [ ] CodexPro `npm run build`。
- [ ] CodexPro完整 `npm run smoke`，必须包含 root-scan / nested-git / async task 等回归。
- [ ] npm dependency audit 无已知生产阻塞。

### 3.2 平台与发布脚本

- [ ] PowerShell 发布脚本 parser 检查。
- [ ] Bash 发布脚本 `bash -n`。
- [ ] Windows `scripts/build.ps1 -Version 0.8.3` 成功。
- [ ] Windows installer frozen payload 完整、安装目录可选。
- [ ] GitHub Release workflow 的 Windows job 成功。
- [ ] GitHub Release workflow 的 Ubuntu 22.04 Linux job 成功并产出 `MCPDevBridge-Linux-x86_64-0.8.3.tar.gz`。

### 3.3 Git 与 Release

- [ ] 功能/文档改动形成一个可审计 release commit。
- [ ] push `release/v0.8.3`，不得 force-push `master` 或改写 v0.9.x 历史。
- [ ] `v0.8.3` tag 精确指向该 release commit。
- [ ] GitHub Release `v0.8.3` 创建并上传 Windows/Linux 正式资产及 checksum。
- [ ] Release 页面、tag、commit、资产版本一致。

### 3.4 Windows 本机切换

- [ ] 用最终 Windows 资产升级/安装当前实例。
- [ ] 更新后只保留预期的 MCPDevBridge 桌面进程。
- [ ] 桌面快捷方式指向最终正式安装位置；删除/替换旧快捷方式。
- [ ] 原运行项目根按升级恢复逻辑重新上线，Hub 能重新连接。

## 4. 发布后维护方向

P0：

- 继续保持多根自动路由的歧义拒绝原则，任何新工具都必须先明确 path/task affinity。
- 将外部 MCP 能力做成按需 profile/动态工具发现，优先评估 Context7、GitHub MCP，避免 tools/list 常驻膨胀。
- 对 Windows/Linux 安装与 live-upgrade 增加更多失败回滚和资产完整性回归。

P1：

- Playwright / Chrome DevTools / MarkItDown 等外部能力按真实需求接入，不重复 CodexPro 已有文件/Git/Shell 能力。
- 在有真实 SteamOS 设备时补做 Desktop Mode frozen package 安装、启动、更新与 KDE desktop/autostart 验收。

非目标：

- 不把 v0.9+ AgentRuntimeLoop / 多 Agent 调度重新合入 v0.8.3。
- 不恢复“入口项目决定权限”“必须手动 switch workspace”或“一个 session 只能 active 一个根”的旧设计。
- 不为消除歧义而做静默猜测；宁可要求绝对路径。

## 5. 状态来源

- 架构事实：`项目架构.md`。
- Agent 约束：`AGENTS.md`。
- 实际测试/构建/Release 结果：`进度验收.md`。
- 历史版本：`docs/en/CHANGELOG.md` 与 Git tags。
