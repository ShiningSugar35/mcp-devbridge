# 开发计划.md

> 完整开发计划与每个 Phase 的验收标准。进度与验收结果见 进度验收.md；架构见 项目架构.md。
> 维护规则：新增需求/变更 → 先更新本文件（或新增 Phase），完成阶段后更新 进度验收.md。

## 项目总目标

Windows 桌面程序（PySide6 单窗口，中文界面）：
1. 选择本地开发项目，配置权限模式/端口/测试命令；
2. 一键启动独立的 MCP Server 子进程（仅监听 127.0.0.1）；
3. 通过 **Cloudflare Named Tunnel** 提供**长期固定、永不变化**的 HTTPS MCP 地址；
4. 认证与 URL 分离：Bearer 令牌独立配置；
5. 附测试、文档（方案/开发计划/开发状态/问题记录 + 五份英文文档）、打包产物（PyInstaller onedir + Inno Setup 安装器）。

## 全局验收标准（所有 Phase 共用）

- [ ] 不修改 `D:\AStockMultiAgent`；测试写入一律在 `.test-workspace`。
- [ ] 所有测试可复现：`$env:PYTHONIOENCODING="utf-8"; .venv\Scripts\python.exe -m pytest tests/ -q` 全绿。
- [ ] 中文界面，关键路径（启动/停止/令牌/隧道/连接测试）无卡死（异步 + 线程）。
- [ ] 后端只监听 127.0.0.1；公网经 Named Tunnel，URL 固定不变。
- [ ] 密钥不入日志、不入 URL、不入仓库；日志脱敏。

---

## Phase 0 — 环境与需求核对（✅ 已完成）

**目标**：确认环境、需求、外部平台事实、风险。
**产出**：环境审计、原型备份（`D:\Environment\mcp\backups-prototype\dev-mcp.py`）、外部事实核实（ChatGPT/Gemini/cloudflared）。

### 验收标准
- [x] 确认 Windows 10 19045 / Python 3.12.10 / uv 0.11.25 / git 2.47.1 / Node v22.19.0 / 无 pwsh。
- [x] 核实 MCP SDK 2.0.0 关键 API（`streamable_http_app`、`add_tool`、`ToolAnnotations`、middleware 协议、client 用法）。
- [x] 核实 ChatGPT 自定义 MCP 要求（developer mode 位置；Pro 仅读/获取，完整写入需 Business/Enterprise/Edu，**不得谎称已绕过**）。
- [x] 核实 Gemini Spark 自定义 Connected Apps 要求（US+18+个人账号+Keep Activity on+标准 MCP）。
- [x] 核实 cloudflared 命令与 Quick Tunnel URL 解析（实测 2026.7.3 可用）。
- [x] 完成风险识别与结论记录（见 问题记录/开发状态）。

---

## Phase 1 — 后端核心 + 33 个 MCP 工具（✅ 已完成核心，测试全绿）

**目标**：实现全部 33 个 MCP 工具的纯逻辑层（不依赖 HTTP）。
**产出**：`constants.py`、`models.py`、`config_store.py`、`secrets.py`、`audit.py`、`shell.py`、`processes.py`、`permissions.py`、`tools.py`。

### 验收标准
- [x] 33 个工具全部实现并注册（server_factory `_register_tools`）。
- [x] 路径边界：`workspace` 模式禁止项目外读写（`PermissionError`），排除 .git/.venv/node_modules/.env 等敏感项。
- [x] 写操作原子化（临时文件 + rename）；`replace_text` 要求 expected_count 匹配；`apply_patch` 任一 hunk 失败则整体不生效。
- [x] 编码容错：UTF-8 / UTF-8 BOM / GB18030 回退读取。
- [x] 命令执行：PowerShell（`powershell.exe`），超时进程树终止，输出截断保护。
- [x] 受管进程：启动/轮询增量输出/停止（含子进程树）。
- [x] 权限三模式（read_only / workspace / system）正确生效，destructive 工具标注正确。
- [x] 工具注解（read_only_hint / destructive_hint / idempotent_hint）准确，**不**把破坏性工具标成只读。
- [x] 单元测试：文件系统、命令、Git（临时仓库）、配置/密钥、diff 解析——全绿。
- [x] Git 命令一律 `git -C <workspace>` 执行，默认 timeout 120s（push 600s）。

---

## Phase 2 — HTTP 服务 + 认证 + 子进程运行（✅ 已完成核心，测试全绿）

**目标**：后端可独立子进程运行，通过 Streamable HTTP 提供服务，认证/审计/限速生效。
**产出**：`server_factory.py`、`server_main.py`、`standalone_server.py`、`selftest.py`。

### 验收标准
- [x] `build_backend(rc)` → MCPServer + Starlette app；`/mcp`（streamable_http_path）、`/health`、`/control/status`、`/control/stop-procs`。
- [x] 认证：本机匿名放行；`/control/*` 强制 Bearer；公网（cf-connecting-ip 等）必须 Bearer；constant-time 比较。
- [x] 限速：10 次认证失败锁 5 分钟（429 + Retry-After）。
- [x] 认证中间件为纯 ASGI（BaseHTTPMiddleware 缓冲 SSE 会坏流，已弃用）。
- [x] `server_main` CLI：`--config` / `--port`；端口占用检测；`ensure_access_token()` 自举令牌。
- [x] 审计中间件：tools/call 记 JSONL，参数脱敏。
- [x] 集成测试走**真实后端子进程** + MCP 客户端（in-process uvicorn SSE 不兼容，禁止使用）。
- [x] `selftest.py` 自测链路（initialize/list_tools/get_workspace_info/list_directory/get_capabilities）。
- [x] 集成测试：initialize、工具数≥30、文件往返、路径逃逸拒绝、只读模式拒绝写、未知工具报错、Bearer 成功/失败、限速锁——全绿。

---

## Phase 3 — 桌面控制程序（PySide6 单窗口）（🚧 进行中 → 下一步）

**目标**：单窗口中文 GUI，完成项目管理、后端启停、状态展示、令牌与 URL 展示、连接自测。
**产出**：`desktop_main.py`（+ 必要的 `desktop/` 子模块），`localdev-mcp-bridge` 脚本入口接通。

### 界面要素（任务书）
1. 项目区：选择/新增项目目录（文本框 + 浏览），保存后进入 `projects.json`（自动补全检测到的命令建议）；
2. 配置区：权限模式（radio/下拉）、本地端口、测试/检查命令、shell；
3. 控制区：「启动」「停止」后端按钮、后端状态（运行中/端口/workspace/进程数）、自动刷新 `/health`；
4. 令牌区：「生成/复制访问令牌」（一次性明文显示），复制固定 MCP URL；
5. 自测区：「测试连接」按钮 → 用 `selftest.py` 跑本地连接测试并展示步骤结果；
6. 日志区（Phase 6 扩展）：简版可先显示自测输出与后端启动日志；
7. 系统提示：首次选 `system` 权限时一次性风险确认对话框；Quick Tunnel 模式显著警告（临时地址，重启可能变化）。

### 验收标准
- [ ] 启动后自动加载已有项目列表与上次工作区（`active_workspace`）。
- [ ] 新增项目 → 显示检测到的 test/lint/typecheck/build 命令建议，可编辑保存。
- [ ] 启动后端：写 runtime.json → 拉起 `server_main` 子进程 → 轮询 /health 直至就绪 → 状态由「已停止」变「运行中（端口/工作区）」；失败有明确中文错误。
- [ ] 停止后端：正常 SIGTERM，超时强杀；受管进程一并停止（exit_stop_managed 可配）。
- [ ] 令牌：显示/复制；令牌存 Windows Credential Manager（或 DPAPI 回退），绝不明文落盘。
- [ ] 自测按钮：显示每个步骤（initialize/list_tools/get_workspace_info/list_directory/get_capabilities）的成功与否与详情。
- [ ] `system` 权限首次选择弹一次性风险确认；Quick Tunnel 模式显示「临时地址、重启会变化」警告。
- [ ] 无卡死：所有 HTTP/进程操作用线程（QThread/QThreadPool），UI 始终可响应。
- [ ] 手动冒烟通过（offline：仅启动/停止/自测；本机）。

---

## Phase 4 — 隧道模块（Named Tunnel 固定 URL）（🚧 下一步）

**目标**：实现 Named Tunnel 的配置、创建、启动、状态、固定 URL 展示；Quick Tunnel 仅作调试。
**产出**：`tunnel.py`（cloudflared 管理）、state.json 持久化、`desktop` 隧道面板。

### 核心设计（验收重点）
- [ ] **强制验收**：MCP URL = `https://mcp.<user-domain>/mcp`，经 Named Tunnel，**重启/重连后 URL 不变**。
- [ ] 首次配置引导：无账号 → 提示打开 `https://one.dash.cloudflare.com`；有账号 → 提供 `cloudflared login`（弹浏览器）、`cloudflared tunnel create`、`cloudflared tunnel route dns <tunnel> mcp.<user-domain>` 的引导命令与说明（用户手动执行或应用内复制）。
- [ ] 配置持久化到 `state.json`（tunnel_id、tunnel_name、hostname、origin_domain、credentials_source、created_at）。
- [ ] 启动：`cloudflared tunnel --url http://127.0.0.1:<port> run <tunnel_id>`，日志重定向到 `process_logs`。
- [ ] 状态：子进程存活 + 拉取 `/health` 经公网域名可达（自测可选用公网 URL）。
- [x] **transport_security/DNS rebinding 处理**：`server_factory.build_transport_security()` 方案 B（allowed_hosts 追加公网域名，防护保持开启），`RuntimeConfig.public_hostname` 贯通（store/standalone `--public-hostname`）；测试 12 项全绿。公网 Host 下 `/health` 与 `/mcp` 真机验证仍待联调（需 Cloudflare 账号）。
- [ ] Quick Tunnel：仅调试模式，UI 显著警告「临时地址，重启会变化，仅用于测试」；**不作为**验收路径。
- [ ] 凭证（tunnel token / cert）经 SecretsStore（Win Cred Manager / DPAPI），不明文落盘。
- [ ] 未登录/未创建隧道时给出清晰指引，绝不让用户被「有墙内问题」的假设卡住（文档注明：如无法访问 Cloudflare，提供本地+SSH 替代方案说明，但验收以 Named Tunnel 为准）。

---

## Phase 5 — Git 桌面参数配置（桌面侧）

**目标**：桌面可配置 Git 相关参数（不增加工具，仅补配置与校验）。
**产出**：`ProjectConfig` 扩展（git_user_name / git_user_email / default_push_remote 等，可空）+ 桌面 Git 设置区 + 校验。

### 验收标准
- [x] Git 设置区：user.name、user.email、默认 remote/分支（可空）；保存后供工具使用说明（工具层已支持 `git -C`）。
- [x] 校验：输入非法值（含空格/引号等）给出中文提示并拒绝保存（`models.git_field_error`，保存与启动双路径拦截）。
- [x] 不影响现有 33 工具；新增字段向后兼容（旧 JSON 缺省值可用）。

---

## Phase 6 — 日志浏览页 + 自动清理

**目标**：桌面可浏览审计日志与进程日志；自动清理策略生效。
**产出**：`audit.py` 查询接口（按天/级别/工具过滤）、桌面日志页（表格 + 筛选）、后台清理任务。

### 验收标准
- [x] 日志页展示 mcp-*.jsonl（时间/工具/成败/耗时/客户端），支持按日/工具/成败筛选。
- [x] 进程日志页：按进程查看输出（增量追读）。
- [x] 清理：超过 `log_retention_days`（默认 14）的 jsonl 自动删除；总量超 50MB 轮转（保留最近 2 个文件）（audit._rotate_if_needed）。
- [x] 脱敏在展示层同样生效（写时已脱敏 + `key.upper()` 标记匹配修复）。
- [x] 桌面退出时无遗留句柄/线程错误（offscreen 冒烟通过）。

---

## Phase 7 — 打包与全文档

**目标**：可分发安装包 + 完整中英文文档。
**产出**：PyInstaller onedir 构建脚本、Inno Setup 脚本、`release/` 产物目录、五份英文文档 + 中文文档补齐。

### 验收标准
- [x] `pyinstaller --onedir` 构建出可运行桌面程序（`dist\LocalDevMCPBridge\`，启动冒烟通过；cloudflared 捆绑；`packaging/local-dev-mcp-bridge.spec`，`scripts/build.ps1` 一键：pytest→ruff→构建）。
- [x] Inno Setup 脚本产出安装器（Inno Setup 6.7.3 已装；`release\LocalDevMCPBridge-Setup-0.1.0.exe` 已编译并**静默安装成功**：EXIT=0、程序未项、图标已建；per-user 免 UAC，`PrivilegesRequired=lowest`）。
- [x] 产物位于 `release/`，附构建说明（构建/安装命令与产物清单见 docs/en/DEVELOPMENT.md）。
- [x] 文档齐全：中文文档（本文件等）+ 英文 `README.md`(根 README 已补英文段)、`docs/en/ARCHITECTURE.md`、`docs/en/SECURITY.md`、`docs/en/COMPATIBILITY.md`、`docs/en/DEVELOPMENT.md`、`docs/en/CHANGELOG.md`（均已创建）。
- [ ] 打包后冒烟：安装 → 启动桌面 → 选项目 → 启动后端 → 本机自测通过（**安装+启动冒烟已过**；剩「选项目→启动→自测」为 GUI 交互操作，待用户真机补跑）。

---

## Phase 8 — MCP OAuth（Gemini 兼容，不动 Bearer）

**目标**：公网 MCP 走标准 OAuth 2.1（MCP 517 + RFC 9728），Gemini 可接入自定义 MCP；本地/仅 ChatGPT 路径零影响。
**产出**：`oauth_provider.py`（`LocalOAuthProvider`）、`gateway.py`（uvicorn 网关：OAuth 路由 + 同意页 + `/mcp` 代理）、`ServiceCoordinator` 编排集成、`httpx` 依赖、`test_oauth.py`。

### 验收标准
- [x] 发现元数据：`/.well-known/oauth-authorization-server` + protected-resource 元数据（issuer=固定公网域名）。
- [x] 动态客户端注册（DCR）：public/confidential；scope 只接受 `ACCESS_VIEW_MANAGE_MCP_CONTENT`，其它一律拒绝。
- [x] 授权码 + PKCE S256 + state 回传 + 精确 redirect_uri；浏览器同意页允许/拒绝，同意态/授权码过期 410/400。
- [x] access 1 小时 / refresh 60 天滚动轮换 / code 单次使用 / RFC 7009 撤销 / RFC 8707 resource 绑定。
- [x] `/mcp` 反向代理：OAuth 校验后换发引擎 Bearer（secrets 同源）；旧版 ChatGPT Bearer 常量时间直通；本机回环匿名放行；无令牌/无效/过期一律 401（带 resource_metadata 的 WWW-Authenticate）。
- [x] 密钥零落盘：同意、授权码、access token 仅内存；client 注册与 refresh 密文存储（SecretsStore/DPAPI）。
- [x] 编排：仅非 LOCAL 模式于 codex 就绪后拉起网关（8786）；域名取隧道固定 URL；失败→清理返回 ERROR；停止顺序 Codex→桥→网关→隧道。
- [x] 测试 27 项全绿，全量 **181 全绿**；ruff/pyright 新代码零新增告警。
- [ ] 真机验收（待用户）：Cloudflare 路由 Service URL `localhost:8787` → `localhost:8786`（单次操作）→ Gemini 走 OAuth 授权流程。

---

## Phase 9 — 多项目并行开发 + Windows Shell 修复（✅ 已完成）

**目标**：支持多个项目同时运行，每个项目独立 CodexPro 引擎；修复 MCP session 隔离；
确保 Windows Shell 默认不使用 WSL bash。

**产出**：
- `project_manager.py`：`ProjectManager`、`ProjectUnit`、`ProjectView`（多项目生命周期管理，纯 Python，无 Qt）
- `projects.json` 持久化（`config_store` 管理）
- `tools.py`：`WorkspaceCatalog`（per-session 项目绑定），修复 `session_key()` duck-typing bug
- `desktop_main.py`：多项目表格 UI（名称/路径/状态/端口/启用/入口 + 添加/删除/启动/停止按钮）
- `shell.py`：`ShellInfo`、`detect_shells()`、`default_shell()`、`get_shell_info()`（WSL 永不自选）

### 验收标准
- [x] 两个项目同时启动不同端口的 CodexPro 引擎，互不影响（`test_parallel_lifecycle_isolation`、`test_parallel_real_engines`）
- [x] GPT session 绑定项目 A，Gemini session 绑定项目 B，互不泄漏（`test_two_sessions_isolated_workspace_switch`）
- [x] `switch_workspace` 只影响当前 session（`test_switch_changes_only_calling_session`）
- [x] enabled 标记持久化，桌面启动自动恢复已启用项目引擎（`test_start_enabled_auto_restore`）
- [x] PowerShell / pytest / pyright / git status 执行成功（`test_commands.py`、`test_git.py`、`test_shell_info_shape`）
- [x] WSL 不会被默认调用（`test_shell_info_shape`：检测到 wsl 但 default type != "wsl"）
- [x] `shell_info` MCP 工具返回正确的 shell 信息
- [x] `list_projects` 列出所有项目及状态
- [x] 全量测试 258 全绿

---

## Phase 10 — 多项目桌面交互与前端体验重构（🚧 代码/构建完成，发布接力收尾中）

**目标**：把当前“全局配置 + 多项目列表”的混合交互改为真正的**按项目独立配置**，修复项目状态不实时、滚轮误改下拉框、服务控制冗余、Gemini 配置常驻等问题，并补齐诊断、视觉状态和发布切换能力。

**产出**：
- `ProjectConfig`：新增客户端类型与 Gemini Redirect URI 等按项目持久化字段；保留旧配置兼容迁移。
- `desktop_main.py`：项目表去掉“启用”列，操作列负责单项目服务启停；权限/客户端/连接方式下拉禁用滚轮误切换；服务配置、Git、端口、令牌、MCP 地址、自测按选中项目独立；Gemini OAuth 区按客户端类型动态显示；服务控制只保留单一启停按钮；新增状态卡和一键诊断。
- 连接方式恢复四种：仅本机、Cloudflare 固定、ngrok 固定、Quick Tunnel 临时地址。
- `app_state.py` / `gateway.py`：项目令牌贯穿 Gateway，Gemini 授权时未选工作区或工作区未运行必须阻断，不能继续发码。
- 发布链：修复 Ruff，完整 pytest/ruff/pyright/build；更新中英文文档、版本、安装器、Release 与桌面快捷方式；最终切换使用脱离当前旧版进程的自恢复脚本，避免升级过程把 MCP 会话永久断开。

### 验收标准
- [x] 项目表列为“名称/路径/状态/端口/入口/操作”，无“启用”列；点击操作列启动后状态 1 秒级刷新为“启动中/已连接/失败”等真实状态，运行中按钮切换为“停止服务”。
- [x] 桌面不再按历史 `enabled` 自动恢复项目，也不使用固定 4 秒等待“启动所有服务”。
- [x] “权限模式”“客户端”“连接方式”QComboBox 在鼠标悬停时滚轮只滚动窗口，不改变选项。
- [x] 每个项目独立记忆权限模式、客户端类型、连接方式、公网域名、Windows 桥开关、Git 参数、Gateway/内部端口、Gemini Redirect URI；所有输入完成后自动保存，切换项目再返回不得重置。
- [x] 访问令牌与 Cloudflare Tunnel Token 按项目用 SecretsStore 加密保存；已有全局旧令牌无损迁移，升级后现有连接不因令牌变化被强制失效。
- [x] 客户端选择“Gemini Spark”时显示 Gemini OAuth 配置；选择“ChatGPT 网页端”时隐藏；Gemini 凭据仍只经 SecretsStore 保存且 Secret 不进日志/JSON。
- [x] 连接方式四种全部可选：Local / Cloudflare Named / ngrok reserved domain / Quick Tunnel；字段启用、提示与临时 URL 标识正确。
- [x] 服务控制区只有一个动态“启动服务/停止服务”按钮（另保留“高级设置”）；无“重启服务”和第二个停止按钮。
- [x] 控制页增加组件状态卡；“连接诊断”独立页提供一键诊断 + 真实 MCP 自测 + 开发环境检测，错误用中文给出明确原因和下一步。
- [x] Gemini OAuth 同意页：未选择项目或所选项目 CodexPro 未运行时返回可理解错误并拒绝 approve/code，不允许静默回落。
- [x] README/安全与架构文档明确：桌面默认权限是“完全访问（危险）”（用户明确意图），首次仍保留一次性风险确认。
- [x] 关闭窗口不在 GUI 主线程同步等待所有子进程停止。
- [ ] `pytest tests -q`、`ruff check src tests`、`pyright` 全绿；PyInstaller + Inno Setup 构建成功，安装版启动/选项目/启动/停止/自测冒烟通过。
- [ ] Git commit + push 成功；GitHub Release 更新为新版本并附安装器；桌面旧快捷方式替换为新版快捷方式，升级切换后 MCP DevBridge 能自动恢复服务。

---

## 风险与缓解（记录）

| 风险 | 缓解 |
|---|---|
| ChatGPT Pro 对自定义 MCP 仅读/获取 | 明确文档：完整写需 Business/Enterprise/Edu；不伪装绕过；提供兼容提示 |
| Gemini Spark 限制（US/18+/语言） | 文档说明先决条件；本机自测 + OpenAI 兼容检查 |
| DNS rebinding 保护拦公网 Host | Phase 4 显式 transport_security 配置 + 真机验证 |
| Quick Tunnel URL 变化 | 仅调试模式 + 醒目警告；验收用 Named Tunnel |
| 有墙内网络问题 | 文档注明本地+SSH 替代方案；验收以 Named Tunnel 为准 |
| 端口默认值不一致（8765 vs 2865） | **已解决（2026-08-09）**：统一为 `constants.DEFAULT_*_PORT` 集中维护（Gateway 8786 / CodexPro 8787 / Windows-MCP 28731 / Legacy backend 8765），`RuntimeConfig.local_port` 兼容属性，旧配置自动迁移；桌面端口配置 UI + 占用预检 |

## 变更记录

- 2026-08-06 · 创建完整开发计划；Phase 0/1/2 标记完成，Phase 3 进行中。
