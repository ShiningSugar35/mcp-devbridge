# MCP DevBridge 未完成开发计划

## 2026-09-10 · D: Windows bridge 残留子进程导致项目重启风暴（源码候选已验证，运行态待维护窗口部署）

- 执行 run：`lr_mtv9ak0t_22f338b52c13`。用户明确要求当前 `D:` 正在承载 MCP/项目任务，**不得为了开发/部署主动停止或重启 D: 8789，也不得热更新共享 Hub**。该约束生效后，用户再次报告 D: 已经因旧 supervisor 重启风暴进入 502，因此只做了故障止血：将 grw 的 D: `windows_enabled` 回滚为 `false`，并在确认与 D: CodexPro 不同树后精确清理孤儿 Windows-MCP PID 树；没有人工终止 D: CodexPro。随后旧 supervisor 于 `08:25:18Z` 自行恢复 D: 为 PID 32776。此后源码、测试与文档均只在 `D:\Environment\mcp` 内完成；新候选仍**未部署**，重新启用 Windows 控制必须等维护窗口和 active-task drain。
- 现场复现：grw 设备 `D:` 项目开启 Windows 控制后，`2026-09-10 08:03:44Z/08:03:56Z` CodexPro 健康探针连续两次 `TimeoutError`，supervisor 触发项目恢复；随后 `08:04:00Z` 起恢复反复失败为 `Windows-MCP 本机端口 28732 已被其他进程占用`，失败计数持续增长到至少 45，并不断更换 CodexPro PID。现场同时观察到 `8789` 与 `28732` 实际仍有 listener，而 DevBridge 注册/路由状态可显示 D: 未运行并返回 502，证明内存生命周期状态与真实进程已经失配。
- 已证实根因 1：`BaseEngineProcess.stop()` 在 Windows 先对 `uvx.exe` 仅执行父进程 `terminate()`；只要父进程正常退出就不会进入 `kill_process_tree()` fallback。Microsoft 明确说明终止一个进程**不会自动终止它创建的子进程**，而 `taskkill /T` 才定义为同时终止指定 PID 及其子进程。因此 `uvx → uv → windows-mcp.exe → python` 链可能留下孤儿后代继续占用 28732，下一次 `WindowsBridgeManager.start()` 就稳定撞端口冲突。权威依据：Microsoft Learn `Terminating a Process`、`taskkill /T`。
- 已证实根因 2：故障旧实现的 `ProjectUnit.state` 会把可选 Windows bridge 的 `STARTING/STOPPING/ERROR` 提升成整个项目状态；`_supervisor_tick()` 又把任何非 READY 项目视为核心健康失败，`_recover_project()` 会停止并重建**整套** `ProjectUnit`。因此可选桌面桥的端口残留/启动失败会反向驱动 CodexPro 重启，扩大故障域并制造“启动中/502”风暴。
- 旁支缺陷：现有 `windows-mcp 0.8.2` 在 MuMu UI 树遍历中可抛 `UnboundLocalError: tree_node`，但 HTTP listener 仍能继续响应；这属于上游 UI-tree 稳定性问题，不应成为核心 CodexPro/工作区 READY 的判据。本工作项不修改 uv cache 内第三方包源码。
- 架构裁决：① WindowsBridgeManager 停止必须以**自身拥有的根 PID**为边界回收完整进程树，禁止按进程名扫杀；② 项目路由 READY/health 以 CodexPro 为核心真值，可选 Windows bridge 独立报告 STARTING/ERROR/disabled，不能把核心工作区降为不可路由；③ supervisor 的 CodexPro 自愈不得因 bridge 单独故障形成整项目无限重启；④ 显式用户停止项目仍需同时收口 CodexPro 与 Windows bridge；⑤ bridge 恢复失败应有独立、有界日志/状态并允许核心项目降级继续工作。
- 兼容与安全：仅终止 manager 自己 spawn 的 PID 树；不触碰其它用户进程/其它项目，不改变 50-tool Hub 合同、鉴权、权限或 mutation retry；POSIX 保持原 process-session 语义。当前运行态不部署，因此不会中断 D: 活跃任务。
- 成本：停止动作仅在显式 stop/recovery 边界执行一次 O(owned process tree) 回收；正常健康循环不新增常驻线程/队列。bridge 状态解耦不增加网络探针频率。所有测试缓存/临时输出必须留在项目内并在收尾精确清理。
- 验收（L2）：先新增旧实现可失败的进程树残留与状态耦合反例；修复后 targeted `test_engines/test_project_manager/test_windows_bridge_enabled`、相关集成/负向、Ruff/Pyright 通过；Review 覆盖并发、状态隔离、进程所有权、Windows/Linux；**本轮不安装、不热更新、不重启 grw D:**。源码通过后只记录“待部署”，不得冒充运行态已修复。

## 2026-09-10 · 14:00–14:30 ChatGPT 会话 `mcp-wjp` 被系统禁用事件（调查中）

- 执行 run：`lr_mtv5kir1_453b589dcfc1`。用户报告北京时间 `2026-09-10 14:00–14:30`（UTC `06:00–06:30`）期间，一个原本已能正常调用 `mcp-wjp` 的 ChatGPT 会话在后续调用时进入“工具被系统禁用/不可继续执行”状态。错误文案本身不包含责任层证据，不能直接等同于 OpenAI 风控或 MCP 服务端拒绝。
- 目标：以本机 FlightRecorder、Gateway/Tunnel、项目 supervisor、CodexPro、进程/listener 和认证终态为主证据，区分 DevBridge/MCP 缺陷、Tunnel/网络瞬时故障、Bearer/OAuth/URL capability 稳定性问题、工具合同漂移与 ChatGPT/OpenAI 宿主侧禁用；若本地缺陷可复现，按 defect-first 最小修复并验证运行态。
- 非目标：不通过降低鉴权强度、放行匿名公网访问、扩大工具权限、增加写操作重试或改变 50-tool 公共合同来“避免风控”；不把本机链路健康反推为 OpenAI 一定触发风控；无法访问的 ChatGPT 宿主私有日志必须明确记为证据缺口。
- 取证窗口：主窗口 14:00–14:30，北京时间前后各扩 10 分钟（13:50–14:40 / UTC 05:50–06:40）用于观察前因后果。按 `401/403/404/429/5xx`、`initialize/tools/list/tools/call`、OAuth/Bearer/URL capability、SSE/普通响应、timeout/read error/reset、Gateway/Tunnel/CodexPro 重启、route/workspace 错误分类；只保存状态码、脱敏 trace、时间、PID/端口与哈希，不保存 token。
- 方案裁决（待根因确认）：优先修本地可证状态机/认证/连接缺陷；若只有宿主侧不可见禁用且本机全程无认证/传输/进程异常，则不做无依据代码改动，而是保留宿主证据缺口和后续最小复现建议。若需改运行态，风险等级至少 L2；命中鉴权/安全核心则升 L3，完整验证后才允许热更新。
- 成本与边界：取证采用单次有界扫描和聚合，不新增常驻采集线程/无界日志；任何新增状态或重试必须有硬上限，mutation 继续不自动重放。测试/摘要/临时脚本全部位于 `D:\Environment\mcp` 内，收尾只清理本任务可证明生成的可再生文件。
- 硬验收：给出责任层竞争假设和证据排序；若修复则有旧实现失败反例→新实现通过、相邻负向/并发/鉴权回归、真实 Hub 验证；若不修复则必须证明“当前本地没有足够缺陷证据”而不是凭感觉归咎 OpenAI。最终已完成事项迁入 `进度验收.md`，本节从计划删除。

> 当前正式版本与本机运行版本均为 `v0.8.9.3`；正式发布源为 `39a9f139b98ebb93326219d865244f91aa46f7bc`，维护分支为 `release/v0.8.9.3`。0.8.9.2 长任务恢复修复与 0.8.9.3 Windows 控制桥接修复均已完成、验收并迁入 `进度验收.md`，不再作为待开发事项保留。
>
> 当前只保留平台能力相关的 S8–S10。用户已于 2026-09-02 明确要求 S8–S10 暂停执行，除非后续重新授权，不主动推进；当前真实 Connector 仍报告 `nativeMcpTasksExtension = not emitted by baseline connector`，因此这些事项同时保持 `blocked_platform`。平台能力 durable run：`lr_mtfcuqcl_6abdc2169c31`。
>
> 已知但没有独立反例/兼容证明的后续优化（例如极端 subprocess 大输出流式化、历史安装依赖目录瘦身、Gateway 高复杂度函数拆分）不在本计划中伪装成当前开发任务；只有用户提出或形成可验证故障/性能目标后再建立新的独立工作项。

## 1. 当前能力边界

本地 durable fallback 已实现并验收：后台任务、plan/checkpoint/evidence、terminal receipt、compact transport 与按需 detail retrieval 都可在 Connector/会话重连后作为持久事实源恢复。当前平台边界是：DevBridge 可以让本地任务跨 ChatGPT turn/断连继续执行，但不能在当前 assistant turn 被宿主结束后自行创建下一轮 ChatGPT assistant turn。

Regular Chat 浏览器控制器继续只作为隔离实验源码/fixture 保留，不进入正式桌面、CLI、PyInstaller、Windows/Linux build 或 installer，也不得用于自动提取登录态 ChatGPT Output 绕过平台 capability gate。

## 2. S8 · Native MCP Tasks：`blocked_platform` / `paused_by_owner`

### 2.1 恢复开工门

只有同时满足“用户重新授权继续 S8–S10”以及以下任一平台条件，才允许进入实现：

1. ChatGPT/目标 MCP 客户端在协议协商中真实声明 `io.modelcontextprotocol/tasks`；或
2. OpenAI 官方提供明确允许、可由自定义 MCP server 使用的等价异步 continuation primitive，并有公开可运行文档/SDK 路径。

若仍为 `not emitted by baseline connector`，S8 继续阻断；只允许更新能力证据，不写伪实现、不改公开工具合同来模拟 native Tasks。

### 2.2 解阻后的实现约束

- capability-gated：未声明能力的客户端继续走现有 `tools/call + bash/long_run` fallback；
- 单一身份：native task handle 只能映射到现有 workspace/bash task/long-run identity，不复制第二套成功/失败真值；
- terminal 状态只来自 BashTaskManager terminal receipt 或显式 durable resolution，unknown 一律 fail-closed；
- 默认结果继续 compact，大输出仍通过 `task_output / run_detail` 分页读取；
- create/cancel/get/result 必须按 identity 幂等，网络失败不能重复 mutation；
- registry、TTL、事件、日志和映射数量必须有硬上限；
- workspace/path/permission/token/redaction 与普通 MCP 工具完全一致；
- 不使用浏览器私有自动化、cookie/token 或登录态页面抓取绕过宿主能力。

### 2.3 解阻后的硬验收

至少覆盖 capability 未声明拒绝、create→running→completed/result、failed/cancelled/input-required、重复 create/cancel 不产生重复副作用、Connector/Gateway/CodexPro 替换后同一 task identity 恢复、跨 workspace/伪造/stale id 拒绝、transport budget 不回退、安全边界一致、registry/内存/日志有界。只有真实 ChatGPT Connector 跨断连取回同一 native task 的证据成立，S8 才可完成。

## 3. S9 · 长任务宿主验收：`blocked_platform` / `paused_by_owner`

S9 只在 S8 或等价 host async primitive 真实可用并通过 conformance 后执行；现有本地 background process、LongRunState、terminal receipt、compact transport 与 detail retrieval 不重复开发。

解除阻断后执行真实 ChatGPT host 的长时 soak（至少 20 分钟，建议 45–60 分钟），包含多阶段读写、build/test、review/rework，并按计划注入 Connector 短断连/重连、单个服务替换、短公网故障、后台任务取消及真实 host/context 恢复场景。计划内重启前仍必须 active-task drain，不允许以重启误杀其它会话任务。

硬指标：task identity 恢复率 100%；duplicate mutation=0；错误 workspace/device route=0；terminal 均有 receipt/evidence；unknown 不猜成功；compact payload 不回退；无 active task 被维护更新误杀；CPU/RSS/句柄/task registry/checkpoint/log 在既定上限内；新会话可以按 `AGENTS.md → 项目架构.md → 开发计划.md → 进度验收.md → durable run` 零猜测恢复。

## 4. S10 · Native-host 最终 completion 与发布门：`blocked_platform` / `paused_by_owner`

S10 等待 S8/S9。平台能力未解除且用户未重新授权前，对应平台 durable run 必须保持 blocked/rework，不能用本地 fallback 已完成来冒充 native-host 整体 PASS。

解除阻断后的最终顺序：完成 S8 conformance → 完成 S9 真实宿主 soak → defect-first Review（P0/P1/P2=0）→ 按 L3 重跑相关 Windows/Linux 构建与真实 MCP/权限/升级门 → 更新架构/验收/CHANGELOG → 精确清理本轮可再生临时物 → `git diff --check`/敏感信息/worktree 审计 → commit/push → 最新 revision `long_run_review(pass)` → `long_run_complete`。

不得创建/移动历史 tag 来伪装完成；不得删除用户 `installer/`、配置、凭据、正式 release 资产或其它会话文件；任一未来 native-host 路径出现兼容回归时，关闭 capability gate 回退现有 durable fallback，已有 task/run 持久事实不得丢失。

## 5. 新会话接管检查表

1. 工作区 `D:\Environment\mcp`，维护分支 `release/v0.8.9.3`；先读 `AGENTS.md → 项目架构.md → 开发计划.md → 进度验收.md`。
2. 使用 `git_status/show_changes` 核对现场；用户原有 `installer/`、诊断包和未知来源未跟踪文件不得清理或纳入提交。
3. 首先实测 `server_config.longRunPolicy.nativeMcpTasksExtension`；若仍未协商 native Tasks，S8/S9/S10 保持阻断。
4. 即使平台 capability 已出现，也必须先取得用户对 S8–S10 的重新授权，再按 S8→S9→S10 顺序执行；每个已完成工作项立即迁入 `进度验收.md` 并从本文件删除。
5. 长任务使用唯一 durable run；重连后从持久事实源恢复，不依赖聊天记忆。
