# MCP DevBridge 未完成开发计划

> 当前正式GitHub Release仍为`v0.8.9.1`同版本重发源；本机已包含2026-09-09长任务恢复修复。用户现明确授权发布新补丁`0.8.9.2`，本轮在独立分支`release/v0.8.9.2`执行；平台阻断的S8–S10继续保持暂停，不借本次补丁伪实现Native MCP Tasks。

## 0. 0.8.9.2 发布计划（2026-09-09）

根因基线：北京时间12:00—13:00本地证据确认一次`wait_task` Gateway→CodexPro响应头前`httpx.ReadError`导致47ms HTTP 502，以及50次无progress轮询均约30秒才产生JSON终态。修复已先红后绿：只读`wait_task`允许一次安全内部重试，无progress观察窗收紧到15秒；写/副作用工具不重放。OpenAI公开状态未登记目标小时匹配事故，本地也没有宿主私有`Resume stream unavailable`字符串或HAR/request关联，因此本轮只宣称消除已证实MCP诱因，不宣称控制OpenAI宿主恢复层。

资源/复杂度目标：复测Gateway不可变工具合同缓存、HTTP/任务有界队列、FlightRecorder/log轮转、Bash输出保留和安装载荷；对Python/TS热点做复杂度审查。仅合入有明确收益且不改变公开50-tool合同、权限/路由、durable事实源或写操作幂等边界的低风险优化。已知`src/local_dev_mcp_bridge/shell.py`大输出先全量收集再截断、安装目录历史重复dist-info等属于潜在改进，但若缺独立反例/兼容证明则只登记，不在补丁发布前做高风险重构或盲删。

本轮审计结果：固定50-tool目录1000次复测wall 2.8883ms、单次峰值71561B、wire 71008B、fingerprint不变；5秒进程采样中桌面/管理员broker/五个Node引擎/cloudflared均可归属于当前安装根，未发现可安全判定为孤儿的重复实例。Python C901审计报告42个既有复杂函数，主要热点包括`gateway._proxy`=57、`gateway._mcp_endpoint`=39、`gateway._exec_local_tool_sync`=24，说明Gateway应在后续版本按协议解析/鉴权路由/上游代理/本地执行拆模块，但该重构会触及当前故障核心路径，不纳入0.8.9.2。目录审计：`release/`约4.06GB主要由历史正式/回滚资产构成，受发布历史保护；当前安装`_internal`约327MB且可见click/cryptography/httpcore2/httpx2/pydantic旧新版dist-info并存。没有候选payload清单级删除证明前不盲删这些文件；本轮只在发布结束清理自身可再生临时构建物。

成本/风险：新增一次`wait_task`重试只在响应头前瞬时HTTP错误触发，最多增加一次loopback请求；15秒观察窗会提高长任务状态查询频率，但不会缩短后台进程生命周期。主要发布风险是版本/资产不一致、升级误杀其它会话任务、公开schema fingerprint漂移和安装目录历史文件误清理；全部以L3门、active-task drain、哈希/冻结版本核验和显式文件白名单控制。

回滚：历史tag/Release不改写；0.8.9.2若构建/安装/真实数据面任一门失败则不发布或停止升级，保留当前0.8.9.1正式Release与本机可回退资产。升级不得删除用户`installer/`、配置、凭据、历史release或未知文件。

硬验收：五源版本统一0.8.9.2；Ruff、Windows/Linux Pyright、全量pytest、uv lock、PowerShell/Bash语法、CodexPro build/full smoke、Windows/Linux同源构建全部通过；annotated tag/CI/Release资产同源；安装前所有运行根/opened workspace无其它active task；正式Windows资产本机升级后5/5根、Gateway/Tunnel/broker、50-tool fingerprint、鉴权、路由/full-system和真实长任务继续通过；最终文档、diff-check、commit/push、review/complete闭环。
>
> 当前维护分支：`release/v0.8.9.2`。
>
> 当前发布 durable run：`lr_mttp9fqa_7fb54be0e7d2`；平台能力 durable run：`lr_mtfcuqcl_6abdc2169c31`。
>
> S1–S7、Q2 与本地 S10 工程门均已完成并迁入 `进度验收.md`。本文件只保留仍未完成或被平台能力阻断的事项；用户已于 2026-09-02 明确要求 S8–S10 暂停执行，除非后续重新授权，不主动推进。

## 1. 当前能力边界

本地 durable fallback 已实现并验收：后台任务、plan/checkpoint/evidence、terminal receipt 与重连后的零猜测恢复均可用。实现与测试证据见 `进度验收.md`。

当前真实 ChatGPT → DevBridge 连接仍报告：

```text
nativeMcpTasksExtension = not emitted by baseline connector
```

因此 DevBridge 可以让本地任务跨 ChatGPT turn/断连继续执行，但不能在当前 assistant turn 结束后自行要求 ChatGPT 创建下一轮 assistant turn。Regular Chat 浏览器控制器继续只作为隔离实验源码/fixture 保留，不进入正式桌面、CLI、PyInstaller、Windows/Linux build 或 installer，也不得用于自动提取登录态 ChatGPT Output 绕过平台能力门。

## 2. 已完成前置依赖（只引用，不重新列为待开发）

2026-09-02 已完成大型项目/复杂任务的信息可达性补强：默认 compact tool-result 与按需完整取回分层，`codexpro(action="task_output")` / `codexpro(action="run_detail")` 可分页恢复 retained task output 与 durable run 旧 evidence/checkpoint/task resolution；UTF-8 字节预算、opaque HMAC cursor、stale/cross-object fail-closed、active-task 优先、真实运行态与 L3 门均已验收。完整事实见 `进度验收.md §10`。后续 S8/S9 不得回退这套 transport budget，也不得重新扩大默认 `list_tasks / wait_task / long_run_status` 结果。

## 3. S8 · Native MCP Tasks：`blocked_platform`

### 3.1 目标与非目标

目标是在**真实 ChatGPT/目标 MCP host 明确声明 Tasks extension（或 OpenAI 明确支持的等价异步 primitive）之后**，把现有 durable fallback 映射到 host 可恢复的原生异步任务生命周期，使同一任务在 Connector/host 重连后仍按稳定 identity 继续查询、取消和取结果。

非目标：

- 不通过浏览器私有自动化、cookie/token、登录态页面抓取或伪造 ChatGPT Output 绕过平台能力；
- 不建立与 `LongRunState + BashTaskManager terminal receipt` 平行的第二套成功/失败事实源；
- 不因为平台 capability 仍缺失就修改公开工具合同、增加无效 Tasks 响应或宣称已支持 native Tasks；
- 不回退 `task_output / run_detail` 的按需取详情能力。

### 3.2 恢复开工门（新会话第一步）

每次接管先实测 `server_config.longRunPolicy.nativeMcpTasksExtension` 和真实 Connector capability；只有满足以下任一条件才进入实现：

1. ChatGPT/目标 MCP 客户端在协议协商中真实声明 `io.modelcontextprotocol/tasks`；或
2. OpenAI 官方提供明确允许、可由自定义 MCP server 使用的等价异步 continuation primitive，并有可运行文档/SDK 路径。

若仍为 `not emitted by baseline connector`，S8 维持 `blocked_platform`，禁止为了“完成计划”写业务代码；只允许更新能力证据或做不改变行为的文档审计。

### 3.3 实现架构

解除阻断后按以下顺序实施：

1. **Capability gate**：只有客户端 opt-in 后才启用 native task 路径；普通客户端继续走现有 `tools/call + bash/long_run` fallback。
2. **单一身份映射**：native task handle 必须稳定映射到现有 task/run identity；建议服务端保存 `native_task_id ↔ workspace_id / bash_task_id / long_run_id / work_revision` 的有界映射，不复制 stdout/evidence 真值。
3. **状态映射**：至少覆盖 queued/running/input-required/completed/failed/cancelled；terminal 状态只能来自 BashTaskManager terminal receipt 或显式 durable resolution，重连后 unknown 必须 fail-closed。
4. **结果读取**：原生 task result 默认仍返回 compact summary；大输出继续通过现有 `task_output / run_detail` 分页取回，禁止把 native Tasks 变成新的大包通道。
5. **取消语义**：host cancel → 精确 task cancellation；不得取消同根其它任务，不得把客户端断连等同于 cancel。
6. **幂等与重放**：create/cancel/get/result 重试必须按 task identity 幂等；网络失败不得重复启动 mutation。
7. **生命周期上限**：native-task registry、TTL、terminal retention、事件/日志均有硬上限；不新增无界 Map、线程或持久日志。
8. **安全边界**：workspace/path/permission/token/redaction 与普通工具完全一致；native task id 不携带路径、secret 或可伪造权限信息。

### 3.4 数据流与失败策略

```text
host capability opt-in
    → tools/call / native task create
    → existing workspace route + permission checks
    → BashTaskManager / LongRunState
    → terminal receipt remains source of truth
    → native tasks/get/result = bounded projection
    → large detail = task_output / run_detail
```

Connector、Gateway 或 CodexPro 重启时，不根据“旧进程消失”推断任务成功；能从 durable receipt 恢复则恢复，不能证明终态则返回 unknown/input-required 风格的显式状态并阻断 completion。任何 native 层损坏都可关闭 capability gate，回退到现有普通 MCP durable fallback，不迁移/删除已有 LongRunState。

### 3.5 defect-first / conformance 验收

解除阻断后先写失败测试，再实现；至少覆盖：

- capability 未声明时 native task 路径不可达；
- create → running → completed/result 正常；failed/cancelled/input-required 正常；
- 重复 create/cancel/get 不产生 duplicate mutation；
- Connector/Gateway/CodexPro 替换后同一 task identity 可恢复；
- stale/未知 task、错误 method、跨 workspace/task handle、伪造 id fail-closed；
- 任务大 stdout/长 evidence 不突破现有 transport budget，可通过 detail retrieval 精确取回；
- secret/path/permission/redaction 与普通工具一致；
- registry/TTL/内存/句柄/日志有界。

只有**真实 ChatGPT Connector**跨断连取回同一 native task 的证据成立，S8 才可从 `blocked_platform` 改为 `done`。

## 4. S9 · 长任务宿主验收：`blocked_platform`

### 4.1 前置条件

S9 只能在 S8 或等价 host async primitive **真实可用并通过 conformance** 后执行。当前本地 background process、LongRunState、terminal receipt、compact transport 与 detail retrieval 已通过，不重复开发。

### 4.2 真实 soak 方案

执行至少 20 分钟、建议 45–60 分钟的真实无人值守复杂任务，使用实际 ChatGPT host，不以本地 fixture 代替。任务必须包含多阶段源码读取、编辑、build/test、review/rework，并持续写 durable checkpoint。期间按预注册时间点注入：

1. Connector 短断连/重连；
2. Gateway 或单个 CodexPro 项目引擎替换；计划内重启前必须 active-task drain；
3. 短公网故障；
4. 一个后台任务取消；
5. host/context compaction 或新 turn 恢复场景（只能用平台真实行为，不做浏览器抓取）。

### 4.3 硬验收指标

- 同一 native/durable task identity 恢复率 = 100%；
- duplicate mutation = 0；错误 workspace/device route = 0；
- 已有 conversation/task identity 丢失 = 0；
- terminal status 均有 receipt/evidence，unknown 不被猜成成功；
- compact `list_tasks / wait_task / long_run_status` 继续有界，历史详情可由 `task_output / run_detail` 按需恢复；
- 没有 active task 被计划内热更新误杀；
- CPU/RSS/句柄/task registry/checkpoint/log 文件在预设硬上限内，无随轮询持续线性泄漏；
- 用户不需要重复粘贴上下文即可从 `AGENTS.md → 项目架构.md → 开发计划.md → 进度验收.md → durable run` 恢复。

任何指标失败均回到对应实现层返工，不能用“最终结果正确”掩盖 identity、重复副作用或资源泄漏。

## 5. S10 · durable completion 与发布门

### 5.1 当前状态

本地工程能力已完成；S10 只等待 S8/S9。S8/S9 未解除前，现有平台 durable run 必须继续保持 blocked/rework，`long_run_complete` 必须 fail-closed，不得登记整体 PASS。

### 5.2 最终收口顺序

平台能力出现后严格执行：

1. 重新读取事实源与当前 Git/运行态，确认无并发未提交修改；
2. 完成 S8 conformance + 真实 Connector 验收并迁入 `进度验收.md`，从本计划删除 S8；
3. 完成 S9 长时 soak 并迁入验收，从本计划删除 S9；
4. defect-first 最终 Review，P0/P1/P2 必须为 0；
5. 按 L3 跑与 release 相关的 Windows/Linux 构建、正式 MCP/Hub/权限/升级门，所有资产来自同一 commit；
6. 复核默认 compact 结果与 detail retrieval 没有回归，大型项目信息可达性与长会话 context 压力同时满足；
7. 更新 `项目架构.md / 进度验收.md / CHANGELOG`，清理仅本轮可再生临时物，`开发计划.md` 最终只剩真正阻断项或变为空；
8. `git diff --check`、敏感扫描、worktree 审计、commit/push；
9. 最新 work revision 独立 `long_run_review(pass)` 后才能 `long_run_complete`；
10. 只有全部正式门通过且用户未要求继续保留预发布状态时，才允许处理版本 Tag/Release。

### 5.3 禁止事项与回滚

- 不得创建/移动历史 `v0.9.0` Tag 来伪装当前分支完成；在正式发布裁决前不创建 Release；
- 不得宣称 DevBridge 能在平台不支持时自行创建新的 ChatGPT assistant turn；
- 不得删除或改写用户 `installer/`、配置、凭据、正式 release 资产或其它会话未提交文件；
- 任一 native host 路径出现兼容回归时，关闭 capability gate 回退普通 durable fallback；已有 task/run 持久事实不得丢失。

## 6. 新会话接管检查表

1. 工作区 `D:\Environment\mcp`，分支 `release/v0.8.9.1`；先读 `AGENTS.md → 项目架构.md → 开发计划.md → 进度验收.md`。
2. 运行 `git_status`；用户原有 `installer/` 保留，不清理、不暂存。
3. 首先查 `server_config.longRunPolicy.nativeMcpTasksExtension`；若仍未协商 native Tasks，则诚实保持 S8/S9/S10 阻断，不开发伪实现。
4. 即使平台 capability 已出现，也必须先确认用户重新授权 S8–S10；获得授权后才按 S8 → S9 → S10 顺序执行，每个完成项立即迁入 `进度验收.md` 并从本文件删除。
5. 长任务使用唯一 durable run；重连后从事实源恢复，不依赖聊天记忆。

## 7. 回滚与非目标

- durable terminal-receipt 增量可回滚到正式 v0.8.9 CodexPro runtime；回滚不修改项目配置、Gateway、Tunnel 或凭据；
- Regular Chat 实验路径默认不可达，移除实验目录不影响正式 MCP 数据面；
- 未来 Tasks 实现必须 capability-gated；协商缺失时继续使用当前工具级 durable fallback；
- 不以浏览器私有自动化、登录态抓取或伪造 host 证据替代平台能力。
