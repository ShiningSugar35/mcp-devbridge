# MCP DevBridge v0.8.9 端到端连接治理开发计划

> 状态：s1–s9 已完成，s10 正在返工，s11–s14 未完成。本文件只保留当前尚未完成或尚未通过验收的工作。2026-08-29 已实际启动三轮 s10 编排，但 r1/r2 证据无效、r3 明确失败；当前没有可冒充通过的正式 soak 在运行。必须继续唯一 durable run `lr_mtb6w0ih_a803aa0ad300`，不得新建 long-run。
>
> 唯一 durable run：`lr_mtb6w0ih_a803aa0ad300`
>
> 当前源码分支：`release/v0.8.8`；当前 HEAD 与 `origin/release/v0.8.8` 均为 `eed7df219667b2c14e78a168a5d57ac35f9f7f8b`；正式 `v0.8.8` tag 仍解引用到 `104bb9efa42f55e7df76c78d56b3394616d56efc`。不得移动旧 tag、force-push、reset、clean 或覆盖现有未提交成果。
>
> 已完成并有证据的需求分析、首轮/第二/第三现场取证、OAuth 连续性、固定 50-tool contract 基础实现、call-stage policy gate、deterministic handle rehydrate 基础实现、legacy `ws-` 进程内 affinity 回归修复、多根无强证据 workspace-affine 调用 fail-closed、v2 原子 route state/双实例防丢失与失效清理、深 MCP canary、CodexPro graceful lifecycle 基础实现、Bash active cap/Windows 绝对启动器、scoped analysis 第一阶段及开发事实源治理，均已迁入 `进度验收.md`，不在本文件重复保存施工记录。

## 1. s10 反证现场、隧道启动韧性与持续轻量探针

### 新现场与根因裁决

- r3 正式编排于 2026-08-29 05:35（本地）启动；05:40 仅重启项目 D，D PID 被替换且 Gateway、项目 C、既有 Tunnel 未被误重启，但旧 deterministic workspace handle 随即被 Gateway 当作永久失效并写入删除语义，本地/公网 old-handle matrix 均未恢复。
- 公网 `read` 于 05:45:57 发起后直到 06:37:08 才终止，耗时约 3,070,930 ms（51 分 11 秒）；因此现有 45 秒上游 deadline 没有覆盖公网流式连接的完整终止链，r3 必须判失败，不能进入发布。
- 用户 06:37:30 再次“启动所有项目”，06:38:11–06:38:39 的 `tunnel.log` 明确记录系统 DNS 对 `argotunnel.com` 的 SRV 查询超时并导致 Cloudflare API 不可达，`cloudflared` 随后退出；约 06:40:22 DNS/API 检查已自行恢复。直接诱因是瞬时 DNS/网络故障，产品缺陷是共享 Tunnel 启动没有有界重试、一次外网抖动会把整批项目启动判失败并回滚，且故障后缺少足够的持续取证。
- 原始现场已只读保存到 `.ai-bridge/forensics/20260829-0637-tunnel-failure/`；r1–r3 目录保留，不覆盖、不删改，后续正式证据从新编号开始。
- 2026-08-29 r4 最新 staging 实机预检又捕获到独立 Tunnel P1：05:06:16 起既有 cloudflared 四条 QUIC 边缘连接连续 `timeout: no recent network activity`，而本机 Gateway 与 C/D CodexPro 始终健康、PID 未变化；05:06:48 Tunnel-only restart 后，cloudflared 预检明确 `UDP region2 FAIL`、两路 `TCP HTTP/2 PASS`、`suggested_protocol=http2`，但默认 `auto` 仍继续 QUIC 退避。Cloudflare 当前官方文档说明 `auto` 会按 2/4/8/16/32/64 秒退避后才切 HTTP/2；我们的 `TunnelManager.wait_ready=90s` 在官方 fallback 完成前于 05:08:18 终止首个重建进程，造成公网中断窗口。随后第二次启动在网络恢复后 05:08:36 注册成功。同期 `show_changes(include_diff=true)` 本地审计 15.234s / success=true、Gateway/CodexPro未重启，但 ChatGPT 侧因 Tunnel 重建窗口收到 502，因此 full-diff 不是崩溃根因。现场：`.ai-bridge/forensics/20260829-s10-r4-full-diff-502/`，manifest SHA-256 `aeb22fb5fb0aa4b91902a599ef02056aa37bfe36687bbfa740bfa86388f70f51`。对应源码修复已完成并迁入 `进度验收.md` §34：默认 `auto` 命令保持兼容，只有预检明确建议 HTTP/2 时才提前结束该 auto/QUIC 尝试，并在下一次有限重试显式 `--protocol http2`。
- r5 真实 named Tunnel fault 暴露第三个独立 false-green P1：05:39:11 对 r5 `cloudflared.exe` 精确施加 UDP/7844 阻断并终止其 PID 14836 后，该 PID 确实消失，当前机器只剩系统级 `C:\Program Files (x86)\cloudflared\cloudflared.exe --token-file ...`；由于另一 connector 继续为同公网入口提供 200，`transport-health` 在 05:39:18–05:40:52 持续记录 local/public=200，且完全没有 `tunnel_restart`。这证明 `_transport_health_tick()` 只看 URL health、不看 `self.tunnel.is_running`，会把**自己拥有的 Tunnel 进程死亡**永久误判为健康。fault r2 harness 本身因 `$ErrorActionPreference=Stop` 把预期 curl timeout stderr 提升为 `RemoteException` 而未完成汇总，finally 已确认 firewall 规则删除且 final local/public=200；该 harness 缺陷不改变上述 PID/transport 直接证据。修复裁决：公网模式下自有 Tunnel 进程死亡是责任层硬信号，必须在 cooldown/有界 retry 约束下触发 Tunnel-only recovery，即使外部/冗余 connector 让公网 URL 仍为 200；不得重启 Gateway 或 C/D 项目。
- r5 源码返工已闭合到 full Python/static 门：defect-first `63bfd260...` 保持 1 failed；修复后 `6a91aee9...`=3 passed，交叉回归 `482925cd...`=78 passed，fresh full pytest `be4948f1...`=501 passed/3 skipped，full Ruff `15b63abd...` PASS，full Pyright Windows `a720776c...` 与 Linux `4a463996...` 均 0/0；先前失败的 `c6880f4e...` 与 `268ebc32...` 均保留原失败终态。当前修复逻辑在 URL health 前先检查自有 Tunnel 进程存活，死亡时仅走既有 cooldown 下的 Tunnel-only recovery。setup SHA `78acfccb...` 构建于 r4/r5 源码修改之前，已成为过期候选，**不得**用于宣告最新 revision preflight/soak 通过。最新 revision 的 r6 staging 已由 task `1c46b4cb-59c7-4b65-86ba-e1bd16349f26` 完成（exitCode=0；501 passed / 3 skipped；Ruff PASS；Pyright Windows/Linux 0/0；PyInstaller PASS），输出为 `dist/staging-0.8.9-r6/MCPDevBridge`；因使用 `-SkipInstaller`，它不是可发布 installer。独立审查时当前实际运行进程仍来自 `dist/staging-0.8.9-r5/MCPDevBridge`，所以 r6 尚未进入 live fault preflight。下一步必须先加载/验证 **r6** 的 live fault preflight，之后才允许启动 fresh r4+ 30–60 分钟正式 soak。

### 实施顺序

1. 修复 deterministic handle 生命周期：配置仍存在但项目短暂非 READY 时保留 route record/tombstone 边界，返回可恢复的暂时不可用语义；数据面恢复后先 rehydrate 再执行只读调用。只有项目真正删除、根目录越界/失效时才清理持久 route。
2. 为 Cloudflare/ngrok 公网启动和运行时重建增加有界、可取消、带退避的重试；记录 attempt、分类后的 DNS/网络/凭据/配置错误、PID/exit code、脱敏日志摘要与恢复结果。瞬时外网失败不得停止已成功启动的项目引擎。
3. 加入低开销 flight recorder/probe：固定间隔记录 desktop/Gateway/Tunnel/各项目 PID、listener、状态、最近成功探测、DNS/公网 health、活跃 MCP 请求计数与最老请求年龄；请求链记录 `request_started → upstream_headers/body/stream → terminal/cancel/deadline`，只保存哈希/分类信息，不保存 token、OAuth code、请求正文或完整环境。
4. 探针文件按日 JSONL、单行追加、大小/天数双上限；写失败 fail-open，不影响 MCP 热路径。异常阈值触发一次受限快照，禁止无界线程、socket、进程枚举或日志增长。
5. 补红测并先确认旧实现失败：短暂 project unready 不删除 handle、真实 project deletion 仍 fail-closed；Tunnel 首次 DNS 失败后重试成功、不可恢复配置错误不重试、重试耗尽不跨层重启；公网/本地 stream deadline/cancel 均有明确终态；probe 无 secret、状态有界。
6. 构建新 staging，完成本机真实 local/public/OAuth/old-handle/Tunnel DNS fault 定向预检；预检通过后启动全新 30–60 分钟正式 r4+ soak，任何普通调用分钟级挂起、handle 永久丢失或错误全局重启均立即判失败并返回开发。

### 验收标准

- 瞬时 DNS/网络故障在有界窗口内自动恢复；项目引擎不因共享 Tunnel 一次失败被停止，UI 给出可理解的“正在重试/已恢复/需人工处理”状态。
- old deterministic handle 跨项目短暂重启仍命中原 root；项目真实删除与根失效仍 fail-closed，且无副作用调用重复执行。
- 任一 MCP 请求均有开始和唯一终态记录；普通调用在其预算内收敛，探针能从断连后的磁盘证据还原责任层、最近成功时间、PID/listener/网络状态。
- 探针 CPU/I/O/状态有界、日志脱敏；full gate 与新正式 soak 通过后方可进入 s11。

### 并行文件所有权与架构审查（即时同步）

- **产品修复会话 Track A** 暂时独占 `gateway.py` / `routing_state.py` 的 route-rehydrate 状态机，以及 `app_state.py` / `project_manager.py` / `engines.py` / `desktop_main.py` 中的 Tunnel 重试、连接降级与 flight recorder 接线，并负责直接关联的 targeted tests。其 checkpoint 必须先写红测、再写最小实现、最后给出明确 task exitCode 与源码 SHA；不得用延长 timeout、动态隐藏工具或全局重启掩盖根因。
- **本架构会话 Track B** 独占 soak/fault harness 与证据编排：`.ai-bridge/v089_soak.py`、新的 `.ai-bridge` 取证/编排脚本及不与 Track A 重叠的 harness 测试。Track B 不修改 Track A 当前正在写入的核心文件；发现 P0/P1/P2 且 Track A 无活动任务时，先记录 finding、目标 SHA/mtime 与最小接管范围，再做 SHA-guarded 返工。
- `flight_recorder.py` 独立模块契约已通过并迁入 `进度验收.md` §27；本计划只保留 Gateway/ServiceCoordinator 接线、持续探针与正式 soak 验收。复合敏感键脱敏、路径/身份哈希、单终态及磁盘/内存上限已重测；若该模块再次变化，旧 PASS 自动失效并重开 review/test。
- Track A 的第一轮红基线 task `14f7027e-0e7e-4c9b-acba-03acec4474c4` 为 **6 failed / 16 passed**；后续组合 task `06ed3af7-38dc-49d2-b828-1447792e2c22` 仍为 **9 failed / 23 passed**，暴露 tunnel port/READY/single-flight/deep probe 等返工问题。最新 task `03d3d393-8ba3-470a-8538-98b5cba822a6` 已为 **32 passed / exitCode=0**，证明当前 targeted 基础链已接通；这不是 s10 或 full gate PASS。
- 每当 Track A 的相关源码 SHA、测试终态或本节文档发生变化，本架构会话立即复核：transient-offline 与真实 deletion 的边界、tombstone 语义、read-only one-shot replay、mutation no-replay、response owner/cancel、Tunnel 责任层隔离、资源上限及 Windows/Linux 兼容。发现删测、放宽断言、无明确终态却记 PASS、项目暂时离线即永久删除 route、mutation 自动重放或 Tunnel 故障跨层停止项目时，立即登记 finding 并接管最小返工。
- Route transient-offline / old-handle rehydrate / mutation no-replay 与 Tunnel DNS 分类 / 可取消退避的 targeted 审查已完成，证据已迁入 `进度验收.md` §29；本计划只保留它们在最新 staging、真实 local/public/OAuth fault preflight 与 r4+ 正式 soak 中的运行态门。
- 桌面“项目引擎可用 / 公网入口未就绪”降级语义已由红测→修复→全量回归闭合，证据待本轮迁入 `进度验收.md`；剩余只在最新 staging/live 中验证真实界面、固定域名不变和恢复后状态回正。
- 2026-08-29 本机复核：正式安装登记与当前 `D:\Environment\mcp\MCPDevBridge.exe` 仍属于 0.8.8；GitHub latest release 仍为 `v0.8.8`。本地虽存在 `release\MCPDevBridge-Setup-0.8.9.exe`（2026-08-28 08:28:15Z，SHA-256 `0fd4d975230c647d3a04179259b18392dad8491a76c9b82560f4e70446ae24e6`）及 `dist\staging-0.8.9`，但它们早于 2026-08-28 23:45Z 后续 route/Tunnel/flight-recorder/harness 等修复，属于**过期候选**，禁止直接覆盖本机。应用到本地必须从 Track A/Track B 合并后的最新 worktree 重新构建全新 staging，先通过真实 local/public/OAuth preflight 与 r4+ soak，再由同一 SHA 生成正式 Release/安装资产。
- 三份事实源采用 SHA 前置校验原位更新：`开发计划.md` 只保留未完成项，`进度验收.md` 只登记已验证事实，`项目架构.md` 只描述当前真实代码与已验证边界。并发写入冲突时必须重读合并，禁止覆盖另一会话成果。

### Track B 当前验收与剩余门

- harness 核心返工已迁入 `进度验收.md` §28：matrix/operation 运行于可丢弃子进程，协作取消卡死时由双层 wall-clock watchdog 以明确 `exitCode=124` 收尸；故障分类、ExceptionGroup、缺失证据假绿、显式 expected 绕过、永久 route 丢失、hard-timeout summary 与 fault-epoch 恢复证据共 **7/7 PASS**，`py_compile`、Ruff、Pyright 0/0 全绿。
- `cycle_error`、TaskGroup/ExceptionGroup、401、5xx、timeout、expected fault 与 unexpected failure 已分别计数；故障阶段不再把全部失败一概标 expected，summary 对缺少 operation、fresh local/public signal、resource sample 或 probe evidence 均 fail-closed。D restart 还必须看到故障开始后 local/public 两侧各自成功，故障前信号不能复用。
- 最新 targeted 组合矩阵（route、Tunnel、flight recorder、deadline、connection governance）task `bebd6b78-c0e8-40fa-bc28-c978cf3df59a` 为 **69 passed**；这只关闭源码/targeted 交叉回归，不替代 staging/live fault 门。
- 最新工作树的 r6 staging 已由 task `1c46b4cb-59c7-4b65-86ba-e1bd16349f26` 完成构建（exitCode=0；501 passed / 3 skipped；Ruff PASS；Pyright Windows/Linux 0/0；PyInstaller PASS），但该命令使用 `-SkipInstaller`，仅是 staging，不形成可发布资产。仍未完成：用 **r6** 做短 live fault preflight；验证 owned-Tunnel PID 死亡后的 Tunnel-only 自动恢复与 Gateway/C/D 隔离，以及 local/public、old/new handle、same/refresh bearer、progress/SSE、downstream cancel、mutation no-replay、route/OAuth/task bounds 的真实信号；所有 signal 必须属于本轮且晚于对应 fault epoch。
- 2026-08-29 15:57（本地）已基于既有 r5 reload/fault 机制生成 r6 专用 `runtime_reload_staging_r6.ps1`、`schedule_runtime_reload_r6.ps1`、`r6_named_tunnel_udp_fault.ps1`；显式绑定仓库 cwd 后 PowerShell parser 三份均 PASS。r6 reload 额外 fail-closed：仅当 r6 ready、无 fallback、CodexPro 包与源码指纹一致、Python 源码未晚于 staging、8786/8788/8791 listener 全在、项目配置不变、resume 请求被消费且 broker 指向 r6 时才 exit 0。
- r6 运行态 reload 已完成且 `.ai-bridge/forensics/20260829-s10-runtime-reload-r6/runtime-reload-result.json` 明确 `passed=true`：r6 EXE SHA-256=`de16d58dd2529f629934b2f77327546ee87f9d1a2e30f89e99daa133c8fde54b`，CodexPro package/source fingerprint 完全一致，Python late-file=0，8786/8788/8791 listener 全部恢复，项目配置前后一致，resume request 被消费，broker 指向 r6，`fallback_used=false`。切换窗口当前 ChatGPT MCP 曾收到一次 502，随后同一旧 workspace handle `ws_1d45a4204ebfa51587df4f20` 已重新接回 r6。
- r6 named-Tunnel fault r1 task `0709e783-e6be-409f-a7be-4e8c74a14e1d` 明确 exitCode=1，不能计产品 PASS。故障开始后预期 `curl` timeout 被 PowerShell `$ErrorActionPreference=Stop` 提升为 `RemoteException`，harness 在恢复观察循环前提前退出；finally 已确认 firewall rule 删除且 final local/public health 都为 200。r2 `b805714d-26b7-4e66-9cab-e422ea6a0bdd` 修复 native curl 捕获后证明 Tunnel-only PID replacement 与 Gateway/broker/C/D 隔离成立，但因当前 WLAN 为 Private 且 Private firewall Disabled，UDP/7844 规则并未实际生效，因此仍按 FAIL 保留。r3 临时启用 Private profile 后真实打中 QUIC 故障并看到 auto→HTTP2 4 秒恢复，但 cleanup 后 2 秒采样过早使最终 public=0，仍不记 PASS。
- r6 named-Tunnel 终局 r4 task `cd3fd3ff-5bb8-4f50-b493-2efbc85c8cf9` **exitCode=0 / passed=true**：真实阻断两路 UDP/7844 后，QUIC 两路 FAIL、TCP/HTTP2 两路 PASS；`tunnel_protocol_fallback auto→http2` 后第2次有界尝试成功，fallback=5秒；Tunnel PID 被替换而 Gateway/broker/C/D PID 全保持；防火墙规则删除且 Private profile 恢复原 Disabled；cleanup public 首次0、5秒后200，最终 local/public=200。r1–r3 证据中通过 Win32_Process 捕获到的命令行敏感值已就地脱敏，并生成 `.ai-bridge/forensics/20260829-r6-evidence-redaction-receipt.json`；r4 采集前即脱敏，扫描 `token_leaks=0 / bearer_leaks=0`。
- r6 综合 preflight task `d0b6954b-2b48-45ab-a76a-dcffbf24af71` **exitCode=0 / passed=true**：17/17 请求成功，fresh local/public matrix 均为本轮信号，同一 old handle 跨 local/public，50-tool contract/fingerprint 单一稳定，OAuth refresh=200，零401/5xx/ExceptionGroup/hard-timeout，ordinary max=5.47s，route/OAuth/task/resource bounds 均通过。preflight 模式对 progress/idle-SSE/downstream-cancel 是显式豁免，三个实际计数仍为0，不冒充正式观测。
- r6 独立 Gateway targeted-restart preflight task `5a74d5da-bc69-4706-a1d2-7ad7a3fb84f6` **exitCode=0 / passed=true**：18786 隔离 Gateway 1.425s 恢复，同一 bearer、同一 deterministic old handle、同一50-tool fingerprint均保持，生产 Gateway/D/C/Tunnel PID 完全不变。相对地 Quick Tunnel 独立 probe task `0a943738-d664-4308-89de-6ae3d1e77b8d` 因 trycloudflare 临时域名 DNS/边缘 530 明确 FAIL 且进程已清理；正式发布使用 fixed named Tunnel，因此正式 soak 不再依赖该外部 Quick 服务，而改用已验证的真实 named UDP→HTTP2 fault。
- 正式 r7 soak 已完成启动前编排：`run_v089_soak_formal_r7.ps1` 目标 duration=2100s（35分钟），第300s只重启 D 并要求 fault 后 old-bearer/old-handle local+public matrix 均重新成功；第750s执行独立 `r6_named_tunnel_udp_fault_r5_formal.ps1`，要求 Tunnel PID replacement、Gateway/C/D 不变、auto→HTTP2≤30s 且 fresh public matrix，再写 fresh Tunnel marker；第1200s执行 r6 隔离 Gateway targeted restart 并写 fresh Gateway marker。正式 summary 仍要求实际 `progress_events>0`、`idle_sse_waits>0`、`downstream_cancels>0`、duration≥1800s、资源/状态有界、无 unexpected failure/hard timeout/schema drift/global restart。三份脚本语法 PASS，固定 marker 与 r7 三个证据目录启动前均不存在。
- r7 正式 soak 于 `08:22:03Z` 启动后在 fault 前第1轮 public matrix 即触发发布阻断：public `initialize` 3.351s 成功，随后 `tools/list` 在25.0s operation hard wall 超时；heartbeat 在164s已为 `hard_timeouts=1 / cycle_errors=1`，故本会话主动停止 soak PID，让 Scheduled Task 经 catch/finally 收尾并注销，`s10-fault-state.json` 已恢复 `active=false`，生产 Gateway/D/C/Tunnel PID 均保持。对应 evidence：`20260829-s10-v089-soak-r7/events.jsonl` 与 `20260829-s10-orchestrator-r7/orchestrator-result.json`，本轮绝不计正式时长/PASS。
- 同窗取证已写入 `.ai-bridge/forensics/20260829-s10-r7-public-tools-list-timeout-analysis.json`：08:22:13 Gateway 已收到超时请求并完成 `request_context`，直到08:22:34才返回，期间没有 `upstream_request`；08:22:26 本机 `/health` 同时连续5.031s超时，证明 CodexPro/Tunnel尚未进入该请求的责任路径，Gateway event loop 在 bearer 路由/鉴权准备阶段被同步 I/O 阻塞。代码对应 `_mcp_endpoint` 在 OAuth 识别前无条件调用 `_workspace_for_credential()`，后者遍历项目并同步访问 project credential store。
- 新增 defect-first `tests/test_v089_oauth_hotpath.py`；task `38af3531-ba4d-44ef-ac90-559b1cda89d6` 明确 **2 failed**：Gateway 重建后的持久 OAuth bearer 仍扫描 `p1/p2` 两个项目凭据；project bearer 慢凭据函数使 event loop `asyncio.sleep(0.05)` 实际延迟到0.610s（门槛<0.25s）。当前窄 P1 只允许调整 bearer 认证顺序/线程卸载：hub bearer快速判断→OAuth access record→仅未命中时 project-bearer兼容扫描，后者用 `asyncio.to_thread`，不得改变 route/rehydrate/tool contract/权限语义；修复后必须重新 full gate、重建 staging、重新 live preflight 和全新 formal soak 编号。
- OAuth P1 修复后 targeted=`3 passed`，跨 OAuth/contract/route/rehydrate/deadline=`144 passed`，Ruff PASS、Pyright0/0；最新 full Python task `d4b7973e-8b98-4f5c-ac72-e62252bede08` **504 passed / 3 skipped**，full Ruff PASS、Windows/Linux Pyright0/0，CodexPro build/smoke/stress PASS，npm audit 0 vulnerabilities，uv lock/pip check 与 `git diff --check` 全 PASS。r8 staging 由 task `02e2d2d4-244a-4dd9-b6dc-4a3b5e3b0b53` 独立构建到 `dist/staging-0.8.9-r8`，构建链自身再次 **504 passed / 3 skipped**、Ruff/Pyright双平台全绿、PyInstaller PASS；EXE SHA-256=`d8a3fffcb90d2040c6be0d004c16537079483139e8941ee4c13419799514236a`。
- r8 live reload `.ai-bridge/forensics/20260829-s10-runtime-reload-r8/runtime-reload-result.json` 已 `passed=true`：无 fallback，CodexPro package/source fingerprint一致，Python late-file=0，8786/8788/8791、项目配置、resume request、broker目标均正确。r8 named-Tunnel live fault task `bd276393-bd49-4f09-b18c-e34bcc3d6da5` **exitCode=0 / passed=true**：真实UDP/7844故障后 Tunnel-only PID replacement，auto→HTTP2=3秒，Gateway/broker/C/D PID保持，最终local/public恢复。r8综合preflight task `dc071d8c-92f3-4e1c-bdc4-fe0c189617d0` **exitCode=0 / passed=true**：17/17请求成功、fresh local/public old-handle matrix、50-tool fingerprint稳定、OAuth refresh200、hard_timeout=0，ordinary max=3.789s；r8隔离Gateway restart task `b40b085a-8bb4-4811-b754-69570d38121c` exit0。
- 正式 r9 35分钟 soak 已完成启动前编排：使用 r8 staging，第300s只重启D并要求fault后local/public old-handle matrix重新成功；第750s执行独立r8 fixed named Tunnel UDP→HTTP2真实故障并要求Tunnel PID replacement、Gateway/C/D隔离、fallback≤30s与fresh public matrix；第1200s执行r8隔离Gateway targeted restart。formal summary继续硬要求实际 progress/idle-SSE/downstream-cancel非零、duration≥1800s、hard_timeout=0、schema/route/OAuth/task/resource有界且无跨层全局重启。三份formal脚本语法PASS，固定marker和r9证据目录启动前均不存在。
- r9 在300秒 D-only fault 本身已通过：D PID `40844→14068`，Gateway/C/Tunnel PID全不变，fault epoch之后local/public old-handle matrix均重新成功。但 worker 对唯一预期 operation 失败（`server_config: 指定工作区尚未启动或不存在`，该 op 已 `expected_fault=true`）再包装成 `ExceptionGroup` 时，cycle-level 分类重新按 generic TaskGroup 文本判成 unexpected，导致 formal summary 必然 FAIL。故 r9 于约318秒主动终止，Scheduled Task/fault-state已清理；不计正式时长/PASS。
- harness 窄修复只在 `.ai-bridge/v089_soak.py`：新增 `_child_failure_is_expected`，只有 child metrics 至少存在一个失败 operation、所有失败 op 均已独立判 expected、无 hard-timeout、且所有嵌套 cycle error 也 expected 时，顶层 wrapper 才记录为 `ExpectedFault`；generic ExceptionGroup 仍 release-blocking，401/route-loss/hard-timeout 仍不能被 explicit/child wrapper 掩盖。`test_v089_soak_harness.py` 新增 expected-wrapper 与 blocker/hard-timeout 防假绿测试；task `e47ba51b-1dcc-4cd1-80d6-ae456b78cc6b` **9/9 PASS**，Ruff task `1de24cb0-5616-4ea0-a893-bde0996d852c` PASS，Pyright task `41d21eb8-9378-4094-af93-226071b39998` 0/0。产品源码/二进制未变，因此继续使用已通过 full/live preflight 的 r8 staging。
- 正式 r10 已于 `2026-08-29T09:04:15Z` 由最高权限一次性 Scheduled Task 启动，35分钟/2100秒，使用全新 soak/orchestrator/tunnel/gateway evidence 名称；启动前 parser PASS 且 fixed markers/r10 evidence dirs 均不存在。formal 门与 r9 相同：300s D-only、750s真实 named Tunnel UDP→HTTP2、1200s隔离 Gateway restart；summary仍要求 progress/idle-SSE/downstream-cancel 实际非零、duration≥1800s、hard_timeout=0、unexpected failure/cycle error=0、schema/route/OAuth/task/resource有界。
- 2026-08-29 17:23（本地）独立审查已判定 **r10 当前同样不能成为 formal PASS**：heartbeat cycle43/1125.6s 虽为 `hard_timeouts=0`，但已有2个 cycle error。其中 cycle12 的本地 D-down `ExpectedFault: 指定工作区尚未启动或不存在` 已正确标 `expected_fault=true`；cycle11 的公网 `read` 只得到泛化 `MCPError: Server returned an error response`，operation 自身 `expected_fault=false`，随后 child wrapper 为 `ExceptionGroup` 且同样 `expected_fault=false`。这不能用“正处于 project_d_restart 窗口”整体豁免，否则会重新引入假绿。允许 r10 继续跑完以收集 Tunnel/Gateway/资源证据，但无论最终时长是否达到2100秒，都不得用它推进 s10/s11；必须先定位公网 D-fault 为什么丢失责任层错误语义，并在不放宽 401/route-loss/TaskGroup/hard-timeout 规则的前提下修复产品或证明可安全分类，然后启动**全新编号**的 formal soak。
- 2026-08-29 20:06（本地）fresh r13 在 D-only fault 已恢复且 Tunnel fault 尚未开始时捕获新的发布阻断 P1：公网 `server_config` operation 在 `fault_phase=""` 下 20.023s 客户端超时；同一请求的 flight recorder 为 `request_started/request_context → 约21.5s后 response_started=200`，期间**没有 `upstream_request`**，而同期 local/public health 均为200。责任层因此收敛到 Gateway 路由热路径。源码复核发现 OAuth bearer 的 native store lookup 已迁出 event loop，但 route-specific `_credential_for_workspace()` 仍同步调用 `workspace_credential_registry`；桌面实现最终进入 Windows Credential Manager，随机阻塞会冻结 Gateway event loop。r13 立即保留失败证据并停止，不能以已通过的 D/Tunnel isolation 冒充正式 soak。实施只允许：先补 routed OAuth + 慢 project credential 的 defect-first 红测；再把该 native lookup 移出 event loop，并让运行中项目的 upstream credential 在启动时预热为内存读取；随后重跑 affected/full gate、重建全新 staging/source audit/live preflight，并启动全新 formal soak。旧 r10 staging/source audit/r13 soak 不得为新生产代码背书。
- r3 原始失败目录与备份 `.ai-bridge/v089_soak_r3_failed.py` 保持只读；后续正式 soak 任一普通操作 hard-timeout、永久 route 丢失、unexpected cycle error、故障后 fresh matrix 缺失或错误跨层重启都判 FAIL。

## 2. workspace / durable route affinity 运行态与跨重启终验

### 需求

保证 `D:\Environment\mcp` 的 CodexPro workspace 工具与 durable/task follow-up 始终绑定原项目；Hub 本地 `run_command/run_program` 使用注册的 `D:\` 项目路由，并在显式 `cwd=Environment\mcp` 时进入仓库，Gateway/CodexPro 重启后不得漂移到 `C:\`；unknown/stale root、项目删除及远端设备场景 fail-closed。

### 已复现根因线索

- 实机复核已纠正早期误判：`run_command(cwd='.', devbridge_workspace_id=ecb6fea6)` 实际稳定落在项目根 `D:\`；`git not a repository` 只是因为 DevBridge 项目根是整盘 D:，而仓库位于其子目录，不是显式 project route 失效。仓库短命令必须显式使用 `cwd=Environment\mcp`。
- 多根环境下无强路由证据时静默选择 stable bootstrap 的源码缺陷已按红测修复：workspace-affine调用现在在进入upstream/runner前返回`-32006`；显式route、单根和Hub管理工具保持兼容。尚未完成的是让当前运行实例/正式安装版加载该修复，并在Gateway/CodexPro restart与旧session下持续验证。
- restart窗口曾出现同一 deterministic handle 的`bash`漂到C:与attached durable run not found；当前真实链已恢复：附着唯一`long_run_id/s6`的bash在170ms完成、root为`D:\Environment\mcp`，随后`long_run_status`确认task归入同一run。仍需restart/旧session故障注入，不能把当前恢复当作跨生命周期终验。
- deterministic/legacy handle、v2 route state并发merge/tombstone/失效清理及当前task/durable owner均已绿；剩余缺陷集中在让运行Gateway加载最新源码，并完成Gateway/CodexPro restart、旧session、remote、side-effect no-replay与正式安装版持续验证。

### 方案与实施步骤

1. 让当前运行Gateway加载最新源码，实机验证多根无route返回`-32006`、显式D: route仍命中`D:\`、`cwd=Environment\mcp`进入仓库、绝对/相对子目录按证据路由且Hub管理工具不受影响。
2. 对Gateway restart、CodexPro restart、双实例短窗口和旧ChatGPT session做故障注入；对拍`workspace-routes.json` v2、hydrated/inflight、task/durable owner与实际upstream端口，定位任何间歇漂移。
3. 实机验证unknown/stale handle、失效canonical root与项目删除的清理；远端device请求不得绑定本机project，由远端Gateway独立执行policy/route。
4. 验证side-effect调用序列最多为`write → open_workspace`，绝不能自动出现第二次`write`。

### 验收标准

- 新红测修复后全绿；Hub local `run_command/run_program` 在显式D: route与仓库cwd下命中 `D:\Environment\mcp`，CodexPro tools、task/durable follow-up 均命中原 workspace。
- Gateway/CodexPro restart 后 old deterministic handle、Hub local route及task/durable follow-up均保持原 root。
- 未绑定或歧义调用明确失败，不静默落到 `C:\`、`D:\` 或 bootstrap root。
- project deletion、root invalidation、remote device 有负向测试。

### 风险与回滚

路由优先级改动可能影响旧客户端。保持现有强证据优先级；新增行为局限于补齐显式 route/affinity，不引入全局 current workspace。若持久 route 损坏，忽略缓存并 fail-closed。

## 3. 489 秒普通请求、starvation、取消与 restart 生命周期

### 需求

消除 70–645 秒普通工具调用和第二现场约 489 秒旧 `/mcp` 请求；保证普通调用有界、下游断开可取消、上游半开可收敛、Gateway restart 不留下旧 listener/pool/SSE，也不在 restart 后制造 bearer-breaking 401。

### 已证实根因与待验证边界

- 第二现场存在约 19 个 `/mcp` 慢请求，其中一个约 489860 ms；Gateway health 至少两次约 5 秒 timeout 后触发 restart，随后出现多次 401。
- 第五现场证明当前正式安装运行实例仍允许普通调用继续约 126 秒；工作树源码的分类总 deadline 尚未加载到该运行实例，必须通过正式重载、故障注入和持续终验消除运行态落差。
- safe retry 目前仅限 `initialize/ping`，`tools/call` 不盲重试；response/stream 已有单 owner close wrapper，这是必须保留的安全边界。
- workspace error 检查当前依赖 `Content-Length` 在 0–64 KiB；无 `Content-Length` 的 chunked/SSE 错误可能绕过受控 rehydrate 分支。
- 尚未完成的源码边界集中在下游取消、upstream half-open、restart drain、无 `Content-Length` 的受控 rehydrate 前缀检查及 restart 后 bearer 连续性；已通过专项验收的分类 deadline、SSE 终态审计、single-owner close、buffered one-shot 复用和特殊工具响应体上限不再作为待办重复施工。

### 方案与实施步骤

1. 让当前运行实例及后续正式安装版加载已验收的分类 deadline 与终态审计实现；以第五现场同类 search/show_changes、SSE、`open_workspace`、`bash` 和显式 long-poll 做运行态故障注入，确认不再执行旧 900 秒路径。
2. 用结构化取消贯穿 send、buffer read、stream generator、rehydrate 和 shutdown；下游取消后关闭 upstream response，不吞 `CancelledError`。
3. 对 half-open、chunked、无 Content-Length、partial JSON/SSE 增加受限前缀检查或明确不重放路径，避免 double read/double close。
4. Gateway restart 采用 stop accept → bounded drain/cancel → close pool → release listener → start new listener；记录旧 in-flight 终态。
5. 对 restart 边界 OAuth provider/SecretsStore、stale bearer、provider swap 与 401 时间线做故障注入。
6. 为普通 UI 保留用户可理解错误，高级日志记录责任层与根异常，不展示 TaskGroup 堆栈/token/PID。

### 验收标准

- 普通窄调用不再出现分钟级等待；timeout 返回明确有界错误并释放连接。
- client cancel、half-open、chunked/SSE、absent Content-Length、restart in-flight、old bearer 场景均有绿测。
- response/client/listener 无 double close、泄漏或旧 listener 占端口；无副作用调用不重复执行。
- restart 后未过期 bearer 连续有效，401 只能由真实 expiry/revoke/resource mismatch 触发。

### 风险与回滚

deadline 过短会误伤合法长工具。长工作继续使用 `bash/wait_task/long_run_*`；显式 progress/SSE 保持单独预算。回滚时可恢复旧 timeout 常量，但不得恢复 tools/call 盲 retry 或无界普通调用。

## 4. Stable 50-tool Hub contract 的运行态与客户端兼容终验

### 需求

固定公网 Hub `tools/list` 为 exactly 50，冻结 canonical fingerprint，并证明项目启停、权限模式、bootstrap、JSON/SSE/error/partial upstream、old/new client 及 restart 均不改变公开 schema；解释并消除真实 Connector 只加载约 25/38 工具的故障态。

### 当前实现与未完成边界

- 代码已嵌入 41-tool CodexPro full source contract，并注入 9 个 Hub tool；公开 count=50、contract version=1、canonical fingerprint=`489cd30e8e8676ad382c752bed7c46ba9bc2f308fa2d98b2a699793d0c5ee780`，模块加载时校验 count/unique/fingerprint。
- 当前 ChatGPT 连接器在本会话展示 50 个工具；内部 `codexpro(list_actions)` 当前为 37 actions。二者是不同层级，不能把 action count 当 public tools/list count，也不能仅凭本会话 schema 宣布所有 restart/old client 场景通过。

### 方案与实施步骤

1. 完整跑 `test_v089_contract.py`，补 project start/stop、permission mode、bootstrap、old/new client、JSON/SSE/error/partial upstream、restart 矩阵。
2. 诊断输出 contract version、count、fingerprint、runtime upstream capability count，明确区分 public contract 与内部 action inventory。
3. 实机连续多轮 `tools/list` 对拍 name+description+inputSchema canonical hash；不得拿旧 41-tool source fingerprint冒充 Hub fingerprint。
4. 若 Connector 仍只加载 25/38，核对实际 HTTP payload、content type、schema size、host refresh/cache/session object，不改动态 schema绕过。

### 验收标准

- 所有运行态公开 `tools/list` exactly 50，fingerprint恒为 `489cd30e...`，无重复工具。
- 项目启停、权限、bootstrap、上游异常与 restart 不改变公开 contract。
- diagnostics/Release notes 明确版本与 fingerprint；old/new MCP client 均可解析。
- 真实 ChatGPT 连接器首次及持续使用后都显示/调用完整 contract。

### 风险与回滚

公开 schema 变化会触发客户端缓存刷新。v0.8.9 内不改变 contract v1；若发现嵌入 manifest 损坏应启动失败而非动态回退。旧 41-tool source snapshot只作为构建来源与审计证据。

## 5. call-stage permission enforcement 扩展审查

### 需求

保持 fixed public schema，同时在调用阶段严格区分 `permission_denied` 与 `feature_unavailable`；`codexpro(action=...)`、Gateway local tools、remote device 均不得绕过。

### 当前实现与未完成边界

- 基础红测已由 `2 failed / 1 passed` 修复为 `3/3 PASS`，后续扩展矩阵曾达到 8 passed。
- 本地调用在进入 Gateway local/upstream 前判定，supertool 会 unwrap；system allow，workspace 拒绝 full-only，read_only 拒绝 mutation；remote 不套本机项目模式而由远端 Gateway二次 enforcement。
- 尚需完整 negative matrix、模式切换、无 permission context、remote device 与第二层 CodexPro 对拍。

### 方案与实施步骤

1. 扩展 read_only mutation、workspace full-only、system allow、supertool alias/nested args、Gateway local command、remote device、unknown mode 测试。
2. 检查 policy constants 与真实 50-tool manifest 一致；新工具必须明确归类，不能默认放行 mutation。
3. 验证 Gateway 拒绝发生在 upstream 前；CodexPro 自身 enforcement 保留。

### 验收标准

- 所有模式/包装/远端负向与正向用例通过。
- 被拒调用 upstream call count=0；错误类型和文案准确。
- 不通过删测试、放宽模式或动态隐藏工具换取绿色。

### 风险与回滚

错误分类可能阻断旧客户端。只按工具副作用/正式模式表调整；保留 CodexPro 第二层防线，回滚不得移除 call-stage gate。

## 6. rehydrate 响应边界与取消终审

### 需求

在已验收的 deterministic handle persistence、v2 原子 route state、single-flight、状态上限和失效清理基础上，只完成尚未验收的 chunked/SSE、缺失 `Content-Length`、response ownership、waiter/downstream cancel 与旧 listener 并存边界；不得重复施工已迁入 `进度验收.md` §20 的锁、merge、tombstone 或有界状态。

### 当前实现与待验证根因

- keyed rehydrate single-flight、inflight hard cap、8 秒 timeout、exact handle equality、read-only one-shot replay 与 side-effect no replay 已有 targeted 绿证据。
- v2 route 文件已具备进程内/跨进程锁、锁内 merge、tombstone、原子发布和项目/root 失效清理；该部分不是当前待办。
- 当前 workspace error 检查仍依赖 `Content-Length` 处于 0–64 KiB；无长度的 chunked JSON、partial body 或 SSE/error 可能绕过受控 rehydrate，或在错误处理时形成 double read/double close。
- shared rehydrate task 的取消语义、下游断连、旧 listener drain 与 response owner 在组合场景下仍缺终验。

### 方案与实施步骤

1. 扩展 `test_v089_rehydrate.py` / streaming/deadline 回归，覆盖 buffered JSON、chunked JSON、absent `Content-Length`、partial JSON、SSE/error 与 malformed upstream。
2. 对错误前缀采用严格字节上限与单 owner 读取策略；不能安全判定时不自动 replay，避免把流式正常响应缓冲成无界 body。
3. 覆盖 same-handle waiter cancel、downstream disconnect、Gateway shutdown/drain 和 upstream half-open，确认共享恢复不被单个 waiter误取消且 finally 清理 inflight/response。
4. 复验 side-effect调用序列最多为 `mutation → open_workspace`，绝不出现第二次 mutation；remote device 继续由远端 Gateway 自行 rehydrate/policy。

### 验收标准

- chunked/无长度/partial/SSE场景无 double read、double close、无界缓冲或错误重放。
- waiter/downstream cancel 后无残留 inflight、response、socket或旧 listener；共享必要恢复的取消语义明确且有绿测。
- read-only最多 one-shot replay；mutation不自动 replay；无法确认时 fail-closed。

### 风险与回滚

前缀探测过宽会破坏流式透传，过窄会漏掉可恢复错误。保持严格小上限、单 owner与“不确定即不重放”；回滚不得恢复无界 read、双重 response ownership 或副作用盲重试。

## 7. structured analysis、search/show_changes 延迟与资源治理

### 需求

常规窄路径 `search/show_changes` P95 <10 秒；full diff有界；避免并发全库建图、`spawnSync git ENOBUFS`、70–645 秒无进度调用。

### 当前实现与剩余验证

- scoped analysis 已把 inventory root 与 cache key 限制到请求 scope，历史 `inventoryFiles 20 != 2` 红点已修复。
- analysis 已实现 same-key single-flight、全局 inflight hard cap、finally 清理、deadline 与 AbortSignal/waiter cancel；专项 smoke 覆盖同 key 并发只建一次、取消/超时不取消共享 owner、8/8 cap 后拒绝第9个 build。
- `show_changes(include_diff=false)` 已改为 stats-only 路径，不物化完整 diff；`include_diff=true` 使用有界 Git 输出并给出明确 truncation。当前 workspace stats-only 调用及独立大 diff 复现均未再出现 `spawnSync git ENOBUFS`。
- 剩余不是实现缺口，而是 s9 全量质量门与 s10 至少30分钟真实负载下的 p50/p95/max、RSS、inflight/cache/handle 是否单调增长验收。

### 已实施方案

1. analysis cache 使用 same-key single-flight、全局 inflight hard cap 与 finally 清理。
2. inventory/extract/search 阶段传递 deadline/AbortSignal；调用方取消或超时不会误杀共享 owner。
3. `show_changes(path=...)` 使用 scoped Git；`include_diff=false` 不捕获完整 diff；全 workspace diff 有字节上限和明确截断标识。
4. targeted 已覆盖大 diff 与并发/取消/超时；长期资源与延迟曲线继续由 s10 soak 记录。

### 验收标准

- 窄 search/show_changes P95 <10 秒；无 >60 秒普通调用。
- same-key并发只建一次 inventory；取消/超时后无残留 inflight。
- `include_diff=false` 不再 ENOBUFS；full diff明确 bounded/truncated。
- cache≤8，inflight有硬上限，无单调内存增长。

### 风险与回滚

截断可能影响完整审查。保留显式 scoped/后台导出路径；不得用无界 buffer恢复“完整性”。

## 8. Windows 标准后台任务 `powershell.exe ENOENT` 回归修复

### 需求与非目标

当前 `mcp-wjp` 实机使用标准后台 `bash`/task 工具启动长命令时再次返回 `spawn ... powershell.exe ENOENT`。必须保证 Windows 正式安装版与当前运行实例的标准后台任务入口不依赖偶然的 PATH 状态：合法 workspace/cwd 下应立即返回 `task_id`，随后可由 `wait_task/get_task/cancel_task` 正常观察或取消。不得把同步 `run_command/run_program` 当作长任务替代方案，也不得通过要求用户手工修改系统 PATH、重装 PowerShell、关闭 UAC 或改用另一套 long-run 来绕过。

### 复现、已知事实与待验证根因

- 本次实机红点是标准后台任务启动阶段直接返回 `spawn ... powershell.exe ENOENT`；这是新的运行态回归，历史 `async-task-smoke` PASS 不能覆盖当前失败。
- 当前工作树 `third_party/codexpro/src/bashOps.ts` 已有 `SystemRoot/WINDIR → System32\\WindowsPowerShell\\v1.0\\powershell.exe` 的绝对路径探测，并在找不到候选时回退裸 `powershell.exe`；因此必须区分“运行实例未加载当前 build / packaged runtime 仍旧”“SystemRoot/WINDIR/ProgramFiles/ComSpec 或 PATH/Path 环境形态异常”“候选文件不存在/不可见”和“cwd 不存在”四类原因，未取证前不得把其中任何一项写成最终根因。
- Node.js 官方 `child_process.spawn()` 文档明确：命令不存在或 `cwd` 不存在都会触发 `ENOENT`；Windows 环境变量大小写不敏感，传入同时含 `PATH`/`Path` 等重复变体时只会有一个进入子进程。因此验收必须同时记录 launcher、cwd 与必要的非敏感环境键形态，不能只凭错误字符串判断。
- Microsoft Learn 明确 Windows PowerShell 5.1 的可执行文件位于 `%windir%\\System32\\WindowsPowerShell\\v1.0\\powershell.exe`，PowerShell 7 使用 `%ProgramFiles%\\PowerShell\\7\\pwsh.exe`；Windows 默认后台 shell 的解析应优先使用已验证存在的绝对可执行文件。

权威来源：
- Node.js `child_process`：https://nodejs.org/api/child_process.html
- Microsoft Learn `about_Run_With_PowerShell`：https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_run_with_powershell
- Microsoft Learn `about_PowerShell_exe`：https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_powershell_exe?view=powershell-5.1

### 架构裁决、数据流与失败策略

1. 沿 `bash tools/call → shellInfo/launcher resolve → child_process.spawn → BashTaskManager → task_id → wait/get/cancel` 逐层取证，先判定 ENOENT 来自 executable 还是 cwd；日志只记录脱敏后的候选路径、存在性、cwd 与环境键是否存在，不输出完整环境变量或凭据。
2. Windows 默认 shell 必须在 `spawn` 前解析为**已验证存在的绝对路径**；不得把裸 `powershell.exe` 作为“看似兼容”的静默成功路径。Windows PowerShell 不存在时，只能在明确验证 PowerShell 7 兼容后使用绝对 `pwsh.exe`；若没有受支持 shell，应返回可行动的结构化 `shell executable not found` 错误并列出已检查的脱敏候选，而不是泄漏原始 Node `ENOENT`。
3. 规范化 Windows 继承环境时避免 `PATH`/`Path` 等大小写重复键造成不可预测的子进程环境；必须保留启动器解析需要的 `SystemRoot/WINDIR`、`ProgramFiles`、`ComSpec`，但不得扩大权限、绕过 execution profile、PathGuard、dangerous-command hard deny 或 UAC 边界。
4. `cwd` 在 `spawn` 前独立 canonicalize/存在性校验；cwd 无效应返回明确的 cwd 错误，不能误报为 PowerShell 缺失。
5. 工作树源码、`npm run build` 生成的 `dist`、打包 `_internal/third_party/codexpro/dist` 与当前运行实例必须做同源/哈希或行为对拍；若只是 runtime stale，仍需完成重载与正式安装资产验证，不能以源码绿冒充实机绿。

### 复杂度、资源与兼容性

启动器候选探测为固定小集合 `O(k)`（k 为常数），只增加少量 `exists/stat` I/O，不引入后台线程、无界缓存、长期句柄或网络请求。Linux/POSIX shell 路径不改；Windows 自定义 shell 语义保持兼容。不得为了规避 ENOENT 自动切换到语义不同的 `cmd.exe` 执行 PowerShell 命令，也不得新增系统级安装依赖。

### 实施顺序

1. 新增能稳定复现当前红点的 Windows targeted regression，覆盖 packaged/runtime 路径而不仅是源码 helper 单测。
2. 冻结当前运行实例的 launcher/cwd/必要环境键与实际 `dist/bashOps` 版本证据，裁定 executable-vs-cwd-vs-stale-runtime 根因。
3. 以最小改动修复 resolver/env/cwd 或 build/runtime 装载链，并补结构化错误；随后重建 CodexPro runtime。
4. 先在工作树执行 targeted async-task 回归，再重载当前实例做标准 `bash` 实机任务，最后进入 s7 targeted gate、s9 full gate、s10 soak 与 s13 真实 ChatGPT 后台持续终验。

### 验收标准

- Windows 在刻意移除 PATH 中系统目录、以及存在 `PATH`/`Path` 大小写变体的受控环境下，标准 `bash` 仍使用已存在的绝对 shell 路径启动并立即返回 `task_id`。
- 对真实 `D:\\Environment\\mcp` workspace/cwd 启动长命令，`get_task/wait_task` 最终取得明确 `exitCode=0`；不得出现 `spawn ... powershell.exe ENOENT`、task 丢失或以“未取得退出码”伪报通过。
- 无效 cwd 与 shell 真缺失分别返回可行动、可区分的错误；日志不泄漏完整环境或 secret。
- `cancel_task` 能终止该标准后台任务的完整进程树；restart/half-open/long-run 附着不因本修复退化。
- `npm run build`、Windows `async-task-smoke`、新增 targeted regression、打包 `_internal` runtime smoke、当前 `mcp-wjp` 实机调用全部 PASS；源码/dist/packaged runtime 必须来自同一待发布 revision。
- s7/s9/s10/s13 后续门均把“标准后台任务可真实启动并取得终态退出码”列为阻断条件；任何一个门复现 ENOENT 即回到本节修复，不得发布。

### 风险与回滚

环境归一化错误可能误删用户 PATH，shell fallback 可能改变命令语义，runtime 重载可能与当前并发任务冲突。实现应只规范化大小写重复键并保留原值，不重写用户 PATH 内容；不兼容 shell 不自动替换。若修复引入新的启动回归，回滚代码但保留本节红测与实机阻断门，继续停在未完成状态。

## 9. Targeted gate 与 defect-first review

### 实施步骤

1. 重跑 OAuth矩阵、`test_v089_rehydrate.py`、`test_v089_rehydrate_concurrency.py`、`test_v089_policy.py`、`test_v089_contract.py`、连接治理/streaming/app_state/stability专项。
2. CodexPro build、analysis smoke、async task smoke、HTTP lifecycle smoke、HTTP/long-run/stress专项。
3. 逐 diff审 correctness、concurrency、cancellation、response ownership、resource leak、permission bypass、retry safety、duplicate side effect、schema drift、restart storm、false-green、Windows/Linux和worktree contamination。
4. 任一 P0/P1/P2：`FAIL → 修复 → 重跑 → re-review`。

### 验收标准

新增回归全部通过；无已知 P0/P1/P2；review证据对应最新 work revision。

### 风险与回滚

不得删测、放宽断言或把环境错误当绿色。无法运行的门保持未完成并记录真实阻塞。

## 10. 全量质量门与同源构建

### 实施步骤

- Python：full pytest、Ruff、Pyright Windows/Linux、`uv lock --check`、locked sync。
- CodexPro：`npm ci`、build、smoke、stress、analysis/async task/HTTP lifecycle、`npm audit --audit-level=high`。
- 脚本与安装器语法检查。
- Windows：`scripts\build.ps1 -Version 0.8.9`。
- Linux：`scripts/build_linux.sh 0.8.9` 或 same-source CI。

### 验收标准

所有门在同一最新源码 revision通过；Windows/Linux资产可追溯到同一 commit。

### 风险与回滚

任一门失败即停止发布；保留正式 v0.8.8 installer作为回滚基线。

## 11. 30–60 分钟真实 HTTP soak / fault injection

### 实施步骤

至少 30 分钟，目标 60 分钟，持续模拟 initialize、tools/list、server_config、open_workspace、read、narrow search、show_changes、background bash、wait/get task、handle/bearer reuse、SSE/progress、Gateway/CodexPro/Tunnel restart、client cancel与half-open。

记录 request total、success%、tool count/fingerprint、5xx、401、timeout、p50/p95/max、RSS、socket/handle、active/retained task、route count、OAuth record count、recovery count、wrong global restart count。

### 验收标准

无无界资源增长、schema漂移、duplicate side effect、错误全局重启、分钟级普通调用或全工具突然 ExceptionGroup；恢复符合责任层边界。

### 风险与回滚

故障注入仅针对受控本机实例；正式数据/项目文件不做破坏性操作。任何复现立即保存新现场并返回开发阶段。

## 12. v0.8.9 正式发布、本机升级与真实 ChatGPT 持续终验

### 实施步骤

1. 全部门通过后 bump 0.8.9，更新 CHANGELOG/release notes，worktree audit、secret scan、commit、push release branch。
2. 创建 annotated `v0.8.9`，Windows/Linux same-source CI，GitHub Release与SHA256；不得覆盖旧 tag/release。
3. 下载正式 Windows asset并核验SHA256，升级当前实例；保留projects、running states、fixed hostname、shortcut、config。
4. 正式安装版真实 ChatGPT终验：initialize、tools/list=50/fingerprint、server_config、open D:、read/search/show_changes、bash/wait、OAuth continuity、Gateway/CodexPro restart handle、read-only retry、side-effect no replay。
5. 持续使用后再次重复全部调用，专门验证“初始正常 → 一段时间后全部 tools 同时 ExceptionGroup”。

### 验收标准

正式 Release、安装版本0.8.9、本机状态恢复、持续 ChatGPT终验全部通过；若整体失效再次出现，Release gate不完成并回到取证/修复。

### 风险与回滚

升级失败使用正式 v0.8.8资产回滚，不移动tag、不覆盖配置。需要用户本人授权/凭据的真实步骤才可标阻塞，不能用候选本机构建冒充正式Release资产。

## 13. 文档与 durable completion gate

### 实施步骤与验收标准

- 每项验收通过后立即从本文件删除，证据迁入 `进度验收.md`；架构变化同步更新 `项目架构.md`。
- s1–s14全部done且有证据；所有历史unknown task按真实终态逐项resolve；历史红task不冒充新绿task。
- `long_run_review(verdict="pass")`覆盖最新work revision后，执行`long_run_complete`。
- 只有`long_run_complete`成功后才可向用户报告“正式完成”；否则只能报告仍在执行或具体阻塞。

### 风险与回滚

文档不得提前写完成；证据冲突时保持未完成状态，以最新代码、测试、构建、运行工件和正式Release为准。