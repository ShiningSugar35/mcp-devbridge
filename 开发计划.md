# 开发计划.md

> 完整开发计划与每个 Phase 的验收标准。进度与验收结果见 进度验收.md；架构见 项目架构.md。
> 维护规则：新增需求/变更 → 先更新本文件（或新增 Phase），完成阶段后更新 进度验收.md。

## 项目总目标

Windows 桌面程序（PySide6 单窗口，中文界面）：
1. 选择本地开发项目，配置权限模式/端口/测试命令；
2. 一键启动独立的 MCP Server 子进程（仅监听 127.0.0.1）；
3. 通过 **Cloudflare Named Tunnel** 提供**长期固定、永不变化**的 HTTPS MCP 地址；
4. 认证与 URL 分离：Bearer 令牌独立配置；
5. 附测试、文档（方案/开发计划/开发状态/问题记录 + 五份英文文档）、打包产物（PyInstaller onedir + Inno Setup 安装器）。

## 全局验收标准（所有 Phase 共用）

- [ ] 不修改 `D:\AStockMultiAgent`；测试写入一律在 `.test-workspace`。
- [ ] 所有测试可复现：`$env:PYTHONIOENCODING="utf-8"; .venv\Scripts\python.exe -m pytest tests/ -q` 全绿。
- [ ] 中文界面，关键路径（启动/停止/令牌/隧道/连接测试）无卡死（异步 + 线程）。
- [ ] 后端只监听 127.0.0.1；公网经 Named Tunnel，URL 固定不变。
- [ ] 密钥不入日志、不入 URL、不入仓库；日志脱敏。

---

## Phase 0 — 环境与需求核对（✅ 已完成）

**目标**：确认环境、需求、外部平台事实、风险。
**产出**：环境审计、原型备份（`D:\Environment\mcp\backups-prototype\dev-mcp.py`）、外部事实核实（ChatGPT/Gemini/cloudflared）。

### 验收标准
- [x] 确认 Windows 10 19045 / Python 3.12.10 / uv 0.11.25 / git 2.47.1 / Node v22.19.0 / 无 pwsh。
- [x] 核实 MCP SDK 2.0.0 关键 API（`streamable_http_app`、`add_tool`、`ToolAnnotations`、middleware 协议、client 用法）。
- [x] 核实 ChatGPT 自定义 MCP 要求（developer mode 位置；Pro 仅读/获取，完整写入需 Business/Enterprise/Edu，**不得谎称已绕过**）。
- [x] 核实 Gemini Spark 自定义 Connected Apps 要求（US+18+个人账号+Keep Activity on+标准 MCP）。
- [x] 核实 cloudflared 命令与 Quick Tunnel URL 解析（实测 2026.7.3 可用）。
- [x] 完成风险识别与结论记录（见 问题记录/开发状态）。

---

## Phase 1 — 后端核心 + 33 个 MCP 工具（✅ 已完成核心，测试全绿）

**目标**：实现全部 33 个 MCP 工具的纯逻辑层（不依赖 HTTP）。
**产出**：`constants.py`、`models.py`、`config_store.py`、`secrets.py`、`audit.py`、`shell.py`、`processes.py`、`permissions.py`、`tools.py`。

### 验收标准
- [x] 33 个工具全部实现并注册（server_factory `_register_tools`）。
- [x] 路径边界：`workspace` 模式禁止项目外读写（`PermissionError`），排除 .git/.venv/node_modules/.env 等敏感项。
- [x] 写操作原子化（临时文件 + rename）；`replace_text` 要求 expected_count 匹配；`apply_patch` 任一 hunk 失败则整体不生效。
- [x] 编码容错：UTF-8 / UTF-8 BOM / GB18030 回退读取。
- [x] 命令执行：PowerShell（`powershell.exe`），超时进程树终止，输出截断保护。
- [x] 受管进程：启动/轮询增量输出/停止（含子进程树）。
- [x] 权限三模式（read_only / workspace / system）正确生效，destructive 工具标注正确。
- [x] 工具注解（read_only_hint / destructive_hint / idempotent_hint）准确，**不**把破坏性工具标成只读。
- [x] 单元测试：文件系统、命令、Git（临时仓库）、配置/密钥、diff 解析——全绿。
- [x] Git 命令一律 `git -C <workspace>` 执行，默认 timeout 120s（push 600s）。

---

## Phase 2 — HTTP 服务 + 认证 + 子进程运行（✅ 已完成核心，测试全绿）

**目标**：后端可独立子进程运行，通过 Streamable HTTP 提供服务，认证/审计/限速生效。
**产出**：`server_factory.py`、`server_main.py`、`standalone_server.py`、`selftest.py`。

### 验收标准
- [x] `build_backend(rc)` → MCPServer + Starlette app；`/mcp`（streamable_http_path）、`/health`、`/control/status`、`/control/stop-procs`。
- [x] 认证：本机匿名放行；`/control/*` 强制 Bearer；公网（cf-connecting-ip 等）必须 Bearer；constant-time 比较。
- [x] 限速：10 次认证失败锁 5 分钟（429 + Retry-After）。
- [x] 认证中间件为纯 ASGI（BaseHTTPMiddleware 缓冲 SSE 会坏流，已弃用）。
- [x] `server_main` CLI：`--config` / `--port`；端口占用检测；`ensure_access_token()` 自举令牌。
- [x] 审计中间件：tools/call 记 JSONL，参数脱敏。
- [x] 集成测试走**真实后端子进程** + MCP 客户端（in-process uvicorn SSE 不兼容，禁止使用）。
- [x] `selftest.py` 自测链路（initialize/list_tools/get_workspace_info/list_directory/get_capabilities）。
- [x] 集成测试：initialize、工具数≥30、文件往返、路径逃逸拒绝、只读模式拒绝写、未知工具报错、Bearer 成功/失败、限速锁——全绿。

---

## Phase 3 — 桌面控制程序（PySide6 单窗口）（🚧 进行中 → 下一步）

**目标**：单窗口中文 GUI，完成项目管理、后端启停、状态展示、令牌与 URL 展示、连接自测。
**产出**：`desktop_main.py`（+ 必要的 `desktop/` 子模块），`localdev-mcp-bridge` 脚本入口接通。

### 界面要素（任务书）
1. 项目区：选择/新增项目目录（文本框 + 浏览），保存后进入 `projects.json`（自动补全检测到的命令建议）；
2. 配置区：权限模式（radio/下拉）、本地端口、测试/检查命令、shell；
3. 控制区：「启动」「停止」后端按钮、后端状态（运行中/端口/workspace/进程数）、自动刷新 `/health`；
4. 令牌区：「生成/复制访问令牌」（一次性明文显示），复制固定 MCP URL；
5. 自测区：「测试连接」按钮 → 用 `selftest.py` 跑本地连接测试并展示步骤结果；
6. 日志区（Phase 6 扩展）：简版可先显示自测输出与后端启动日志；
7. 系统提示：首次选 `system` 权限时一次性风险确认对话框；Quick Tunnel 模式显著警告（临时地址，重启可能变化）。

### 验收标准
- [ ] 启动后自动加载已有项目列表与上次工作区（`active_workspace`）。
- [ ] 新增项目 → 显示检测到的 test/lint/typecheck/build 命令建议，可编辑保存。
- [ ] 启动后端：写 runtime.json → 拉起 `server_main` 子进程 → 轮询 /health 直至就绪 → 状态由「已停止」变「运行中（端口/工作区）」；失败有明确中文错误。
- [ ] 停止后端：正常 SIGTERM，超时强杀；受管进程一并停止（exit_stop_managed 可配）。
- [ ] 令牌：显示/复制；令牌存 Windows Credential Manager（或 DPAPI 回退），绝不明文落盘。
- [ ] 自测按钮：显示每个步骤（initialize/list_tools/get_workspace_info/list_directory/get_capabilities）的成功与否与详情。
- [ ] `system` 权限首次选择弹一次性风险确认；Quick Tunnel 模式显示「临时地址、重启会变化」警告。
- [ ] 无卡死：所有 HTTP/进程操作用线程（QThread/QThreadPool），UI 始终可响应。
- [ ] 手动冒烟通过（offline：仅启动/停止/自测；本机）。

---

## Phase 4 — 隧道模块（Named Tunnel 固定 URL）（🚧 下一步）

**目标**：实现 Named Tunnel 的配置、创建、启动、状态、固定 URL 展示；Quick Tunnel 仅作调试。
**产出**：`tunnel.py`（cloudflared 管理）、state.json 持久化、`desktop` 隧道面板。

### 核心设计（验收重点）
- [ ] **强制验收**：MCP URL = `https://mcp.<user-domain>/mcp`，经 Named Tunnel，**重启/重连后 URL 不变**。
- [ ] 首次配置引导：无账号 → 提示打开 `https://one.dash.cloudflare.com`；有账号 → 提供 `cloudflared login`（弹浏览器）、`cloudflared tunnel create`、`cloudflared tunnel route dns <tunnel> mcp.<user-domain>` 的引导命令与说明（用户手动执行或应用内复制）。
- [ ] 配置持久化到 `state.json`（tunnel_id、tunnel_name、hostname、origin_domain、credentials_source、created_at）。
- [ ] 启动：`cloudflared tunnel --url http://127.0.0.1:<port> run <tunnel_id>`，日志重定向到 `process_logs`。
- [ ] 状态：子进程存活 + 拉取 `/health` 经公网域名可达（自测可选用公网 URL）。
- [x] **transport_security/DNS rebinding 处理**：`server_factory.build_transport_security()` 方案 B（allowed_hosts 追加公网域名，防护保持开启），`RuntimeConfig.public_hostname` 贯通（store/standalone `--public-hostname`）；测试 12 项全绿。公网 Host 下 `/health` 与 `/mcp` 真机验证仍待联调（需 Cloudflare 账号）。
- [ ] Quick Tunnel：仅调试模式，UI 显著警告「临时地址，重启会变化，仅用于测试」；**不作为**验收路径。
- [ ] 凭证（tunnel token / cert）经 SecretsStore（Win Cred Manager / DPAPI），不明文落盘。
- [ ] 未登录/未创建隧道时给出清晰指引，绝不让用户被「有墙内问题」的假设卡住（文档注明：如无法访问 Cloudflare，提供本地+SSH 替代方案说明，但验收以 Named Tunnel 为准）。

---

## Phase 5 — Git 桌面参数配置（桌面侧）

**目标**：桌面可配置 Git 相关参数（不增加工具，仅补配置与校验）。
**产出**：`ProjectConfig` 扩展（git_user_name / git_user_email / default_push_remote 等，可空）+ 桌面 Git 设置区 + 校验。

### 验收标准
- [x] Git 设置区：user.name、user.email、默认 remote/分支（可空）；保存后供工具使用说明（工具层已支持 `git -C`）。
- [x] 校验：输入非法值（含空格/引号等）给出中文提示并拒绝保存（`models.git_field_error`，保存与启动双路径拦截）。
- [x] 不影响现有 33 工具；新增字段向后兼容（旧 JSON 缺省值可用）。

---

## Phase 6 — 日志浏览页 + 自动清理

**目标**：桌面可浏览审计日志与进程日志；自动清理策略生效。
**产出**：`audit.py` 查询接口（按天/级别/工具过滤）、桌面日志页（表格 + 筛选）、后台清理任务。

### 验收标准
- [x] 日志页展示 mcp-*.jsonl（时间/工具/成败/耗时/客户端），支持按日/工具/成败筛选。
- [x] 进程日志页：按进程查看输出（增量追读）。
- [x] 清理：超过 `log_retention_days`（默认 14）的 jsonl 自动删除；总量超 50MB 轮转（保留最近 2 个文件）（audit._rotate_if_needed）。
- [x] 脱敏在展示层同样生效（写时已脱敏 + `key.upper()` 标记匹配修复）。
- [x] 桌面退出时无遗留句柄/线程错误（offscreen 冒烟通过）。

---

## Phase 7 — 打包与全文档

**目标**：可分发安装包 + 完整中英文文档。
**产出**：PyInstaller onedir 构建脚本、Inno Setup 脚本、`release/` 产物目录、五份英文文档 + 中文文档补齐。

### 验收标准
- [x] `pyinstaller --onedir` 构建出可运行桌面程序（`dist\LocalDevMCPBridge\`，启动冒烟通过；cloudflared 捆绑；`packaging/local-dev-mcp-bridge.spec`，`scripts/build.ps1` 一键：pytest→ruff→构建）。
- [x] Inno Setup 脚本产出安装器（Inno Setup 6.7.3 已装；`release\LocalDevMCPBridge-Setup-0.1.0.exe` 已编译并**静默安装成功**：EXIT=0、程序未项、图标已建；per-user 免 UAC，`PrivilegesRequired=lowest`）。
- [x] 产物位于 `release/`，附构建说明（构建/安装命令与产物清单见 docs/en/DEVELOPMENT.md）。
- [x] 文档齐全：中文文档（本文件等）+ 英文 `README.md`(根 README 已补英文段)、`docs/en/ARCHITECTURE.md`、`docs/en/SECURITY.md`、`docs/en/COMPATIBILITY.md`、`docs/en/DEVELOPMENT.md`、`docs/en/CHANGELOG.md`（均已创建）。
- [ ] 打包后冒烟：安装 → 启动桌面 → 选项目 → 启动后端 → 本机自测通过（**安装+启动冒烟已过**；剩「选项目→启动→自测」为 GUI 交互操作，待用户真机补跑）。

---

## Phase 8 — MCP OAuth（Gemini 兼容，不动 Bearer）

**目标**：公网 MCP 走标准 OAuth 2.1（MCP 517 + RFC 9728），Gemini 可接入自定义 MCP；本地/仅 ChatGPT 路径零影响。
**产出**：`oauth_provider.py`（`LocalOAuthProvider`）、`gateway.py`（uvicorn 网关：OAuth 路由 + 同意页 + `/mcp` 代理）、`ServiceCoordinator` 编排集成、`httpx` 依赖、`test_oauth.py`。

### 验收标准
- [x] 发现元数据：`/.well-known/oauth-authorization-server` + protected-resource 元数据（issuer=固定公网域名）。
- [x] 动态客户端注册（DCR）：public/confidential；scope 只接受 `ACCESS_VIEW_MANAGE_MCP_CONTENT`，其它一律拒绝。
- [x] 授权码 + PKCE S256 + state 回传 + 精确 redirect_uri；浏览器同意页允许/拒绝，同意态/授权码过期 410/400。
- [x] access 1 小时 / refresh 60 天滚动轮换 / code 单次使用 / RFC 7009 撤销 / RFC 8707 resource 绑定。
- [x] `/mcp` 反向代理：OAuth 校验后换发引擎 Bearer（secrets 同源）；旧版 ChatGPT Bearer 常量时间直通；本机回环匿名放行；无令牌/无效/过期一律 401（带 resource_metadata 的 WWW-Authenticate）。
- [x] 密钥零落盘：同意、授权码、access token 仅内存；client 注册与 refresh 密文存储（SecretsStore/DPAPI）。
- [x] 编排：仅非 LOCAL 模式于 codex 就绪后拉起网关（8786）；域名取隧道固定 URL；失败→清理返回 ERROR；停止顺序 Codex→桥→网关→隧道。
- [x] 测试 27 项全绿，全量 **181 全绿**；ruff/pyright 新代码零新增告警。
- [ ] 真机验收（待用户）：Cloudflare 路由 Service URL `localhost:8787` → `localhost:8786`（单次操作）→ Gemini 走 OAuth 授权流程。

---

## Phase 9 — 多项目并行开发 + Windows Shell 修复（✅ 已完成）

**目标**：支持多个项目同时运行，每个项目独立 CodexPro 引擎；修复 MCP session 隔离；
确保 Windows Shell 默认不使用 WSL bash。

**产出**：
- `project_manager.py`：`ProjectManager`、`ProjectUnit`、`ProjectView`（多项目生命周期管理，纯 Python，无 Qt）
- `projects.json` 持久化（`config_store` 管理）
- `tools.py`：`WorkspaceCatalog`（per-session 项目绑定），修复 `session_key()` duck-typing bug
- `desktop_main.py`：多项目表格 UI（名称/路径/状态/端口/启用/入口 + 添加/删除/启动/停止按钮）
- `shell.py`：`ShellInfo`、`detect_shells()`、`default_shell()`、`get_shell_info()`（WSL 永不自选）

### 验收标准
- [x] 两个项目同时启动不同端口的 CodexPro 引擎，互不影响（`test_parallel_lifecycle_isolation`、`test_parallel_real_engines`）
- [x] GPT session 绑定项目 A，Gemini session 绑定项目 B，互不泄漏（`test_two_sessions_isolated_workspace_switch`）
- [x] `switch_workspace` 只影响当前 session（`test_switch_changes_only_calling_session`）
- [x] enabled 标记持久化，桌面启动自动恢复已启用项目引擎（`test_start_enabled_auto_restore`）
- [x] PowerShell / pytest / pyright / git status 执行成功（`test_commands.py`、`test_git.py`、`test_shell_info_shape`）
- [x] WSL 不会被默认调用（`test_shell_info_shape`：检测到 wsl 但 default type != "wsl"）
- [x] `shell_info` MCP 工具返回正确的 shell 信息
- [x] `list_projects` 列出所有项目及状态
- [x] 全量测试 258 全绿

---

## Phase 10 — 多项目桌面交互与前端体验重构（🚧 代码/构建完成，发布接力收尾中）

**目标**：把当前“全局配置 + 多项目列表”的混合交互改为真正的**按项目独立配置**，修复项目状态不实时、滚轮误改下拉框、服务控制冗余、Gemini 配置常驻等问题，并补齐诊断、视觉状态和发布切换能力。

**产出**：
- `ProjectConfig`：新增客户端类型与 Gemini Redirect URI 等按项目持久化字段；保留旧配置兼容迁移。
- `desktop_main.py`：项目表去掉“启用”列，操作列负责单项目服务启停；权限/客户端/连接方式下拉禁用滚轮误切换；服务配置、Git、端口、令牌、MCP 地址、自测按选中项目独立；Gemini OAuth 区按客户端类型动态显示；服务控制只保留单一启停按钮；新增状态卡和一键诊断。
- 连接方式恢复四种：仅本机、Cloudflare 固定、ngrok 固定、Quick Tunnel 临时地址。
- `app_state.py` / `gateway.py`：项目令牌贯穿 Gateway，Gemini 授权时未选工作区或工作区未运行必须阻断，不能继续发码。
- 发布链：修复 Ruff，完整 pytest/ruff/pyright/build；更新中英文文档、版本、安装器、Release 与桌面快捷方式；最终切换使用脱离当前旧版进程的自恢复脚本，避免升级过程把 MCP 会话永久断开。

### 验收标准
- [x] 项目表列为“名称/路径/状态/端口/入口/操作”，无“启用”列；点击操作列启动后状态 1 秒级刷新为“启动中/已连接/失败”等真实状态，运行中按钮切换为“停止服务”。
- [x] 桌面不再按历史 `enabled` 自动恢复项目，也不使用固定 4 秒等待“启动所有服务”。
- [x] “权限模式”“客户端”“连接方式”QComboBox 在鼠标悬停时滚轮只滚动窗口，不改变选项。
- [x] 每个项目独立记忆权限模式、客户端类型、连接方式、公网域名、Windows 桥开关、Git 参数、Gateway/内部端口、Gemini Redirect URI；所有输入完成后自动保存，切换项目再返回不得重置。
- [x] 访问令牌与 Cloudflare Tunnel Token 按项目用 SecretsStore 加密保存；已有全局旧令牌无损迁移，升级后现有连接不因令牌变化被强制失效。
- [x] 客户端选择“Gemini Spark”时显示 Gemini OAuth 配置；选择“ChatGPT 网页端”时隐藏；Gemini 凭据仍只经 SecretsStore 保存且 Secret 不进日志/JSON。
- [x] 连接方式四种全部可选：Local / Cloudflare Named / ngrok reserved domain / Quick Tunnel；字段启用、提示与临时 URL 标识正确。
- [x] 服务控制区只有一个动态“启动服务/停止服务”按钮（另保留“高级设置”）；无“重启服务”和第二个停止按钮。
- [x] 控制页增加组件状态卡；“连接诊断”独立页提供一键诊断 + 真实 MCP 自测 + 开发环境检测，错误用中文给出明确原因和下一步。
- [x] Gemini OAuth 同意页：未选择项目或所选项目 CodexPro 未运行时返回可理解错误并拒绝 approve/code，不允许静默回落。
- [x] README/安全与架构文档明确：桌面默认权限是“完全访问（危险）”（用户明确意图），首次仍保留一次性风险确认。
- [x] 关闭窗口不在 GUI 主线程同步等待所有子进程停止。
- [ ] `pytest tests -q`、`ruff check src tests`、`pyright` 全绿；PyInstaller + Inno Setup 构建成功，安装版启动/选项目/启动/停止/自测冒烟通过。
- [ ] Git commit + push 成功；GitHub Release 更新为新版本并附安装器；桌面旧快捷方式替换为新版快捷方式，升级切换后 MCP DevBridge 能自动恢复服务。

---

## Phase 11 — v0.6 桌面产品 UX 重构（✅ 已完成）

**目标**：在不改变 MCP/安全核心语义的前提下，把 0.5 的功能型控制台重构成更清晰、克制、可理解的桌面产品，并修复全局 busy 导致跨项目误置灰的问题。

### 产品原则
- 项目状态和交互按项目隔离：项目 A 运行不能锁住项目 B 的启动、选择、编辑与诊断。
- 运行中的项目只锁真正不能热改的启动参数；停止、复制、诊断、自测、日志始终可用。
- 新用户不携带开发者本机路径、域名、Token、Git 或 Gemini 配置；现有端口架构沿用 0.5，不引入 8789。
- 主界面优先显示项目、状态、启动/停止、MCP 地址与必要连接设置；技术解释移入 tooltip、诊断或高级设置。
- 使用一致的卡片层级、状态色、主次按钮、间距和中文自然语言。

### 验收标准
- [x] 任一项目 READY 时，其停止服务按钮可点击；其它 IDLE/ERROR 项目的启动/重新启动按钮同时可点击。
- [x] 选择未运行项目时，其配置与操作只按自身状态决定，不受其它项目运行影响。
- [x] STARTING/STOPPING 只短暂锁对应项目冲突操作，不使用全局 busy 锁死整个项目列表。
- [x] 添加项目在任何项目运行时仍可用；删除只按目标项目自身生命周期保护。
- [x] 项目表、状态、主操作、连接信息与设置区重做视觉层级；常驻小字精简为 tooltip 或按需提示。
- [x] 新用户隔离配置测试证明：项目列表为空，active_workspace/path/hostname/Git/Gemini/Token 不含开发机值；仅保留现有通用端口默认/自动分配逻辑。
- [x] UI 在 900x650、1200x850、1920x1080 下无横向溢出（Qt offscreen 三档验证横向滚动最大值均为 0）。
- [x] 标题栏“—”保持普通最小化到任务栏；“×”默认隐藏到系统托盘且不在任务栏保留窗口；托盘可恢复窗口并右键退出。
- [x] 设置页可切换“最小化到托盘 / 直接退出”，选择持久化；托盘退出复用异步服务清理。
- [x] 全量 pytest、Ruff、Pyright、Qt offscreen UI 冒烟、PyInstaller/Inno Setup 全绿：294 passed，Ruff 全绿，Pyright 0/0，staging EXE 冻结版冒烟通过，0.6.0 安装包已生成。
- [x] v0.6.0 commit/push + GitHub Release + detached 本机升级 + 桌面快捷方式替换 + 当前 D:\ 服务自动恢复全部成功。

---

## Phase 12 — v0.7 Multi-Device Hub + 新手可用性（✅ 已完成）

**目标**：让一个 ChatGPT MCP URL 同时连接多台不同网络/IP 的 MCP DevBridge；同时修复日志可用性，把诊断、帮助和关键网络配置改造成无网络基础用户也能理解的产品体验。

### 12.1 Multi-Device Hub
- [x] 新增稳定设备身份（device_id / device_name）与远程设备目录；设备 Bearer、心跳密钥只进入 SecretsStore，不明文写 JSON/日志。
- [x] Hub 生成一次性配对码；另一台 DevBridge 在已有公网连接（Cloudflare/ngrok/Quick）后，输入 Hub 地址 + 配对码即可注册，无需开放家庭路由器端口。
- [x] 配对成功后远程设备定时向 Hub 上报心跳、设备名称和最新 MCP URL；Quick Tunnel 重启导致 URL 改变时 Hub 自动更新，无需修改 ChatGPT 中的 Hub URL。
- [x] Gateway 新增 `devbridge_list_devices / devbridge_get_current_device / devbridge_switch_device`，设备绑定按 `mcp-session-id` 隔离，不影响其它聊天/客户端会话。
- [x] 仅 1 台设备在线时自动选择该设备；多台在线且当前会话未显式切换时保持本机为默认，并允许 AI/用户切换目标设备。
- [x] 远程设备选择后，普通 MCP 请求透明代理到远程 DevBridge；远程设备自己的 `list/switch workspace` 继续管理该设备上的项目，不把远端路径暴露为本机路径。
- [x] tools/list 注入必须按工具名去重，Hub→远程 Hub 级联时不得出现重复设备工具。
- [x] “设备”页展示本机身份、在线设备、当前公网地址、配对码、加入 Hub 表单、在线/离线状态和移除操作，文案面向普通用户。

### 12.2 Quick Tunnel 产品确认
- [x] 当前代码使用 `cloudflared tunnel --url http://127.0.0.1:<gateway>`，解析随机 `*.trycloudflare.com` 并自动补 `/mcp`。
- [x] 单机 Quick Tunnel 的体验定义：无需 Cloudflare 账号，启动后复制生成的 MCP URL 到 ChatGPT 即可；每次重新建立 Quick Tunnel 都会获得新随机 URL，因此 ChatGPT 侧需要更新 URL。
- [x] 使用手册和连接方式帮助浮层明确说明 Quick / Cloudflare Named / ngrok / Local 的差异、使用场景和取舍。

### 12.3 日志与诊断
- [x] 修复“进程日志”：当前实现错误地把 `ProcessLog` 当可迭代对象，且始终读取公网入口项目；改为读取选中项目对应的项目服务/Windows 控制日志，公网连接日志只读取入口项目 Tunnel。
- [x] 修复“审计日志”：当前桌面主链为 Gateway→CodexPro，Python backend 的 AuditMiddleware 并未覆盖该链路；Gateway 必须对实际 `tools/call` 写统一脱敏审计记录。
- [x] 日志界面不再暴露 Codex/Gateway 等内部名词作为主要信息；显示“项目服务 / Windows 控制 / 公网连接”“读取文件 / 修改文件 / 执行命令”等自然语言，空状态明确告诉用户何时会产生记录。
- [x] Gateway/连接日志加工为“什么时候、发生了什么、是否成功、需要处理什么”，原始技术细节保留在按需展开/复制能力中。
- [x] 诊断输出先给总结果（可以正常使用 / 需要处理），再逐项给原因与下一步；组件级技术状态移出工作台，只在诊断页按需展示。
- [x] 工作台删除独立“连接自测”卡片；真实 MCP self-test 合并进诊断流程，避免功能重复。

### 12.4 使用手册与上下文帮助
- [x] 新增顶层“使用手册”：可搜索主题，左侧主题导航，右侧分步骤教程，并支持上一篇/下一篇。
- [x] 提供“连接方式选择助手”，根据是否跨设备、是否长期使用、是否有固定域名等条件推荐 Local / Quick / Cloudflare Named / ngrok。
- [x] 覆盖首次使用、ChatGPT 连接、Gemini 连接、四种连接方式、多设备 Hub、权限、安全、日志、诊断与常见故障。
- [x] “连接信息 / 公网入口端口 / 连接方式 / 公网域名 / 隧道令牌”旁增加半透明 `?` 帮助按钮；悬停或点击显示非模态浮层，移开或点击空白自动消失。
- [x] 帮助文案以无网络基础用户为目标，不要求用户理解端口、反向代理、隧道、Gateway 等术语后才能完成操作。

### 12.5 工作台与视觉修正
- [x] 修复 GroupBox 标题与白色卡片边界重叠；标题和卡片主体有明确垂直层级。
- [x] 工作台仅保留项目、当前状态/主操作、连接信息、运行记录；删除“Codex / Gateway / Tunnel / Windows Bridge”常驻组件小字。
- [x] 关键状态使用用户语言：未启动 / 正在连接 / 可以使用 / 连接失败；技术组件状态移到诊断详情。
- [x] 900×650、1200×850、1920×1080 三档无横向溢出；帮助浮层不遮挡主要操作且可自动消失。

### 12.6 发布验收
- [x] 新增设备路由、配对/心跳、Quick URL 更新、单在线设备自动选择、跨会话隔离、Gateway 审计、进程日志、手册搜索/帮助浮层回归测试。
- [x] 全量 pytest、Ruff、Pyright、Qt offscreen、多设备 Gateway 集成、PyInstaller、Inno Setup 全绿：304 passed，Ruff 全绿，Pyright 0/0；7 个顶层页三档布局通过；staging 冻结版启动通过；0.7.0 安装器完整生成。
- [x] v0.7.0 commit/push + GitHub Release + 完整安装包校验 + detached 本机升级 + 桌面快捷方式替换 + 当前 `D:\` 服务自动恢复全部成功。

---

## Phase 13 — v0.7.2 默认异步命令任务（✅ 已完成）

**目标**：彻底移除用户命令 timeout 和“普通 / 大型任务”分类。所有 `bash` 命令统一异步启动、立即返回 task_id，由同一套任务管理工具查询或取消。

### 13.1 统一执行模型
- [x] `bash` MCP schema 删除 `timeout_ms`，公开用户任务不设置 execution timer。
- [x] `bash` 本身改为任务启动器，调用后立即返回 `task_id`；删除重复的 `start_task`。
- [x] 保留 `get_task / wait_task / list_tasks / cancel_task`；`wait_task` 最多等待 60 秒仅用于轮询，不限制任务执行。
- [x] 增加 600 秒编排无接续看门狗：记录 `lastObservedAt`，超时后仅返回 `orchestrationStale/resumeHint`，不杀后台任务；任何任务管理调用会刷新观察时间。
- [x] Server instructions / tool description 明确所有 shell 命令均按后台任务执行，不再要求 Agent 判断任务规模。

### 13.2 安全与生命周期
- [x] 所有任务继续复用 Bash session guard、PathGuard、safe/developer/full 策略和危险命令拦截。
- [x] `cancel_task` 结束完整进程树；Windows 使用 `taskkill /t /f`。
- [x] stdout/stderr 使用有界滚动缓存，超出缓存只丢弃较早输出，不因日志量终止任务；返回 MCP 前继续脱敏。
- [x] 完成任务内存保留 24 小时、最多 100 条；运行任务不被历史清理驱逐；任务不跨 DevBridge/CodexPro 重启持久化。
- [x] `--bash off` 同时隐藏 `bash` 与全部任务管理工具；minimal / standard / full 工具目录正确注册。

### 13.3 回归与发布
- [x] TypeScript build 通过；`async-task-smoke` 验证立即返回、自然完成、取消进程树和列表恢复。
- [x] 主 MCP smoke 验证 `bash` schema 不再含 `timeout_ms`，并通过真实 MCP `bash → wait_task` 完成任务。
- [x] v0.7.1 真机发现 TaskManager 错绑 MCP Server/session；v0.7.2 提升为进程级共享，并新增“HTTP session A 启动 → session B wait_task”回归。
- [x] 完整 `npm run smoke` 全绿；根项目 304 passed；Ruff 全绿；Pyright 0 errors / 0 warnings。
- [x] watchdog 合入后重新生成 PyInstaller / Inno Setup v0.7.2，冻结版冒烟、安装器 SHA-256 和 detached updater Dry Run 全部通过：安装器 66,977,760 bytes，SHA-256 `09f6e87f699fdc806404a961846cf78d1ece1fbdcaff80d3a6b4a1243c577510`。
- [x] commit/push + GitHub Release v0.7.2 + 桌面快捷方式替换 + 正式安装版重启 + `D:\` 自动恢复全部成功；安装后现场确认 `bash` 默认返回 task_id、独立 MCP `wait_task` 能找回同一任务、安装版 `server.js` 无 `timeout_ms`、watchdog 阈值 600000 ms 生效。

---

## Phase 14 — v0.8.0 单域名 Hub / OAuth 路由 / 桌面可靠性（🚧 进行中）

**目标**：让用户和好友只通过一个固定 Hub 域名连接 ChatGPT，在同一个 App 中自由切换设备和多个工作区；修复桌面启停/退出/重复实例/配对反馈，并加入内置更新。

### 14.1 Hub OAuth 与工作区路由
- [x] OAuth consent 删除工作区单选，token 只授权 Hub；新 token subject 为 `local-user`，旧 workspace-bound token 保持兼容。
- [x] 工作区改为 MCP session 级运行时选择；唯一运行工作区自动选，多项目时优先公网入口项目；`devbridge_switch_workspace` 仅影响当前 session。
- [x] direct project Bearer 继续固定到对应项目，避免破坏旧客户端。
- [x] 切换设备时清除本地 workspace 选择；远端 workspace 工具透明代理到目标设备。
- [x] 增加真实代理回归：session A 切到 B 后请求确实走 B 端口，session B 仍走默认入口 A。

### 14.2 Cloudflare / Multi-Device 拓扑
- [x] 明确主 Hub Named Tunnel Token 只能由主 Hub 使用；远端设备使用 Quick/ngrok/独立域名回传。
- [x] 远端已配对且检测到与 Hub 相同 hostname 的 Named Tunnel 时，桌面自动迁移到 Quick 并删除该项目保存的 Tunnel Token。
- [x] 新配对前发现同域名 Named Tunnel 时直接阻止并用自然语言解释 `Client ID not found` 风险。
- [x] 设备注册增加 1800 秒幂等 receipt，支持注册响应丢失后的安全重试；首次配对成功后设备目录与心跳凭据持久化，双方软件/电脑重启不再重复配对；客户端最多重试 3 次。
- [x] “已连接的电脑”改名“在线设备”，配对成功明确说明下一步仍使用主 Hub App。

### 14.3 桌面状态与单实例
- [x] QLockFile 单实例锁：第二个桌面实例不再启动，避免重复配置/端口/托盘状态互相覆盖。
- [x] 在线升级安装前清理所有旧 MCPDevBridge.exe 实例，修复旧版本遗留重复进程。
- [x] READY/IDLE/ERROR 稳定状态自动清除 stale busy；READY 的停止按钮不再被 busy 误置灰。
- [x] 托盘退出显示“正在退出…”，防重复点击，并增加 12 秒退出 watchdog。

### 14.4 反馈与客户端更新
- [x] 复制地址/令牌/Gateway/Gemini/配对码统一提供短暂绿色成功反馈。
- [x] 新增 GitHub Release 检查模块：仅有新版时显示顶部更新按钮；启动后检查，之后每 12 小时检查一次。
- [x] 更新弹窗展示 Release notes；下载后校验 size/SHA-256，再调用 bundled live_upgrade 静默安装与恢复项目。
- [x] PyInstaller bundle 内置 `live_upgrade.ps1`；无项目的新用户也能完成客户端更新。
- [x] 正式安装包内置固定版本 Node.js 22.19.0、uv/uvx 0.11.25 与 cloudflared 2026.7.3；优先使用私有运行时，不污染系统 PATH；启动/诊断自动检查组件完整性。
- [x] 可选 Windows 控制继续由内置 uvx 首次联网获取锁定的 `windows-mcp==0.8.2`，避免在安装器/启动器中静默修改系统级开发环境。

### 14.5 验收与发布
- [x] OAuth / Multi-Device / v0.7 UX / v0.8 专项回归 66 项全绿，Ruff/Pyright 专项门通过。
- [x] 全量 pytest 311 passed；Ruff 全绿；Pyright 0 errors / 0 warnings；CodexPro TypeScript build 通过。PowerShell parser / uv lock / diff check 在最终 commit 前再执行一次静态确认。
- [x] PyInstaller/Inno Setup v0.8.0 构建成功；staging 强制校验内置 Node/uv/uvx、cloudflared 与 `live_upgrade.ps1`；安装器 SHA-256 `9F98E029C1297CC936A3D304E57281C4232BFE01DDCFA36FB44941A341560C65`；detached updater Dry Run 已返回 `ok=true`。冻结版启动/单实例真机冒烟并入最终 0.7.2→0.8.0 接力，避免提前断开当前 QuickTunnel 开发会话。
- [x] v0.8.0/v0.8.1 已完成 commit/push/Release/在线升级；单实例、快捷方式、D:\ 与多项目恢复、固定域名 OAuth 均已真机验收。


---

## Phase 15 — v0.9.0 双固定域名 / Stateless Multi-Device / Agent Pool（✅ 开发与发布完成；Jerry 第三方 Tunnel 实网验收待用户配置）

**目标**：让两台电脑既能继续作为一个 Hub 的设备，也能各自拥有稳定固定 MCP App；同时把“手工新开很多 ChatGPT 会话”升级为本地受控多 Agent 并发开发。

### 15.1 多设备重启与显式路由
- [x] 现场确认远端 Quick URL 重建后 heartbeat 会更新 Hub：主 Hub 已从失效的 `dublin-*` 切到新的 `brandon-*`，并能直接列出远端 C/D/E/F 工作区。
- [x] 明确旧 Quick URL 直连 App 无法由 DevBridge 自动改 ChatGPT 配置；长期独立 App 必须使用固定域名。
- [x] `devbridge_list_workspaces / get_current_workspace / switch_workspace` 增加正式 `device_id`，远端查询/切换可跨 transport 无状态路由。
- [x] 安装版 + `mcp.shiningsugar.shop` 真机验证正式 `device_id` schema、远端工作区调用及一次性 route 不污染 session 默认设备。

### 15.2 每台电脑独立固定 Tunnel
- [x] 不同电脑的独立 OAuth Gateway 必须使用不同 Tunnel UUID/Token；同一 `localhost:8786` 不冲突，因为 loopback 属于各自主机。
- [x] 远端已配对设备可使用自己的独立固定 hostname；只有 hostname 与 Hub 相同才阻止/迁移，`jerry.shiningsugar.shop` 不会被误判。
- [x] README/Help 明确 `mcp` 主 Tunnel 与 `jerry` Tunnel 的配置方式，以及 Quick App 仅临时使用。
- [ ] 用户在 Cloudflare Dashboard 创建 `jerry` 独立 Tunnel/route 后做双固定域名真机验收（需要该独立 Tunnel 的实际 token，程序不会凭空生成 Cloudflare 凭据）。

### 15.3 Agent Pool
- [x] 新增 `agent_pool.py`：任务持久化、bounded queue、默认 4/硬上限 16 物理并发、单批最多 64。
- [x] 执行器自动探测 OpenCode CLI / Claude Code CLI；Gateway lazy init，普通 MCP 使用不创建线程池。
- [x] 写入任务强制 Git worktree + `mcp-agent/<id>` 分支隔离；主 checkout 不被并发 Agent 直接修改。
- [x] MCP 生命周期工具：capabilities/spawn/spawn_batch/list/get/wait/cancel/collect/cleanup；wait 单次最多 30 秒，取消杀完整进程树。
- [x] 重启后历史 running/queued 标记 interrupted；不伪造后台任务继续运行。
- [x] 完整全量 pytest / Ruff / Pyright / PyInstaller / Inno / frozen smoke / detached updater / 真机多执行器 capability / provider 失败诊断验收。

### 15.4 发布
- [x] 版本统一为 0.9.0、更新 Help/README/Architecture/AGENTS/Changelog。
- [x] commit + push + GitHub v0.9.0 Release；最终资产 104,378,692 bytes，SHA-256 `D8AC193201CB8A87BE803380887766A47567AF094C2E92C0B52D11E19F0D2908`。
- [x] 本机在线升级最终 0.9.0，确认单实例、快捷方式、运行项目、安装版正式 device route 与 Agent Pool tool scan。

---

## Phase 16 — v0.9.1 全局连接 / 多项目可靠性 / SteamOS / Agent Orchestrator（🚧 发布前验收）

**目标**：让一台多项目电脑只配置一次固定公网入口，并让连续/批量服务控制在轮询 UI 下保持确定、可恢复。

### 16.1 设备全局连接
- [x] AppConfig 增加设备连接方式/hostname；Cloudflare global credential 独立加密存储，并兼容历史/项目级 credential。
- [x] 工作台连接信息新增设备全局配置；保存后批量同步现有项目，新项目自动继承。
- [x] Gateway 端口改为设备全局语义；项目内部 CodexPro/Windows 端口继续独立。

### 16.2 工作台与全部启停
- [x] 删除“当前项目”重复卡片；单项目服务按钮只保留项目表格；高级设置移入项目设置。
- [x] “移除项目”右侧新增“一键启动所有服务 / 一键停止所有服务”状态切换按钮。
- [x] 一键启动使用一个 coordinator 公网入口 + 并行其余 ProjectUnit；一键停止并行 coordinator/其它 ProjectUnit，逐项目聚合异常。

### 16.3 Stop 可靠性
- [x] busy flag 不再由 poll 提前清除，只由 async completion 释放；重复操作在入口处防重。
- [x] 项目表按 id 原位更新，cellWidget 按钮跨轮询保持同一对象，不再每秒销毁/重建。
- [x] 新增 4 项目连续 stop、4 项目 bulk stop、bulk start、按钮 identity、global inheritance 等回归。

### 16.4 SteamOS / Linux Desktop 原生兼容
- [x] 将配置目录、Shell/进程树、runtime 查找、文件管理器打开方式、Windows-MCP 能力判定改为显式跨平台实现；SteamOS/Linux 使用 XDG 用户目录和 bash/POSIX 进程组，不依赖 Wine/Proton。
- [x] Linux Secrets 优先使用 freedesktop Secret Service（`secret-tool`），无 Secret Service 时使用用户级 AES-GCM 加密 fallback（0600 key/file），避免明文凭据。
- [x] 新增 Linux 私有 Node.js/cloudflared runtime 准备、PyInstaller onedir、用户态安装器、Desktop Entry/autostart 与 `live_upgrade.sh`；不修改 SteamOS 只读系统分区。
- [x] GitHub CI/Release 增加 `ubuntu-22.04` Linux/SteamOS 构建与测试 job；最终发布同时提供 Windows Installer 与 `MCPDevBridge-Linux-x86_64-*.tar.gz`。

### 16.5 Agent Orchestrator / Spawner
- [x] 在底层 Agent Pool 之上增加持久逻辑 Agent：`spawn_agent / list_agents / get_agent / message_agent / cancel_agent / wait_agents`；每个逻辑 Agent 可跨多个 executor turn 继续工作。
- [x] `message_agent` 对 queued executor 直接追加 prompt；对已运行的一次性 OpenCode/Claude CLI，则在同一 worktree/branch 排 continuation turn，不伪装不存在的 live stdin。
- [x] 新增 `spawn_agent_team / get_agent_team`：worker 并行执行、写任务独立 `mcp-agent/*` worktree/branch；worker 完成后自动 read-only Reviewer；最后在独立 `mcp-team/*` integration worktree 启动 Merger，要求合并成功 worker、解决冲突、运行验证、禁止 push。
- [x] Agent turn 自动本地 commit，Reviewer 获取 worker branch/diff；Merger 若仍有 unmerged Git 冲突则判失败，不会把 conflict marker 当成功结果。
- [x] Orchestrator MCP 工具正式支持 `device_id`，spawn/team 同时支持 `project_id`，可直接在远端设备/项目启动团队而不依赖 session switch 状态。

### 16.6 发布
- [x] 全量 pytest / Ruff / Pyright / CodexPro smoke+stress / Windows PyInstaller+Inno / frozen smoke / detached updater Dry Run。
- [x] GitHub Windows + Linux/SteamOS CI 全绿，并由 Release workflow 生成/验证 Linux x86_64 包。
- [x] 在线升级当前 Windows 机器并真机验证全局配置、连续停止、一键全部启停、Agent Orchestrator tools、单实例和快捷方式。
- [x] commit/push + GitHub v0.9.1 Release（Windows + Linux/SteamOS 双资产）。

## Phase 17 — v0.9.2 Agent 可靠性 / 非 Git / 控制面 / 包体瘦身（🚧 发布收尾）

### 17.1 Agent 执行语义
- [x] Gateway-local Agent 工具返回标准 MCP `content + structuredContent`，新增回归覆盖 ChatGPT `CallToolResult` 形态。
- [x] 写 Agent 支持 `auto / git_worktree / direct`；非 Git 目录/盘符根可 direct 写，显式 `git_worktree` 在非 Git 目录 fail closed。
- [x] spawn/team 支持 bounded `target_path`，只能落在当前 routed workspace 内。
- [x] 真实 OpenCode/Claude executor 要求 `MCP_AGENT_RESULT`；plain text、OpenCode JSON event/Claude stream-json 均可解析。
- [x] Team 默认 `all_required`；worker/Reviewer/Merger 的未完成或未验证不再被部分成功掩盖。
- [x] 新增 logical `cleanup_agent / cleanup_agent_team`，终态元数据、worktree、临时分支可真正清理。

### 17.2 控制面与执行器
- [x] 桌面新增 Agent 管理面板 + `Ctrl+Shift+A`：状态/角色/模型/目标目录/隔离/分支/耗时/输出 + message/cancel/cleanup。
- [x] OpenCode 强制 `--pure`；未指定模型默认 `opencode/nemotron-3-ultra-free`，可由 `MCP_DEVBRIDGE_OPENCODE_FREE_MODEL` 覆盖。
- [x] 本机核验 Codex Desktop deep link 的 `chat/work/codex` mode；Chat mode deep link只预填 prompt，不作为自动本地 executor。
- [x] 本机 Codex CLI 0.148.0-alpha.15 无 `chat` 子命令；ChatGPT 登录仍属于 Codex/agentic usage，不作为“不耗 Codex 额度”的普通 Chat executor。

### 17.3 复杂度与安装包
- [x] Agent Gateway 分派从 OAuth Gateway 主文件抽离；`_exec_local_tool` 复杂度代理从 99 降至 34。
- [x] 新增 `prepare_codexpro_runtime.py`，按 package-lock 复制 production dependencies；CodexPro runtime 约 55 MiB → 15 MiB。
- [x] 专项 9+6+59+6 全绿；全量 356 passed，Ruff 0，Pyright 0/0。
- [ ] CodexPro build/smoke/stress + v0.9.2 PyInstaller/Inno/frozen/updater/live-upgrade 全链。
- [ ] 清理旧 build/dist/tmp/release 冗余，保留最新与必要回滚资产。
- [ ] commit/push + GitHub v0.9.2 Release，并验证线上资产 digest/size。

## Phase 18 — v0.9.3 普通 ChatGPT Chat 多 Agent（🚧 发布收尾）

### 18.1 Desktop Chat executor
- [x] 逆向只读确认 Desktop Deep Link 的稳定 mode 为 `chat/work/codex`，`prompt` 仅 prefill；旧 `AppActivate + Enter` 测试判定作废。
- [x] 用 CDP 精确定位 ordinary Chat composer 与真实 `发送`；修复当时 Xray core 未运行导致的 `ERR_PROXY_CONNECTION_FAILED` 假阴性。
- [x] 验证普通 Chat 可直接调用已连接 mcp-wjp：真实 Gateway audit + 文件双验，完成 Chat → MCP → 本地 write/read，不进入 Work/Codex。
- [x] 新增 `chatgpt_desktop.py`：Deep Link、loopback-only CDP、Send/Stop、bridge state、prepare/restore；禁止私有 ChatGPT HTTP API / Token 抽取。
- [x] AgentPool 新增 `chatgpt` executor；bridge ready 时 `auto` 首选 ChatGPT Chat，OpenCode/Claude 后备。

### 18.2 正确性与隔离
- [x] child Chat 禁止隐式 workspace switch；每次 MCP call 固定 routed root + formal `devbridge_workspace_id`，兼容 C:/D: 与 transport recreation。
- [x] task-id receipt + MCP read-back 作为唯一 completion truth；自然语言 DONE 不足以完成 task。
- [x] conversation id 启动即持久化；cancel 定位对应 sidebar chat 并点击真实 Stop。
- [x] Git Chat writer 使用 routed-drive worktree；真机验证 README/NEW.txt 只改 isolated checkout，主 checkout 不变，collect diff 正确，cleanup 删除 worktree/branch/receipt。
- [x] managed 3-Agent direct batch：3/3 completion_verified，单任务 66.4/74.3/90.2s，wall ~92s，约 2.5× 并行收益。

### 18.3 控制面与发布
- [x] Agent 管理面板新增 Chat bridge 状态、准备、恢复；Gateway 新增 status/prepare/restore MCP 工具，prepare 默认不允许隐式重启。
- [x] 89 项 Chat/Pool/Orchestrator/Gateway/UI 专项全绿；Ruff / Pyright 0。
- [x] 全量 pytest **368 passed**、CodexPro build/smoke/stress、Ruff、Pyright、Windows PyInstaller/Inno、frozen、updater Dry Run 全绿；Windows 安装器 97,781,771 bytes（93.25 MiB），SHA-256 `F99457B346A524FAA3A70B3D839DB998B18E07C35E34F9ABFAF960F37AB1D4AA`。
- [x] 真机在线升级至 v0.9.3：单实例、Gateway 8786、升级前运行的 D:/8788 恢复，C:/8791 保持未启动；安装版 `agent_pool_capabilities` 显示 `chatgpt` 首选/ordinary_chat/19222 ready，并完成 installed build managed Chat task（真实 write/read + receipt，completion_verified=true）。
- [x] GitHub Release workflow `32205230390` 双平台全绿：Windows job 5m42s、Linux/SteamOS job 3m23s；CI Windows installer **98,925,517 bytes（94.34 MiB）** / SHA-256 `42DEA40DAAEEF512DB8AD887632D6B3AB3DB35644BCC98FB958B094A8430BD40`，Linux x86_64 **154,938,689 bytes（147.76 MiB）** / SHA-256 `41A2A5E818A5D7093EE522952BAB7127FCD3682EBFBE1AC7C8E166789639614B`。关键 Linux payload 与 production-only CodexPro runtime 已复验；最终文档提交后创建/push `v0.9.3` tag + GitHub Release 并上传这两份 CI 资产。

## 风险与缓解（记录）

| 风险 | 缓解 |
|---|---|
| ChatGPT Pro 对自定义 MCP 仅读/获取 | 明确文档：完整写需 Business/Enterprise/Edu；不伪装绕过；提供兼容提示 |
| Gemini Spark 限制（US/18+/语言） | 文档说明先决条件；本机自测 + OpenAI 兼容检查 |
| DNS rebinding 保护拦公网 Host | Phase 4 显式 transport_security 配置 + 真机验证 |
| Quick Tunnel URL 变化 | 仅调试模式 + 醒目警告；验收用 Named Tunnel |
| 有墙内网络问题 | 文档注明本地+SSH 替代方案；验收以 Named Tunnel 为准 |
| 端口默认值不一致（8765 vs 2865） | **已解决（2026-08-09）**：统一为 `constants.DEFAULT_*_PORT` 集中维护（Gateway 8786 / CodexPro 8787 / Windows-MCP 28731 / Legacy backend 8765），`RuntimeConfig.local_port` 兼容属性，旧配置自动迁移；桌面端口配置 UI + 占用预检 |

## 变更记录

- 2026-08-06 · 创建完整开发计划；Phase 0/1/2 标记完成，Phase 3 进行中。
