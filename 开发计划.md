# MCP DevBridge v0.9.0 未完成开发计划

> 正式生产基线：`v0.8.9`，commit `b679268ee6cbbed49f5a2ca93c6e81d8e94d6184`。
>
> 当前研发分支：`release/v0.9.0`。
>
> 唯一 durable run：`lr_mtfcuqcl_6abdc2169c31`。
>
> S1–S7 已完成并迁入 `进度验收.md`；本文件只保留仍被平台能力阻断的 S8/S9，以及由该阻断导致不能完成的 S10 终局门。不得把本地后台执行连续性表述成 ChatGPT host 已能自行创建下一轮 assistant turn。

## 1. 当前已验证边界

### 1.1 已完成的本地 durable fallback

当前 CodexPro 已实现并在本机安装实例热加载验证：

- 带 `long_run_id` 的 Bash 任务在进程启动前预留并持久绑定 `task_id`。
- completed / failed / cancelled 终态由 BashTaskManager 自动写入同一 LongRunState，不依赖用户或 Agent 碰巧调用 `wait_task/get_task`。
- spawn 前失败同样以 failed 终态收敛；重复 terminal event 同状态幂等，冲突状态拒绝覆盖。
- terminal receipt 落盘中的任务继续阻断 review / completion，不能把“进程已结束、持久化尚未完成”误判为完成。
- Windows 立即取消使用有界的进程树终止 grace/retry，避免只杀 PowerShell 壳而遗留孙进程。
- CodexPro 进程替换后，已落盘终态可从 `.ai-bridge/long-runs/` 恢复，不再变成 unknown。

详细实现、红测、重测、运行时 SHA 与实机重启证据见 `进度验收.md` §48 及其后续终局补充。

### 1.2 当前 ChatGPT host 能力

当前真实 ChatGPT → DevBridge 连接的 `server_config` 明确报告：

```text
nativeMcpTasksExtension = not emitted by baseline connector
```

因此当前客户端没有与服务端双向协商 `io.modelcontextprotocol/tasks`。DevBridge 可以让本地进程跨 ChatGPT turn/断连继续运行并保存 checkpoint，但服务端不能单方面：

```text
结束当前 ChatGPT assistant turn
→ 无新的 host/client 请求
→ 自动创建下一轮 assistant turn
→ 再次调用 MCP 继续模型推理
```

Regular Chat 浏览器控制器只保留为实验源码、离线 fixture 与恢复算法资产；正式桌面、正式 CLI、PyInstaller、Windows/Linux 构建和 installer 均不得引用、暴露或打包该实验路径。不得用自动抓取 ChatGPT 网页 Output 绕过 host capability gate。

## 2. S8 · Native MCP Tasks：平台阻断

### 2.1 目标

只有在真实客户端与服务端完成 Tasks extension 双向协商后，才能实现：

- `tools/call` 返回 durable task handle。
- `tasks/get` / `tasks/cancel` / 必要的 update 生命周期。
- Connector / DevBridge 重连后按同一 task id 恢复。
- input-required、取消、TTL、状态上限和错误语义。
- 与现有 LongRunState 建立单一事实源映射，禁止再建第二套任务真相源。

### 2.2 当前阻断

- 当前 ChatGPT Connector 未声明 `io.modelcontextprotocol/tasks`。
- 服务器不能向未 opt-in 的客户端擅自返回 Tasks extension 结果。
- 现有工具级 durable fallback 已通过，但它不是 native MCP Tasks，也不能控制 host assistant-turn 生命周期。

S8 在 durable run 中保持 `blocked_platform`。这不是本地代码测试失败，也不能用私有网页自动化替代。

### 2.3 恢复开工条件

以下条件全部满足后才允许解除阻断：

1. 当前 ChatGPT/目标 MCP 客户端在真实请求 capability 中明确声明 Tasks extension。
2. 服务端 SDK 与 HTTP transport 对 create/get/cancel 路径有可运行实现，而非仅类型或草案。
3. 本地 conformance 先覆盖协商缺失、错误 method、断连、取消、重复请求、状态上限和 secret redaction。
4. 再在真实 Connector 上验证同一 task id 跨断连恢复。

## 3. S9 · 长任务宿主验收：依赖 S8，平台阻断

### 3.1 目标

在受支持的 host async primitive 存在后，运行至少 20 分钟的真实无人值守工作流，覆盖：

- 多轮工具调用与本地 build/test。
- Connector / DevBridge / CodexPro 进程替换。
- 短网络故障和取消。
- 同一 durable task identity 恢复。
- duplicate mutation=0、错误新会话=0、conversation/task loss=0。
- CPU、RSS、句柄、状态文件、checkpoint 和日志有界。

### 3.2 当前可验证与不可验证部分

已验证：本地 background process、LongRunState、terminal receipt 与下一次 MCP 调用时的零猜测恢复。

当前不可由 DevBridge 单方面验证：host 在结束一个 assistant turn 后，无用户或平台触发地自动创建新 turn 并继续模型推理。

因此 S9 在 durable run 中保持 `blocked_platform`。不得把“本地命令仍在运行”写成“ChatGPT 已无人值守继续推理”。

## 4. S10 · 文档、审查与 durable completion 终局门

### 4.1 本地收口已完成，剩余仅为 durable/platform gate

S10 的本地可控工作已经完成：三份事实源已对齐；历史 pre-fix unknown task 均按 failed/not accepted 保守解析；正式 UI/CLI/build/PyInstaller/wheel 与实验 Regular Chat 边界已审查。全量基线 r6 为 **16/16 PASS**（task `3d301cb9-12a9-4384-991f-a3ecfe2af12a`）；r6 后的并发/资源缺陷完成 defect-first 红→绿，当前 r9 受影响层门为 **8/8 PASS**（task `51ba783e-6581-427f-a86d-2f3a11bec5ec`，摘要 `.ai-bridge/forensics/20260901-s10-current-delta-r9/summary.json`），Regular Chat 当前为 37 unit + 13 Playwright fixture。

剩余动作只有 durable/platform gate 与独立改进提交：

1. S8/S9 继续保留 `blocked_platform`，不得伪造 host/browser 证据。
2. 对当前最新 work revision 执行最终 defect-first review；本地代码/边界可以判定通过，但整个 durable objective 因 S8/S9 未满足不得登记 PASS completion review。
3. 调用 `long_run_complete` 验证其因 S8/S9（以及由此不能满足的 S10 PASS review/completion 条件）正确拒绝；该拒绝就是当前真实终局。
4. 允许把已经验收的 CodexPro durable terminal receipt、取消竞态、诊断、实验源码和测试 commit/push 到 `release/v0.9.0`；不得创建 `v0.9.0` tag、Release、正式安装资产或宣称 Phase B 完成。

### 4.2 发布裁决

当前不得发布一个宣称“ChatGPT 自动开新轮次、无人值守持续推理”的 v0.9.0 正式版本，也不得把实验 Regular Chat Controller 打入正式资产。

允许保留并提交经过验收的独立改进：CodexPro durable task terminal receipt、取消竞态修复、诊断消歧、测试与实验源码。但正式 version tag、Release 和“Phase B 完成”报告必须等 S8/S9 capability gate 真正解除后再执行。

### 4.3 回滚

- durable terminal-receipt 增量可回滚到正式 v0.8.9 CodexPro runtime；回滚不修改项目配置、Gateway、Tunnel、凭据或浏览器 profile。
- Regular Chat 实验能力默认不可达，删除实验目录不会影响正式 MCP 数据面。
- 任何 Tasks extension 未来实现必须 feature/capability gated；协商缺失时继续使用现有工具级 durable fallback。

## 5. 当前终局判定

当前项目已经可靠具备：

```text
短 MCP 工具调用
+ 无固定执行时限的本地后台任务
+ durable plan / checkpoint / evidence / terminal receipt
+ CodexPro/DevBridge 重连后的零猜测恢复
```

当前仍由 ChatGPT host/client 控制、尚未满足：

```text
当前 assistant turn 结束
→ host 自动创建下一轮 assistant turn
→ 模型继续推理并再次调用 MCP
```

所以本轮可完成所有本地可控工程与审查工作，但在平台能力未变化前，S8/S9 与 `long_run_complete` 必须保持真实阻断状态。
