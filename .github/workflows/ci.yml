name: CI

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          python-version: 3.12

      - name: Install dependencies
        run: uv pip install --system -e ".[dev,package]"

      - name: Lint (ruff)
        run: python -m ruff check src tests

      - name: Typecheck (pyright)
        run: python -m pyright src tests

      - name: Unit tests (pytest)
        run: python -m pytest tests/ -q --disable-warnings

      - name: CodexPro engine build (TypeScript)
        working-directory: third_party/codexpro
        run: npm ci && npm run build

      - name: Verify build script syntax
        run: |
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile(
            "scripts/build.ps1", [ref]$null, [ref]$errors) | Out-Null
          if ($errors.Count -gt 0) { $errors | ForEach-Object { Write-Error $_ }; exit 1 }