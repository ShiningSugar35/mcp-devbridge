# 进度验收.md

> 记录当前开发进度与已完成 Phase。开发中必须保持最新；恢复上下文时以此为准。
> 计划与验收标准见 开发计划.md；架构见 项目架构.md；项目引导见 AGENTS.md。

## 最近更新

- 2026-08-17 · **Phase 15 / v0.9.0 发布候选完成 — 双固定域名、多设备显式路由与 Agent Pool**：现场确认朋友电脑重启 Quick Tunnel 后，主 Hub heartbeat 已保存新的 `brandon-*.trycloudflare.com`，并从主机通过该地址完成 MCP initialize + 列出朋友 C/D/E/F 四个运行工作区；旧 `dublin-*` 失败的是直接绑定旧 Quick URL 的 App，而不是配对凭据丢失。共享 Hub 的 `devbridge_list_workspaces / get_current_workspace / switch_workspace` 已增加正式 `device_id`，Agent Pool 工具也支持正式 `device_id`，spawn/spawn_batch 再支持 `project_id`；这些正式参数为一次性无状态路由，不污染后续 session 默认设备/工作区。Agent Pool 已实现 lazy init、单批 64、默认物理并发 4/硬上限 16、Git worktree + `mcp-agent/<id>` 分支隔离、spawn/list/get/wait/cancel/collect/cleanup、重启 interrupted、tracked/untracked diff 收集。执行器同时探测真正可非交互运行的 OpenCode CLI 与 Claude Code CLI：当前正确识别 `D:\Environment\npm\opencode.cmd` 与 `C:\Users\hp\.local\bin\claude.exe`，不再误选 Electron 桌面版；真实 provider 冒烟中 OpenCode provider 返回 `Unexpected server error`，Claude Code 当前账号返回 `Credit balance is too low`，属于外部模型服务/账户状态，Agent Pool 会保留失败并把 provider 输出写入 task.error，不自动跨 provider 重跑写任务。最终质量门：Python **334 passed**、Ruff 全绿、Pyright **0 errors / 0 warnings**；CodexPro `npm run build`、完整 smoke、stress 全绿；PyInstaller + Inno Setup 通过；冻结版 `FINAL_FROZEN_STARTUP_OK`；`live_upgrade.ps1` bundled；detached updater Dry Run `ok=true`。最终安装器 **104,378,692 bytes**，SHA-256 `D8AC193201CB8A87BE803380887766A47567AF094C2E92C0B52D11E19F0D2908`。本机已用最终二进制在线升级：D:\、C:\、AStockMultiAgent 自动恢复；安装版 `tools/list` 52 tools，正式远端 device route、stateless 默认设备、多执行器 capabilities 全部实测通过；仅 **1 个** `MCPDevBridge.exe`，shortcut 正确，安装 EXE / `live_upgrade.ps1` 与最终 staging 逐字节一致。剩余发布动作仅 commit/push + GitHub v0.9.0 Release；朋友的 `jerry.shiningsugar.shop` 独立 Tunnel 需要用户在自己的 Cloudflare 账户创建并取得该 Tunnel 专属 token，DevBridge 不伪造第三方凭据。
- 2026-08-17 · **v0.8.1 多工作区热修**：实机复现 C:\ 项目 CodexPro 已在 8791 READY，但 ChatGPT `devbridge_switch_workspace` 下一调用重新落回 D:\；根因一是 ChatGPT custom app 会在工具调用批次间重建 MCP transport，而 v0.8.0 路由只依赖 `mcp-session-id`；根因二是 D:\:8788 与 C:\:8791 两个 CodexPro 各自维护独立 MCP session 表，直接转发入口项目 session 会触发 `Session not found`。v0.8.1 增加显式 workspace/device 路由提示，并为每个 client-session × workspace/device 独立虚拟化上游 MCP session；同时修复 Gateway 本地命令 arguments、Qt 异步完成回调丢失、盘符根目录空名称，并将同步 `run_command/run_program` 限为默认 10 秒/最多 20 秒，长任务统一后台 `bash + wait_task/get_task` 以降低工具等待导致的 ChatGPT stream timeout 风险。专项 OAuth **58 passed**，项目/桌面 **18 passed**，多项目升级接力 **2 passed**，全量 **320 passed**，Ruff 全绿，Pyright **0 errors / 0 warnings**；真实 D:\:8788 ↔ C:\:8791 双引擎端到端验证 `LIVE_DUAL_WORKSPACE_OK`。最终 PyInstaller + Inno Setup 构建通过，安装器 **104,361,240 bytes**，SHA-256 `00C7E4E8F748ABC8A8A75A44CE20E1F3363A7FB27970DB5D148F4FABB9DFE6B4`，staging 包含 `live_upgrade.ps1`，detached updater Dry Run `ok=true`。0.8.0→0.8.1 detached 真机升级后 `upgrade-result ok=true`，仅 1 个 `MCPDevBridge.exe`，快捷方式正确，安装版 EXE / `live_upgrade.ps1` 与最终 staging 逐字节一致；D:\:8788、C:\:8791 与升级前其它监听项目均自动恢复。通过 `https://mcp.shiningsugar.shop/mcp` 再走一轮临时 OAuth + 同一 client session D:\→C:\ 真实工具调用，得到 `FIXED_DOMAIN_DUAL_WORKSPACE_OK`，临时 OAuth 记录已清理。GitHub **v0.8.1 Release 已正式发布**（非 Draft / 非 Prerelease），资产 `MCPDevBridge-Setup-0.8.1.exe` 服务端大小 **104,361,240 bytes**、digest `sha256:00c7e4e8f748abc8a8a75a44ce20e1f3363a7fb27970db5d148f4fabb9dfe6b4`，与本地最终安装器完全一致。
- 2026-08-17 · **Phase 14 — v0.8.0 发布前验收进行中**：
  - 配对注册成功 receipt 从 90 秒延长为 **1800 秒**；同一 `pair_code + device_id` 在窗口内幂等返回原凭据。配对后的设备目录、Hub 地址与心跳凭据继续持久化，新回归覆盖 registry 重建后无需再次配对、可直接恢复心跳。
  - 客户端更新改为启动后检查一次、之后每 **12 小时**检查；README/使用手册/架构/CI 已同步。
  - `wait_task` 为减少 ChatGPT Web/MCP 长同步等待导致的 message delivery timeout，改为默认 **15 秒**、单次硬上限 **30 秒**；后台命令自身继续无固定执行时长上限。
  - 正式安装包改为内置私有 Node.js 22.19.0、uv/uvx 0.11.25、cloudflared 2026.7.3；不静默全局安装、不修改 PATH。构建脚本会准备并校验固定运行时，启动/诊断会检查组件完整性；Windows-MCP 仍由内置 uvx 按锁定 0.8.2 首次获取。
  - 当前代码验证：全量 **311 passed**；Ruff 全绿；Pyright **0 errors / 0 warnings**；CodexPro TypeScript build 通过；运行时准备脚本真机通过。
  - v0.8.0 PyInstaller + Inno Setup 已按最终 15/30 秒轮询策略重新构建，staging 强制验证 `_internal/runtime/node.exe`、`uv.exe`、`uvx.exe`、`_internal/scripts/live_upgrade.ps1` 与 `cloudflared.exe` 均存在；冻结版启动/单实例冒烟通过。最终安装器 `MCPDevBridge-Setup-0.8.0.exe` 为 104,348,634 bytes，SHA-256 `8CB74D274B69ABC7903E381492AD6D60940506BF5205A431B594C061D4257B39`。
  - detached updater Dry Run 已由计划任务真实启动 worker 并写出 `ok=true`；最终 PowerShell parser、`uv lock --check`、`git diff --check`、Ruff、Pyright 均再次通过；CodexPro 完整 smoke/stress 也全绿。旧 GitHub `v0.8.0` Release 资产因最终 15/30 秒改动已过期，发布收尾将以本轮 104,348,634 bytes / `8CB74D...7B39` 安装器覆盖。剩余：提交/推送与 Release 同步、0.7.2→0.8.0 在线升级、快捷方式/`D:\` 服务恢复及 ChatGPT QuickTunnel→固定域名切换。
- 2026-08-16 · **Phase 13 — v0.7.2 默认异步命令任务**：
  - `bash` 默认任务化、公开 schema 无 `timeout_ms`、删除 `start_task`；保留 `get_task / wait_task / list_tasks / cancel_task`，任务无固定执行时长上限。
  - v0.7.1 发布后真机验收发现 TaskManager 挂在单个 MCP Server/session，导致下一次工具调用可能找不到 task_id；v0.7.2 已提升为 CodexPro 进程级共享、查询仍按 workspace 隔离。
  - 新增跨 HTTP MCP session 回归：session A 启动并关闭后，session B 能成功 wait_task 同一 task_id。
  - 新增 600 秒编排看门狗：无任务观察超过阈值时下一次快照标记 STALE 并提示接续；只结束无意义等待，不终止后台任务；单次 wait_task 仍严格不超过 60 秒。
  - watchdog 合入后完整复验：TypeScript build、完整 `npm run smoke`、`npm run stress`、304 pytest、Ruff、Pyright、lock/diff check 全绿。最终 v0.7.2 staging/冻结版/安装器/Dry Run 全部通过；安装器 66,977,760 bytes，SHA-256 `09f6e87f699fdc806404a961846cf78d1ece1fbdcaff80d3a6b4a1243c577510`。功能提交 `16d79b0` 已 push；GitHub `v0.7.2` Release 正式发布且线上资产 digest/size 与本地一致。正式安装版 PID 30252，桌面快捷方式指向 `%LOCALAPPDATA%\Programs\MCP DevBridge\MCPDevBridge.exe`，`D:\` 自动恢复，8786/8788 监听正常；独立 `bash → wait_task` 跨 MCP 调用真机通过，安装版 `server.js` 无 `timeout_ms`，运行时快照含 `orchestrationStaleAfterMs=600000`。
- 2026-08-13 · **Phase 12 — v0.7 Multi-Device Hub + 新手可用性**：
  - 已确认 Quick Tunnel 当前行为与 Cloudflare 官方定义一致：随机 `trycloudflare.com` 地址、无需账号、每次重建会换 URL；v0.7 将在多设备配对后自动向 Hub 更新远端 Quick URL。
  - 已定位日志两个真实缺陷：进程页错误读取 `ProcessLog` 且绑死入口项目；审计页读取 Python backend 日志，但桌面正式链路实际经 Gateway→CodexPro，导致无记录。
  - 设备目录、一次性配对/15 秒心跳、会话级设备路由、单在线设备自动选择、远端 Quick URL 自动更新、Gateway 真实审计均已实现；Multi-Device 集成测试 6 项通过。
  - 工作台删除重复自测与组件小字；诊断改为结论/问题/下一步；日志改为运行情况/操作记录/网络连接；使用手册、搜索、连接选择助手与 5 类上下文 `?` 帮助已落地。
  - 当前验证：全量 **304 passed**；Ruff 全绿；Pyright **0 errors / 0 warnings**；所有 7 个顶层页面在 900×650 / 1200×850 / 1920×1080 无横向溢出。
  - 构建：PyInstaller `dist/staging-0.7.0/MCPDevBridge` 完整生成并含 `cloudflared.exe`；冻结 EXE 启动冒烟通过；Inno Setup 安装器 `MCPDevBridge-Setup-0.7.0.exe` 完整生成（66,971,613 bytes，SHA-256 `47cd083eb99b26cb4907aa27fea863e0c1102596ca52e4ecea74648cc4cc43e1`）；detached updater Dry Run 通过。
  - 发布/接力：`fc610f2` 已 push 至 `origin/master`；GitHub Release `v0.7.0` 已发布，线上安装器与本地最终文件一致（66,971,613 bytes，SHA-256 `47cd083eb99b26cb4907aa27fea863e0c1102596ca52e4ecea74648cc4cc43e1`）。detached updater 真机升级成功，最终安装版 PID 14656，桌面快捷方式指向 `%LOCALAPPDATA%\Programs\MCP DevBridge\MCPDevBridge.exe`，`D:\` 自动恢复，Gateway 8786 / Codex 8788 就绪。安装后直接调用 `devbridge_list_devices` 成功返回本机在线，并确认单在线设备自动选择逻辑在正式安装版生效。
- 2026-08-11 · **Phase 11 — v0.6.0 桌面产品 UX 重构**：
  - 修复 0.5.0 全局 busy 误置灰：运行项目“停止服务”可用，未运行项目仍可独立启动、选择和编辑；启停过渡只锁目标项目。
  - 顶层导航收敛为工作台 / 项目设置 / 诊断 / 日志 / 设置；三类日志合并为日志二级页；常驻技术小字大幅精简。
  - 新增托盘交互：标题栏“—”仍进任务栏；“×”默认隐藏到托盘；托盘可恢复/退出；设置可改为关闭即退出。
  - 新用户隔离配置验证：项目列表为空，路径/域名/Git/Gemini 等不带开发机数据；连接信息在未选项目时显示“选择项目后显示”；既有通用端口架构不改，不引入 8789。
  - 验证：全量 **294 passed**；Ruff 全绿；Pyright **0 errors / 0 warnings**；Qt offscreen 跨项目启停/托盘行为与 900×650、1200×850、1920×1080 三档布局通过。
  - 构建：PyInstaller `dist/staging-0.6.0/MCPDevBridge` 与 Inno Setup `release/MCPDevBridge-Setup-0.6.0.exe` 已生成，`cloudflared.exe` 已随 staging 打包，冻结版 EXE 启动冒烟通过；detached updater Dry Run 通过。
  - 发布/接力：`0dab72a` 已 push 至 `origin/master`；GitHub Release `v0.6.0` 已发布并用完整安装器资产覆盖校验（66,939,551 bytes，SHA-256 `d18e9e7210208bcf5aabb2833127a3eacf2f3285dba027427825746127a50a10`）。首次切换发现构建命令超时留下未完整安装器后，staging fallback 成功自动救回会话；重新完整编译并真机静默安装 exit=0 后再次接力成功。最终安装版 PID 26240，桌面快捷方式指向 `%LOCALAPPDATA%\Programs\MCP DevBridge\MCPDevBridge.exe`，`D:\` 公网入口自动恢复，Gateway 8786 / Codex 8788 均就绪。
- 2026-08-10 · **Phase 10 — v0.5.0 桌面交互/项目级配置/在线升级**：
  - 项目表移除“启用”列，状态 1 秒刷新；项目行与服务控制统一动态单按钮；删除 UI 独立停止/重启按钮。
  - 权限/客户端/连接方式禁止滚轮误切换；默认权限明确为 `system + full_system`，首次风险确认保留；Gemini OAuth 仅在 Gemini 客户端模式显示。
  - 每项目独立保存 UI 配置、Gateway/Codex/Windows 端口、Bearer、Cloudflare Token、Git 与 Gemini URI；项目级敏感值进入 SecretsStore。
  - Cloudflare/ngrok/Quick/Local 四种连接全部恢复；公网 Tunnel 统一终止在 Gateway，Local 无 cloudflared 依赖；Quick/ngrok URL 带 `/mcp`。
  - Gateway 支持项目 Bearer/OAuth workspace 路由与目标项目上游凭据；Gemini consent 空 workspace=400、未运行=409。
  - 新增组件状态、一键诊断、项目级真实 self-test、异步窗口关闭和 `upgrade-resume.json` 接力。
  - 验证：全量 **289 passed**；Ruff 全绿；Pyright **0 errors / 0 warnings**；Qt offscreen 项目切换/显隐/配置恢复/升级接力冒烟通过。
  - 构建：v0.5.0 改用 `dist/staging-0.5.0`，在旧版 EXE 正运行锁住历史 dist 的情况下成功完成 PyInstaller + Inno Setup；冻结 EXE 独立启动冒烟通过；安装器 `release/MCPDevBridge-Setup-0.5.0.exe` 已生成。
  - 发布/接力：`5a8d6b6` 已 push 至 `origin/master`；GitHub Release `v0.5.0` 已发布；detached updater 真机接力成功，新版安装进程 PID 22800，桌面快捷方式已替换并指向安装版，`D:\` 公网入口自动恢复，Gateway 8786 验证就绪。
- 2026-08-09 · **Phase 9 — 多项目并行开发 + Windows Shell 执行修复**：
  - **核心修复**：`WorkspaceCatalog.session_key()` 中 `isinstance(headers, dict)` 对 Starlette `Headers`
    （`MutableMapping`，非 `dict`）返回 False，导致所有 MCP 会话落入同一个 `DEFAULT_BUCKET`，
    workspace 切换会泄露到其他 session。修复为 duck-typing 迭代（尝试 `.items()`），使
    `mcp-session-id` 正确区分 GPT / Gemini 等不同客户端会话。
  - **多项目并行（已完成代码）**：
    - `project_manager.py`：`ProjectManager`（catalog + 单位生命周期）、`ProjectUnit`
      （每项目一 CodexProManager + WindowsBridgeManager，独立 root/port）、`ProjectView`（只读快照）。
    - `projects.json` 持久化（`config_store` 管理） + 项目列表 CRUD + 端口自动分配 + `enabled`
      标记自动恢复。
    - `desktop_main.py`：多项目表格（名称/路径/状态/端口/启用/入口），添加/删除/启动引擎/停止引擎
      按钮，每个项目独立 port；启用标记持久化，桌面启动后台自动恢复。
    - `tools.py`：`WorkspaceCatalog` 实现 per-session 项目绑定；`list_projects` / `switch_workspace`
      工具（切换仅影响当前 session，不影响其他）；`shell_info` 工具返回 shell 检测信息。
  - **Windows Shell 检测（已完成）**：
    - `shell.py`：`ShellInfo`（name/path/kind/is_wsl/version）、`detect_shells()`、
      `default_shell()`（pwsh > Windows PowerShell > cmd > Git Bash；WSL 从不自动选择）、
      `get_shell_info()`（JSON 报告）。
    - `execution_profile.py`：safe / developer（默认）/ full_system 三档 + 危险命令硬拦截。
  - **测试覆盖**：
    - `test_project_manager.py`：10 项（add/ports/parallel/auto-restore/remove/real-engines）
    - `test_workspace_switch.py`：8 项（session key parsing / default / switch isolation /
      unknown id keeps binding / list_projects / shell_info shape / WSL not default）
    - `test_mcp_integration.py`：1 项（`test_two_sessions_isolated_workspace_switch`，真实后端）
    - `test_execution_profile.py`：26 项（safe/developer/full_system / 危险命令 / config 贯通）
    - `test_commands.py`：11 项（PowerShell / 超时杀树 / 截断 / 程序运行）
    - `test_shell_info_shape`：验证至少检测到一种 shell，WSL 不会被选为默认
    - 全量 **258 全绿**；ruff/pyright 0 错误；双引擎真机并行 spawn 验证通过；offscreen UI 冒烟通过。
  - **重建**：PyInstaller onedir `dist\MCPDevBridge\MCPDevBridge.exe` 构建成功。
- 2026-08-09 · **Phase UI responsive layout completed（桌面 UI 响应式重构 + 权限模式合一）**：
  - **布局**：主窗口改为 QMainWindow → QScrollArea（垂直 AsNeeded / 水平 AlwaysOff）→ 内容 QWidget；
    默认 1200×850、最小 900×650；1366×768/1920×1080/900×650 均验证：需要时出滚动条、无横向滚动、无内容丢失。
  - **服务配置区**：Token / MCP 地址 / Cloudflare Service URL / Gemini Client ID·Secret 全部改为
    只读 QLineEdit（可选中、长文本内部滚动）；提示文字自动换行，不再重叠。
  - **最近消息**：QTableWidget → QPlainTextEdit（默认 ≥150px、随窗口扩展、自动滚动到底、上限 300 行、NoWrap+横滚）。
  - **按钮区**：启动/停止/重启 靠左，高级设置 靠右（stretch 隔离），按钮最小宽度策略，窗口缩小时不被挤压。
  - **字体与间距**：全局字号 10pt，标题 14px/600、正文 12px；控件间距 8px、GroupBox 内边距 12px。
  - **权限模式合一（GUI）**：只读=read_only+safe、默认=workspace+developer、完全访问=system+full_system；
    移除独立「命令执行档位」下拉；系统风险确认与 full_system 确认合并为一次（兼容旧配置用户）。
  - **约束**：仅改 `desktop_main.py`（UI 层）；后端逻辑、MCP 工具、服务流程、配置结构均未变。
  - **验证**：离线 GUI 冒烟（4 种分辨率滚动行为 + 档位映射）；全量 **239 全绿**；ruff/pyright 0 错误。
- 2026-08-09 · **Shell 执行档位重构 + 环境自检（safe/developer/full_system）**：
  - 新模块 `execution_profile.py`：`check_execution(command, profile)` 纯策略；默认档位 developer
    （命令 → 开发工具白名单 pytest/pyright/ruff/mypy/git 全量/npm/uv/python/tsc/…）；
    safe = 原项目内命令行为；full_system = 任意命令 + 一次性确认
    （桌面首次启用确认框存 `AppConfig.full_system_risk_accepted`；CLI `--confirm-full-system`）。
  - **危险命令全档位硬拦截**：format/diskpart/shutdown/reboot/bcdedit/reg delete/msiexec/cipher/
    takeown/icacls…，`rm -rf /`、`del /s C:`、`Remove-Item -Recurse C:\Windows`、`rd /s` 盘根等；
    保守设计（宁可拦截也不删盘）。
  - `shell.py`：`ShellInfo / detect_shells / default_shell / get_shell_info`；默认顺序
    pwsh > Windows PowerShell > cmd > Git Bash；**WSL Bash 永不自动选**。
  - `tools.py`：34 个工具；新增 `shell_self_test`（Shell + python/git/pytest/pyright 探测，
    pytest/pyright 走 `python -m` 规避 uv trampoline）；run_command/run_program/start_process 均过档位。
  - CodexPro fork：`CODEXPRO_BASH_MODE` 新增 **developer**（bashOps.ts 首词白名单 + 危险拦截；
    safe/off/full 行为不变）；`build_codex_env` workspace+developer → developer。
  - 桌面：新增「命令执行档位」下拉（与权限模式正交）+「开发环境检测」按钮。
  - 测试：`test_execution_profile.py` 26 项；全量 **239 全绿**（运行中实例占用 8787 时 13 项
    app_state 失败，停实例后全绿）；ruff/pyright 0 错误；fork `npm run build` 通过；`build.ps1` 全链通过
    （PyInstaller onedir + ISCC 安装包已产出）。
  - **待用户**：重启桌面 → 建议默认 developer 档 → 选择项目 → 启动 → GPT 侧可执行
    `pytest` / `pyright` / `ruff` / `git` 命令；可点「开发环境检测」确认工具链。
- 2026-08-06 · 建立四份项目文档（AGENTS/项目架构/开发计划/进度验收）；Phase 1、2 完成；Phase 3 开始。
- 2026-08-06 · **tools.py 损坏恢复进行中**（详见下节）：v9 修复脚本将 U+FFFD 从 153 降至 5，行数不再丢失（1007 vs 1004），但 compile 仍未通过（line 155 f-string 未闭合 + 若干行常量重复）；恢复未完成，tools.py 仍为损坏源文件。
- 2026-08-07 · **tools.py 恢复完成**（见 问题记录.md）；引擎层落盘：`engines.py`（CodeyPro/Windows 桥进程管理）、`tunnel_manager.py`（Cloudflare/ngrok/Quick/Local）、`app_state.py`（协调状态机：启停顺序、URL 一次性固定、失败清理）；`desktop_main.py`（PySide6 单窗口，后端 + 令牌 + 自测 + 日志，QThreadPool 防卡 UI）。
- 2026-08-08 · **新增三套单元测试**（`test_engines.py`/`test_tunnel_manager.py`/`test_app_state.py`），全量测试 **60 → 124 全绿**；修复两处真实缺陷：`tunnel_manager.py` 漏导入 `SpawnError`（缺二进制时 NameError）、`redact_line` 未遮蔽 `--auth-key/--token/--secret` 命令行值。
- 2026-08-08 · **Phase 3 收尾**：`desktop_main.py` 已切换到 `ServiceCoordinator`（CodexPro + Windows 桥 + 隧道一体化启停）；连接方式下拉（仅本机/Cloudflare 固定/ngrok 固定/Quick）、域名与隧道令牌输入、Windows 桥自生成令牌、固定/临时 URL 标识、Quick Tunnel 与 system 权限风险确认；QThreadPool 防卡 UI；`PySide6` 桌面冒烟通过。
- 2026-08-08 · **Phase 4 代码完成**：`server_factory.build_transport_security()`（DNS rebinding 方案 B：防护保持开启，仅把公网域名加入 `allowed_hosts`）；`RuntimeConfig.public_hostname` 贯通（含 `standalone --public-hostname`）；新增 `test_transport_security.py` 12 项，全量一度 **136 全绿**。
- 2026-08-08 · **Phase 4 真机准备就绪**：用户已在 Cloudflare 仪表盘创建路由 `mcp.shiningsugar.shop` →（CNAME `0079be0a-…cfargotunnel.com`）→ Service URL `http://localhost:8787`（与 `CODEXPRO_LOCAL_PORT` 一致），DNS 解析已生效（经 CF 边缘加 A/AAAA 应答）；`.tools/cloudflared.exe 2026.7.3` 就绪。
- 2026-08-08 · **修复令牌模式命令**：`TunnelManager.start()` 的 Cloudflare 令牌分支原为 `cloudflared tunnel --url …`（Quick Tunnel 语法，不会走已建路由）→ 改为 `cloudflared tunnel run --token <token> --no-autoupdate`（远端托管命名隧道，2026.7.3 参数已确认）；`ConnectionMethod` 改 `StrEnum`；新增命令注入断言测试，全量 **137 全绿**。
- 2026-08-08 · **Phase 4 真机验收通过 ✅**：桌面（D:\AStockMultiAgent + Cloudflare 固定地址 + 令牌）启动成功（已连接）；对 `https://mcp.shiningsugar.shop/mcp` 无 Bearer 请求返回 **HTTP 401**（此前 502 仅边缘层通）→ 全链路（CF 边缘 → 隧道 → localhost:8787 引擎 → 认证拦截）验证完成。`--no-autoupdate` 子命令解析问题修复后生效。
- 2026-08-08 · **Phase 5 完成（桌面侧）**：`ProjectConfig` 扩展 `git_user_name / git_user_email / default_push_remote / default_push_branch`（可空，旧 JSON 向后兼容）；`models.git_field_error()` 中文校验（空格/引号/控制符/元字符拒绝，邮箱格式）；桌面新增「Git 参数」区 + 保存按钮，保存与启动时校验拦截；新增 8 项测试。
- 2026-08-08 · **Phase 6 完成**：`audit.py` 新增查询接口 `query_logs / AuditQuery / available_tool_names`（按日期/工具/成败/条数，跳过损坏行与无时间戳记录）；桌面改三 Tab（控制 / 进程日志 / 审计日志）：进程日志按进程（Codex/桥/隧道）追读旧尾 400 行，审计日志按日期范围/工具/成败筛选展示（参数摘要仍为写时脱敏）；顺手修复**真实缺陷**：`redact_params` 敏感标记匹配用大写标记对小写键名，`api_key` 等从未被遮蔽 → 改为 `key.upper()` 匹配（新增回归测试断言）。测试 9 项，全量 **154 全绿**。
- 2026-08-08 · **排查「隧道建立失败」**：tunnel.log 显示 cloudflared 打印帮助而非连接错误 → 定位 `--no-autoupdate` 置于 `tunnel run` 子命令后解析失败（2026.7.3 实测：`tunnel run --token X --no-autoupdate` 直接打 help 退出；`tunnel run --token X` 正常，仅报「Provided Tunnel token is not valid」验真路径）。已从令牌分支移除该标志（Quick 分支 `tunnel --url … --no-autoupdate` 实测正常保留），测试 14 项绿。**待用户桌面重试**。

- 2026-08-08 · **Phase 8 实现完成**：新增 `oauth_provider.py`（本地 OAuth Provider：单用户、无注册表）与
  `gateway.py`（uvicorn 网关 127.0.0.1:8786：SDK OAuth 路由 + 浏览器同意页 + `/mcp` 反向代理）；
  OAuth 2.1 发现/动态注册/PKCE S256/授权码/刷新轮换/撤销/RFC 8707 resource 绑定/`ACCESS_VIEW_MANAGE_MCP_CONTENT`
  单 scope；网关把 OAuth 校验后的请求换成引擎 Bearer 转发，旧版 ChatGPT Bearer 直通；本机匿名放行仅限回环；
  接入 `ServiceCoordinator`（仅公网模式启动，配置目录无明文令牌落盘，secrets 同源）；
  `httpx>=0.28` 加入运行时依赖；新增 `test_oauth.py` 27 项，全量 **181 全绿**。
  待办：Cloudflare 路由 Service URL 8787→8786（用户操作）+ Gemini 真机接入。

---

## 总体状态

<!-- [status] 三者取其一：进行中 / 已完成 / 阻塞 -->
**当前 Phase：Phase 14（v0.8.0 发布前最终验收进行中）**

- Phase 0：✅ 完成
- Phase 1：✅ 完成（后端核心 + 33 工具，测试全绿）
- Phase 2：✅ 完成（HTTP + 认证 + 子进程，测试全绿）
- Phase 3：✅ 完成（desktop_main.py 已接线 ServiceCoordinator，桌面冒烟通过）
- Phase 4：✅ 真机验收通过（`https://mcp.shiningsugar.shop/mcp` 无 Bearer 返回 401，全链路验证）
- Phase 5：✅ 完成（Git 桌面参数 + 校验，全量 145 全绿）
- Phase 6：✅ 完成（三 Tab 日志页 + 轮转 + 脱敏修复，全量 154 全绿）
- Phase 7：✅ 完成（安装器已编译并静默安装、安装版启动冒烟通过；剩「选项目→启动→自测」GUI 闭环待用户操作）
- Phase 8：✅ OAuth 实现完成（27 项测试，全量 181 全绿；待路由切换 + Gemini 真机）
- Phase 9：✅ 多项目并行 + Windows Shell 修复
- Phase 10：✅ 全部完成（289 passed；Ruff/Pyright 0；0.5.0 安装包与 GitHub Release 已发布；本机接力、快捷方式替换、`D:\` 服务自动恢复均验收通过）
- Phase 11：✅ 全部完成（294 passed；Ruff/Pyright 0；0.6.0 安装包与 GitHub Release 已发布；托盘、跨项目交互、本机接力、快捷方式替换、`D:\` 服务自动恢复均验收通过）
- Phase 12：✅ 全部完成（304 passed；Ruff/Pyright 0；Multi-Device Hub、Quick URL 心跳更新、真实审计/进程日志、自然语言诊断/日志、使用手册/帮助浮层、0.7.0 Release 与本机接力均验收通过）
- Phase 13：✅ 全部完成（默认异步无 timeout、进程级共享 TaskManager、跨 session task lookup、wait_task≤60s、600s 非破坏性 orchestration watchdog；完整 smoke/stress + 304 pytest + Ruff/Pyright + v0.7.2 Release/安装/接力真机全通过）
- Phase 14：🚧 发布前最终门（单域名 Hub/session 路由、1800 秒配对幂等、重启免配对、单实例、12 小时更新检查、私有 Node/uv/cloudflared 运行时、内置升级均已实现；311 pytest / Ruff / Pyright / v0.8.0 构建 / SHA-256 / detached Dry Run 已通过，剩 GitHub 发布与本机 0.7.2→0.8.0 接力验收）

---

## Phase 1 验收清单（✅ 已达成）

- [x] 33 个 MCP 工具实现并注册。
- [x] 路径边界（workspace 内读，禁止逃逸，排除敏感目录/文件）。
- [x] 原子写 / replace_text expected_count / patch 全量校验（支持无文件头 hunk）。
- [x] 编码容错（UTF-8/BOM/GB18030）。
- [x] 命令执行（powershell.exe）、超时进程树终止、输出截断。
- [x] 受管进程（启动/增量轮询/树停止）。
- [x] 权限三模式 + destructive 工具标注。
- [x] 工具注解准确（read_only/destructive/idempotent）。

测试：`test_fs.py`(27)、`test_commands.py`(11)、`test_git.py`(3)、`test_config.py`(8) 全绿。

## Phase 2 验收记录——✅ 已交付

- [x] `streamable_http_app` /mcp + /health + /control/status + /control/stop-procs。
- [x] 认证（本机匿名、/control 强制 Bearer、公网必须 Bearer、constant-time 比较）。
- [x] 限速（10 次失败锁 5 分钟 429）。
- [x] 纯 ASGI 认证中间件（不使用 BaseHTTPMiddleware）。
- [x] server_main CLI（--config/--port、端口占用检测、令牌自举）。
- [x] 审计 JSONL + 参数脱敏。
- [x] 集成测试走真实后端子进程（in-process uvicorn SSE 不兼容，已废弃）。
- [x] selftest.py 自测链路。
- [x] 集成 + 自测测试（test_mcp_integration.py 9 项、test_selftest.py 2 项）全绿。

## 测试汇总

```
$env:PYTHONIOENCODING="utf-8"; .venv\Scripts\python.exe -m pytest tests/ -q
============================= 124 passed in ~44s =============================
| 文件                          | 数量 | 覆盖范围                              |
|-------------------------------|------|--------------------------------------|
| tests/test_fs.py              | 27   | 文件操作/路径边界/编码/diff/排除      |
| tests/test_commands.py        | 11   | PowerShell/超时杀树/截断/程序直接运行 |
| tests/test_git.py             |  3   | status/commit/branch/diff/restore    |
| tests/test_config.py          |  8   | 配置回环/损坏容错/DPAPI 密文验证      |
| tests/test_mcp_integration.py |  9   | 真实子进程 MCP、认证、限速            |
| tests/test_selftest.py        |  2   | selftest 成功/失败路径                |
| tests/test_engines.py         | 23   | 脱敏/命令构建/日志/端口探测/启动校验  |
| tests/test_tunnel_manager.py  | 13   | URL 解析/连接方式/缺可执行文件报错    |
| tests/test_app_state.py       | 22   | 状态机/启停顺序/URL 固定性/失败清理   |
| tests/test_oauth.py           | 27   | OAuth 2.1 发现/注册/授权码/PKCE/刷新/撤销/网关代理/无密文落盘 |

## 引擎层（Phase 3/4 地基，纯逻辑无 Qt，已单测）

- `engines.py`：`CodexProManager` / `WindowsBridgeManager` —— 令牌长度校验先于 spawn；
  日志行 + 命令展示双重脱敏（新增 `--auth-key/--token/--secret <值>` 遮罩）。
- `tunnel_manager.py`：Cloudflare Named / Quick / ngrok / 仅本机；Quick URL 正则解析；
  固定域名以「连接确认」为就绪信号；缺可执行文件/凭据 → `SpawnError`。
- `app_state.py`：`ServiceCoordinator` 状态机 IDLE→STARTING→READY→ERROR；
  校验失败不动状态；隧道/桥失败自动清理；停止顺序 Codex→Windows→隧道；
  固定 URL 会话内一次性确定、重启不变（Quick Tunnel 例外标注 `url_mutable`）。
- 桌面端「启动服务」按钮后续由 `BackendManager` 迁移到 `ServiceCoordinator`。

---

## 已知问题 / 待办（重要，恢复上下文先看）

| # | 问题 | 影响 | 处理 |
|---|------|------|------|
| 1 | ~~**默认端口不一致**~~ **已解决（2026-08-09）**：端口默认值集中 `constants.DEFAULT_*_PORT`（Gateway 8786 / CodexPro 8787 / Windows-MCP 28731 / Legacy 8765）；废弃 2865 与 `local_port`（`RuntimeConfig.local_port` → 兼容属性）；旧配置自动迁移；桌面 Gateway 端口 UI +「高级设置…」内部端口 + 启动前占用预检 | — | 见 tests/test_port_config.py 16 项 |
| 2 | **DNS rebinding 防护**：`streamable_http_app()` 自动只放行 localhost Host；Named Tunnel 公网 Host 会被拒 | Phase 4 验收阻塞点 | Phase 4 加 `transport_security` 显式配置 + 真机验证 |
| 3 | `standalone_server.py` 为 CLI 简化入口，非桌面 | 文档/入口混乱 | `localdev-mcp-bridge` 脚本入口指向 `desktop_main.py`（已接通） |
| 4 | ~~desktop_main 未接线引擎层~~ **已解决**：桌面已切 `ServiceCoordinator`（连接方式/域名/Quick 警告/URL 固定标识） | — | 冒烟通过；真机自测在工作区存在 codexpro 构建产物后进行 |
| 5 | in-process uvicorn + MCP SSE 流不兼容 | 测试设计约束 | 集成测试一律走真实子进程（已固化） |
| 6 | Windows 无 pwsh，仅 powershell.exe | 命令族只能走 Windows PowerShell 5.1 | 已固化 `find_powershell` |

---

## ✅ tools.py 损坏与恢复（已解决，2026-08-07 收尾）

**现象**：`src/local_dev_mcp_bridge/tools.py` 曾被 PowerShell GBK 编码错误解释，153 处 U+FFFD、损坏约 112 行；SyntaxError 无法编译，全量测试被阻断。

**恢复方案（已验证有效）**：从 `__pycache__/tools.cpython-312.pyc` 提取损坏前编译的字符串常量（`C:\Users\hp\AppData\Local\Temp\opencode\const_by_line.json`：pyc 行→常量），按「pyc 函数 firstlineno ↔ 当前 def 行」以 name+出现顺序配对得行偏移，再用常量逐行重建损坏内容。

**损坏模式**：`\ufffd?` 替换「1 个中文字 + 1 个 ASCII 标点/空格」，字符串闭合引号常被吞。

**修复脚本迭代**（均在 Temp\opencode\，可复用）：
- v2 `fix_tools.py` 29 → v3 `fix_tools3.py` 32 → v4 9/103（弃）→ v6 **基线 71/41，行数保持** → v7 57/55 → v8 111/1 但行合并 bug（弃）→ **v9 `fix_tools9.py` 111/1，行数保持，153→9** → v9.1 → 修复 5 处残留（f-string 未闭合、常量重复、`"未应用任何修改。"` 补全）→ 替换 `tools.py`，`compile()` 通过，全量测试全绿。

**当前**：tools.py 正常，33 工具 + 集成测试全绿（如上表 124 项）。pyc 与 const_by_line.json 保留备查。

---

## 下一步（按优先级）

1. **Phase 7 收尾**：
   - PyInstaller onedir 构建已成功（`dist\LocalDevMCPBridge\`，启动冒烟通过，cloudflared 已捆绑）；
   - Inno Setup 脚本 `scripts/installer.iss` 待 ISCC 实际编译（需安装 Inno Setup 6）；
   - 发行说明 `release\` 目录待生成（首次编译后自动产出安装器）。
2. 冒烟跟进：真机 work 后补跑「启动 → 自测 → 停止 → 重启」闭环。

## 变更记录

- 2026-08-06 · 创建本文件；记录 Phase 0/1/2 完成、Phase 3 开始。
- 2026-08-06 · 记录 tools.py 损坏恢复（v9 进展：FFFD 153→2、行数保持，compile 未通过）。
- 2026-08-07 · tools.py 恢复完成；落地 engines.py / tunnel_manager.py / app_state.py / desktop_main.py。
- 2026-08-08 · 新增 test_engines / test_tunnel_manager / test_app_state（64 项），全量 124 全绿；
   修复 tunnel_manager 漏导入 SpawnError、redact_line 缺 CLI 标志遮罩；本节文档同步更新。
- 2026-08-08 · Phase 3 收尾：desktop_main 切 ServiceCoordinator（连接方式/URL 固定标识/令牌键），桌面冒烟通过。
- 2026-08-08 · **Phase 5 完成**：Git 桌面参数（4 字段 + `models.git_field_error` 双路径校验），全量 145 全绿。
- 2026-08-08 · **Phase 6 完成**：三 Tab 日志页 + 自动轮转 + `key.upper()` 脱敏修复（`api_key` 等实修），全量 154 全绿。
- 2026-08-08 · **Phase 7 进行中**：PyInstaller onedir 构建成功（spec ROOT 修正 `SPECPATH.parent`）、启动冒烟通过、
    cloudflared.exe 捆绑；`scripts/build.ps1` + `scripts/installer.iss` + `docs/en/*` 五份英文文档就绪；
    待装 Inno Setup 6 编译安装器并实测安装冒烟。
- 2026-08-08 · **Phase 7 完成**：安装器编译+静默安装+安装版启动冒烟通过（详见 AGENTS.md 变更记录）。
- 2026-08-08 · **安装版修复 + 令牌记忆**：`CodexPro 构建产物缺失` → `_resolve_dist_dir()` 候选链 + spec 捆绑
    codexpro dist（安装版已验证存在）；隧道令牌留空自动用上次值、新值输入即加密保存并下次预填；全量 154 全绿，
    重建+重装冒烟通过。
- 2026-08-08 · **Phase 8 完成**：oauth_provider.py + gateway.py（SDK OAuth 组件 + 同意页 + /mcp 代理），
    接入 ServiceCoordinator；httpx 运行时依赖；test_oauth.py 27 项；全量 181 全绿；待路由切换 + Gemini 真机。
- 2026-08-08 · **Phase 8 真机验收通过 ✅**：CF 路由已切 8786；重建 PyInstaller（含 gateway/oauth_provider）+
    重装后修复冻结 `unable to configure formatter 'default'`（`uvicorn.Config(log_config=None)`）；
    公网 502 消除：匿名 401（resource_metadata）、带令牌 initialize 200（CodexPro 0.29.0）。
    待用户：Gemini 自定义 MCP 走 OAuth 授权流程。
- 2026-08-08 · **Gemini 静态 OAuth Client**：预注册 confidential client 路径（DCR 保留）：
    `get_or_create_gemini_client()` + `gemini_oauth` CLI + 桌面「Gemini OAuth 凭据」对话框；
    secret 仅存 SecretsStore/掩码显示；`test_gemini_oauth.py` 11 项，全量 **192 全绿**；
    重建+重装完成，待用户粘贴凭据真机验证。
## Phase 8 — OAuth（标准 MCP OAuth，不动 Bearer）

**状态**：✅ **实现完成（2026-08-08）**。新增 `oauth_provider.py` + `gateway.py`，接入
`ServiceCoordinator`（仅公网模式），`httpx` 加入依赖，`test_oauth.py` 27 项全绿，全量 **181 全绿**。

**调研结论摘要**
- 公网链路：Cloudflare Named Tunnel → localhost:8787 = CodexPro(Node) 引擎；Bearer = 引擎的 CODEXPRO_HTTP_TOKEN（桌面从 SecretsStore 同源读取）。Python server_factory.AuthMiddleware 目前**不在公网路径上**。
- MCP SDK 2.0.0 自带完整 OAuth 组件：mcp.server.auth.routes（/.well-known/oauth-authorization-server、/authorize、/token、/register、/revoke + protected-resource 元数据/路由）、create_auth_routes、OAuthAuthorizationServerProvider（需实现 register_client/authorize/load_authorization_code/exchange/refresh/load_access_token/revoke）、TokenHandler 自带 PKCE(S256)+code_challenge 校验、redirect_uri 一致性校验。==> 结论：**用 SDK 组件，不自己发明协议**。
- Gemini Spark（定制 app）兼容要点（2026-08-06 最新论坛/PR 情报）：要求 OAuth 2.1 发现（Authorization Server Metadata + Protected Resource Metadata）、Dynamic Client Registration、PKCE S256、state 回传、精确 redirect_uri、access token 短期限；**关键 scope：必须声明并接受 ACCESS_VIEW_MANAGE_MCP_CONTENT，绑定到 code，经 refresh 保持，发放，且拒绝其它任何 scope**（Google One Support 官方口径）。
- 单隧道、单域名不变；需唯一操作：Cloudflare 路由 Service URL 8787 → 网关端口（拟 8786）——由用户在地控制台改（或授权我们改）。

**已确认**：无 OAuth 相关既有代码；Python 3.12.10；mcp==2.0.0；pyright 1.1.411 / ruff 0.16.1；pytest 全量 154 项通过；问题记录.md 尚不存在（已列入开发计划.md 缺口）。

**实现要点**
- 网关 `gateway.py`：uvicorn `127.0.0.1:8786`（`GATEWAY_HOST/GATEWAY_PORT`）；
  SDK `create_auth_routes`（发现/authorize/token/register/revoke）+ protected-resource 元数据 +
  `/consent` 浏览器同意页 + `/health`；`/mcp` 反向代理到引擎（`upstream_url`），
  OAuth 校验通过后替换为引擎 Bearer（`upstream_legacy_token`，secrets 同源）；
  旧版 ChatGPT Bearer 常量时间比对直通；本机匿名放行仅限 127.0.0.1/::1/localhost；
  401 带 `resource_metadata` WWW-Authenticate；无任何审计/日志输出（密钥零落盘）。
- Provider `oauth_provider.py`：`LocalOAuthProvider(OAuthAuthorizationServerProvider)`；
  DCR 仅接受 `ACCESS_VIEW_MANAGE_MCP_CONTENT` 单 scope（注册/授权/刷新三重拒绝其它）；
  同意窗口 10 分钟、code 5 分钟单次使用、access 1 小时、refresh 60 天轮换；
  client 注册密文落盘（SecretsStore），token 只存内存（access 哈希索引 / refresh 加密哈希键）。
- 编排：`ServiceCoordinator` 在**非 LOCAL 模式**于 codex 就绪后拉起网关（8786 端口探测），
  公网域名取隧道固定 URL；失败→清理；停止顺序 Codex→Windows→网关→隧道；
  `component_states` 暴露 gateway 状态。
- 测试 27 项覆盖：元数据/发现、DCR（public+confidential+scope 拒绝）、同意允许/拒绝/过期/410、
  PKCE 失配、code 单次、刷新轮换、过期、撤销、代理（OAuth 换发 Bearer 透传/旧版直通/无令牌 401/
  无效 401/过期 401/上游 5xx 透传）、明文令牌零落盘。

**待用户操作（真机验收）**
1. Cloudflare 仪表盘：路由 `mcp.shiningsugar.shop` 的 Service URL `localhost:8787` → `localhost:8786`。
2. Gemini 自定义 MCP：URL `https://mcp.shiningsugar.shop/mcp` 走 OAuth 授权流程验证。
