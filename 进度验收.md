# MCP DevBridge v0.8.6 进度验收

> 本文件记录 v0.8.6 当前可复现验收事实。GitHub CI、Release 和本机最终升级必须在对应动作完成后继续补写；不得用计划状态冒充实际状态。

## 1. 当前状态

- 分支：`release/v0.8.6`
- 版本：`0.8.6`
- 目标：在不重新引入 v0.9+ 多 Agent runtime 的前提下，让 ChatGPT 复杂开发目标尽可能在同一 assistant turn 内连续自动推进，降低 `Connection interrupted` / 空闲 transport 中断，同时在 `Message delivery timed out` 等宿主级 hard timeout 发生后保留可恢复状态。

## 2. 本轮实现验收

### 2.1 SSE transport liveness

已通过：

- Gateway 仅对 `text/event-stream` 增加 12 秒空闲 comment keepalive。
- keepalive 只在完整 SSE event 边界插入；上游 event 被网络 chunk 拆开时不会在中间插帧。
- 非 SSE 响应不注入任何 keepalive 字节。
- buffered/streaming response 继续保持单一所有权；下游取消、正常结束和异常都会关闭 upstream。
- 新增 idle keepalive、partial-event、cancel/close、content-type 检测回归；既有 StreamConsumed 回归保持通过。

### 2.2 Streamable HTTP reconnect replay

已通过：

- 每个 CodexPro HTTP session 使用有界 EventStore：默认 256 events / 4 MiB / 30 min TTL。
- 正常事件生成 event id，可按 `Last-Event-ID` 重放仍保留的同 stream 后续事件。
- event count / byte budget / TTL 有界，避免 resumability 形成无界内存增长。
- 单个超大 event 不阻塞原始响应；EventStore 只保存 gap marker。重放从 gap 之后开始可继续，跨越 gap 时显式失败。
- `event-store-smoke.mjs` 覆盖有序重放、不同 stream 隔离、eviction、byte cap 和 oversize replay gap。

### 2.3 progress-aware `wait_task`

已通过：

- 后台 `bash` 任务本身仍无固定执行时长上限。
- 客户端没有请求 progress token 时，一次 `wait_task` 实际等待最多 30 秒。
- 客户端提供 MCP progress token 且 SDK 提供 notification sender 时，可请求最多 120 秒，并约每 8 秒发送标准 `notifications/progress`。
- 已经 terminal 的 task 直接返回，不再为已完成工作发送 progress。
- progress notification 发送失败不取消后台任务。
- running `wait_task` 只传输 stdout/stderr 各约 2 KiB UTF-8 安全尾部，并返回 omitted-byte 数；终态结果仍保留既有有界完整快照。
- poll hint 按任务时长扩展为 5 / 15 / 60 / 120 秒，降低长时间轮询次数和上下文重复。

### 2.4 Goal-like 同回合连续推进

已通过工具契约与模型指令验收：

- 目标仍清晰可执行且无需真实用户输入、授权或安全确认时，running task 不能成为结束 assistant turn 的理由。
- `wait_task` 和 long-run summary 返回无需用户手动“继续”的提示。
- blocked long-run step 不会错误承诺 `user_reply_required=false`。
- 工具密集长阶段建议约 45–60 秒或阶段切换时给简短 commentary，然后继续执行。
- durable `.ai-bridge/long-runs/<run_id>.json` 仍是跨 turn / reconnect 的恢复事实源。

### 2.5 Tunnel 策略

- 本轮没有盲目强制 `cloudflared --protocol http2`；仍保留 auto/QUIC 优先与现有 local/public health/TTFB tunnel-only recovery。
- 当前 bundled `cloudflared`：`2026.7.3`。
- 若后续真实日志持续证明 UDP/QUIC idle reset 明显占优，再按文档做 `auto` vs `http2` A/B，而不是在本轮把网络猜测固化成默认配置。

## 3. 缺陷审查与返工记录

本轮不是一次通过，实际打回并修复过以下问题：

1. 初版 SSE keepalive 可能在一个被拆成多个网络 chunk 的 SSE event 中间插入注释帧。已改为 event-boundary gate，并增加 partial-event 回归。
2. 初版 EventStore 对超过 replay byte budget 的单事件直接抛错，可能为了“可重放”反过来让合法大工具响应失败。已改为原响应正常发送 + replay gap marker。
3. 更强 Linux parser gate 发现 `scripts/install_linux.sh` 旧有反引号替换语法错误。已修正并增加 POSIX CI `bash -n` 回归。
4. progress 语义复核后补充：已 terminal task 不发送 `notifications/progress`。
5. 第一轮 GitHub Ubuntu 22.04 CI 暴露出 keepalive 测试自身的跨平台竞态：实现把测试用 keepalive 下限夹到 50 ms，而测试首 chunk 只延迟 40 ms，因此更快的 Linux runner 合法地没有产生 keepalive，Windows 偶然因调度变慢而通过。已把 idle/partial-event 测试延迟改为 120 ms、显式 keepalive 改为 50 ms，使断言真正跨过保活周期；partial-event 用例也因此实际覆盖“中间等待超过一个 keepalive 周期仍不得插帧”，而不再是 vacuous pass。

最终独立 Codex defect-first review 结论：

```text
No actionable correctness defects were identified in the staged, unstaged, or untracked changes.
```

审查同时确认 SSE event boundary / response close、EventStore bounds/replay-gap、无 progress 客户端 30 秒 cap，以及 CodexPro TypeScript build + complete smoke。审查器自身的 system Python 缺 `mcp`，因此其 Python 命令没有作为测试证据；Python 正式门使用项目 `.venv` 单独执行并通过。

## 4. 2026-08-25 本机源码质量门

### Python

最终 Windows release build 内再次执行：

```text
pytest:  352 passed, 3 skipped
Ruff:    All checks passed!
Pyright: 0 errors, 0 warnings, 0 informations
```

其中 Windows 的 3 个 skip 包含平台限定项；新增 Linux shell parser 测试在 POSIX CI 实际执行，本机另外用 Git Bash 对四个 Linux 脚本逐一执行 `bash -n` 验证通过。

### CodexPro

```text
npm run build                         pass
npm run smoke                         pass
npm audit --omit=dev --audit-level=high -> 0 vulnerabilities
```

完整 smoke 包含：

```text
analysis / analysis CLI
main MCP smoke
root scan (571 entries / 3 warnings)
nested Git
async task
event-store
long-run durable
skill precedence
HTTP
widget
Pro CLI
doctor/settings
execute/watch/loop handoff
release guard
```

新回归还实际验证：running wait 的大 stdout 被压到 2 KiB、omitted-byte 可见、request-tied progress notification 能收到、同回合 autonomy hint 存在。

### 锁与发布脚本

```text
uv lock --check                              pass
PowerShell scripts/build.ps1 parser          pass
Git-Bash bash -n build_linux.sh              pass
Git-Bash bash -n install_linux.sh            pass
Git-Bash bash -n live_upgrade.sh             pass
Git-Bash bash -n prepare_runtime_linux.sh    pass
git diff --check                             pass（文档最终更新后发布前还会再跑一次）
```

## 5. Windows 正式候选构建

最终本机构建命令：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/build.ps1 -Version 0.8.6
```

结果：`Build OK (version 0.8.6)`。

构建过程确认：

- Node.js runtime `v22.19.0`
- uv / uvx `0.11.25`
- cloudflared `2026.7.3`
- CodexPro production runtime 96 packages，42 dev packages skipped，约 15.15 MiB
- Python 352 passed / 3 skipped
- Ruff pass
- Pyright 0 errors / 0 warnings
- PyInstaller onedir pass
- Inno Setup installer pass
- bundled runtime + `live_upgrade.ps1` verification pass

本机候选安装器：

```text
release/MCPDevBridge-Setup-0.8.6.exe
size:   97,874,140 bytes
SHA256: 9EE3F6646A5557CE9E816DE4446D78DCF3E253E28291EA3D4E249565BB566B40
```

该哈希只代表本机构建候选。GitHub Actions 从 release commit 重建的正式资产可能因打包元数据不同产生不同字节哈希，线上正式资产必须独立计算并记录。

## 6. 外部依据与边界结论

架构依据已经写入 `docs/en/GOAL_LONG_RUNNING_TASKS.md`：

- OpenAI 对耗时较长的 Responses 明确建议 background mode，从产品架构上把长计算与脆弱的前台请求生命周期分离。
- MCP 定义 request `_meta.progressToken` 和 `notifications/progress`，且 progress 只能引用仍 active / in-progress 的请求。
- Cloudflare 文档明确指出 Tunnel 代理的长连接可能因服务路径/维护重连而中断；idle QUIC 对短 UDP idle timer 更敏感，并建议使用 application-layer keepalive 或在问题环境中测试 HTTP/2。Cloudflare 同时说明 `text/event-stream` 能让 Tunnel 按流转发而不是缓存完整响应。

因此本轮可承诺的是：减少可控的 idle/transport/context-pressure 风险，增强同一 ChatGPT turn 的自动连续推进和 reconnect 恢复；**不能承诺 MCP 服务端能绕过 ChatGPT 浏览器↔OpenAI 自身的 hard `Message delivery timed out` 上限**。如果宿主仍强制结束 turn，本地 background task 与 durable long-run 状态继续保存，下一次连接可以恢复而无需重做。

## 7. 发布与安装终验

2026-08-25 最终闭环已完成：

- release 分支 `release/v0.8.6` 已 push；最终 release 源提交为 `2161eb6926d1f9f3a6ebdc38f41f945c6612b2d6`。
- annotated tag `v0.8.6` 指向该提交；未改写任何 v0.9.x 历史。
- GitHub Release build run `32815318577` 从该提交执行完成，Windows 与 Ubuntu 22.04 jobs 均为 `success`。
- CI Windows installer：`MCPDevBridge-Setup-0.8.6.exe`，98,892,627 bytes，SHA-256 `336A67268CD51AD3D438DFB8BFE12B3104DDEDA2BF3EDAF578307558D931B139`。
- CI Linux tarball：`MCPDevBridge-Linux-x86_64-0.8.6.tar.gz`，154,906,492 bytes，SHA-256 `DD0070F35B7947E6FC7F56EA6B6BA9091957E0BC0D0919F25069995E09FB5009`。
- GitHub Release `v0.8.6` 已正式发布，包含上述两平台资产与 `SHA256SUMS.txt`，线上 asset digest 与独立本地 SHA-256 一致。
- 当前 Windows 实例已通过 `scripts/live_upgrade.ps1` 的 detached worker 使用正式 CI installer 原地升级；`upgrade-result.json` 返回 `ok=true`，新进程 PID `25744`，安装路径 `D:\Environment\mcp\MCPDevBridge.exe`。
- 升级窗口内 ChatGPT/MCP 出现过预期的短暂 502；随后同一会话重新连通，`D:\`（8788）与 `C:\`（8791）两个原运行根均恢复为运行中，验证了断线后无需人工重建项目状态。
- 桌面快捷方式 `MCP DevBridge.lnk` 已由 live-upgrade worker 重建，目标精确指向 `D:\Environment\mcp\MCPDevBridge.exe`。
- 进程拓扑符合预期：1 个 `MCPDevBridge.exe`；其子进程包含 2 个项目 CodexPro `node.exe` 与 1 个 DevBridge-managed `cloudflared.exe`。系统中另有 PID 4312 的独立 cloudflared 服务，不属于 DevBridge 进程树，不做误杀。
- 健康探针必须从后台 `bash` 进程执行，不能在 Gateway 本地同步 `run_command` handler 内自请求 8786（那会因为占用同一 event loop 形成自死锁假象）。按正确探测路径，local `http://127.0.0.1:8786/health` 返回 HTTP 200 / 0.001819 s，public `https://mcp.shiningsugar.shop/health` 返回 HTTP 200 / 1.297708 s。

因此 v0.8.6 已满足 `开发计划.md` §6 的发布闭环。剩余边界仍与设计一致：MCP 服务端可以减少 idle transport、轮询上下文和 reconnect 风险，但无法覆盖 ChatGPT 浏览器↔OpenAI 宿主本身的 hard `Message delivery timed out` 上限；此类 hard timeout 后 durable long-run/background task 仍可恢复。

## 8. v0.8.7 长稳修复与发布验收（2026-08-26）

### 8.1 根因与实现

- 已确认旧 v0.8.6 的后台 502 不是单一 Tunnel 故障：包括 CodexPro 子进程死亡后 READY 缓存假状态、Gateway `/health` 假阳性、旧 30 分钟 MCP protocol-session TTL 与 upstream session 缓存、Gateway 本地失败不自愈，以及 Windows Modern Standby idle transition。
- CodexPro 已升级到 MCP TypeScript SDK v2 + Zod 4，`/mcp` 使用 `createMcpHandler(..., { legacy: "stateless" })`；生产数据面不再存在 `Mcp-Session-Id → transport` Map、30 分钟 protocol-session TTL、prune timer 或 session-specific EventStore。
- Gateway 永远剥离外部 `Mcp-Session-Id` 后访问 CodexPro，不再为不同根虚拟化 upstream session，也不因 `Session not found` 自动重放工具调用。
- workspace handle、后台 task 与 durable long-run 提升为进程级业务状态；selected-workspace / `show_changes(last_shown)` 使用有界 client-affinity 隔离。workspace registry 上限 4096，client selection/review checkpoint 上限 2048，Gateway affinity cache 上限 16384。
- `EngineManager` 会把已退出子进程从 READY 切到 ERROR；`ProjectManager` 对用户期望运行的项目执行认证 `/healthz` 数据面探针并仅恢复失败项目；用户显式 stop 后不会被 supervisor 复活。共享 Gateway 连续本地失败达到阈值时只重建 Gateway；public-only 失败继续 tunnel-only recovery。
- Windows 公网 Hub 使用 `SystemAwakeGuard` 持有 `ES_CONTINUOUS | ES_SYSTEM_REQUIRED`，不使用 `ES_DISPLAY_REQUIRED`。
- 后台 `bash` 任务没有 execution timeout；`wait_task` 的 30/120 秒只是一次轮询窗口。600 秒 `orchestrationStale` 只保留 advisory 诊断语义，明确不得把 stale 当作 task 完成、不得因此要求用户回复“继续”或提前结束 assistant turn。

### 8.2 更新器

- 应用内更新不再只按相邻版本升级：扫描正式 GitHub Releases 后，过滤 draft、prerelease、非稳定 SemVer、缺当前平台资产以及 tag/资产版本不一致的 release，并按 SemVer 选择最高可安装正式版。
- 自动测试覆盖 `0.8.4 → 0.8.7` 一次直升；v0.9.x 历史 branch/tag 没有正式 Release，不会被当前维护线误选。

### 8.3 本机发布前质量门

- `git diff --check`：PASS。
- Ruff：PASS。
- Windows Pyright：`0 errors, 0 warnings, 0 informations`；新增 `--pythonplatform Linux` 交叉类型门同样 `0 errors, 0 warnings, 0 informations`，并已写入 `scripts/build.ps1`，防止 Windows 本地漏掉 Linux 专属静态错误。
- 全量 pytest：最终修正版构建内 `360 passed, 3 skipped in 93.32s`。
- CodexPro `npm run build`：PASS。
- CodexPro 完整 `npm run smoke`：PASS（analysis / root-scan / nested-git / async-task / long-run / skill / HTTP stateless / widget / pro / doctor / settings / handoff / release-guard）。
- `npm audit --omit=dev --audit-level=high`：`found 0 vulnerabilities`。
- `uv lock --check`：PASS；PowerShell parser 与 4 个 Linux shell `bash -n`：PASS。
- 最终修正版 Windows 正式构建 `scripts/build.ps1 -Version 0.8.7`：`Build OK (version 0.8.7)`；构建脚本内部再次通过 pytest 360/3、Ruff、Windows Pyright、Linux Pyright、CodexPro production build。
- 最终修正版本地安装器：`release/MCPDevBridge-Setup-0.8.7.exe`，98,129,021 bytes，SHA-256 `6B80DA57B6DEF9A68EA077C34B2346DDB708B955C6B536877F17AAEE4A3763B4`。

### 8.4 首轮 CI 返工与尚待发布动作

- 首个 release workflow run `32926559611` 来自候选提交 `0bc22261e837fc26fbffd0cca92b304f71ac6d38`：Windows job 成功，Linux job 在 Pyright 阶段因 `ctypes.WinDLL/get_last_error` 的 Linux 类型存根不可用而失败；因此**没有创建 GitHub Release，也没有发布半套资产**。
- 已将 Windows 专属 ctypes API 改为跨平台可导入/可类型检查的运行时获取，并新增本地 Linux Pyright 发布门；修正版 Linux/Windows Pyright 均为 0/0。
- 下一步将提交修正版、让尚未正式发布的 `v0.8.7` tag 改指最终 release commit，重新执行 Windows + Ubuntu 22.04 双平台 workflow。GitHub Release、本机正式升级、快捷方式、`powercfg /requests` 与真实 MCP 数据面终验仅在实际完成后追加。
