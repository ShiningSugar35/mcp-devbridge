# MCP DevBridge v0.8.8 进度验收

> 本文件只记录当前版本的可复现施工与验收证据。已发布旧版本的详细流水以 Git 历史、Tag 与 GitHub Release 为准，不再把 0.8.6/0.8.7 的完整施工日志长期堆在当前工作文档里。

## 0. 当前状态

- 目标版本：`0.8.8`
- 分支：`release/v0.8.8`
- 当前阶段：**v0.8.8 正式发布、正式资产安装与本机终验完成**；仅保留本文件的发布后审计记录与 durable final review/complete。
- durable run：`lr_mt9z91x0_526e04e0b7b4`（接续已因进程重启导致旧 task id 全部失效而取消的 `lr_mt9ovxro_83d924b3bafa`）。
- 已完成：需求与根因、权威调研与架构计划、无漂移多根路由、full_system 跨根访问、Windows 高完整性 broker、管理员授权与可恢复启动返工、普通用户界面语言治理、历史遗留清理、资源优化、最新 revision 全量质量门、正式双平台 CI/Release、正式 Windows 资产安装，以及 C-D 双根 / stale-handle / 管理员链 live validation。
- 正式产品源码 SHA：`104bb9efa42f55e7df76c78d56b3394616d56efc`；GitHub Actions：`32976242166`；`v0.8.8` annotated tag 解引用到同一 SHA。发布后本文件的审计提交不移动 tag、不改变已发布二进制。

## 1. 需求 / 根因恢复

已读取并以 `AGENTS.md`、`项目架构.md`、`开发计划.md` 和当前工作树为依据恢复语境。

现场复现：

- DevBridge 注册根：`D:\ -> ecb6fea6:8788`、`C:\ -> e1860a65:8791`。
- CodexPro 可正常打开 `D:\Environment\mcp`，但旧 DevBridge `get_current_workspace` 仍可能返回 `C:\`。
- 根因不是文件读写能力本身，而是 opaque CodexPro workspace、DevBridge route workspace 与 legacy session soft selection 三层状态的优先级/同步语义混乱。
- system/full_system 过去仍把 `CODEXPRO_ALLOWED_ROOTS` / `PathGuard` 当成硬根边界，造成“完全访问”名实不符。
- Windows 应用内的“完全访问”权限位不会自动产生高完整性 Windows token；需要正规 UAC 授权后的 elevated 执行边界。

## 2. 权威架构调研与治理固化

已将后续版本必须执行的 Phase A-G 工作流固化进 `AGENTS.md`：

需求分析 → 根因定位与权威调研 → 架构计划与复杂度/资源评估 → 开发与证据记录 → defect-first 代码审查/返工 → 专业测试/返工 → 发布/清计划/更新架构/正式资产升级。

本轮架构依据包含：

- MCP 2026-07-28 规范：协议核心无会话状态；需要状态的应用应显式传递 handle，而不是依赖隐藏全局 current workspace。
- Microsoft Windows Elevated Task / Administrator Broker / Task Scheduler RunLevel：高权限采用一次正规 UAC 授权注册的最高权限任务 + broker，不关闭 UAC、不利用 bypass。

## 3. 多根 / 多会话路由

实现：

- 路由优先级改为：task affinity / 绝对路径与 cwd → opaque workspace handle → legacy soft context → 唯一相对路径匹配 → 仅兼容性 bootstrap fallback。
- `open_workspace/open_current_workspace` 返回的 opaque workspace handle 与 DevBridge route id 同时保留，避免后续相对路径重新猜根。
- legacy `Mcp-Session-Id` 只作为有界 soft context；不同会话互不污染。
- `devbridge_get_current_workspace` 在无显式上下文时不再把稳定 fallback 根伪装成“当前工作区”。
- 运行根快照增加 350ms 有界缓存，避免高频路由每次重新读取项目配置。
- CodexPro `WorkspaceManager` 增加 `root -> workspace_id` 索引，已有根查找由线性扫描改为均摊 O(1)。

回归证据：

- 红基线：4 failed / 76 passed / 1 skipped。
- 修复后：`tests/test_workspace_autoroute.py + tests/test_oauth.py` → 82 passed / 1 skipped。
- 加入 broker 专项后同组总回归：88 passed / 1 skipped。

## 4. full_system 与 Windows 管理员执行

### 4.1 跨工作区访问

- `CODEXPRO_SYSTEM_ACCESS=1` 只在 system/full_system 启用。
- system/full_system 下 workspace 退化为默认 cwd / 语境，不再是文件系统硬边界；绝对路径可访问工作区外。
- workspace/read_only 仍保持原根边界。
- realpath、blocked-glob、写入符号链接拒绝等安全校验仍保留。
- CodexPro `system-access-smoke` 已通过。

### 4.2 高完整性 broker

新增 `src/local_dev_mcp_bridge/elevation.py`：

- 桌面主程序保持 `asInvoker`。
- 首次启用完全访问时，通过标准 `Start-Process -Verb RunAs` 触发一次 UAC；注册 Task Scheduler `RunLevel Highest + Interactive` 任务。
- 后续由普通进程按需启动已注册任务，不再每条命令弹 UAC。
- broker 只监听 `127.0.0.1`，请求有大小上限，响应输出有界。
- broker 认证值存于现有 Credential Manager / DPAPI 受保护存储，状态文件只记录 pid/port/epoch/elevated，不落认证值。
- broker 侧对短命令再次执行 `full_system` execution policy 校验。
- 高权限 CodexPro 由 broker 持有；Windows 下尽量加入 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` Job Object，正常退出也会显式停止全部子进程。
- 如果系统实际 token 不是 elevated，客户端拒绝把该 broker 当成管理员执行链。
- Gateway 的 system `run_command/run_program` 在 broker 已注册时走高完整性链；未注册的非桌面开发场景不会偷偷触发 UAC。

专项证据：

- `tests/test_elevation.py` 已扩充到 64 位 WinAPI token、已提权不重复授权、stale task、最小任务 ACL 等回归；与桌面授权/恢复/UI 组合专项共 **20 passed**。
- Ruff（本轮相关 Python）：All checks passed。
- Pyright（本轮相关 Python）：0 errors / 0 warnings。
- 实机确认当前桌面为 High Integrity，但旧 ctypes WinAPI 声明错误会把它误判为非管理员；显式声明 pointer-sized `argtypes/restype` 后同一进程由 False 修复为 True。
- 实机 source-runtime 与最终 frozen 任务均已复验：Medium token 可查询/运行已授权任务，broker health 返回 `elevated=true`；最终 Task Scheduler action 指向 `D:\Environment\mcp\MCPDevBridge.exe --elevated-broker`，原用户 ACL 仅 read+execute，SYSTEM/Administrators 保持 full control。

## 5. 清理与复杂度 / CPU / 内存 / 磁盘优化

### 5.1 清理

- 删除已完成的旧施工文档 `稳定性方案_v0.8.7.md`。
- `开发计划.md` 已按发布流程清空为“当前无待开发任务”。
- 清理 `.pytest_cache`、`.ruff_cache`、`tests/__pycache__`。
- `release/` 历史 CI / publish / 上传临时物与 0.8.3–0.8.6 安装包共释放约 **1827.75 MiB**。
- 发布前只保留 `MCPDevBridge-Setup-0.8.7.exe` 作为可回滚基线；0.8.8 发布时生成新资产。
- 清除本轮编辑误带入的 UTF-8 BOM（Python / JSON / TypeScript 共 6 个文件）。

### 5.2 资源与算法

- Gateway active-root snapshot：350ms TTL，避免单次工具调用链反复从磁盘加载 `projects.json`。
- CodexPro workspace root index：已打开根查找均摊 O(1)，避免每次 O(W) 扫描；workspace 数量仍有 4096 上界、client selection 有 2048 上界。
- broker 状态 IPC：`ElevatedCodexProManager.state` 增加 250ms 合并缓存；同一轮 UI / supervisor 连续状态读取只触发一次 IPC。专项测试验证两次连续读取仅调用一次 `child_status`。
- broker 本身按需启动，空闲且无子进程 10 分钟自动退出；不引入永久常驻 Windows Service。
- 请求体上限 1 MiB、单次输出上限 256 KiB，防止异常命令输出导致无界内存增长。

## 6. 代码审查与发布前专业测试（2026-08-26）

### 6.1 defect-first 审查与返工

S6 不是一次通过。审查阶段实际发现并返工了权限降级仍沿用 elevated manager、可写临时提权脚本 TOCTOU、broker 未授权静默降权、broker children 并发遍历、批量启动 broker state 竞态、stop 假 IDLE、单项目确认顺序、批量/自动恢复遗漏 elevated 参数、版本元数据残留等 P1/P2 问题。返工后 targeted regression 为 **110 passed / 1 skipped**，targeted Ruff 全绿，targeted Pyright 0/0，`git diff --check` 通过。

### 6.2 全量质量门

最终源码门：

- Ruff `src tests`：**All checks passed**。
- Pyright Windows：**0 errors / 0 warnings / 0 informations**。
- Pyright Linux cross-platform：**0 / 0 / 0**。
- 全量 pytest（最新 live-fix revision）：**386 passed / 3 skipped in 99.36s**；最新 Windows build 内再次为 **386 passed / 3 skipped in 95.31s**。
- CodexPro `npm run build`：PASS。
- CodexPro 完整 `npm run smoke`：在显式清除 live `CODEXPRO_SYSTEM_ACCESS` 等运行态变量的干净发布环境中 PASS，包含普通 containment、`system-access smoke: ok`、stateless HTTP、nested Git、async task、durable long-run、settings、handoff 与 release guard。一次 settings 本地端口 readiness 超时已由独立 settings smoke + 随后完整 smoke 全绿判定为瞬时环境抖动。
- `npm audit --omit=dev --audit-level=high`：**found 0 vulnerabilities**。
- `uv lock --check`：PASS。
- PowerShell parser：`build.ps1` / `live_upgrade.ps1` / `prepare_runtime.ps1` PASS。
- Git Bash 5.2.37 `bash -n`：`build_linux.sh` / `install_linux.sh` / `live_upgrade.sh` / `prepare_runtime_linux.sh` 全部 PASS。
- `git diff --check`：PASS。

### 6.3 Windows 最终候选构建与本机 live validation

`scripts/build.ps1 -Version 0.8.8` 在最新 live-fix revision：**Build OK (version 0.8.8)**。构建内部再次通过 pytest 386/3、Ruff、Windows/Linux Pyright、CodexPro production build、PyInstaller onedir、Inno Setup 与 bundled runtime 校验；本地候选 `release/MCPDevBridge-Setup-0.8.8.exe` 为 **98,138,293 bytes**，SHA-256 `09AE77AFC601FA2F09339CACC79812B29C28DAE7EA1900A91F25D4FE450061C6`。

候选安装终验已真实执行：

- `live_upgrade.ps1` 使用修复后的 detached worker 完成安装，`upgrade-result.json` 返回 `ok=true`；桌面快捷方式指向 `D:\Environment\mcp\MCPDevBridge.exe`。
- `C:\` 与 `D:\` 两个项目均恢复为运行中和完全访问。
- Task Scheduler action 指向当前安装的 `D:\Environment\mcp\MCPDevBridge.exe --elevated-broker`；broker 状态 `elevated=true`。
- 连续两次管理员只读探针 `fsutil dirty query C:` 均成功，且无 `consent.exe` 残留，证明一次授权后的管理员执行不再逐命令弹确认。
- 真实 MCP stale-handle 回归：先获得 C: opaque handle，再携带该 handle 读取 `D:\Environment\mcp\AGENTS.md`，Gateway 成功按绝对路径切到 D: 且未再报 `Unknown workspace_id`；反向用 D: handle 读取 `C:\Windows\win.ini` 同样成功。D: handle 的相对 `AGENTS.md` 仍稳定留在 D:。
- 升级脚本 dry-run 已验证管理员发起时 `launcher_elevated=true / worker_elevated=true`；此前 `exit=5` 权限降级路径已关闭。

该候选已经完成行为验收；随后最终源码 commit `104bb9e...` 由 GitHub Windows/Linux CI 同源重建并发布，正式资产哈希见下一节。

## 7. 最终正式发布与安装证据

### 7.1 同源发布

- 最终产品源码 commit：`104bb9efa42f55e7df76c78d56b3394616d56efc`，已 push 到 `origin/release/v0.8.8`。
- GitHub Actions run `32976242166` 从该 SHA 构建：Windows **5m12s PASS**；Ubuntu 22.04 / Linux-SteamOS **3m42s PASS**。
- 远端 annotated tag `v0.8.8^{}` = `104bb9efa42f55e7df76c78d56b3394616d56efc`；未改写任何 v0.9.x 历史。
- GitHub Release `v0.8.8` 为正式发布（非 draft / prerelease），最终资产与 CI 下载件 digest 一致：
  - Windows `MCPDevBridge-Setup-0.8.8.exe`：99,157,133 bytes；SHA-256 `16a23dd760a35ee5ce0f12300a3132a090a58221942e97e7c595336ab7b266d7`。
  - Linux/SteamOS `MCPDevBridge-Linux-x86_64-0.8.8.tar.gz`：157,081,527 bytes；SHA-256 `5a2d240c90d13c969aa46dc9daccf4fe7bc411516897f3640daff15e43510c71`。
  - `SHA256SUMS.txt` 已随 Release 发布。

### 7.2 正式 Windows 资产安装终验

- 使用上述 **最终 CI/Release Windows installer** 执行 `live_upgrade.ps1`，`upgrade-result.json` 返回 `ok=true`；安装位置保持 `D:\Environment\mcp`。
- 桌面快捷方式 `MCP DevBridge.lnk` 指向 `D:\Environment\mcp\MCPDevBridge.exe`。
- `C:\` 与 `D:\` 两个项目均恢复为运行中，并恢复到完全访问。
- 再次模拟普通双击启动：桌面进程 integrity SID = `S-1-16-8192`（Medium），管理员执行进程 = `S-1-16-12288`（High），证明普通 UI 本身不常驻管理员权限。
- 管理员执行状态 `elevated=true`；连续两次 `fsutil dirty query C:` 均成功，之后 `CONSENT_PROCESSES=0`，证明首次正规授权后的管理员操作不再逐命令弹确认。
- 最终任务 action = `D:\Environment\mcp\MCPDevBridge.exe --elevated-broker`；ACL 保持 SYSTEM/Administrators full control + 原用户 read/execute，普通 token 无权改写高权限 action。

### 7.3 最终真实路由验收

- C: 会话取得 opaque handle 后，用该 handle 显式读取 `D:\Environment\mcp\AGENTS.md`：Gateway 按绝对路径正确切到 D:，并清除 foreign handle，没有再出现 `Unknown workspace_id`。
- 反向用 D: handle 读取 `C:\Windows\win.ini` 同样成功；D: handle 的相对 `AGENTS.md` 仍稳定留在 D:。
- 这证明强 path/cwd/task 证据可覆盖 stale handle，而相对/pathless follow-up 仍由自己的显式 handle 保持会话隔离。
- `system/full_system` 跨根访问已由上述真实数据面验证；`workspace/read_only` containment 由全量 Python 回归与 CodexPro 普通 containment smoke 覆盖，`system-access-smoke` 单独验证放开边界，不互相污染。

### 7.4 收尾

- `开发计划.md`：当前无待开发任务。
- `项目架构.md`：已更新为 v0.8.8 实际路由、管理员执行、任务 ACL、普通用户界面语言和升级器架构。
- `docs/en/CHANGELOG.md`：已记录最终 v0.8.8 修复。
- durable run：`lr_mt9z91x0_526e04e0b7b4`；完成本审计提交后执行 final review PASS 与 long_run_complete。
