# MCP DevBridge v0.8.2 进度验收

> 本文件只记录当前 v0.8.2 维护线的发布前事实与可复现证据。旧版本施工流水已移出本文件，历史行为见 `docs/en/CHANGELOG.md` 与 Git tags。

## 1. 当前状态

维护分支：

```text
release/v0.8.2
```

版本：

```text
0.8.2
```

本轮目标已经完成代码和本机发布门验证：多根自动路由、路径安全、桌面多项目生命周期、Windows 自定义安装目录、Linux/SteamOS Desktop Mode 用户级构建/安装/升级链均已落入维护线。

本文件记录的是**tag 前 release candidate 验证**。GitHub tag / Release / CI 资产的最终状态以 GitHub Release 元数据为准，不在这里伪造“已发布”结果。

## 2. 功能验收

### 2.1 多根自动路由

已通过：

- 所有 READY 项目根同时 active，无面向用户的“入口项目”权限优先级。
- 启动 `C:\` / `D:\` 后，其全部后代路径自动继承访问边界，不要求逐级注册项目。
- 绝对路径选择最具体的包含根。
- 相对路径只有唯一命中时自动路由；多个根存在同名目标时返回歧义并要求绝对路径。
- 新文件可按唯一最深已存在父路径判断目标根。
- `task_id` 保持产生任务的根 affinity。
- CodexPro opaque `workspace_id=ws_...` 只在没有更强 path/cwd/task 证据时使用。
- 路由优先级：显式 DevBridge override > task > 本次路径/cwd/patch/command > opaque workspace handle > bootstrap fallback。
- direct project Bearer 不再把普通 Hub 会话锁死在单根。
- 单调用出现明确跨根强证据时不静默猜测目标。

### 2.2 路径与文件系统安全

已通过：

- Python Gateway containment 使用 canonical/real path 语义。
- `..` 不能越过运行根。
- symlink/junction 不能用“文本路径在根内、真实目标在根外”的方式逃逸。
- Gateway 本地 `run_command/run_program` 的 `cwd` 必须位于路由根内。
- CodexPro root tree/list/inventory/inspect 对 `EACCES/EPERM` 子目录 warning + skip，整次扫描继续。
- scoped Git 从目标路径发现最近嵌套仓库，盘根本身无需为 Git 仓库。

### 2.3 桌面生命周期

已通过：

- 项目表固定为五列：名称 / 路径 / 状态 / 端口 / 操作。
- 1 秒状态刷新复用项目操作按钮，不在鼠标交互期间销毁重建控件。
- busy 状态按项目隔离。
- “启动所有项目 / 停止所有项目”批量控制已落地。
- 删除重复“当前项目”启停控制面。
- “连接与权限”可保存到全部项目，同时保留项目独立 Git 参数和端口。
- 停止一个运行根只停止对应 ProjectUnit；仍有其它运行根时共享 Hub/Gateway/Tunnel 保持运行。
- 最后一个运行根停止后才关闭共享 Hub/Tunnel。

### 2.4 Windows

已通过：

- Windows 10/11 x64 发行链保留。
- Inno Setup 为 per-user 安装。
- `DisableDirPage=no`，安装时可由用户选择目标目录。
- 正式 frozen payload 包含私有 Node.js、uv/uvx、cloudflared 与 `live_upgrade.ps1`。
- detached live-upgrade 和项目恢复逻辑保留。

### 2.5 Linux / SteamOS Desktop Mode

已完成代码/CI/打包链：

- Windows/POSIX 进程启动差异集中在 `platform_support.py`。
- 有效的绝对 `XDG_CONFIG_HOME` / `XDG_DATA_HOME` 被采用；相对 XDG 值按无效处理并回退默认目录。
- Linux/SteamOS 密钥优先桌面 Secret Service；fallback 为 AES-GCM 用户级加密存储。
- POSIX shell、进程组终止和文件权限处理已恢复。
- `prepare_runtime_linux.sh`、`build_linux.sh`、`install_linux.sh`、`live_upgrade.sh` 已纳入维护线。
- `install.sh --target-dir` 支持自定义用户目录，并拒绝 `/`、`$HOME`、`$HOME/.local` 等危险目标和未知非空目录覆盖。
- Desktop Entry `Exec=` 对自定义路径中的保留字符做安全转义。
- GitHub Linux build baseline 使用 Ubuntu 22.04。

按本轮产品决策，**SteamOS Desktop Mode 真实设备安装/升级闭环不作为 v0.8.2 发布阻塞项**。本文件不声称已完成 SteamOS 真机验收；获得目标设备后可作为额外兼容性证据补测。

## 3. 2026-08-21 本机发布门

### 3.1 Python / 路由专项

专项组合：

```text
tests/test_linux_compat.py
tests/test_workspace_autoroute.py
tests/test_oauth.py
tests/test_v082_desktop_maintenance.py
```

结果：

```text
88 passed, 2 skipped
```

全量：

```text
350 passed, 2 skipped
```

Ruff：

```text
All checks passed!
```

Pyright：

```text
0 errors, 0 warnings, 0 informations
```

依赖锁：

```text
uv lock --check → pass
```

### 3.2 CodexPro

TypeScript：

```text
npm run build → pass
```

生产依赖：

```text
npm audit --omit=dev → 0 vulnerabilities
```

完整 smoke：

```text
analysis smoke                 pass
analysis CLI smoke             pass
main MCP smoke                 pass
root scan smoke                569 entries, 3 warnings
nested Git smoke               pass
async task smoke               pass
skill precedence               pass
HTTP smoke                     pass
widget smoke                   pass
Pro CLI smoke                  pass
doctor/settings                pass
execute/watch/loop handoff     pass
release guard                  pass
```

真实 `C:\` 根扫描中的受保护目录以 warning 返回，没有让整次扫描失败。

### 3.3 脚本与 Git 差异

```text
git diff --check                                      pass
PowerShell *.ps1 parser                               pass
Git-Bash bash -n build/install/live_upgrade/runtime   pass
```

Windows 自带 `bash.exe` 指向 WSL 启动器，而当前机器没有可用 WSL distro，因此 Linux shell parser 使用已安装 Git-Bash 执行；脚本本身 `bash -n` 通过。

### 3.4 Windows 正式构建

执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/build.ps1 -Version 0.8.2
```

结果：

- Python 全量测试在构建流程内再次通过：`350 passed / 2 skipped`。
- Ruff 再次通过。
- Pyright 再次为 `0 errors / 0 warnings`。
- CodexPro production runtime：96 production packages，42 dev packages skipped，约 15.05 MiB。
- PyInstaller onedir 成功。
- Inno Setup 成功。
- bundled runtimes + `live_upgrade.ps1` 校验成功。

本机候选安装器：

```text
release/MCPDevBridge-Setup-0.8.2.exe
size:   97,723,248 bytes
SHA256: A804C03DBE5BCC0D600B7388FF3B8B4BCD0D5DE373B989B7F7E8A29EE25C0FBB
```

这个 SHA-256 对应 **2026-08-21 当前本机构建**。GitHub Actions 重新构建的正式发布资产可能因打包元数据而产生不同字节级哈希，线上资产应单独校验，不得把本地哈希冒充线上哈希。

## 4. 文档与清理验收

本轮已完成：

- `.gitignore` 删除 4 行乱码回退残留。
- `AGENTS.md` 收敛为当前维护规则，不再引用开发者机器上的外部项目路径或旧 Phase。
- `README.md` 删除旧 v0.8.0/v0.8.1 入口/switch 主工作流，改为 v0.8.2 active-root 用户模型。
- `项目架构.md` 删除重复、过时的 Python backend/tool-count/六列表格/入口项目描述，改写为当前 Gateway/CodexPro/多根/跨平台架构。
- `开发计划.md` 删除已经完成但仍被标记“进行中”的旧 Phase 列表，改为当前 release gate 与后续维护目标。
- `docs/en/ARCHITECTURE.md`、`COMPATIBILITY.md`、`DEVELOPMENT.md`、`SECURITY.md` 同步当前多根和 Linux/SteamOS 语义。
- `docs/en/CHANGELOG.md` 新增 0.8.2，并修复旧 0.7.0 重复标题。
- 生成目录、运行安装文件、构建缓存继续保持 `.gitignore` 管理，不作为源码 track。

## 5. 发布完成定义

v0.8.2 的源码 release candidate 已满足本机代码/构建门。最终发布动作仍必须保证：

1. 维护分支提交并推送，工作树 clean。
2. `v0.8.2` tag 指向最终 release commit。
3. GitHub Release workflow 从该 tag 同时构建 Windows 与 Ubuntu 22.04 Linux assets。
4. GitHub Release 上传并校验 Windows installer、Linux tarball 与 checksum/metadata。
5. 当前 Windows 安装实例切换到最终正式资产并重启。
6. 桌面快捷方式指向最终正式安装位置，运行中的项目根恢复上线。

以上 tag/Release/安装后的实际状态应以 Git/GitHub/本机运行态直接验证；不要用本文件的“计划完成定义”代替真实检查。
