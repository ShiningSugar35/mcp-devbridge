# MCP DevBridge v0.8.9 进度验收

> 本文件只接收已经实现且通过相应验收的事实与可复现证据；尚未实现、尚未重测或尚未通过发布/真实终验的事项保留在 `开发计划.md`，不得提前写成完成。
>
> v0.8.8 的正式发布证据作为当前维护线基线保留；v0.8.9 是尚未发布的端到端连接治理补丁。

## 0. 当前状态

- 目标版本：`0.8.9`（未发布）。
- 源码分支：当前仍在既有维护分支工作树上开发，正式 `release/v0.8.9` 分支仅在发布门通过后按规范创建或使用；旧 tag/Release 未移动。
- 当前阶段：唯一 durable run `lr_mtb6w0ih_a803aa0ad300` 的 **s1–s9 已完成，s10 正在返工**。2026-08-29 已实际执行 r1–r3 三轮 s10 编排：r1/r2 证据无效，r3 于本地 05:35–06:37 明确失败；当前没有可冒充通过的正式 soak 在运行。s11–s14 仍未完成，以 durable run 与 `开发计划.md` 的最新并行分工为准。
- 正式基线：annotated `v0.8.8` 解引用到 `104bb9efa42f55e7df76c78d56b3394616d56efc`；旧 tag/Release 未移动。
- 已完成并已在本文件登记：v0.8.8 正式发布基线；2026-08-27 开发事实源治理；v0.8.9 多轮现场取证与分层裁定、OAuth 重启连续性、fixed 50-tool contract、call-stage policy gate、deterministic handle rehydrate、legacy affinity、多根无强证据 fail-closed、v2 原子 route state/失效清理、分层真实 MCP canary、CodexPro graceful lifecycle、Bash active cap/Windows PATH-less 启动、analysis deadline/single-flight/cancel、bounded `show_changes`、strict half-open、s7 targeted gate、s8 defect-first review，以及 s9 全量质量门与 Windows/Linux 0.8.9 同源候选构建。r3 失败现场与 06:37 Tunnel DNS 故障的取证/根因裁定已完成；其后 transient route 恢复、Tunnel 可取消有界重试、Flight Recorder 持续探针与 soak harness 防假绿已分别完成源码/targeted/静态验收（见 §27–§31），但最新 staging/live fault preflight 与正式 r4+ soak 尚未通过，因此这些源码 PASS 不能提升为 s10 PASS。
- s9 最新明确证据：Windows `scripts/build.ps1 -Version 0.8.9` task `571c3b0a-cc43-461e-adb2-2cee441d4784` completed/exitCode=0，内部 pytest **466 passed / 3 skipped**、Ruff PASS、Pyright Windows/Linux 0 errors、PyInstaller/Inno Setup PASS；Windows 完整 CodexPro smoke task `5aca2a0b-0c86-4c3f-a9d7-e3e8062de74c` exitCode=0。Ubuntu 22.04 普通用户 Linux build task `83265602-9f23-4071-9ec2-33a8569e7adb` completed/exitCode=0，pytest **464 passed / 5 skipped**、Ruff/Pyright/完整 smoke/PyInstaller/frozen headless/package 全绿。该轮 Windows SHA256=`0fd4d975230c647d3a04179259b18392dad8491a76c9b82560f4e70446ae24e6`、Linux SHA256=`2c9e648838f6c16d438f65cb7a2d617f37f5947bdaf957de0885a5899041e306` 已因后续 s10 源码变化成为**过期候选**，不得用于本机升级或正式发布；必须从最新 revision 重建 staging/同源资产并重新经过 live preflight 与正式 soak。
- 尚未完成：从最新 revision 完成新的 Windows/Linux 同源构建与 staging/live preflight，随后执行全新 r4+ 至少 30 分钟正式 fault soak；只有这些门与后续 review/release gate 均通过，才可依次进入 s11 正式发布、s12 正式资产升级、s13 持续 ChatGPT MCP 终验与 s14 durable completion。详见 `开发计划.md`。

## A. v0.8.8 正式发布基线（历史证据）

以下第1–7节记录v0.8.8正式发布时的真实证据；其中“当前/收尾”等措辞均以该次发布时点为准，不代表v0.8.9现状。

## 1. 需求 / 根因恢复

已读取并以 `AGENTS.md`、`项目架构.md`、`开发计划.md` 和当前工作树为依据恢复语境。

现场复现：

- DevBridge 注册根：`D:\ -> ecb6fea6:8788`、`C:\ -> e1860a65:8791`。
- CodexPro 可正常打开 `D:\Environment\mcp`，但旧 DevBridge `get_current_workspace` 仍可能返回 `C:\`。
- 根因不是文件读写能力本身，而是 opaque CodexPro workspace、DevBridge route workspace 与 legacy session soft selection 三层状态的优先级/同步语义混乱。
- system/full_system 过去仍把 `CODEXPRO_ALLOWED_ROOTS` / `PathGuard` 当成硬根边界，造成“完全访问”名实不符。
- Windows 应用内的“完全访问”权限位不会自动产生高完整性 Windows token；需要正规 UAC 授权后的 elevated 执行边界。

## 2. 权威架构调研与治理固化

已将后续版本必须执行的 Phase A-G 工作流固化进 `AGENTS.md`：

需求分析 → 根因定位与权威调研 → 架构计划与复杂度/资源评估 → 开发与证据记录 → defect-first 代码审查/返工 → 专业测试/返工 → 发布/清计划/更新架构/正式资产升级。

本轮架构依据包含：

- MCP 2026-07-28 规范：协议核心无会话状态；需要状态的应用应显式传递 handle，而不是依赖隐藏全局 current workspace。
- Microsoft Windows Elevated Task / Administrator Broker / Task Scheduler RunLevel：高权限采用一次正规 UAC 授权注册的最高权限任务 + broker，不关闭 UAC、不利用 bypass。

## 3. 多根 / 多会话路由

实现：

- 路由优先级改为：task affinity / 绝对路径与 cwd → opaque workspace handle → legacy soft context → 唯一相对路径匹配 → 仅兼容性 bootstrap fallback。
- `open_workspace/open_current_workspace` 返回的 opaque workspace handle 与 DevBridge route id 同时保留，避免后续相对路径重新猜根。
- legacy `Mcp-Session-Id` 只作为有界 soft context；不同会话互不污染。
- `devbridge_get_current_workspace` 在无显式上下文时不再把稳定 fallback 根伪装成“当前工作区”。
- 运行根快照增加 350ms 有界缓存，避免高频路由每次重新读取项目配置。
- CodexPro `WorkspaceManager` 增加 `root -> workspace_id` 索引，已有根查找由线性扫描改为均摊 O(1)。

回归证据：

- 红基线：4 failed / 76 passed / 1 skipped。
- 修复后：`tests/test_workspace_autoroute.py + tests/test_oauth.py` → 82 passed / 1 skipped。
- 加入 broker 专项后同组总回归：88 passed / 1 skipped。

## 4. full_system 与 Windows 管理员执行

### 4.1 跨工作区访问

- `CODEXPRO_SYSTEM_ACCESS=1` 只在 system/full_system 启用。
- system/full_system 下 workspace 退化为默认 cwd / 语境，不再是文件系统硬边界；绝对路径可访问工作区外。
- workspace/read_only 仍保持原根边界。
- realpath、blocked-glob、写入符号链接拒绝等安全校验仍保留。
- CodexPro `system-access-smoke` 已通过。

### 4.2 高完整性 broker

新增 `src/local_dev_mcp_bridge/elevation.py`：

- 桌面主程序保持 `asInvoker`。
- 首次启用完全访问时，通过标准 `Start-Process -Verb RunAs` 触发一次 UAC；注册 Task Scheduler `RunLevel Highest + Interactive` 任务。
- 后续由普通进程按需启动已注册任务，不再每条命令弹 UAC。
- broker 只监听 `127.0.0.1`，请求有大小上限，响应输出有界。
- broker 认证值存于现有 Credential Manager / DPAPI 受保护存储，状态文件只记录 pid/port/epoch/elevated，不落认证值。
- broker 侧对短命令再次执行 `full_system` execution policy 校验。
- 高权限 CodexPro 由 broker 持有；Windows 下尽量加入 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` Job Object，正常退出也会显式停止全部子进程。
- 如果系统实际 token 不是 elevated，客户端拒绝把该 broker 当成管理员执行链。
- Gateway 的 system `run_command/run_program` 在 broker 已注册时走高完整性链；未注册的非桌面开发场景不会偷偷触发 UAC。

专项证据：

- `tests/test_elevation.py` 已扩充到 64 位 WinAPI token、已提权不重复授权、stale task、最小任务 ACL 等回归；与桌面授权/恢复/UI 组合专项共 **20 passed**。
- Ruff（本轮相关 Python）：All checks passed。
- Pyright（本轮相关 Python）：0 errors / 0 warnings。
- 实机确认当前桌面为 High Integrity，但旧 ctypes WinAPI 声明错误会把它误判为非管理员；显式声明 pointer-sized `argtypes/restype` 后同一进程由 False 修复为 True。
- 实机 source-runtime 与最终 frozen 任务均已复验：Medium token 可查询/运行已授权任务，broker health 返回 `elevated=true`；最终 Task Scheduler action 指向 `D:\Environment\mcp\MCPDevBridge.exe --elevated-broker`，原用户 ACL 仅 read+execute，SYSTEM/Administrators 保持 full control。

## 5. 清理与复杂度 / CPU / 内存 / 磁盘优化

### 5.1 清理

- 删除已完成的旧施工文档 `稳定性方案_v0.8.7.md`。
- `开发计划.md` 已按发布流程清空为“当前无待开发任务”。
- 清理 `.pytest_cache`、`.ruff_cache`、`tests/__pycache__`。
- `release/` 历史 CI / publish / 上传临时物与 0.8.3–0.8.6 安装包共释放约 **1827.75 MiB**。
- 发布前只保留 `MCPDevBridge-Setup-0.8.7.exe` 作为可回滚基线；0.8.8 发布时生成新资产。
- 清除本轮编辑误带入的 UTF-8 BOM（Python / JSON / TypeScript 共 6 个文件）。

### 5.2 资源与算法

- Gateway active-root snapshot：350ms TTL，避免单次工具调用链反复从磁盘加载 `projects.json`。
- CodexPro workspace root index：已打开根查找均摊 O(1)，避免每次 O(W) 扫描；workspace 数量仍有 4096 上界、client selection 有 2048 上界。
- broker 状态 IPC：`ElevatedCodexProManager.state` 增加 250ms 合并缓存；同一轮 UI / supervisor 连续状态读取只触发一次 IPC。专项测试验证两次连续读取仅调用一次 `child_status`。
- broker 本身按需启动，空闲且无子进程 10 分钟自动退出；不引入永久常驻 Windows Service。
- 请求体上限 1 MiB、单次输出上限 256 KiB，防止异常命令输出导致无界内存增长。

## 6. 代码审查与发布前专业测试（2026-08-26）

### 6.1 defect-first 审查与返工

S6 不是一次通过。审查阶段实际发现并返工了权限降级仍沿用 elevated manager、可写临时提权脚本 TOCTOU、broker 未授权静默降权、broker children 并发遍历、批量启动 broker state 竞态、stop 假 IDLE、单项目确认顺序、批量/自动恢复遗漏 elevated 参数、版本元数据残留等 P1/P2 问题。返工后 targeted regression 为 **110 passed / 1 skipped**，targeted Ruff 全绿，targeted Pyright 0/0，`git diff --check` 通过。

### 6.2 全量质量门

最终源码门：

- Ruff `src tests`：**All checks passed**。
- Pyright Windows：**0 errors / 0 warnings / 0 informations**。
- Pyright Linux cross-platform：**0 / 0 / 0**。
- 全量 pytest（最新 live-fix revision）：**386 passed / 3 skipped in 99.36s**；最新 Windows build 内再次为 **386 passed / 3 skipped in 95.31s**。
- CodexPro `npm run build`：PASS。
- CodexPro 完整 `npm run smoke`：在显式清除 live `CODEXPRO_SYSTEM_ACCESS` 等运行态变量的干净发布环境中 PASS，包含普通 containment、`system-access smoke: ok`、stateless HTTP、nested Git、async task、durable long-run、settings、handoff 与 release guard。一次 settings 本地端口 readiness 超时已由独立 settings smoke + 随后完整 smoke 全绿判定为瞬时环境抖动。
- `npm audit --omit=dev --audit-level=high`：**found 0 vulnerabilities**。
- `uv lock --check`：PASS。
- PowerShell parser：`build.ps1` / `live_upgrade.ps1` / `prepare_runtime.ps1` PASS。
- Git Bash 5.2.37 `bash -n`：`build_linux.sh` / `install_linux.sh` / `live_upgrade.sh` / `prepare_runtime_linux.sh` 全部 PASS。
- `git diff --check`：PASS。

### 6.3 Windows 最终候选构建与本机 live validation

`scripts/build.ps1 -Version 0.8.8` 在最新 live-fix revision：**Build OK (version 0.8.8)**。构建内部再次通过 pytest 386/3、Ruff、Windows/Linux Pyright、CodexPro production build、PyInstaller onedir、Inno Setup 与 bundled runtime 校验；本地候选 `release/MCPDevBridge-Setup-0.8.8.exe` 为 **98,138,293 bytes**，SHA-256 `09AE77AFC601FA2F09339CACC79812B29C28DAE7EA1900A91F25D4FE450061C6`。

候选安装终验已真实执行：

- `live_upgrade.ps1` 使用修复后的 detached worker 完成安装，`upgrade-result.json` 返回 `ok=true`；桌面快捷方式指向 `D:\Environment\mcp\MCPDevBridge.exe`。
- `C:\` 与 `D:\` 两个项目均恢复为运行中和完全访问。
- Task Scheduler action 指向当前安装的 `D:\Environment\mcp\MCPDevBridge.exe --elevated-broker`；broker 状态 `elevated=true`。
- 连续两次管理员只读探针 `fsutil dirty query C:` 均成功，且无 `consent.exe` 残留，证明一次授权后的管理员执行不再逐命令弹确认。
- 真实 MCP stale-handle 回归：先获得 C: opaque handle，再携带该 handle 读取 `D:\Environment\mcp\AGENTS.md`，Gateway 成功按绝对路径切到 D: 且未再报 `Unknown workspace_id`；反向用 D: handle 读取 `C:\Windows\win.ini` 同样成功。D: handle 的相对 `AGENTS.md` 仍稳定留在 D:。
- 升级脚本 dry-run 已验证管理员发起时 `launcher_elevated=true / worker_elevated=true`；此前 `exit=5` 权限降级路径已关闭。

该候选已经完成行为验收；随后最终源码 commit `104bb9e...` 由 GitHub Windows/Linux CI 同源重建并发布，正式资产哈希见下一节。

## 7. 最终正式发布与安装证据

### 7.1 同源发布

- 最终产品源码 commit：`104bb9efa42f55e7df76c78d56b3394616d56efc`，已 push 到 `origin/release/v0.8.8`。
- GitHub Actions run `32976242166` 从该 SHA 构建：Windows **5m12s PASS**；Ubuntu 22.04 / Linux-SteamOS **3m42s PASS**。
- 远端 annotated tag `v0.8.8^{}` = `104bb9efa42f55e7df76c78d56b3394616d56efc`；未改写任何 v0.9.x 历史。
- GitHub Release `v0.8.8` 为正式发布（非 draft / prerelease），最终资产与 CI 下载件 digest 一致：
  - Windows `MCPDevBridge-Setup-0.8.8.exe`：99,157,133 bytes；SHA-256 `16a23dd760a35ee5ce0f12300a3132a090a58221942e97e7c595336ab7b266d7`。
  - Linux/SteamOS `MCPDevBridge-Linux-x86_64-0.8.8.tar.gz`：157,081,527 bytes；SHA-256 `5a2d240c90d13c969aa46dc9daccf4fe7bc411516897f3640daff15e43510c71`。
  - `SHA256SUMS.txt` 已随 Release 发布。

### 7.2 正式 Windows 资产安装终验

- 使用上述 **最终 CI/Release Windows installer** 执行 `live_upgrade.ps1`，`upgrade-result.json` 返回 `ok=true`；安装位置保持 `D:\Environment\mcp`。
- 桌面快捷方式 `MCP DevBridge.lnk` 指向 `D:\Environment\mcp\MCPDevBridge.exe`。
- `C:\` 与 `D:\` 两个项目均恢复为运行中，并恢复到完全访问。
- 再次模拟普通双击启动：桌面进程 integrity SID = `S-1-16-8192`（Medium），管理员执行进程 = `S-1-16-12288`（High），证明普通 UI 本身不常驻管理员权限。
- 管理员执行状态 `elevated=true`；连续两次 `fsutil dirty query C:` 均成功，之后 `CONSENT_PROCESSES=0`，证明首次正规授权后的管理员操作不再逐命令弹确认。
- 最终任务 action = `D:\Environment\mcp\MCPDevBridge.exe --elevated-broker`；ACL 保持 SYSTEM/Administrators full control + 原用户 read/execute，普通 token 无权改写高权限 action。

### 7.3 最终真实路由验收

- C: 会话取得 opaque handle 后，用该 handle 显式读取 `D:\Environment\mcp\AGENTS.md`：Gateway 按绝对路径正确切到 D:，并清除 foreign handle，没有再出现 `Unknown workspace_id`。
- 反向用 D: handle 读取 `C:\Windows\win.ini` 同样成功；D: handle 的相对 `AGENTS.md` 仍稳定留在 D:。
- 这证明强 path/cwd/task 证据可覆盖 stale handle，而相对/pathless follow-up 仍由自己的显式 handle 保持会话隔离。
- `system/full_system` 跨根访问已由上述真实数据面验证；`workspace/read_only` containment 由全量 Python 回归与 CodexPro 普通 containment smoke 覆盖，`system-access-smoke` 单独验证放开边界，不互相污染。

### 7.4 收尾

- `开发计划.md`：当前无待开发任务。
- `项目架构.md`：已更新为 v0.8.8 实际路由、管理员执行、任务 ACL、普通用户界面语言和升级器架构。
- `docs/en/CHANGELOG.md`：已记录最终 v0.8.8 修复。
- durable run：`lr_mt9z91x0_526e04e0b7b4`；完成本审计提交后执行 final review PASS 与 long_run_complete。

## 8. 2026-08-27 开发事实源治理规则验收

- 状态：**PASS**。本项仅修改仓库治理文档，不改变运行时代码、性能、协议或发布资产，因此运行时测试/性能基准/发布验证不适用。
- 根因：旧 `AGENTS.md` 已分散包含“计划只保留未完成项”“长任务使用 durable run”等规则，但已完成事项的迁移时机主要落在 Phase G 发布阶段，且没有集中定义断连后的五级事实源及“禁止依赖聊天记忆/超长接管提示词”，存在重复与解释冲突空间。
- 修改：在 `AGENTS.md` §1 原位新增 §1.1“持久事实源与开发文档职责”，明确 `开发计划.md` 只保留未完成任务、`进度验收.md` 只接收已验收完成事实、`项目架构.md` 只维护当前真实架构、长任务同步维护唯一 durable run。
- 流程合并：同步修改 Phase D，将未验收中间证据留在 `开发计划.md` + durable run；同步修改 Phase G，发布阶段只复核逐项迁移而不再等待整轮结束后批量清计划；同步修改 §3.5，恢复顺序统一为 `AGENTS.md → 项目架构.md → 开发计划.md → 进度验收.md → durable run`。
- 断连规则：聊天记忆不再是事实源；Connector 重连、新会话、上下文压缩后禁止要求用户重新编写或粘贴超长接管提示词，必须从仓库文档与 durable state 自恢复。
- 完成真实性：明确禁止“计划做/预计通过/代码已写但未验收”进入完成记录；事实源冲突时维持未完成/阻塞状态，使用代码、日志、测试、构建或发布资产重新裁定。
- 自验证：修改后完整复读 `AGENTS.md` §1–§3.5，确认 §1.1、Phase D、Phase G、§3.5 语义一致且未削弱既有安全、review、test、release gate；本项临时计划记录已在验收通过后立即从 `开发计划.md` 删除，按新规则完成首个自举迁移。

## 9. v0.8.9 需求恢复、现场取证与红测阶段验收

### 9.1 唯一事实源恢复与工作树保护

- 状态：**PASS**。唯一 workspace 为 `D:\Environment\mcp` / `ws_1d45a4204ebfa51587df4f20`，唯一 durable run 为 `lr_mtb6w0ih_a803aa0ad300`；未创建平行 run。
- 已完整读取 `AGENTS.md → 项目架构.md → 开发计划.md → 进度验收.md → docs/en/LONG_RUNNING_TASKS.md`，并以 durable JSON、Git、测试与运行工件复核聊天交接信息。
- 当前分支/HEAD/origin 已重新核实为 `release/v0.8.8` / `eed7df219667b2c14e78a168a5d57ac35f9f7f8b`；`v0.8.8` tag 仍解引用到正式 SHA `104bb9efa42f55e7df76c78d56b3394616d56efc`。
- 未执行 reset、clean、checkout `.`, force push 或覆盖未提交成果；当前 v0.8.9 worktree 全部按原状态保留。

### 9.2 第一现场：14:43 pre-restart freeze

- 状态：**PASS**。原始证据位于 `C:\Users\hp\AppData\Local\LocalDevMCPBridge\forensics\20260827-1443-pre-restart`，gitignored 副本位于 `.ai-bridge/forensics/20260827-1443-pre-restart`。
- `manifest.csv` 记录用户指定 10 个文件的 bytes、UTC mtime 与 SHA-256，冻结发生在任何 restart/stop/日志清理之前。
- 脱敏分析确认 Gateway 8500 行日志中有 6 次真实 `/mcp` 502，p95 约 1688 ms、最大约 644875 ms；MCP audit、Cloudflare origin refused/unreachable、项目 supervisor 与 transport false-green 能形成跨层时间线。
- 证据仅保存哈希、脱敏字段与运行事实，不写入 bearer/token/credential 明文。

### 9.3 第二现场：17:24 post-restart forensic recovery

- 状态：**PASS（post-restart recovery，非 pre-restart freeze）**。10/10 指定文件恢复成功，missing=0、error=0；manifest SHA-256=`4ecd4e591952e30cf5b58394862157057b62b4f46a8e557827a24694053862c8`。
- 脱敏窗口事件 SHA-256=`1f521f1d0d7f9f2d8fcc4326057463fd507dd8652649eb0996e7a6286f68fa58`：09:25:06Z 与 09:25:26Z Gateway health 各约 5 秒 timeout，09:25:26Z restart，09:25:32Z restart_ok；窗口内没有 project supervisor restart，随后 `/mcp` 多次 401。
- 慢事件对拍 SHA-256=`fdcbedf59b5236a15156210d72db53eee212a997a22852c30c3e218e50f60c85`：09:10–09:27Z 共 19 个 Gateway `/mcp` ≥5 秒，其中一个约 489860 ms；MCP audit 同窗仅有 3 个约 5.2 秒成功 `wait_task`。
- 结论仅限证据支持范围：第二现场强烈指向 protocol-layer starvation / 长请求占用 / lifecycle race，并显示 restart 边界的 bearer 401；不能简化为纯 ChatGPT 平台问题。489 秒请求、旧 SSE ownership与 401 根因仍在 `开发计划.md`。

### 9.4 权威依据、架构裁决与 defect-first 红基线

- 状态：**PASS**。已按 2026-08-27 当前官方资料复核 MCP Tools/Tasks 与 stateless core、MCP Python/TypeScript SDK、OpenAI Developer mode/full MCP、HTTPX、Starlette、AnyIO、Uvicorn、Cloudflare Tunnel，并完成责任层边界裁决。
- 正式方案为“端到端连接治理”：真实 MCP canary + 稳定 Hub contract + OAuth/handle连续性 + 单 owner资源生命周期 + 分层局部自愈 + 有界普通工具；明确拒绝“遇到 502 就全局重启”。
- `tests/test_v089_connection_governance.py` 历史修复前曾 8/8失败，覆盖旧selftest、SSE tools/list误判0、AsyncClient未关闭、broker控制面误判、OAuth restart丢access、offline_access、stale handle回退与route不持久。
- 后续红点还包括：safe initialize恢复、stable 50-tool contract、healthz false-green、OAuth cap/prune、Bash active hard cap、CodexPro graceful HTTP lifecycle、READY只看日志/TCP；均在实现阶段有对应修复与专项回归，不以删测或放宽权限换绿。

## 10. OAuth restart continuity 与有界状态验收

- 状态：**PASS（targeted/full OAuth回归范围）**；正式 soak 与发布资产终验仍未执行。
- access token不持久化明文：使用 token SHA-256派生的 SecretsStore key保存 `client_id/scopes/expires_at/resource/subject`；provider重建后可验证未过期旧token。
- access index有可配置硬上限，过期/淘汰记录同步删除；authorization code与consent有TTL和条目上限，调用路径执行prune。
- refresh token保持单次旋转语义；expiry/revoke/resource mismatch继续fail-closed。
- discovery与Protected Resource metadata同时声明业务scope和`offline_access`；authorization阶段必须包含业务scope，`offline_access`不能单独进入consent；不请求offline_access的legacy客户端兼容。
- 已知scope红测修复后6项OAuth重点回归通过；随后`tests/test_oauth.py`全量发现legacy `ws-` affinity回归为1 failed/65 passed，修复兼容边界后为 **66 passed in 18.28s**。

## 11. Stable Hub Tool Contract 基础实现验收

- 状态：**PASS（代码与targeted contract基础范围）**；old/new client、项目启停、持续真实Connector终验仍留在计划。
- 新增 `src/local_dev_mcp_bridge/hub_tool_contract.py`，把隔离full-mode CodexPro的41-tool source contract作为受版本控制构建输入，再由Gateway注入9个Hub tool，公开contract固定为 **50 tools**。
- contract version=`1`；canonical Hub fingerprint=`489cd30e8e8676ad382c752bed7c46ba9bc2f308fa2d98b2a699793d0c5ee780`；41-tool source fingerprint=`ca573be849151a10c26c52c31f8a6517a9b51a51df04bd14c757d7d9aba35ffa`。两者用途不同，禁止用source fingerprint冒充public Hub fingerprint。
- Gateway模块加载时校验count=50、name唯一和fingerprint；`tools/list`直接返回stable contract，不再由任一bootstrap CodexPro项目的动态list决定。
- Gateway diagnostics已记录contract version/count/fingerprint，tools analyzer可区分tools_result、JSON-RPC error、malformed/streamed outcome，不把错误响应伪报成“0工具正常”。
- 本会话连接器公开可见工具数为50；同一时刻内部`codexpro(list_actions)`为37 actions，已按“公开Hub schema / 当前上游action inventory”分层记录，不混为同一指标。

## 12. call-stage permission gate 验收

- 状态：**PASS（现有基础与扩展targeted矩阵）**；remote/device与完整negative matrix终审仍留在计划。
- `tests/test_v089_policy.py`历史红基线为 `2 failed / 1 passed`：read_only mutation与workspace full-only错误到达upstream，system allow正常。
- Gateway现于local tool/upstream之前执行`_workspace_tool_policy_error`；`codexpro(action,args)`先unwrap，不能用supertool绕过；system允许，workspace对full-only返回`feature_unavailable`，read_only mutation返回`permission_denied`。
- remote device不套用本机project mode，由远端Gateway独立执行同层policy；CodexPro自身第二层enforcement保留。
- 修复后基础 **3/3 PASS**；durable记录的后续policy矩阵为 **8 passed**。被拒调用不通过放宽权限、动态删schema或删除测试实现。

## 13. deterministic workspace route persistence / rehydrate 基础验收

- 状态：**PASS（基础连续性、safe replay边界与现有并发实现）**；Hub local/durable落根、跨实例文件竞争、project deletion等终审仍留在计划。
- 新增 `routing_state.py`：`workspace-routes.json`版本化保存handle→project_id→canonical root→last_used，硬上限512，temp file + `os.replace`原子替换；加载时只接受active project内存在的canonical目录。
- 只有deterministic `ws_`跨restart持久化；旧格式`ws-...`仅保留有界进程内affinity，避免把非确定性handle错误持久化/重放。
- Gateway收到已知handle时先路由原project，再以8秒预算调用内部`open_workspace`；返回handle必须完全相同，否则fail-closed。
- rehydrate按 `(handle, project)` keyed single-flight并有inflight hard cap、done-callback清理；hydrated set、route maps均保持有界。
- Unknown/stale root不回退C:/D:/bootstrap。上游在首次调用返回Unknown workspace时，只读tool最多安全one-shot replay；有副作用tool只恢复上下文并明确要求用户重试，原调用绝不自动第二次执行。
- `tests/test_v089_rehydrate.py`多轮修复后通过；`tests/test_v089_policy.py + tests/test_v089_rehydrate.py`曾共同 **4 passed**；OAuth全量中发现并修复的legacy handle affinity回归最终纳入 **66 passed** 证据。

## 14. 真实MCP健康、自愈与生命周期基础实现验收

- 状态：**PASS（已实现专项范围）**；489秒/starvation、restart旧连接与30–60分钟fault soak仍留在计划。
- `selftest.py`不再调用退役alias；正式canary为 `initialize → tools/list → server_config`（无该tool时退到`open_current_workspace`），支持显式`devbridge_workspace_id`绑定当前项目，并把AnyIO `ExceptionGroup`展开为首个可行动根异常。
- `CodexProManager.wait_ready()`不再仅凭日志或TCP置READY：listener出现后必须在3秒预算内通过真实MCP canary；进程退出/timeout会清理probe credential并给出明确错误。
- `ProjectUnit.data_plane_health()`在authenticated `/healthz`校验root后，低频执行真实MCP canary；broker控制面异常不再在HTTP数据面之前直接误杀健康CodexPro。
- `ServiceCoordinator`每60秒低频执行project/local Gateway/public Gateway三层MCP probe：project健康而local Gateway失败只重启Gateway；local健康而public失败只重启Tunnel；Local模式仍测本机MCP但跳过公网。
- Gateway AsyncClient当前为单实例有界pool（64 connections / 16 keepalive / 5秒pool timeout），safe transport retry仅限`initialize/ping`；`tools/call`不盲retry。response与stream已有单owner close wrapper。
- CodexPro新增`httpLifecycle.ts`：SIGTERM/SIGINT只触发一次shutdown；先停止accept并在有界预算内等待活动 HTTP/SSE 排空，超时后`closeAllConnections`，随后才清理 MCP handler/runtime；cleanup 本身亦有有界超时，并区分成功/失败 exit code，避免在流仍持有会话状态时提前 dispose。
- 专项历史红点修复后，当前记录包括：Python治理targeted **54 passed / 12 warnings**；另一次重连复核 **45 passed / 3 warnings**；CodexPro `npm run build`、`analysis-smoke`、`async-task-smoke`、`http-lifecycle-smoke`均PASS。上述不是全量发布门，不能替代s7–s10。

## 15. Bash资源边界与scoped analysis第一阶段验收

- 状态：**PASS（当前 targeted 专项范围）**；analysis same-key single-flight/deadline/waiter cancel/inflight cap、`show_changes(include_diff=false/true)` 的 ENOBUFS 治理以及 Windows PATH-less 标准后台启动均已取得当前源码或实机明确退出码证据；全量质量门与30–60分钟资源/P95 soak 仍留在后续阶段。
- `BashTaskManager`新增active hard cap=16；达到上限时拒绝新任务，要求观察、取消或等待现有任务；完成任务仍按既有24小时/100条有界保留策略prune。
- Windows工作树源码在 PATH 缺失时从`SystemRoot/WINDIR`解析 Windows PowerShell、cmd、taskkill 绝对路径，并从`ProgramFiles`解析 PowerShell 7；当前已有标准后台 task 与 packaged-runtime smoke 的明确 exitCode=0 证据，但正式 Release 安装态仍须在 s12 重新验收。
- Windows-only async smoke与2026-08-28标准后台入口都曾真实复现`spawn powershell.exe ENOENT`红基线，历史失败事实保留。后续同一治理链已取得标准后台 task `2f9db668-7a13-4297-a3b5-59fd8189cf36` 的 53 passed/exitCode=0，以及 PATH-less packaged runtime task `140d93ae-e4eb-4f64-875b-bbbc222ab691` 的明确 exitCode=0；因此源码/当前 staging 专项门已重新验收，正式安装态仍归 s12。
- structured analysis第一阶段把`inventoryWorkspace`的root从固定`.`改为调用scope，并把normalized root纳入cache key；窄结构化search不再先扫描整个workspace。
- 历史analysis smoke红点为narrow root `inventoryFiles 20 != 2`；修复后`node scripts/analysis-smoke.mjs` PASS。cache原有最大8条保持不变，没有用无界Map换性能。

## 16. 本次事实源对齐验收

- 状态：**PASS（文档治理项）**。`AGENTS.md`新生命周期规则已真实落盘，SHA-256=`522495a00bfde55c5dc6c479b0e954a580004dec93d07ebb80a2d0b7ca783961`，不需要重复重写。
- `开发计划.md`已从旧的“目标架构+已完成施工记录”清理为只保留当前未完成/未验收事项；OAuth、基础rehydrate、policy gate、stable contract、v2 route state等完成事实不再作为待办悬挂。
- `进度验收.md`保持“v0.8.8正式基线 + v0.8.9已验收增量”口径；当前明确 s1–s7 已完成、s8 审查中、s9–s14 未完成，仍未把 full gate、soak、发布、正式安装或真实 ChatGPT 终验提前写为完成。
- 本会话最初把`run_command(cwd='.', devbridge_workspace_id=ecb6fea6)`的`git not a repository`误判为route失效；后续直接输出工作目录确认它实际正确命中DevBridge项目根`D:\`，只是仓库位于`D:\Environment\mcp`子目录，仓库短命令需显式使用`cwd=Environment\mcp`。历史上“多根无强route静默选择stable bootstrap”的源码缺陷已按§19修复并通过 targeted；后续 strict half-open、跨 Gateway/CodexPro restart、旧 bearer/handle、read-only one-shot recovery、mutation no-replay 与 `show_changes(include_diff=false/true)` 有界输出均已取得当前 revision 专项证据，剩余持续稳定性由 s10/s13 验收。

## 17. 第三现场（2026-08-27 20:18）取证与责任层裁定验收

- 状态：**PASS（post-restart forensic recovery 与分层裁定）**；OAuth restart连续性、长请求治理与正式安装版终验仍留在计划。
- 独立证据目录：`.ai-bridge/forensics/20260827-2018-session-failure`；明确标记为 post-restart recovery，未覆盖14:43第一现场或17:24第二现场。
- 指定10/10文件只读复制成功，missing=0；manifest SHA-256=`0a2043a4438c924cf4db8fb8ed1c5f8bd38d9b2a5fafde4536117551fe0cfc60`，sanitized events SHA-256=`8d5155726d7edb84b0704635cc77a5ea2db869c909b1516ea59a9d8efa03dd31`，supplemental analysis SHA-256=`f0f5ad94de195256e7f6fab8d12788ff2a4ff89aab8e62da563ff7d3a704ca47`，结论 SHA-256=`01538bafe295861149e28c7751f014cdf3bcd35a5eec265492f4e29a221d2f61`。
- 精确窗口确认请求已到达Gateway：20:13:52–20:15:44有22条真实`POST /mcp` 401，20:19:05–20:19:29又有4条401；同期`/health`和transport local/public probe持续200，精确窗口没有Gateway 502。
- 20:17–20:21的MCP audit仍有大量成功调用，窗口219条audit全部`success=true`；但普通search出现93.797/82.359/50.530秒，另有95.250/86.109/65.563秒search及53.125秒`show_changes`，证明数据面并非整体死亡，同时存在真实长请求压力。
- append-only对拍显示20:12和20:20各发生一次全栈启动：D:/C:两个CodexPro各新增两个listener启动块；Tunnel在20:12:03与20:20:09分别启动。20:20:42–47重新完成authorize/consent/token，20:23:17起35条`/mcp`恢复200。
- 七层结论：Project engine不是整体死亡主因但承受长请求/全栈restart；Gateway真实可达并在OAuth层401；Tunnel在20:18持续200不是主故障层；OAuth bearer continuity为高置信主根因；Hub schema无直接致因证据；workspace route是独立缺陷；ChatGPT旧session object未换用有效bearer且把401包装为ExceptionGroup，是高置信共同责任层。
- 证据边界已明确：Gateway缺少安全session/token fingerprint，无法把某条401与某个ChatGPT内部对象做密码学级关联；CodexPro process log无逐行时间；客户端内部错误映射不在本机。因此“旧bearer401→ExceptionGroup”为高置信推断，而非伪装成直接平台日志事实。
- 本项完成后已立即从`开发计划.md`删除；restart/old-bearer故障注入、错误文案、长请求治理、route修复和正式持续终验继续作为未完成项。

## 18. legacy `ws-` workspace-handle affinity 回归修复验收

- 状态：**PASS**；运行实例重载后的跨 Gateway/CodexPro restart、旧 session 与 durable/task affinity 持续验证仍是独立未完成项。
- 当前回归红点：`tests/test_oauth.py::test_v082_workspace_handle_affinity_survives_followup_without_path` 中，`open_workspace`已在Beta项目端口18788返回`ws-beta-child`，但后续无路径`show_changes(workspace_id=ws-beta-child)`错误路由到18787。
- 根因：`_remember_persistent_workspace_handle`在写任何handle affinity前统一要求opaque child root在Gateway本机存在；legacy `ws-...`只需要当前进程内project归属且不得持久化，因此映射被错误丢弃。
- 修复：严格区分两类handle。deterministic `ws_`仍必须通过active project containment与本地目录存在校验，才写root、持久route并标记hydrated；legacy `ws-`只在project registry仍有效时写有界`handle→project`内存affinity，即使opaque子根不可本地复核也不写path、不写`workspace-routes.json`、不进入hydrated set、不跨restart replay；任意其他handle或未知project忽略。
- 新增/强化回归：legacy bounded-map用例改用真实`ws-legacy-*`格式；新增不存在子根、未知project、任意handle、持久文件为空的负向断言。
- 当前验收：原失败用例 **1 passed**；legacy/concurrency专项 **7 passed / 4 warnings**；`rehydrate + concurrency + policy + contract` **41 passed / 38 warnings**；OAuth全量因Hub short-call 20秒硬上限按稳定collection拆为两片，各 **33 passed**，合计 **66/66 PASS**。
- 未削弱边界：legacy handle不写持久route、不执行rehydrate；deterministic `ws_`根失效仍fail-closed；side-effect no replay与call-stage policy矩阵继续绿色。
- 本修复已从`开发计划.md`的未完成步骤/验收项移除；计划只保留运行实例重载、跨 Gateway/CodexPro restart、旧 session、remote 与 side-effect no-replay 的持续验收。

## 19. 多根无强路由证据调用 fail-closed 验收

- 状态：**PASS（当前源码与 targeted 矩阵）**；当前运行 Gateway 重载、restart/fault injection 和正式安装版持续终验仍留在计划。
- 事实纠偏：`run_command(cwd='.', devbridge_workspace_id=ecb6fea6)` 实机输出为 `D:\`，证明显式 DevBridge project route 正确；此前 `git not a repository` 仅因项目根是整盘 D:、仓库位于 `D:\Environment\mcp` 子目录。CodexPro deterministic handle 的 `bash → wait_task` 当前也输出 `D:\Environment\mcp`。
- 真正红点：多根运行、全新 transport/session、无显式 route、无 path/cwd/task/workspace handle/credential evidence 时，`_effective_workspace` 会静默选 stable bootstrap，并把普通 `read` 与 Gateway-local `run_command` 送入 Alpha/C:。新增两项红测后基线为 **2 failed**。
- 修复：新增 `_WORKSPACE_CONTEXT_FREE_TOOLS`，仅 Hub 工作区/设备管理与 shell self-test 可以在多根下不依赖目标工作区；其余 `tools/call` 在 route inference 后仍无 workspace 且运行根数大于1时，在 upstream/runner 前返回 JSON-RPC `-32006`，明确要求绝对路径、有效 `workspace_id/task_id` 或 `devbridge_workspace_id`，禁止静默使用 bootstrap。
- 保留的兼容边界：显式 project route、绝对路径、existing relative path、task affinity、deterministic/legacy handle affinity、session/credential affinity、single-root fallback、Hub workspace/device管理工具均保持原语义；公开50-tool schema不因该检查改变。
- 红绿证据：`test_v081_stateless_route_hint_survives_transport_recreation` 与新 `test_v089_gateway_local_tool_requires_strong_route_in_multi_root` 从 **2 failed → 2 passed**；显式Beta route仍命中18788，无route时 upstream/runner调用计数不增加。
- 当前回归：OAuth文件新增后全量 **67/67 PASS**（按23+22+22三片执行）；`workspace_autoroute + multi_device + policy + contract` **62 passed / 1 skipped / 33 warnings**；`gateway.py + test_oauth.py` Ruff **All checks passed**。
- 当前源码 SHA：`gateway.py=272a088be28271fc91990834d56d11fc1f6ed3bdee27918ef554a05b56b6daf7`，`tests/test_oauth.py=9fdf638b38cb8b8a74951e54c880ad03ca8994198b94aa1462fa17869854cd3d`。
- 本项源码修复已迁入验收；`开发计划.md` 仅保留当前运行实例加载，以及 Gateway/CodexPro restart、双实例窗口下的 route/hydrated/inflight 与 task/durable owner连续性、project deletion/root invalidation、remote及side-effect no-replay持续验证。

## 20. v2 原子 workspace route state 与失效清理验收

- 状态：**PASS（当前源码、并发/失效 targeted 与当前真实 durable chain）**；Gateway/CodexPro restart、旧session和正式安装版终验仍留在计划。
- defect-first红基线：两个Gateway以陈旧snapshot依次写不同handle时，整文件覆盖丢失先写route；root删除和project registry失效时只返回错误，内存、hydrated set与磁盘仍保留stale route。新增四项断言后为 **4 failed / 5 passed**。
- 持久层修复：`workspace-routes.json`升级为v2并兼容读取v1；同进程`RLock`、Windows byte-range lock/POSIX `flock`跨进程串行化；锁内读取最新状态后执行handle级upsert与`removed_handles`显式删除；PID/thread/UUID唯一temp、`fsync`与`os.replace`原子发布。
- 状态边界：route最多512条，removal tombstone最多512条。tombstone阻止短暂双实例/旧Gateway用更早`last_used`复活已删除route；真实更晚的重新open可清除tombstone并恢复handle。LRU eviction、invalid root、project deletion均同步删除handle→project/path、route record与hydrated标记，并原子写入removal。
- Gateway兼容：legacy `ws-`仍只保留进程内project affinity，不写route/tombstone；显式stale `ws_`或`ws-`均fail-closed，不降级bootstrap；deterministic rehydrate、read-only one-shot replay及side-effect no replay边界不变。
- 红绿证据：首轮四项红测修复为 **9/9 PASS**；继续新增stale resurrection与32-thread writer压力后，tombstone红点被修复，专项最终 **11/11 PASS**，32个route全部保留且无`workspace-routes.json.*.tmp`残留。
- 组合回归：`rehydrate + concurrency + policy + contract + workspace_autoroute` **67 passed / 1 skipped / 42 warnings**；OAuth全量 **67/67 PASS**（23+22+22）；Ruff **All checks passed**；targeted Pyright **0 errors / 0 warnings**。
- 当前真实链复验：`bash(workspace_id=ws_1d45..., long_run_id=lr_mtb6w0ih_a803aa0ad300, step=s6)`成功附着并在170ms完成，root/stdout均为`D:\Environment\mcp`；随后`long_run_status`确认task `2aea3856-...`属于唯一run且status=completed，workRevision升至52。
- 当前源码SHA：`routing_state.py=5feaee836cbe9d42eed2d216e229015380201a2e48b6960d7a8cba00371d0274`，`gateway.py=811df61829bdb1e50c8236517fd06e7d7b38df33f8906bf42dc0f2723dbca2fb`，`test_v089_rehydrate_concurrency.py=e85a4a0be5da871246a03f611372f4e328c97904006d3bf08ea0cac578dcbfd9`。
- 当前架构文档已同步v2 route/tombstone/锁/原子merge真实实现；计划只保留运行实例重载与跨restart/old-session/remote/side-effect持续验收。

## 21. 第四现场（2026-08-27 21:58）即时故障恢复与分层裁定验收

- 状态：**PASS（immediate post-failure recovery 与分层裁定）**；错误路由根因修复、运行实例重载、客户端/session 持续终验仍留在 `开发计划.md`。
- 独立证据目录：`.ai-bridge/forensics/20260827-215851-session-failure`。README SHA-256=`9ec6e25438eba6aec9ceec495a5a32419621c603ce9b9766926360e17800a0bf`；manifest SHA-256=`62674e5f0c13f7f9cc709368919f9ae1c5c77d1fc2bfc3c7142a3071cad02fe8`；11项运行文件复制成功，SecretsStore仅记录metadata/hash且未复制内容。
- 精确窗口为本地21:56:30–22:02:00、UTC 13:56:30–14:02:00。Gateway `/mcp` 共32条，全部HTTP 200；min=46 ms、p50=210.5 ms、p95=671 ms、max=1765 ms，无401/502/≥5秒请求。同期四次local/public transport probe均200且`degraded=false`；MCP audit 30/30记录`success=true`。
- 直接本地缺陷证据：21:58:23.761，`read(path="tests/test_v089_deadlines.py", workspace_id="ws_1d45a4204ebfa51587df4f20")`被审计为`workspace="C:\\"`；21:58:31显式重新打开`D:\\Environment\\mcp`后，21:58:42同一handle/同一路径才恢复审计为`workspace="D:\\"`。因此不能用HTTP 200或审计success掩盖workspace route错误。
- 冻结的`gateway_workspace_binding.json`仍把deterministic open root指向`D:\\Environment\\mcp`/project `ecb6fea6`，其SHA-256=`4c8527cf8b30dba5e5a1dd470357be3d24f5da8d815c9b268d4545a61847ca27`。证据支持运行进程内route/affinity precedence、旧session软状态、restart状态重建或运行实例未加载v2代码中的至少一项存在缺陷；不支持“持久绑定已永久改写到C根”的结论。
- 用户侧同时看到`ExceptionGroup: unhandled errors in a TaskGroup`，但本地精确窗口没有该异常且全部HTTP/audit为200/success。故ChatGPT Connector/session的响应交付或TaskGroup包装是高置信共同责任层，但因缺少客户端内部日志，未伪称为已直接证明的平台根因。
- 脱敏分析SHA-256=`5912979b90309285cd905bbbedee75a7caa738e1926fe1c54a6fe7c7290a7623`；分层结论SHA-256=`01f214ba2a27b70029aa0fc0aad4257ec7d7b4e9cd3f04dda188fc7f8484f4d8`。
- 与前三现场合并后的裁定：至少存在两类可独立发生的机制——(1) starvation/lifecycle/restart/OAuth断裂；(2) workspace route/affinity漂移与客户端session/response lifecycle失效。后续必须分别修复并在fault soak中联合验证。

## 22. 第五现场（2026-08-27 22:32）普通调用无界与取消失效裁定验收

- 状态：**PASS（未重启即时冻结、精确请求关联、运行态版本裁定）**；deadline/cancel/structured search/runtime reload 修复仍留在 `开发计划.md`，不得据此宣称连接治理完成。
- 独立证据目录：`.ai-bridge/forensics/20260827-223230-session-failure`。README SHA-256=`d23dfbde115e6b00f93f581ceaf5807c81aaec5ed14910a38af9eea36d471454`；manifest SHA-256=`f1e777291564590e80b3ce5e897f9e5a38311adf65051757beac9cde9fe73dd5`；13项中11项复制，`workspace-routes.json`不存在，SecretsStore仅记录metadata/hash且未复制内容。整个冻结过程未重启任何层。
- 精确关联到本会话失败请求：`openai-mcp/1.0.0`调用`search(path='.', query='mcp-devbridge-hub', intent='text', max_results=200, workspace_id=ws_1d45...)`，ChatGPT侧返回`ExceptionGroup`；本地MCP audit却在22:31:47.582记录duration=125984 ms、workspace=`D:\\`、success=true，对应Gateway HTTP 200/126015 ms。
- 直接裁定：客户端失败/放弃后本地请求仍继续约126秒；运行实例未执行工作树中的45秒普通调用deadline，downstream cancellation没有终止upstream，terminal audit把迟到完成误记为客户端成功。
- 同窗口另有相同query/max_results=100的119406 ms search、`project_manager.py` symbol search 108875 ms、`show_changes(include_diff=false)` 41516 ms；14次local/public probe全部200且`degraded=false`。因此shallow health继续构成假绿。
- 运行快照SHA-256=`56a47e233cf87cecf52650d717983d45473ecae7b47ec03806eb0a84d91a06b7`：`MCPDevBridge.exe` PID10460于13:56:39Z启动并监听8786；D:项目child PID39488于13:56:51Z执行正式安装目录`_internal/third_party/codexpro/dist/http.js --root D:\\ --port 8788`。仓库`gateway.py`和`server.ts`均在这些进程启动后继续修改，且src新于dist，证明真实运行链未加载当前worktree修复。
- 正式配置目录只有旧`gateway_workspace_binding.json`（SHA-256=`4c8527cf8b30dba5e5a1dd470357be3d24f5da8d815c9b268d4545a61847ca27`），没有v2 `workspace-routes.json`；当前正式运行态仍是旧路由/生命周期实现。
- 与第四现场区分：本次精确search正确审计到D:\，因此第五现场隔离出deadline/cancel/runtime-staleness机制，而非wrong-root机制。聚合窗口的401/测试流量未被错误归因给该精确请求。
- 脱敏分析SHA-256=`48827cc7383ae6397a7ab358435c70c58b8a5908769444d5ec734087e02f8f2a`；分层结论SHA-256=`a2f41fb4ff6216d648fde217ca47c9f14f7852df8c7d44512acff7da3e053642`。

## 23. 分类总 deadline、SSE 终态审计与 response ownership 验收

- 状态：**PASS（当前工作树源码与 targeted 组合矩阵）**；正式运行实例重载、下游取消、half-open、restart drain 与 restart 后 bearer 连续性仍在 `开发计划.md`。
- defect-first 红基线一：streaming/connection-governance/rehydrate 组合回归为 **40 passed / 1 failed**；SSE 已超过总 deadline 并发出协议错误，但 Gateway 在收到响应头时就把工具审计为 `success=true`，形成 false-green。
- defect-first 红基线二：`initialize` 的 one-shot `application/json` stream 已由通用缓冲分支消费并关闭，特殊 identity rewrite 分支再次读取同一 stream，触发 `upstream stream was consumed more than once`。
- defect-first 红基线三：带真实 project route 的 `open_workspace` 无 `Content-Length` SSE 响应体在 30ms 预算下仍实际等待约 **219ms**，证明特殊工具分支绕过总 deadline。
- 修复：按 JSON-RPC/tool class 计算一个绝对 `deadline_at` 并贯穿 response headers、buffer read、SSE/raw stream 与特殊工具响应体；控制面15秒、普通工具45秒，`wait_task`/`long_run_status`按显式预算加10秒grace；buffered body硬上限8 MiB。SSE/raw stream维持单 owner，finally 取消producer并关闭upstream；审计用once-only terminal callback，只在 completed/deadline/downstream-cancel/error终态记录。
- response ownership：`initialize/tools-list`复用已缓冲 one-shot payload；`open_workspace/bash`在未缓冲时使用同一 absolute deadline 与8 MiB上限，timeout返回JSON-RPC `-32008`/HTTP 504，oversize返回`-32009`，不再double read/double close。safe HTTP retry仍仅限`initialize/ping`，`tools/call`不盲重试。
- 红绿证据：deadline专项最终 **7 passed / 7 warnings**；`OAuth + deadline + Gateway streaming + connection governance + rehydrate + rehydrate concurrency + policy + contract`组合任务 `7c3d3f51-6375-4048-8eff-eac3d4d479b2` 为 **144 passed / 48 warnings / 37.65s**；`gateway.py + test_v089_deadlines.py` Ruff **All checks passed**。
- 当前源码SHA：`gateway.py=d7800b596404e15302f8ec7ed78be11608310d4f97db57d9e08c1fc1b92f79b0`；`tests/test_v089_deadlines.py=bef908f1686a4dfaabb7b8dc9bee68560b5456c0b93b988e75ce7deb1a32967c`。
- 本项已从`开发计划.md`移除为源码待办；计划仅保留运行态加载与尚未验收的cancel/half-open/restart/OAuth连续性边界。

## 24. 一键连接诊断与普通用户分层文案验收

- 状态：**PASS（当前工作树 UI / selftest / targeted 矩阵）**；正式安装版与真实 ChatGPT 持续终验仍留在后续发布门。
- `selftest.py` 继续只使用正式 production canary（`server_config`，兼容 `open_current_workspace`），不恢复 `get_workspace_info/get_capabilities/list_directory` 等退役 alias；新增 Streamable HTTP 层、实际 tools/list 数量、canonical schema fingerprint 与可选 fixed Hub contract 校验。
- 固定 Hub 诊断使用当前公开 contract：exactly **50 tools**、version=`1`、fingerprint=`489cd30e8e8676ad382c752bed7c46ba9bc2f308fa2d98b2a699793d0c5ee780`。legacy `server_main` 仅暴露37个旧工具且缺正式只读 canary时明确 fail-closed，不伪报健康。
- 桌面“诊断”按当前 project id 显式绑定真实 MCP 调用，分层显示：项目引擎、本机连接服务、公网入口、MCP initialize、公开工具数量、Schema指纹、只读调用、流式连接、访问授权、工作区连续性、最近自动恢复、ChatGPT会话边界。普通文案不展示raw JSON、PID、TaskGroup/ExceptionGroup或凭据。
- 本机与公网真实 MCP 均绿时，session边界明确提示：若当前 ChatGPT 仍整体失效，应使用新会话继续，已保存的授权与工作区不会因此丢失；只要本机或公网任一层为红，就保持“未判定”，不会把本地故障错误归因给ChatGPT会话对象。
- `ServiceCoordinator.recovery_snapshot()`只暴露最近本机/公网恢复距今秒数和有界失败计数，不包含凭据/PID；UI可据此显示最近自动恢复层。
- 红绿/回归证据：`tests/test_selftest.py + tests/test_v089_connection_governance.py` **20 passed / 3 warnings / 15.18s**；加入桌面与app_state组合为 **42 passed / 3 warnings / 17.42s**；`tests/test_phase12_desktop.py`新增本机READY、public schema mismatch、缺访问授权三类诊断用例后最终 **8 passed / 9.16s**；相关 Ruff **All checks passed**，三份Python文件`py_compile`通过。
- 实施中曾发现两次 zero-context patch 被底层补丁器错误追加到文件尾；均在语法/结构门前被 SHA 前置校验与 `py_compile` 拦截，已原子移回正确类/函数作用域并确认文件尾干净，没有把机械污染计入完成状态。
- 本项已从`开发计划.md`删除；剩余计划只保留真实运行态 restart/cancel/half-open、structured analysis资源治理、Targeted/Review/Full Gate、soak、Release、本机升级与持续ChatGPT终验。

## 25. 第六现场（2026-08-27 23:29–23:34）长分析请求与客户端晚到分裂验收

- 状态：**PASS（现场冻结与根因裁定）**；analysis single-flight/cap/deadline、运行实例重载与客户端持续终验仍留在`开发计划.md`。
- 现场目录：`.ai-bridge/forensics/20260827-233422-session-failure`。README SHA-256=`bbc4e1a1b07803a44918bbd417f4df2c6a7f46b83efc62f1a460306c08f9b02e`；manifest SHA-256=`0b3c7d34f9fbdab3f6aaac91f20389a7834f59fd34ce733e80367ad14c570ea5`；13项运行文件登记，SecretsStore只记录metadata/hash且未复制内容。冻结前未重启、未显式reopen。
- 当前会话三次真实调用返回`ExceptionGroup`：`search(gitDiffShortStat)`、现场`run_command`、`search(forensics)`。其中首个search在MCP audit继续运行138,702 ms、Gateway 138,938 ms后才记HTTP 200；`search(forensics)`继续运行200,031 ms、Gateway 200,062 ms后才记200。证明`success=true`/最终200只是失去客户端owner后的晚到结果，不能代表用户请求成功。
- 同窗口另有`show_changes` 115,187 ms与229,655 ms，多次全工作区search为148,875–238,405 ms；并发昂贵调用与Connector请求生命周期不匹配是本次直接故障机制。
- 源码裁定：`analysis/index.ts`虽已有`MAX_ANALYSIS_INFLIGHT`、`analysisInflight`、deadline常量和`awaitSharedAnalysis`声明，但`inspectWorkspace/searchWorkspaceStructured`尚未接线，实际没有same-key single-flight、不同key cap、总deadline或AbortSignal终止。当前SHA-256=`549cd0ab966113109050036acc4954db57cb5827c6a6dd3bbdf66dedbae497a3`。
- `gitOps.ts`当前工作树已把stats-only改为`git diff --shortstat`和64 KiB maxBuffer（SHA-256=`737bd9e60629c9a285125945e243b16cf23e3088956d8910fe2c7d706d9d3cdf`），但运行CodexPro尚未重载当前build，故不能仅凭源码判完成；同步`spawnSync`和include_diff change analysis仍需并发边界审查。
- 现场周期`/health`在15:34:20Z/15:34:21Z仍间歇200，而紧随其后的两次no-proxy loopback直连均3秒超时且未形成完成日志；裁定为间歇排队/饱和，不是持续Gateway宕机，也再次证明shallow health不足以代表真实MCP可用。
- workspace route均审计在`D:\`，本次未把第四现场的D→C漂移作为主因；少量其它401和Tunnel周期探针也不解释两个最终200的长search。
- 脱敏分析SHA-256=`c492209dbc704d733b28c9f712435c9a8b20091d8efb09436e0db711284c88aa`；分层结论SHA-256=`ec9f893fc83fb135ae4b1294fd004eed488c349b512577d611a5c349b7bf6108`。

## 26. s10 r3 失败现场、Tunnel DNS 故障与并行审查边界验收

- 状态：**PASS（仅现场冻结、根因分层与并行所有权裁定）**；route/Tunnel/harness 产品修复、targeted/full gate 与正式 r4+ soak 均未通过，不得据此进入发布。
- r3 正式编排证据：`.ai-bridge/forensics/20260829-s10-orchestrator-r3/orchestrator-result.json` SHA-256=`9266c8732f4468a06f79e618bc7a2b48f6e2d7fdbec59d367ccc0bb7e5800c90`，记录 2026-08-28 21:35:04Z–22:37:09Z，最终 `ok=false`；soak events SHA-256=`c384cd938bcf64de26b9f578ef88c423dccffb96c17ea3e14a485f6fa84b1399`。
- 责任层隔离：D: CodexPro 由 PID 22508 局部替换为 29316；Gateway PID 8884、C: PID 37428 与当时 Tunnel PID 38888 未因该项目故障被一并重启。service supervisor 在 21:40:26Z 记录 project restart，21:40:29Z `project_restart_ok`，21:40:40Z 起真实 project MCP probe 已恢复。
- old-handle 直接失败：项目短暂离线后，local/public matrix 均持续返回“workspace 上下文已失效且没有可恢复的路由记录”；这证明 transient project unready 被错误提升为持久 route 删除语义。该结论不适用于真实 project deletion/root invalidation，后者仍必须 fail-closed。
- 51 分钟调用分层：harness 在 21:45:57Z 后把一次公网 `read` 记为约 `3,070,930 ms`，但 Gateway 精确窗口没有对应 45 秒 `upstream_deadline_exceeded` 或请求终态，且同期 supervisor 的 local/public MCP probe 持续成功。因而当前证据只能证明**公网客户端/SDK 调用及取消清理的完整 owner 链没有硬 wall-clock 收敛**；不能在缺少 Gateway request-start 证据时，把全部 51 分钟直接归因成 Gateway 上游 deadline 失效。Track B 必须用独立进程/owner watchdog 消除该 false attribution 与无限取消等待。
- 取证摘要：`.ai-bridge/forensics/20260829-s10-r3-failure-analysis/summary.json` SHA-256=`3ae827eaa2a1b4974e066c35afb8a694a868622decf1c025c82edce3bd667d59`；selected events SHA-256=`e876f5eadc9a9f7aed4b82069eefc6e834d1a29ae8f46bcc41d5cda9e31413ff`；窄窗口 summary SHA-256=`780224175bd91ae09f63121449c35d2b1d7c67168ad990026d11065ae4f61083`。
- 06:37 Tunnel 现场已独立冻结：`.ai-bridge/forensics/20260829-0637-tunnel-failure/manifest.json` SHA-256=`e8f73bd34fa9a8b7951dc3dea0266d837b0cf3a9152f20386499b43032795233`；其中 `tunnel.log` SHA-256=`3f23a3519cb4eddbfee531be565249f11404310d9ab9bcb6148c40112bc9fc87`、network snapshot SHA-256=`60e5218406b01e16465d0b42cadee57a6d2d1cb2eb01042268007cbae49cfaf8`。证据支持瞬时 DNS/Cloudflare API 不可达与 Tunnel 启动一次失败，产品仍需有界 retry 且不得跨层停止已启动项目。
- 并行治理：Track A 暂时独占产品 route/rehydrate、Tunnel retry/flight recorder 接线及直接 targeted tests；Track B 独占 soak harness/证据编排。首次尝试写 `开发计划.md` 时 SHA 冲突被安全拒绝，随后重读合并，证明 SHA 前置并发保护生效且没有覆盖另一会话成果。

## 27. Flight Recorder 独立模块 P0 接管与验收

- 状态：**PASS（模块契约与独立测试）**；Gateway/ServiceCoordinator 接线、持续探针与正式 soak 仍由 s10 后续门验收，不因本项通过而提前完成。
- 架构审查在 Track A checkpoint 中发现 `src/local_dev_mcp_bridge/flight_recorder.py:106` 的真实 `SyntaxError: unterminated string literal`；由于 `gateway.py` 已导入该模块，属于源码不可启动的 P0。接管前已在 `.ai-bridge/agent-status.md` 与 `开发计划.md` 记录所有权和失败终态，未覆盖 Track A 其它模块。
- 修复采用 SHA-guarded 字节级替换，只改三处被真实 CR/LF 写穿的字符串；修复后 `flight_recorder.py + gateway.py` `py_compile` exitCode=0。随后强化自由文本脱敏：除共享 `redact_line` 外，再对任意非空白 Bearer 值与 key/token/secret/password/auth 赋值做严格替换，覆盖 `~ + / =` 等合法 token 字符，身份/root/path字段只保存16位 SHA-256摘要。
- 状态与资源边界：active request默认上限512、terminal去重记录上限2048；请求 `started/context/stage/terminal` 单一终态；磁盘按日JSONL、8 MB默认上限、最多当前+一个轮转文件、7天默认留存；单行过大写受限fallback；所有写失败fail-open，不影响MCP热路径。
- defect-first证据：新增 `tests/test_v089_flight_recorder.py`。首轮 task `0561f942-7c1d-4c22-8adf-59e3ce4ae913` 为 **2 failed / 3 passed**，真实暴露自由文本 Bearer 漏脱敏与轮转测试断言过强；修复/校正后 task `9b163311-bd1d-4252-9827-9d2a0e8a968a` 为 **5 passed**。与 Track A 原 `tests/test_flight_recorder.py` 合跑 task `4491c81f-4b1e-4b04-aade-3d94eb58219c` 为 **7 passed**。
- 并发复审：上述首次 PASS 后，Track A 又修改模块以增强复合敏感键识别与 `_path/_root/_project_id/_task_id` 等后缀哈希，旧 PASS 因 work revision变化立即失效；本架构会话未沿用旧结论。最新 revision 重新 `py_compile flight_recorder.py + gateway.py + app_state.py` exitCode=0，组合 task `63cfd789-4aad-4d29-a934-3ffc55af281b` 为 **8 passed**。
- 静态门：Ruff 首轮 task `67e955cb-490e-4024-a746-46e727c22028` 因 `datetime.timezone.utc` 规则失败；最小改为 `datetime.UTC` 后 task `1a0ca527-13a4-413c-bfd5-7676a68469ea` **All checks passed**。最新 revision 再次由 task `7f90c8ba-cfeb-4049-9fb4-5ed33808dfd6` 验证 **All checks passed**；Pyright 最新 task `354655e2-d588-4489-8b6b-6353f7146b92` 为 **0 errors / 0 warnings**。
- 最终 SHA-256：`flight_recorder.py=369e1128f416e40f2d77f0f58fcc6e5b0221776e2bedf907ae159527645d6770`；`tests/test_flight_recorder.py=b567b7918abde1eb99b3c6f83559e8ac34ff2014f6110990234a53dd599ce96c`；`tests/test_v089_flight_recorder.py=d179050d241907709bcde82e0ca1e7055107a954ebe02663efa84b95ca320029`。

## 28. s10 soak/fault harness 硬截止与证据防假绿验收

- 状态：**PASS（harness 源码、独立回归与静态门）**；当前 Track A 最新 staging 的真实 preflight、30–60 分钟 r4+ 正式 soak 仍未执行，本项不代表 s10 完成。
- 根因：r3 的一次公网 `read` 在客户端/harness 侧停留约 3,070,930 ms，原 harness 仅依赖协作式 `asyncio.wait_for`；故障阶段又把任意错误一概视为 expected，`ExceptionGroup` 未进入 summary，缺失 operation/signal/probe 证据仍有假绿空间。
- 实现：每个 matrix 与 downstream-cancel probe 在 Windows `spawn` 子进程中运行；子进程内每个操作还有独立 hard wall timer，协作取消卡死时写受限终态后以明确 `exitCode=124` 退出。父进程按 wall clock 等待，超时后通过 psutil 终止递归子进程树并记录 `worker_terminal`，不再让 SDK context-manager 清理无限拖住整轮 soak。
- 故障分类：401、TaskGroup/ExceptionGroup、永久 workspace route 丢失、duplicate side effect 与 hard timeout 永远不可被标为 expected；即使调用方传 `explicit=True` 也不能绕过。project/Gateway/Tunnel fault 只对责任层内、匹配的临时网络/未就绪错误放行。
- 证据契约：summary 对零 operation、缺少 fresh local/public signal、缺少资源样本、缺少 fresh Gateway/Tunnel probe、hard timeout、unexpected failure/cycle error、schema漂移与 mutation counter不一致全部 fail-closed。matrix success 另保存带 `fault_phase + fault_started_at_utc` 的哈希化历史；formal D restart 必须看到故障 epoch 之后 local/public 两侧都真实成功，不能复用故障前覆盖式信号文件。
- defect-first链：初始 task `c7827c22-291f-4ff7-9b6c-803bbc1a0292` 为 **3 errors**，确认缺少精细分类、ExceptionGroup计数和进程级硬截止；Track A 并行 checkpoint 后 task `19abbf8d-716d-4476-aa2d-b0573508b6de` 为 **5/5 PASS**。本架构审查继续发现 `explicit=True` 绕过，task `95974220-b5f5-4e21-920f-a982af86bcdb` 为 **1 failed / 5 passed**；修复后 task `e5c4b1a0-63e5-4934-a9ad-5c7b20f4193a` 为 **6/6 PASS**。补 fault-epoch 契约后最终 task `e8d4c47b-a7aa-4fd6-bbb3-7cf0d546732b` 为 **7/7 PASS**。
- 静态门：`py_compile .ai-bridge/v089_soak.py + test_v089_soak_harness.py` exitCode=0；Ruff task `9a9d7d63-bcd3-47d2-b1e4-e4288b81cf24` **All checks passed**；Pyright task `c72e7571-5fd7-4621-911e-bf2de1ebd304` 为 **0 errors / 0 warnings**。
- 最终 SHA-256：`.ai-bridge/v089_soak.py=aebda64a69357786d6b17f2a38a9ebc6ef4e6ef073d705914b34c32741bba6d2`；`.ai-bridge/test_v089_soak_harness.py=3cb2e86157a65aad70c6389ae34be58f46b36f6118223b911a8db395bee7a277`。

## 29. transient route 恢复与 Tunnel 可取消重试审查闭环

- 状态：**PASS（当前源码 targeted + 静态门）**；正式 staging/live fault preflight 与持续 soak 仍是后续阻断门。
- Tunnel defect-first：新增 DNS `host not found` 与 stop-interrupt 两项红测后，task `efa3823d-102d-464d-9b56-365be7191051` 为 **2 failed / 19 passed**：宽泛 `not found` 被误归 `configuration_auth`，且 `time.sleep`/生命周期锁让 stop 阻塞约1.2秒。Track A 在本架构会话下补丁前完成同一最小修复，故未发生覆盖：永久标记收窄，DNS lookup/host-not-found归 transient；独立 `_tunnel_retry_stop` 在 `stop()` 等待生命周期锁前置位，退避改用可中断 `Event.wait()`。
- Tunnel 最新证据：task `01b40bd1-ce0d-45d8-8983-50b6462d4e25` 为 **21 passed**；Ruff task `ab7f23ea-bd04-406b-a539-a93f2f9b17a8` PASS；Pyright task `a457aa4b-458d-4bb0-83fd-bf8e4af5071f` 为 **0 errors / 0 warnings**。`app_state.py` SHA-256=`5deb4a7d212cc87a733755fe0e90277c98e0bd221f257a0bb73f8bd74bd4a260`，`test_app_state.py` SHA-256=`b5723761b8450bd1906e5da40f13bf4a56a40d2292ab606a6a533f3a0f4adc8d`。
- Route 独立端到端审查：新增 `tests/test_v089_transient_route_recovery.py`，不改 Track A 原测试文件。真实 ASGI 请求链验证：项目暂时非 READY 时返回502且 route 仍在内存/磁盘；恢复 READY 后 old handle 执行 `show_changes → open_workspace → show_changes` one-shot只读恢复；再次模拟上游重启后 mutation 序列严格为 `write → open_workspace`，原写不自动重放；项目真实删除后 route/tombstone 边界仍 fail-closed并清空。
- Route证据：独立 task `2a49c2f9-0124-452f-9a4b-f760a50cfff0` **1 passed**；与既有 rehydrate/concurrency 合跑 task `2bbb133a-abbb-4828-b9eb-428342b8c1f7` **14 passed**；Ruff task `55a09e58-82ee-48f7-a3d5-c5499cec5e75` PASS；Pyright task `fcfbfa87-0c1c-4514-848b-429f7836f053` 为 **0 errors / 0 warnings**。
- 最终 SHA-256：`gateway.py=84a7082fa5c5097707c487ae233255646af42ed4bed8401482564e53c91c98dd`；`test_v089_transient_route_recovery.py=b9a1418014a4363eb54eeff032cd18f0e4af4c56f5ff5c2396e61b2e4644a2db`。
- 最新跨特性交叉回归：task `bebd6b78-c0e8-40fa-bc28-c978cf3df59a` 将 `app_state + flight recorder + route/rehydrate + deadline + connection governance` 八组文件合跑为 **69 passed / exitCode=0**，证明上述 targeted 修复在当前 revision 未互相破坏；该证据仍不能替代最新 staging/live preflight、full gate 或正式 r4+ soak。

## 30. Linux 发布版本 fail-closed 门验收

- 状态：**PASS（源码/专项门）**；最新 revision 的 Windows/Linux 正式构建仍属于后续 full/release gate。
- `scripts/check_release_version.py` 统一核对 `pyproject.toml`、package `__version__`、PyInstaller `PROJECT_VERSION`、Inno `MyAppVersion` 与 `uv.lock` 中项目版本；缺失、格式错误或任一 mismatch 都以 exit 2 fail-closed。
- `scripts/build_linux.sh` 在 `prepare_runtime_linux.sh`、`npm ci`、venv Python 检查和任何构建副作用之前执行该版本门；因此不能再用任意命令行版本生成“文件名 0.8.9、运行态仍是其它版本”的 Linux tarball。
- 当前专项 task `79131e4d-c9be-4695-88e0-2cf2ad74fac6`：**9 passed / exitCode=0**。
- 当前 SHA-256：`build_linux.sh=402fbbb68251c363301f2729ecd983c880030895be9481df013fcda88d95edc5`；`check_release_version.py=9cda7aa22d8ae33a52057ebae708f04f3f7ed77c39b6fd87aeec7c0c3d96f550`；`test_release_version_gate.py=9ef0bb3c3804aab4924b6adb6273e3593b41a752c6179d2b02e34195fe144a1a`。
- 现存 08:28Z 的 0.8.9 Windows/Linux 资产仍是过期候选，不因版本门通过而恢复发布资格；必须在最新源码 revision 重新构建。

## 31. Flight recorder 持续探针完整性 P1 验收

- 状态：**PASS（当前源码、专项/交叉回归与静态门）**；真实 staging/live 取证质量继续由 s10 preflight/soak 验证。
- 架构复审发现原 recorder 虽已有墙上时间 + `mono_ns`、有界请求生命周期、脱敏和轮转，但周期快照没有显式 Gateway/项目 listener、Tunnel 最后退出码、最近 local/public 成功时间和最近脱敏 Tunnel 输出；HTTP 终态也不能直接区分 MCP SSE 与 OAuth。新增独立红测 task `f7e7969c-375a-4350-a6d0-3c9da44823c9` 明确为 **3 failed**，分别命中这三个缺口。
- 修复后 `TunnelManager` 保留当前/最后 PID 与退出码，即使 readiness 失败后 `_proc` 被清理也可取证；`ServiceCoordinator` 周期快照记录 Gateway PID/port/listener、Tunnel PID/exit code/最近 8 行经 recorder 二次脱敏的输出、local/public 最近成功墙上时间与单调时间、失败计数，以及最多32个项目的 PID/port/listener。项目最近成功状态内部硬上限64，不形成无界 Map；listener TCP 探针使用 50ms 超时。
- Gateway ASGI recorder 在 request start/stage/terminal 记录 `component=http|mcp|oauth`，OAuth 覆盖 authorize/token/register/revoke/consent/well-known；终态记录 `content_type` 与 `streaming`，因此断连后可以直接区分普通 HTTP、MCP SSE 与 OAuth 请求，不保存正文、Header/token/cookie。
- 回归链：初次修复组合 task `1ca5f464-a8ab-4129-868e-d04f82887ecf` 为 **31 passed / 1 failed**，唯一失败是新测试夹具未将 Coordinator 置 READY，未放宽断言；修正夹具后 task `4165739c-6637-41ac-ad9a-36189ba50985` 为 **32 passed**。跨 `route/rehydrate/deadline/connection-governance` task `c040b421-f87b-408e-9504-143ad74c0a93` 为 **72 passed / exitCode=0**。
- 静态门：Pyright 首轮 task `5b10980f-c98b-405e-adbe-b980acd03a13` 抓到 7 个新增类型边界错误并保持 FAIL；收紧 `dict[str, object]` 转换与测试 callable 后 task `4510d046-0b29-483c-bd4c-1d87e64ea285` 为 **0 errors / 0 warnings**。Ruff task `97e1472a-aa75-47f3-a039-c507b129ed98` PASS；最终专项 task `944be9e5-1d9e-4437-b581-af91e833467b` 为 **32 passed**。PowerShell 5.1 不支持 `&&` 导致 task `43f30fbb-fc21-4e17-b28f-4bf9334ebbb1` 在测试执行前 parser FAIL，已拆成独立任务且没有伪报产品 PASS。
- `flight_recorder.py` 本体未再次修改，继续保持 §27 已审 SHA；本次只补接线/进程诊断。最终 SHA-256：`app_state.py=a2dd59df3ab40462df5bd0836d469708df1e4804a6a781e0d7e26c2400006a75`；`gateway.py=d0e4987d3b0affe0eb162d4fc10a4f9ce0f93b1a7ba1cf5429d824a58af6a78a`；`tunnel_manager.py=299c54902b0dfc57f30cc3e372a89073aa2ec981f0993581bb8d573560106103`；`test_v089_probe_contract.py=96810b9c99b0c7d072e260fc7781670c3f595babf14e3c53d42ae61f31fae0ee`。

## 32. 公开仓库表面清理与 README 治理验收

- 状态：**PASS（仓库治理/文档项）**。本项不修改 Gateway、route、Tunnel、OAuth、权限或运行时产品逻辑，不替代 s10 最新 staging/full/soak 门。
- `.gitignore` 已明确将 `/启动桌面程序.bat`、`/开发计划.md`、`/进度验收.md` 作为本地开发/验收辅助文件忽略；后三项原本仍被 Git 跟踪，因此使用 `git rm --cached` 只从索引移除、保留本机文件。task `83430039-6a54-427d-b2fd-faffe044ec81` exitCode=0；`git check-ignore -v` task `8ccb0b45-2f2f-443f-96eb-79c1c8162b2e` 明确命中三条规则。`.gitignore` SHA-256=`183fcf5f5f3811815e738f2977e01913987b5c2c1cbc094eb402b6e43b6728d7`。
- `README.md` 按公开项目入口重新收敛：删除版本更新流水账、维护分支/历史版本说明、内部开发计划/验收文档索引和硬编码 `0.8.8` 安装/构建命令；改为版本无关的项目用途、主要能力、架构概览、安装、快速开始、连接方式、安全、更新、开发入口、公开文档、反馈和许可证。具体版本历史仅链接 `docs/en/CHANGELOG.md` / GitHub Releases。最终 README SHA-256=`d1c8a95d29523b358ad4510e6ff4408a1345a0df9c941ffe7e30645f49027858`；额外文本扫描确认不再出现 `v0.8.8`、`v0.9`、`v0.10`、`开发计划`、`进度验收`、`项目架构`。
- README 裁决依据 GitHub 官方 README 指南：README 应聚焦项目做什么、为何有用、如何使用、如何获得帮助及维护/参与信息；更长的内部/历史材料应放到独立文档。该治理不删除 `docs/en/CHANGELOG.md` 等专业文档，只从 README 移除不属于入口层的施工与版本流水。
- Linux 发布版本一致性门再次独立复测：task `7845fa7f-6d06-4d2f-baa6-f783e8963ba1` 为 **9 passed / exitCode=0**；`项目架构.md` Linux 发布链已同步真实 fail-closed 顺序。

## 33. 最新 revision 全量 Python 门失败返工与复测验收

- 状态：**PASS（Python full gate）**；CodexPro stress/lifecycle、locked sync、最新 Windows/Linux 构建、staging/live preflight 与正式 soak 仍是后续阻断门。
- 首轮全量 task `571ac236-4307-4f97-80cd-7a776e1be731` 明确 FAIL：**495 passed / 2 failed / 3 skipped**。Tunnel 失败来自新增 PID 取证后，旧测试替身仍返回裸 `object()`；测试已改为带 `pid=4321` 的进程替身，不修改或放宽正式 `TunnelManager._spawn` 进程契约。Tunnel 专项随后 task `69263cb9-9446-46e3-b7f0-88dc74eda946` 为 **17 passed**，probe + Tunnel 组合 task `7dbb73d2-01fe-40b6-816f-ed4006e640cf` 为 **20 passed**。
- 另一失败 `test_v081_run_async_keeps_completion_signal_alive` 仅在首轮 pytest 与 Pyright/npm build/smoke 并发高负载时出现 `_ASYNC_SIGNAL_GUARDS` 中仍有其它异步信号桥。该测试单独 task `63c1ed94-ad45-4ec5-9359-ff2d6d1fb8ef` **1 passed**；`test_phase12_desktop.py` 独立 task `35f46e89-3a1a-4f6f-82e6-6bbc9e4e878e` **8 passed**；phase11→目标测试组合 task `a62e2ed3-8489-47aa-bbed-6599cd5c8637` **6 passed**。根因是测试把“全进程 guard 集合为空”误当成本次调用生命周期条件；修正后先冻结调用前集合，要求本次 `_run_async` 恰好创建 1 个新 guard，并在回调后确认该 guard 已从全局集合移除，不清空其它合法并发 guard、未修改 `_run_async` 产品实现。修正后 `test_phase12_desktop.py + test_tunnel_manager.py` task `96d49285-4ced-454f-a81e-ef017010b78a` 为 **25 passed**，相关 Ruff task `dd673c40-62ca-45e3-9af2-94a78ab3229d` PASS；最终 SHA-256：`test_phase12_desktop.py=377a13d196aea2540f42c4092d0ce20534e66c31daf06bd7b7abc16ae4841238`、`test_tunnel_manager.py=62cd6632fe3290b816b8db30fde4920eca2425bb1d6529dab07fa812ddfc51ff`。
- 在不并发运行 Pyright/npm 等重任务的条件下，最新工作树再次完整执行 task `33208f01-deca-4e6d-b5da-203376b0dcf2`，结果 **497 passed / 3 skipped / exitCode=0 in 171.57s**。该任务覆盖新增 probe 契约、Tunnel fixture 修正及全部既有 Python 回归，作为当前 revision 的有效全量 Python 证据。
- 同批静态/依赖/CodexPro 门：Ruff task `63795330-c8fe-484d-8e0c-a2d6c342e033` PASS；Pyright Windows `bca95013-926e-459f-9130-27cc1f42cd04` 与 Linux `0964ff1b-867e-47c2-bd2e-5e8c6158238b` 均 **0 errors / 0 warnings**；CodexPro build `70228459-7dde-4405-8047-f55c2a1d2ee9` exitCode=0；完整 `npm run smoke` task `4c954aee-da26-49e5-8fd7-d34cf6abef85` exitCode=0，16 个 smoke 阶段全部通过；`npm run stress` task `934ad957-8bfd-4417-b8e3-710ae44064cc` **stress test passed / exitCode=0**；`npm audit --omit=dev --audit-level=high` task `3d259a13-e190-4884-8a68-bd1526461f41` exitCode=0；`python -m pip check` task `567d3a1b-fff7-4f4d-9ce5-49183ef75791` 因本项目 venv 无 pip module 明确失败，不作为兼容性结论，仓库标准 `uv pip check --python .venv` task `4fbe903e-668a-4728-8e20-8a8128c5f9f5` 随后 **All installed packages are compatible / exitCode=0**。`git diff --check` task `6398d966-a579-427a-891a-ba2af6c7b406` exitCode=0，仅报告 CRLF working-copy 提示。

## 34. r4 Tunnel QUIC→HTTP/2 自适应回退 P1 验收

- 状态：**PASS（源码、真实现场归因、targeted/full Python 与静态门）**；重新构建后的 r5 staging 真实 Tunnel fault preflight 与正式 soak 仍是后续阻断门。
- r4 fresh staging 现场 `.ai-bridge/forensics/20260829-s10-r4-full-diff-502/` 证明：05:06:16 起公网 health 失败而本机 Gateway/C/D CodexPro 始终健康；05:06:48 仅 Tunnel 重建。cloudflared 启动预检明确 DNS PASS、TCP HTTP/2 两路 PASS、UDP 部分失败、`suggested_protocol=http2`，但默认 auto 继续 QUIC 指数退避；本程序 90 秒 readiness 在 cloudflared 自身 auto fallback 完成前终止首个重建进程。manifest SHA-256=`aeb22fb5fb0aa4b91902a599ef02056aa37bfe36687bbfa740bfa86388f70f51`。同期本地 MCP audit 对 `show_changes(include_diff=true)` 为 **15234 ms / success=true**，所以 ChatGPT 侧 502 是 Tunnel 交付窗口故障，不是 full diff/CodexPro 崩溃。
- 修复采用“协议提示驱动的有限回退”，不是延长 timeout：Cloudflare 新启动仍默认 auto 且保持历史命令形态；`TunnelManager.wait_ready()` 仅当 cloudflared 自身预检明确 `suggested_protocol=http2` 或等价提示时提前结束当前 auto/QUIC 尝试，保存 `recommended_protocol=http2`；`ServiceCoordinator` 在同一固定 `(0,2,8)` 有界重试序列的下一次尝试显式 `--protocol http2`，并记录 `tunnel_protocol_fallback`、当前/建议协议。DNS/双协议失败、认证/配置错误、stop cancellation、重试上限与项目/Gateway隔离语义保持不变。
- defect-first 红基线 task `c635d667-316a-4293-82f6-bb086d6f273d`：**3 failed**，分别证明原实现不会提前识别 HTTP/2 hint、不支持显式 protocol override、Coordinator 不会把 hint 传给下一次重试。首轮实现后 task `0789550f-dc3f-41a0-99cb-62d9eb2747e0` 为 **3 passed**。扩展回归首次 task `b26ef61c-1eb1-47e0-bef4-630172ea029b` 为 **63 passed / 1 failed**，唯一失败是默认 auto 被无必要显式写成 `--protocol auto`；修复为默认不加 flag、仅 fallback 时加 `--protocol http2` 后，task `15cce6fe-c38f-4362-84c3-1d0eb8d7212b` 为 **64 passed / exitCode=0**。
- 静态与全量：Ruff targeted `23900fed-33f6-46a7-afd1-af4d393e8185` PASS、Pyright targeted `070f8176-fc48-4bab-b468-b54d4154b6fa` **0/0**；最新全量 pytest task `3bfc1479-7ae7-446c-a63a-f7856296947b` 为 **500 passed / 3 skipped / exitCode=0**；full Ruff `26d58e0d-0ad3-44bd-a6d8-fe2371d15921` PASS；full Pyright Windows `d8384104-59fe-4161-903a-e6ce764c5dd7` 与 Linux `79e3ebbd-e452-475f-8b50-b1c541af067e` 均 **0 errors / 0 warnings**；最终 `git diff --check` PASS。真实 `cloudflared 2026.7.3` 对 `tunnel --protocol http2 run --help` 解析 exitCode=0，验证参数位置合法。
- 当前 SHA-256：`tunnel_manager.py=fe6f3fb098800634d9cd8f1ea319075005f7334d5097b50c01d913d18e335026`；`app_state.py=865198b09d096db05fa9fe8a1b6005b1169813ec408f2d297c45bd1caac33513`；`test_tunnel_manager.py=530b41d0fdb36fbce3f50293aadcc0244abace297230e3920debb43b650e4bb4`；`test_app_state.py=6d2fc1feecc7fff40c53405bb42816234ac4cd77ec0816006497c765564207b9`。

## 35. r5 自有 Tunnel 进程死亡 false-green P1 验收

- 状态：**PASS（源码、真实 PID/transport 证据、targeted/cross/full Python 与静态门）**；重新构建后的 r6 必须复现同一真实 PID fault 并证明自动恢复，之后才可进入正式 soak。
- r5 真实 fault 证据：`.ai-bridge/forensics/20260829-s10-r5-named-tunnel-udp-fault-r2/result.json` 记录 fault 前 r5 main PID=36152、broker=38652、C=37640、D=34284、stage cloudflared PID=14836，8786/8788/8791 均由预期进程监听；05:39:11Z 对 r5 cloudflared 精确施加 UDP/7844 阻断并终止 PID 14836 后，该进程确实消失。随后系统中只剩独立 `C:\Program Files (x86)\cloudflared\cloudflared.exe --token-file ...`，它继续为同公网入口提供服务。
- false-green 直接证据：05:39:18–05:40:52 `transport-health` 连续记录 local/public HTTP=200，Project MCP 与 public MCP 也持续成功，但没有任何 `tunnel_restart`；因此旧 `_transport_health_tick()` 仅依据外部 URL，无法识别**本实例拥有的 Tunnel 子进程已经死亡**。fault r2 harness 后续因 PowerShell `$ErrorActionPreference=Stop` 把预期 curl timeout stderr 提升为 `RemoteException` 而提前结束，result `passed=false` 只代表 harness 未完成汇总；finally 明确 `firewall_rule_remaining=false` 且最终 local/public=200，不改变 PID 与 transport 的直接根因证据。
- defect-first：新增 `test_dead_owned_tunnel_restarts_even_when_public_health_is_healthy`，明确把 local/public probe 固定为 200，再将 FakeTunnel 置为 not-running；红基线 task `63bfd260-2a9d-4c22-81e9-c7f58a55176f` **1 failed**，`tunnel.starts` 仍为1。修复后公网模式在本机 Gateway 健康后先检查 `self.tunnel.is_running`；自有进程死亡时记录 `tunnel_process_unhealthy`、最后 PID/exit code，并在既有 cooldown 下调用现有 `_restart_public_tunnel()`，直接复用 `(0,2,8)` 有界重试与 HTTP/2 自适应回退，不重启 Gateway/C/D。focused task `6a91aee9-29e4-4113-b837-3278190135ca` 为 **3 passed**。
- 交叉回归首轮 task `c6880f4e-01da-425d-af51-47a77e554bce` 为 **77 passed / 1 failed**；唯一失败是旧 flight-recorder 测试夹具从一开始就把 Tunnel 设成 ERROR，导致新的 ownership gate 正确跳过 public probe，而测试仍要求 previous public success。夹具改为“先 READY 并成功探测 → 再模拟进程退出 → 记录断连快照”，不改变产品逻辑；重跑 task `482925cd-429e-45e0-8f86-34904df2b768` 为 **78 passed / exitCode=0**。
- 静态/全量：Ruff targeted `7f1a08ed-a1bd-468a-a235-eb2960598123` PASS；targeted Pyright 首轮 `268ebc32-2822-419c-aa99-28da9ba3e025` 因测试 `pid` 推断为 int 而对 None 赋值报1个类型错误，显式标注 `pid: int | None` 后 `5af829fe-3ef9-4f6e-a13a-6f4401405c54` **0/0**。最新全量 pytest `be4948f1-5a53-4c2d-8258-9eace87038ff` 为 **501 passed / 3 skipped / exitCode=0**；full Ruff `15b63abd-3da8-42ca-a29e-fef83310ae8c` PASS；full Pyright Windows `a720776c-a582-49d9-a768-80ce8a91537a` 与 Linux `4a463996-a995-463c-964c-aecffc389cc0` 均 **0 errors / 0 warnings**；`git diff --check` PASS。
- 当前 SHA-256：`app_state.py=5c2952492a957fa718904ea327c70aa57897962e2febedadf44eeaf0142e538e`；`test_app_state.py=bd4784f4379eb3524fa0bdec937ca06c22827db38e4a884a05dfc4e56af74326`；`test_v089_probe_contract.py=4d0135647c9d2da9977c69c7e6246e35047ddb71461ffb1be8e7e869aa30c35b`。

## 36. OAuth / project bearer 凭据热路径 event-loop 阻塞 P1 验收

- 状态：**PASS（defect-first + targeted/cross/static）**；由于产品源码再次变化，r6 staging 与其 live preflight 已自动失效，仍须最新 full gate → r8 staging → 重新 live/fault/formal soak。
- r7 正式 soak 的发布阻断现场：`20260829-s10-v089-soak-r7/events.jsonl` 在 fault 前首轮 public matrix 记录 `initialize=3351ms` 成功后 `tools/list` 25.0s hard wall；同窗 flight recorder 显示该请求08:22:13已 `request_context`、直到08:22:34才 response，期间没有 `upstream_request`，且 transport-health 在08:22:26记录本机 `/health` 5.031s超时。说明阻塞发生在 Gateway uvicorn event loop 的 bearer 准备阶段而非 CodexPro/Tunnel。
- 代码根因：旧 `_mcp_endpoint` 对任何非 hub bearer 都先同步 `_workspace_for_credential()`，该函数逐项目调用 native project credential registry；OAuth 记录即使已在 provider 内存或持久存储中也必须先经过这组 Windows Credential Manager/DPAPI 同步读取。`LocalOAuthProvider.load_access_token()` cache miss 同样同步读 protected store。慢 native store 因此既拉长当前 OAuth/tools-list，又冻结同一 uvicorn loop 的 `/health`。
- defect-first `tests/test_v089_oauth_hotpath.py`：首轮 task `38af3531-ba4d-44ef-ac90-559b1cda89d6` **2 failed**；扩展 persisted-store case 后 task `b317fefa-fd8c-498c-9c67-15a8963dbfb9` **3 failed**，分别复现 OAuth bearer 扫描 p1/p2、persisted OAuth store 0.619s 阻塞 event loop、project bearer registry 0.605s 阻塞 event loop。
- 最小修复：Gateway 认证顺序改为 hub bearer → OAuth access record → 仅 OAuth 未命中时 project-bearer 兼容扫描；project scan 用 `asyncio.to_thread`，不改 route/rehydrate/tool contract/权限。OAuth provider 保持 freshly-issued token 的内存 cache-hit 同步快路，仅持久存储 cache-miss 用 `asyncio.to_thread`，因此 Gateway 重建后的 token 连续性仍保留且不会占住 event loop。
- 修复后 task `35ff60d7-35af-4d96-b6b5-760d1dda0c45` **3 passed**；OAuth + connection-governance + contract/policy + rehydrate/concurrency + transient-route + deadline 交叉 task `12f77ef9-9530-41ee-8966-4d70f77480a6` **144 passed / exitCode=0**；Ruff task `63f0fc11-c3a9-4529-81ee-bb9d6faf537c` PASS；Pyright task `1a0eadfa-9f9c-410f-bc79-22b76fb4558e` **0 errors / 0 warnings**。
- 当前 SHA-256：`gateway.py=7a3f93bfa0410372620a668b32847774c8deeef6b8e8b576a99d0e6c00eefeef`；`oauth_provider.py=9e72c78a083de5bf2c51ae72c664840e85ec6ff39db2a1add47308c697f6cf36`；`test_v089_oauth_hotpath.py=a7ccc989f47c662c665f6878b81fcfffe8249f07441e8db4f5dcfbee5d75d15a`。
