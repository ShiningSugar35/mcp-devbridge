# MCP DevBridge 验收记录

> 本文件只记录已经实现并通过相应验收的事实。未完成或平台阻断事项只放在 `开发计划.md`。
>
> 旧版本逐轮 forensic、临时构建目录和一次性 smoke 脚本已在 2026-09-01 维护清理中删除；更细的历史过程仍可从 Git 历史恢复。本文件保留对当前维护真正有价值的里程碑、最终测试、提交和运行态结论，不再依赖已经清理的临时证据路径。

## 0. 当前状态

- 正式生产基线：`v0.8.9`，commit `b679268ee6cbbed49f5a2ca93c6e81d8e94d6184`，已于 2026-08-30 完成发布、安装与真实运行态验收。
- 当前修复研发分支：`release/v0.8.9-fixed`；产品版本元数据仍为 `0.8.9`，尚未形成新的正式 Release。
- 当前本机正式安装目录：`D:\Environment\mcp`；共享 Hub 端口 8786。
- 当前运行根：`D:\Environment\mcp:8787`、`C:\:8788`、`D:\:8789`，三根均可用。
- `v0.8.9-fixed` 修复线的 durable fallback、terminal receipt、取消竞态、资源/并发边界、tool-result budget 与按需详情取回均已实现并通过本地工程/运行态门；Native MCP Tasks 与 ChatGPT 自动创建下一 assistant turn 仍为平台阻断，且用户已于 2026-09-02 明确暂停 S8–S10。
- 2026-09-01 Windows 完全访问/stale 管理员服务回归已修复并热更新，提交 `887e1bf57eb09a68b0e3904d64d4d995b646f8d7` 已推送 `origin/release/v0.9.0`。
- 2026-09-01 路径漂移/开发临时产物清理已释放约 15.85 GiB；正式安装、当前配置/凭据、release 历史/正式资产、`installer/` 用户资产均未删除。

## 1. v0.8.8 正式基线

v0.8.8 完成了多根工作区、权限分层、Windows 管理员执行、Gateway/Tunnel、OAuth、Windows/Linux 安装升级与真实 MCP 数据面的第一轮完整闭环。正式基线 annotated tag 解引用到 commit `104bb9efa42f55e7df76c78d56b3394616d56efc`。

该阶段确立的长期语义继续有效：

- 多个 READY 根同时 active，不存在入口/主项目；
- 绝对路径按最具体运行根路由；显式 workspace handle 优先于模糊上下文；
- `read_only/workspace` 以 canonical path containment 为硬边界，`system/full_system` 允许显式跨根系统访问但仍受危险命令与 secret/path guard 约束；
- Windows 完全访问依赖正规 UAC 授权的高完整性执行链，不绕过 UAC；
- Gateway/Tunnel 是共享 Hub，不属于某个项目。

## 2. v0.8.9 连接治理与正式发布

v0.8.9 围绕长期运行、路由一致性和连接自愈完成了系统性修复，并最终完成正式发布。主要验收事实：

- CodexPro HTTP 数据面采用 stateless MCP transport；外部 session header 不再成为工作区路由事实源；
- deterministic workspace route persistence / rehydrate、legacy handle affinity、relative-path ambiguity fail-closed 与 task affinity 均有回归覆盖；
- Gateway 公共工具 contract 固定为 50 tools，项目 CodexPro full-mode 为 38 tools，二者职责明确；
- call-stage policy 在固定公共 schema 后按 `read_only/workspace/system` 执行权限控制；
- Gateway response/stream 单一所有权、请求 deadline、SSE 边界、bounded response body 与 transport retry 均完成回归；
- Project/Gateway/Tunnel 分层探针与 cooldown 恢复只重启责任层，不再跨层扩大故障域；
- Cloudflare auto/QUIC 明确给出 HTTP/2 建议时，可在有界重试序列中切换 HTTP/2；
- Flight Recorder、service supervisor 与 transport health 记录均有大小/留存/脱敏边界；
- Windows/Linux 同源构建、安装/升级、真实 ChatGPT MCP 路由和持续 fault soak 通过发布门。

正式 release/tag/branch 最终统一指向：

```text
b679268ee6cbbed49f5a2ca93c6e81d8e94d6184
```

正式发布后 v0.8.9 作为当前生产基线保留，后续 v0.9.0 研发没有移动其 tag 或 Release。

## 3. v0.9.0 Stable Hub 与工具范围裁决

独立诊断确认三个工具层级：

```text
CodexPro 项目引擎：38 tools
CodexPro + Windows wrapper source：41 tools
Gateway 对客户端公开：50 tools
```

Gateway 公共 contract version=`1`，canonical fingerprint：

```text
489cd30e8e8676ad382c752bed7c46ba9bc2f308fa2d98b2a699793d0c5ee780
```

真实本机/公网探针均返回 50-tool contract。`server_config.registeredToolCount=38` 明确代表单项目 CodexPro 引擎，不代表 ChatGPT 应看到的 Hub 总数。ChatGPT app 若使用已批准的 frozen tool snapshot，需要在平台侧 Refresh/审核/发布，服务端不能强制改写平台快照。

## 4. v0.9.0 本地 durable fallback

### 4.1 terminal receipt 与重连恢复

带 `long_run_id` 的后台 Bash 任务在进程启动前预留并持久绑定 task id。BashTaskManager 在任务进入 `completed/failed/cancelled` 后自动把有界 terminal receipt 写入 LongRunState，不依赖用户或 Agent 恰好调用 `wait_task/get_task`。

已验证：

- spawn-before-start failure 同样写入 failed；
- 同终态重复回调幂等，冲突终态拒绝覆盖；
- terminal 已出现但 receipt 尚未落盘时继续阻断 PASS review/completion；
- CodexPro/DevBridge 进程替换后，已持久化 receipt 可零猜测恢复；
- 历史无可靠终态的任务按 failed/not accepted 保守收敛，不把 `task not found` 猜成成功。

### 4.2 Windows 取消竞态

Windows 立即取消曾存在“先杀 PowerShell 壳、孙进程继续运行”的竞态。修复后对受管 PID 使用固定 150 ms 建树宽限，再按 `[100, 250, 500, 1000, 2000] ms` 有界执行 `/PID /T /F` 进程树终止；不扫描或终止用户其它进程。

完整 CodexPro smoke、HTTP durable task、restart/observation、spawn failure、cancelled 与冲突终态路径均已转绿。

## 5. Regular Chat 实验资产与正式边界

`third_party/regular-chat-controller/` 与 `experimental/regular-chat-bridge/` 仅作为未发布实验资产保留。正式桌面、CLI、wheel、PyInstaller、Windows/Linux build 和 installer 不引用、不暴露、不打包该路径。

实验工程完成了 dedicated profile/tab、conversation identity、exactly-once、completion/recovery 等状态机验证。最终受影响层测试达到：

```text
37/37 Node unit PASS
13/13 Playwright fixture PASS
```

审查并修复的关键缺陷包括：

- BrowserSupervisor 并发 start/stop 串行化；
- active-tab / run 建立失败时回收投机 page/lease；
- generating 配额拒绝时 turn 保持 `never_sent`；
- 同一 run 并发 mutation 只允许一次真实浏览器提交，相同显式 turn identity 合并，冲突请求 fail-closed。

这些结果只证明实验状态机成立，不代表 ChatGPT host 已支持无人触发续轮。

## 6. v0.9.0 平台能力裁决

当前真实 ChatGPT → DevBridge 连接仍报告：

```text
nativeMcpTasksExtension = not emitted by baseline connector
```

所以当前已可靠具备：

```text
短 MCP 工具调用
+ 本地后台任务
+ durable plan/checkpoint/evidence/terminal receipt
+ CodexPro/DevBridge 重连后的恢复
```

但尚不具备由 DevBridge 单方面实现的：

```text
当前 assistant turn 结束
→ host 自动创建下一 assistant turn
→ 模型继续推理并再次调用 MCP
```

S8 Native MCP Tasks 与 S9 宿主级无人值守验收因此保持 `blocked_platform`。实验浏览器控制器不得替代 capability negotiation，也不得自动抓取登录态 ChatGPT Output 作为正式能力证据。

## 7. Windows 完全访问、stale 管理员服务与端口污染修复

### 7.1 真实根因

2026-09-01 实机复现：计划任务 `MCP DevBridge Elevated Broker` 仍在 Running，但旧 broker 控制状态文件已丢失。由于任务使用 `MultipleInstances=IgnoreNew`，旧实现删除 state 后再次 `/Run` 会被 Windows 忽略，最终只能等待并报管理员服务启动超时。

失联旧 broker 还持有旧项目 child；删除/重建项目后端口被重新分配，导致新 C 盘项目曾命中旧 D 盘监听并收到 HTTP 401。这解释了“运行记录部分可用、项目列表失败”的状态分裂。

该缺陷不是 `2a7fb24` Regular Chat/durable-task 提交新引入；相应权限/项目生命周期代码在更早的 v0.8.8/v0.8.9 设计中已存在。

### 7.2 修复

- `ensure_running()` 在任务注册、state 删除和 `/Run` 之前识别 stale 运行实例；
- 普通启动不会无确认终止可能仍托管长任务的旧管理员服务；UI 明确提供“终止旧服务并重启 / 终止旧服务并改用项目工作区 / 取消”；
- “停止所有项目”作为显式全量回收入口可有界停止管理员服务；
- 工作区项目启停只在健康管理员服务中按同一 `project_id` 精确回收旧高权限 child；
- 新项目端口分配跳过真实 loopback listener，CodexPro/Windows-MCP spawn 前再次拒绝占用端口；
- 项目列表错误消息绑定真实 ProjectUnit 故障；
- 升级接力改为单一批次 worker 顺序恢复全部根，只对失败根重试一次，共享 Hub 只启动一次；
- `live_upgrade.ps1` 只有在 resume 已消费、Gateway 可用、全部原运行 root/port 匹配且 system 根由 elevated broker 托管时才写 `ok=true`。

### 7.3 最终验收

自动门：

```text
受影响回归：97 passed
全仓 pytest：520 passed / 3 skipped
Ruff：PASS
Windows Pyright：0 errors / 0 warnings
Linux Pyright：0 errors / 0 warnings
CodexPro build/smoke：PASS
Windows PyInstaller + Inno Setup：PASS
```

最终正式升级接力回执：

```text
ok=true
expected_project_count=3
ready_project_count=3
resume_consumed=true
elevated_broker_ready=true
```

实机确认 C:\、D:\、D:\Environment\mcp 三根均 `tool_mode=full / bash_mode=full / 38 tools`，共享 Hub 与公网连接正常。C 盘只读高完整性探针 `fsutil dirty query C:` exit=0。

修复提交：

```text
887e1bf57eb09a68b0e3904d64d4d995b646f8d7
fix: recover stale Windows admin service and project roots
```

已推送 `origin/release/v0.9.0`，未创建/移动 Tag 或 Release。

## 8. 2026-09-01 临时产物与路径漂移清理

完成只读盘点后，仅删除可证明属于 MCP DevBridge 的可再生产物和旧路径残留：

- 注销临时 WSL2 构建发行版 `MCPDevBridgeBuild`，并删除 `D:\Environment\wsl-mcp-build` 与其旧快捷方式/插件临时目录；
- 删除旧 `%LOCALAPPDATA%\Programs\MCP DevBridge` 残片；正式快捷方式继续指向 `D:\Environment\mcp`；
- 删除历史 `MCPDevBridge-*` smoke/update 测试目录；
- 删除项目 `build/`、`dist/`、`.tools/`、pytest/Ruff/Python caches、CodexPro/Regular Chat `node_modules/dist`；
- 删除历史 `.ai-bridge` forensic/wheelhouse/一次性 patch/smoke 脚本和已结束 long-run 文件；只保留当前平台阻断 run 与本次清理 run；
- 删除本轮非正式 L3 hotfix installer，保留 `release/` 其它正式/历史资产；
- 保留当前 `MCPDevBridge.exe`、`_internal/`、`.venv/`、当前配置/凭据/路由、运行日志以及用户未跟踪 `installer/`。

清理估算释放：

```text
17,022,941,263 bytes
≈ 15.854 GiB
```

清理后 `mcp / C: / D:` 三根仍运行，mcp CodexPro self-test 无失败，38-tool full mode 正常。

## 9. 2026-09-01 长 Chat 会话 tool-result 放大修复

### 9.1 18:00 起现场裁定

用户在多个 GPT-5.6 Sol Pro 长会话中观察到约百分钟后“本地命令、源码、Git、构建/测试只得到被截断空结果”。北京时间 18:00 起的本机审计并不支持“DevBridge 自己返回空结果”：

- `openai-mcp/1.0.0` 初始冻结窗口共 627 次 tools/call，624 次成功；仅 1 次 `wait_task`、2 次 `search` 为明确 deadline failure；审计 `output_truncated=true` 为 **0 次**；
- FlightRecorder 对 read/git/status/command/task 等工具均记录非零 response body；220 次深层本机 MCP probe 为 0 失败，公网仅 1 次瞬时失败；18:00 后没有项目 supervisor 崩溃/异常重启能解释“所有工具空结果”；
- 可关联的 501 次工具调用累计向客户端返回约 **14.22 MiB**，52 次 >=64 KiB、26 次 >=128 KiB；前三个 15 分钟窗口约为 4.71 / 4.63 / 2.39 MiB；
- 最大放大源是 `list_tasks`：83 次累计约 **7.92 MiB**，单次最高 **495,270 bytes**；`read` 累计约 3.21 MiB，`long_run_status` 约 1.10 MiB；
- 本机日志只记录 `client_name=openai-mcp/1.0.0`，不记录 ChatGPT 具体模型/effort，因此不能从服务端把故障限定为 GPT-5.6 Sol Pro；当前 OpenAI 公开能力说明也没有一个已公布的“Sol Pro 禁用 MCP tool result”规则。完整 MCP/Developer mode 仍属于平台 beta 边界，host 内部如何裁剪长期会话不是 DevBridge 可观测或可配置项。

结论：可见的“空/截断”发生在 DevBridge 之上的 host tool-result 接收/保留/呈现边界；但 CodexPro 存在已直接证明的 **P1 payload amplification**，会显著加速长期工具会话的 host/context 压力，因此属于本地必须修复的触发因素，而不是把责任简单推给模型。

### 9.2 defect-first 修复

根因代码包括：`list_tasks` 把整批完整 `BashTaskSnapshot[]`（command/stdout/stderr 等）返回到 structuredContent；terminal `wait_task/get_task` 重新发送完整 rolling output；durable `summarizeLongRun` 重复返回全部 task resolutions/checkpoints/evidence tails。

新增 `third_party/codexpro/scripts/tool-result-budget-smoke.mjs` 并加入正式 `npm run smoke` 链。红→绿证据：

```text
12 个约20KB历史任务：list_tasks 247,696 B → 约 6.9 KB（首轮）
最终 35-task 压力：list_tasks = 10,819 B
terminal wait/get structured stdout：20,016 B → <= 8 KiB tail
synthetic long-run：416,628 B → 51,522 B（ASCII）
中文字符压力：140,498 B → 51,092 B（UTF-8 byte budget）
```

Review 期间继续发现并关闭两个边界：较早启动的 active task 不能被大量更新 terminal history 挤出 `list_tasks`；超长 command 必须按 UTF-8 **字节**而不是字符数裁剪。最终 transport 语义为：

- `list_tasks` 最多 20 个 compact summary，active task 优先，无 stdout/stderr，返回 total/returned/omitted counts；
- `bash/get_task/wait_task/cancel_task` structured output：running 每流约 2 KiB、terminal 每流最多 8 KiB，command 最多 2,000 UTF-8 bytes，并显式返回 omitted-byte counters；
- durable 磁盘 JSON 保留完整有界事实；MCP 只返回计数 + 最多 20 个 task/resolution + 8 checkpoint tail + UTF-8 byte-bounded evidence/criteria/note；
- `read` 保持既有 structured 单字段 compact，不删除源码正文，避免破坏主要依赖 structuredContent 的客户端。

### 9.3 验证与热加载

最终质量门：

```text
CodexPro npm run build：PASS
CodexPro npm run smoke：PASS（含 tool-result-budget、durable HTTP/restart/observation/long-run）
CodexPro npm run stress：PASS
npm audit --omit=dev --audit-level=high：0 vulnerabilities
Python Gateway/MCP targeted：50 passed
全仓 pytest：520 passed / 3 skipped
Ruff：PASS
Pyright Windows：0 errors / 0 warnings
Pyright Linux：0 errors / 0 warnings
git diff --check：PASS
```

安装态只替换 CodexPro JS runtime，没有重启桌面、Gateway、Tunnel 或管理员 broker。构建/安装态 SHA：

```text
server.js = 98215e5586d4c27e20782b02adb73137f023bbfc50112774adf37552478e5ef8
longRunOps.js = f7c2fde6e714dd79e57563d38097050478e6f55a591f6190436e49311c0f3f6a
```

逐根局部恢复：`C: 9612→23372`、`D: 16076→20128`、`mcp 17652→24548`；三者仍由同一个 elevated broker PID 16984 托管。随后多轮 project MCP probe 和 local/public MCP transport probe 全绿。

真实 Connector/FlightRecorder 热加载后证据：

```text
真实 durable run：39 task / 123 checkpoint → long_run_status HTTP body 27,375 B
约21KB中文 stdout → terminal wait_task HTTP body 9,587 B，stdout_omitted=12,822 B
list_tasks：空 registry 466 B；含1个真实任务 1,050 B
```

修复显著消除了 DevBridge 可控的大包触发因素，但不宣称能够改变或保证 ChatGPT host 的隐藏 context compaction / 单轮生命周期策略。

## 10. 2026-09-02 大型项目/复杂任务按需详情取回

### 10.1 架构裁决

上一轮 transport budget 解决了“默认结果过大”，但也留下信息可达性边界：terminal task 只默认返回约 8 KiB tail，而 BashTaskManager 内部仍保留最多约 120 KiB rolling buffer；LongRunState 磁盘 JSON 仍保存完整有界 evidence/checkpoint/task resolution，但 `long_run_status` 只返回摘要。直接调高默认预算会重新制造长会话上下文放大。

结合 MCP stateless handle、opaque cursor pagination、Tool Result/ResourceLink，以及 OpenAI 对 large intermediate output 的过滤/聚合建议，最终采用 **compact-by-default + explicit detail retrieval**：不新增公开 MCP tool，而是在现有稳定 `codexpro(action,args)` wrapper 中增加只读 `task_output` / `run_detail` alias，因此当前 ChatGPT frozen tool snapshot 无需刷新，CodexPro/Hub 工具合同数量不变。

### 10.2 实现

- 新增 `third_party/codexpro/src/detailRetrieval.ts`：进程级随机 HMAC key 对 opaque cursor 封口；cursor 绑定 kind、task/run id、stream/section、revision 与 offset，篡改、跨对象或 stale revision fail-closed。
- `task_output` 默认 8 KiB、最大 16 KiB，按 UTF-8 byte boundary 分页当前 retained/redacted stdout/stderr；返回 observed/retained bytes、page range、`earlier_output_unavailable` 与 `next_cursor`。rolling retention 已丢失的更早输出明确声明不可恢复。
- BashTaskManager 增加 stdout/stderr observed-byte、per-stream truncation 与 retention metadata；rolling tail 改为 UTF-8-safe，并给 child stdout/stderr 配置 UTF-8 decoder；不新增第二份完整日志、无新无界 Map/缓存。
- `run_detail` 可分页读取 `checkpoints`、`task_resolutions`，或一个 step 的 `acceptance_criteria/evidence/notes`；单页只装完整条目，避免 evidence/checkpoint 被切成半条。
- compact task/long-run 结果只在存在省略历史时提供小型 `detail_retrieval` hint；模型只在当前诊断确实需要旧信息时继续分页。
- server instruction 明确要求按需 retrieval，不允许为了“看全”重新扩大默认结果。

### 10.3 defect-first 与 L3 验证

旧实现红基线首先在 terminal compact result 上失败：存在 omitted bytes，但没有 `detail_retrieval`/公开分页恢复路径。实现过程中先后由测试发现并返工：supertool schema validation 会剥掉隐藏 `detail_mode`，因此改为 wrapper 内部两个显式只读 dispatch；running task 首次分页存在 spawn/output 竞态，测试改为等待 observed bytes 后验证 stale cursor；long-run stale-cursor fixture 初始一页装完，收紧页大小后验证 mutation invalidation；Review 又补上 HMAC cursor seal，防止客户端自行改 offset。

最终 focused 结果：

```text
detail-retrieval-smoke：PASS
约 70 KiB 中英/emoji retained stdout：9 页逐字节重建
最大 task/run detail page：16 KiB data，wire < 40 KiB
>120 KiB rolling output：明确 earlier_output_unavailable，UTF-8 无替换字符
tampered cursor / cross-task / cross-run / running snapshot changed / long-run revision changed：全部 fail-closed
step acceptance/evidence/notes、旧 checkpoint、terminal task resolution：均可分页恢复
tool-result-budget-smoke：list_tasks 10,819 B；synthetic long-run + retrieval hint 52,052 B < 60 KiB
async-task-smoke / long-run-smoke：PASS
```

完整 L3 门：

```text
CodexPro build：PASS
CodexPro full smoke：PASS（已正式包含 detail-retrieval-smoke）
CodexPro stress：PASS
npm production audit：0 vulnerabilities
Gateway/MCP contract targeted：50 passed
全仓 pytest：520 passed / 3 skipped
Ruff：PASS
Pyright Windows：0 errors / 0 warnings
Pyright Linux：0 errors / 0 warnings
git diff --check：PASS
changed-files secret scan：0 findings
```

默认 compact 结果没有因为 retrieval hint 反弹到百 KB；38 个 CodexPro tool 与 50-tool Hub stable contract 保持不变。

### 10.4 真实运行态

安装态热替换文件：

```text
server.js = 806a1278b4d8e3f7d06c3e68cd7d5e91a315896157bb0b1d146b9666093b71ec
bashOps.js = 6eddcc91ebb158cd01e7c25a65ef18313d74fe68cf6875fbaaac1a1c560a267e
detailRetrieval.js = e2fde713084dc485e2e05517e4fe305f5d47e62a12c79d59fc43dfc90de22993
```

C:\、D:\、D:\Environment\mcp 三个实际运行引擎均通过 `codexpro(list_actions)` 返回 `task_output -> get_task` 与 `run_detail -> long_run_status` alias。mcp 根真实验证：35,035-byte 混合中文/emoji stdout 的 compact `wait_task` 省略 26,844 bytes，并返回 retrieval hint；`task_output` 第 1 页 0–8190、第 2 页 8190–16380，cursor 连续有效；当前 durable run 93 个 checkpoint 通过 `run_detail(limit=4096)` 首屏返回 19 条、payload 3,985 bytes，并返回下一 cursor。

### 10.5 热更新 active-task 事故与流程修正

首次逐根热加载时，在未检查 D:\ 项目引擎所有 opened workspace 的 active tasks 前使用 `taskkill /T` 重启 D:\。该引擎当时承载另一个 AStock 会话的后台任务，导致一个正在运行的 pytest task 被中断。AStock durable run 随后基于真实进程/输出证据将该 task 明确登记为 `failed/lost` 并从头重跑，没有把部分输出误判为成功。

该问题定性为**本轮计划内热更新流程缺陷**，不是 retrieval 数据面缺陷。已立即修正规范：任何计划内项目引擎重启/热加载，必须先枚举该引擎全部 opened workspace 并执行 active-task drain；存在 `running/cancelling` task 时延后该根，除非用户明确要求取消；禁止把 `taskkill /T` 当作常规升级手段。规则已写入 `AGENTS.md`、`项目架构.md` 与英文长任务/架构文档。随后复核时 D:\AStockMultiAgent 有 3 个 running task，因此不再进行第二次 D:\ 重启；三个根已经分别通过自动恢复/既有重启加载同一新 runtime，无需再次中断任务。

## 11. 2026-09-02 新会话接管前当前状态

### 11.1 已完成能力

大型项目/复杂任务的信息质量保护已经不再是待开发项：§9 的默认 tool-result budget 与 §10 的按需详情取回均已通过完整 L3 验收并进入当前运行态。当前 `codexpro(list_actions)` 仍暴露：

```text
task_output -> get_task
run_detail -> long_run_status
```

因此模型在正常情况下只接收 compact task/long-run 摘要；当诊断需要旧 stdout/stderr、旧 evidence/checkpoint/task resolution 时，可通过 wrapper alias 按 UTF-8 字节分页取回，不需要重新扩大默认响应。该架构同时保护长会话 context 压力和复杂项目的信息可达性。

### 11.2 当前 Git / 运行边界

2026-09-02 接管前复核：

- 工作区：`D:\Environment\mcp`；当时分支：`release/v0.9.0`（随后在 §12 因版本语义错位更名）；
- `git_status` 除用户原有 `?? installer/` 外无其它未提交项；`installer/` 不属于本轮开发资产，不得清理、修改或暂存；
- 当前 CodexPro `tool_mode=full / bash_mode=full / registeredToolCount=38`；
- `server_config.longRunPolicy.nativeMcpTasksExtension` 仍为：

```text
not emitted by baseline connector; tool-level durable fallback remains compatible with hosts that do not advertise io.modelcontextprotocol/tasks
```

因此当前真正未完成事项不是 retrieval 层，而是 `开发计划.md` 中的 S8/S9/S10 平台能力链。

### 11.3 剩余任务裁定

- **S8 = `blocked_platform`**：只有真实 ChatGPT/目标 MCP host 声明 `io.modelcontextprotocol/tasks`，或 OpenAI 明确提供可供自定义 MCP server 使用的等价异步 primitive，才能开工 native task 映射。当前不得开发伪实现或用浏览器登录态自动化替代 capability negotiation。
- **S9 = `blocked_platform`**：只有 S8/等价 host primitive 真实可用后，才能进行至少 20 分钟、建议 45–60 分钟的真实无人值守 soak，验证跨断连 identity、duplicate mutation=0、active-task drain、资源有界、compact + detail retrieval 不回归。
- **S10 = waiting S8/S9**：在 S8/S9 完成前不得整体 PASS、不得 `long_run_complete`、不得把 v0.9.0 宣称为“ChatGPT 能自动开启下一 assistant turn”的正式完成版本。

### 11.4 新会话执行规则

新会话首先按 `AGENTS.md → 项目架构.md → 开发计划.md → 进度验收.md` 恢复事实，然后检查 Git 与 `nativeMcpTasksExtension`。若 capability 仍缺失，应明确停在平台阻断，不做无依据代码变更；若 capability 已出现，也必须先确认用户是否重新授权 S8–S10，当前用户已于 2026-09-02 明确暂停这三项。所有长任务使用唯一 durable run；任何计划内项目引擎重启前必须 active-task drain。

## 12. 2026-09-02 版本错位核验与修复线更名

用户发现本地桌面仍显示 `0.8.9`，而远端研发分支已使用 `release/v0.9.0`，要求先确认当前运行 MCP 是否真的完成更新；若已更新，则把远端版本/分支语义改成 `0.8.9-fixed` 类标识，并暂停 S8–S10。

### 12.1 根因与运行态裁定

产品版本源始终一致保持 `0.8.9`：`pyproject.toml`、`src/local_dev_mcp_bridge/__init__.py`、`packaging/local-dev-mcp-bridge.spec`、`scripts/installer.iss`、`uv.lock` 均未升级到 0.9.0；桌面标题直接读取 `__version__`，因此 UI 显示 `0.8.9` 本身不能证明运行态仍旧。

实机核验当前主进程为 `D:\Environment\mcp\MCPDevBridge.exe`，三个项目引擎均从同一安装目录 `_internal\third_party\codexpro\dist\http.js` 启动。安装态关键 runtime SHA-256 为：

```text
server.js          806a1278b4d8e3f7d06c3e68cd7d5e91a315896157bb0b1d146b9666093b71ec
bashOps.js         6eddcc91ebb158cd01e7c25a65ef18313d74fe68cf6875fbaaac1a1c560a267e
detailRetrieval.js e2fde713084dc485e2e05517e4fe305f5d47e62a12c79d59fc43dfc90de22993
```

三者与 §10 已验收热更新哈希逐项一致；当前 `server_config` 仍返回 `registeredToolCount=38 / tool_mode=full / bash_mode=full`，此前真实 `task_output/run_detail` 运行探针也已通过。因此裁定为：**本地已经完成修复运行态更新，不需要再次重启或热更新。版本错位来自 Git 研发分支过早命名为 `release/v0.9.0`，不是本地安装失败。**

### 12.2 分支语义修正

保持正式产品版本、`v0.8.9` tag 和既有 v0.8.9 Release 全部不变；当前维护线从本地 `release/v0.9.0` 更名为 `release/v0.8.9-fixed`。先把同一 HEAD 建立为 `origin/release/v0.8.9-fixed` 并设置 tracking，确认推送成功后再删除误导性的远端 `origin/release/v0.9.0`。没有 rebase、force-push、commit rewrite、tag 移动或 Release 改写。`gh release list` 复核远端 GitHub **Latest Release 仍是 MCP DevBridge v0.8.9**；仓库中早于正式 v0.8.9 发布日期存在的 `v0.9.0/v0.9.1/v0.9.4` 历史 tag 没有对应当前 Latest Release，本轮按仓库硬约束不删除、不移动。

版本 gate 继续以正式产品版本 `0.8.9` 为准：

```text
scripts/check_release_version.py --expected 0.8.9 --root . -> PASS
pytest tests/test_release_version_gate.py -q -> 9 passed
```

`AGENTS.md`、`项目架构.md`、`开发计划.md` 与实验边界文档已同步当前维护分支语义；历史记录中“当时推送到 release/v0.9.0”的事实保留，不篡改历史。S8–S10 继续保留为未完成事项，但按用户要求暂停，不在本轮执行。
